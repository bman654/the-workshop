⟦cue: stratumTo(5) at chapter open⟧
⟦cue: diagram MORPH — the build seat hangs the foundry sub-graph (prep → parallel takes → blind judges → one synth) plus the baton self-loop (T8.8 anchors to "it hangs a whole sub-pipeline")⟧

[calm] One agent, one turn, only makes so much. This poured-bronze layer is the answer — reach. [short pause] Watch the build seat: it hangs a whole sub-pipeline. Several takes forged in parallel, judged blind, the best synthesized into the shipped asset. Sound is judged by numbers — the judge's brief is honest about it: you cannot hear — the analysis is how you judge. [pause] Then the baton: a builder holding a swing too big for one turn hands it, mid-work, to a fresh builder. Bounded — three handoffs, and the last one must finish. Makers stopped shying from big work. [short pause]

⟦frame flip → talk/arch-raise.html (T8.6) — deck-driven stone placement; each archStage(k) lands on the marked line's word time (T8.8 anchors)⟧

Here is that idea, as stone. An empty yard.
⟦archStage — springers set⟧
One maker sets the springers — and is gone.
⟦archStage — voussoirs climb⟧
Fresh hands raise the voussoirs, course by course.
⟦archStage — the keystone drops⟧
Another hand drops the keystone.
⟦archStage — the ring locks⟧
And the ring goes rigid — a thing none of them could have finished alone.
