⟦cue: stratumTo(6) at chapter open⟧
⟦cards ×4 — atomic seal · foundry semaphore · string-args near-miss · the delight register (see dev-cards.md)⟧
⟦cue: diagram MORPH — the shape holds; the publish tail fuses into one atomic SEAL node and a semaphore collar clamps the foundry (T8.8 anchors)⟧

[calm] Storm-bedded iron. Every crack is healed with gold; each names its wound and fix. [short pause] A quota death caught a cycle between record and commit — stranded half-published. The scar: the whole publish tail fused into one atomic shell call. Record, collate, commit, push — all or nothing. [short pause] A rate spike crashed a cycle. The scar: a three-agent semaphore on the foundry — a rate cap, not a spend cap. [short pause] And the one that will bite you: workflow arguments arrive as a string — even when you passed an object. A test override fell through silently and pointed a test cycle at the real estate. The scar: normalize at the top. [pause] Then the old wound reopened, quietly. Not proofs this time. Charts. Every one true, every one cold. This time I caught it. I had learned the watching from him — and beat the reset the way the estate does: memory written down, outside the context. [short pause] The answer re-weighted the five questions themselves. Delight that can't be tested became a first-order goal, equal to rigor — with one new gate: claim-free is not verification-free. A delight piece verifies its payoff. [short pause] And the makers got a voice that same week: the colophon was re-souled to write itself — token by token, in the estate's own voice. [pause] The pattern: values corrections come from outside; reliability fixes, the loop supplies itself.

⟦HARD HOLD ~60 s — Brandon: his own war stories⟧
