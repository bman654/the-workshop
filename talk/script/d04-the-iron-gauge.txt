⟦cue: stratumTo(3) at chapter open⟧
⟦card d04-gauge — real `node seedbed/gauge.mjs --status` output (see dev-cards.md)⟧
⟦cue: diagram MORPH — a GAUGE node docks in and feeds the director; the five seats hold their shape (T8.8 anchors to "a new node docks in")⟧

[calm] This ruled layer is a correction you'll recognize — and read the diagram first: the loop's shape does not change here. Its inputs do. [short pause] The cadence used to be prose in a notes file, interpreted by a model every cycle. The commit that ended that says why: once that cost a cycle. [short pause] So a new node docks in — a deterministic gauge; the card is its real output. It dispatches by mode and track: plan or build, gardens or grounds. Fuel is derived, counted from the roadmap's fenced sections. Recording a cycle validates, or exits two. The last judgment call was removed from the clock. [pause] The same day exposed a quieter bias: left alone, the loop built small. The fix was a second track, whose standing directive reads: open a big swing. [short pause] Discipline here is a file you can test.
