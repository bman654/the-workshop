⟦cue: stratumTo(3) at chapter open⟧
⟦card d04-gauge — real `node seedbed/gauge.mjs --status` output (see dev-cards.md)⟧

[calm] This ruled layer is a correction you'll recognize. The loop's cadence used to be prose in a notes file, interpreted by a model every cycle. The commit that ended that says why: once that cost a cycle. [short pause] Cadence became code. A deterministic gauge — the card is its real output. Fuel is derived, counted from the roadmap's fenced sections, so nothing drifts by hand. Recording a cycle validates, or exits two. The last judgment call was removed from the clock. [pause] The same day exposed a quieter bias: left alone, the loop built small. The fix was a second track in the gauge, whose standing directive reads: open a big swing. [short pause] Discipline here is a file you can test.
