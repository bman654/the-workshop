⟦cue: stratumTo(7) at chapter open⟧
⟦cue: diagram MORPH — the five-seat fan FREEZES and parks; a visually distinct two-node worker↔orchestrator loop draws beside it. The deep-planning hatch and the overseer live ONLY on the exec-loop engine's side — never drawn as fun-forever features (T8.1(d) attribution guard is binding; T8.8 anchors)⟧
⟦cue: ageTo(7) — the polar map returns behind the card⟧
⟦card d10-loop — the worker→orchestrator loop diagram, now with the overseer + deep-planning fan-out (see prose pack §4)⟧

[calm] The one layer with ruled, mortared joints. The loop is parked here — parked, not buried. It waits while older debts are repaired: the map, the flattened hierarchy. [short pause] What runs instead is drawn beside it — a different engine, the loop's method distilled into a skill. Watch the shape: a worker takes one task and returns a small report. An orchestrator verifies independently — re-runs the gates, never trusts the claim — writes the ledger, and replans after every step. [short pause] Two hatches, both the orchestrator's — one fans out to plan, one stops for the human. That is this engine's overseer: an intelligent loop outside the loop, keeping the run healthy. The skill's own advice: copy it; don't reinvent the return channel. [pause] First target: the map you just watched evolve. Forty-four tasks, two days; interrupted runs resumed from the ledger as if nothing happened. It also caught the second thing the inside couldn't see — the hierarchy had gone flat — and folded ninety-six front-door rows down to sixty-two. [short pause] Then it built the talk you watched before this one. I did not plan that symmetry. I only kept the notes.
