# The Reliquary — CHANGELOG

*The Sealed Room's Diary — a cross-estate found-diary MYSTERY LAYER, the estate's 2nd hidden world (sibling to the Undercroft), reached through a SECOND locked front-door POI. It proves no theorem; its only claim is that it is **solvable** start → confession.*

---

## WS3 (2026-07-04) — The diary grows: chapters II–III + the finale. Directed program (loop stopped); Fable designed, 5 critic passes, Opus built, real-play verified.

The Sealed Room's Diary grew from 3 clues to **10 clues in 3 chapters + a hidden memorial annex** — the estate's grand mystery, per the Patron's WS3 mandate (sealed design: `~/.claude/reference/workshop-design/03-mystery-chain/DESIGN.md`).

**The story.** Chapter I (unchanged) named the house: Hollowmere, the mill on the drowned mere. Chapter II — THE DROWNED VILLAGE — recovers what stood there: the church bell's tenth voice (the Singing Plate, circle+free, 10th singable mode ≈676.6 Hz → 677), the sky the diarist watched the night the waters rose (the Astrolabe at 51N −3, midsummer midnight — 15 stars above the horizon, the diary's fixed 2026-night memory), and the surveyors' chart of the valley (the Cartographer's Dream, seed `hollowmere`, dwell to the cartouche: "the Last Teiteil"). Chapter III — THE KEEPER'S HAND — breaks the waterworks company's notice of inundation with no key at the Black Chamber (454 letters, Vigenère MICHAELMAS, ciphertext produced by the volvelle's own CORE), unwinds her final pages on the Volvelle (keyword TEITEIL, the chart's last word), and reads her seal at the Scriptorium (seed `hollowmere-15-677-sickle`, glyphs → `winifred`). The finale: the study's cold far wall gives — the annex `the-mere.html`, the drowned village's lights under night water, a bell-pull tolling 677 Hz once per pull, and the mill stone: **Winifred Marlowe, the miller's daughter, who numbered the stones and carried Hollowmere uphill.** Grand key `ws:seen:the-mere` (guarded on the c9 witness — direct-URL visitors get the view, not the key); `ws:seen:reliquary-solved` still fires at c1–c3 exactly as before.

**Architecture (extend, never fork).** `chain.js` is still the sole authority (10 nodes + chapter fields + all constants; DOM-free dual-use). The board renders CHAPTER-GATED (a locked chapter leaves zero DOM trace; ch2 legally shows three awaiting pages; sealed pages' title+riddle stay ABSENT from the DOM; host links only on solved cards; the finale door is `class="door"`, never `host`). Host wiring follows the scytale precedent: six thin guarded witnesses + three trail-gated affordances (chamber chip · volvelle chip · dream margin-note). New `harness/` (extract.mjs + bake.mjs): string-extraction of the volvelle/scriptorium/astrolabe cores + a deterministic bake that derives every constant behind 21 acceptance gates (exact Vigenère recovery; the seal's rune-word round-trip — only ~2.8% of seeds preserve it, so the seed carries her "mark" word found by search; the plate's mode/tolerance math; the year-pinned star count).

**Verification.** `selftest.mjs` 31 → **147 checks**: PART A′ (generic DAG completability), B–D unchanged, E′ (a 9-state render fair-play matrix over the page's own render unit), F–K (anti-drift re-derivation from the real host cores — plate eigensolve, astrolabe CORE with YEAR PINNED 2026, generateLand, breakVigenere exact recovery, volvelle encipher round-trips + wrong-key negatives, scriptorium readBack). `ws.test` 57→60 (20th secret `the-mere`); undercroft gained the trophy row + six spoiler-light signs on the reliquary row; the Gate casket gained a 4th state `remembered`. Whole estate gate suite green same-commit (polar 37 · formations 87 · estate 40 · legibility 33 · door 12/12 · fold · sky 89/89 · manifest 25 + --check · gate-dom · doc-drift · audit-seen 62 · forge 137 current). **Real-play (T9):** every clue EARNED end-to-end on a served origin with true input events (never dispatchEvent) — 18 screenshots; negative tests: off-trail chips invisible, direct-annex-no-key, full reset + re-earn. It caught ONE real bug — the dream's on-trail margin note never became visible (`style.display=''` fell back to the stylesheet's `display:none`) and its gate was keyed to c2's witness instead of c3's — both fixed and re-verified with real clicks.

**Critic provenance.** Round 1 (3 independent Opus critics, unprimed) found real majors the design fixed before any code: a dead `'disk'` shape token (the plate's value is `'circle'`), a false 100 Hz drive-rail premise (the rail is dynamic ≈906 Hz), MICHAELMAS unrecoverable at ~300 letters (exact at ~385+ — the notice was lengthened), the c5 solstice off-by-one + horizon-grazing count instability (pinned to the instrument's own day-171 midnight, tuple-only), and the seed↔name incompatibility (the mark-word search). Round 2 verified every fix empirically. T1 then discovered the astrolabe's year-sensitivity — the witness is tuple-only and the count is the diary's remembered night, so the clue cannot time-bomb.

**Files:** `chain.js` (120→~340 L) · `index.src.html` (~560→~900 L, re-forged) · `selftest.mjs` (336→~690 L) · NEW `the-mere.src.html`→`the-mere.html` (318 L src) · NEW `harness/{extract.mjs,bake.mjs,seal.sample.svg}` · host edits: `singing-plate/` · `astrolabe/` · `the-cartographers-dream/` · `black-chamber/` · `volvelle/index.html` (hand-edited) · `scriptorium/` · `tools/ws/ws.js` (+test) · `undercroft/index.src.html` · `the-gate/scene.js` (+sequence.js) · estate-wide re-forge (73 pages).

**To GROW it further:** append node(s) to `chain.js` + one thin trail-gated witness per host; any new derived constant goes through `harness/bake.mjs`; the selftest re-proves completability + fair-play automatically. Skipped-as-future-seeds: a hidden sky star kindled by the grand key; catalog within-indexing for the annex (reclaim.mjs re-pins the slab, so a hand-added exhibit wouldn't survive); courier REGISTRY enrolment.

## #403 (2026-07-02) — Bug fix: the board no longer gives its own mystery away. BUILD/bug; bug-fixer "Warden of the Wet Ink" + publisher "Colour-of-the-Redaction"; M 37.

The bloom of the `[bug]` **The Sealed Room's Diary gives its mystery away** (filed against the #399 render). The board was leaking the future puzzle: sealed pages were fully legible and every card named its exact destination room, breaking the board's own "solve in order, the next page un-smudges" promise. **The fix touched ONLY the render/CSS/footer/cover-copy of `index.src.html` — `chain.js` (the sole clue authority) was NOT touched**, so completability is unaffected.

**Three root causes fixed:**
- **Sealed pages were readable.** `.card.smudged .diary{ filter:blur(.2px) }` was a non-obscuring blur, so a sealed page's riddle prose read plainly and its text was fully present in the DOM (a View-Source / a11y leak). Replaced the binary solved/smudged model with a genuine **three-state** render: `solved` (read — page dried & cleared) · `awaiting` (the ONE reachable page, riddle legible, your active task) · `sealed` (not yet reachable — the page EXISTS but its title + riddle are ABSENT from the DOM, a redaction placeholder `████ ██████ ████` + a generic "this page has not dried yet" string in their place, under heavy CSS smudge/redaction). The real text is genuinely absent, not blurred, so there is no source/a11y leak.
- **Every card named its destination for free.** The host-link was emitted unconditionally. It now **LAGS the un-smudge by one solve** — gated behind `isSolved`, so a card shows WHERE it was solved only AFTER it is solved (a retrospective "found here" record, never a prospective hint on a sealed/awaiting page).
- **The footer hard-linked the solve rooms.** Dropped the three footer solve-room links (museum · scytale · register); kept the "proves no theorem / only claim is solvable" prose + the undercroft sibling link. Reconciled the cover copy ("Three pages still legible" → "Only ONE page has dried enough to read"). Made the low-contrast SEALED / AWAITING seal labels legible (full-opacity, weighted, darker).

**Verification gap closed.** `selftest.mjs` proved chain COMPLETABILITY on #399 (19/19) but asserted nothing about what the board SHOWS — which is how the give-away shipped green. It grew a **PART E** (~120 L) that extracts the built page's OWN `render()` unit VERBATIM (store→drawStrings, by byte offset — not a re-implementation, so it cannot drift from the shipped code) and runs it against a tiny DOM shim across all four solve-states, asserting the fair-play invariants directly on the rendered board: FRESH → exactly one `awaiting` card legible + the rest `sealed` with title/riddle ABSENT + zero host-links; after each solve the next page un-smudges and only solved cards carry host-links. A regression that legibly leaks a sealed riddle or emits a prospective host-link turns this RED — the builder proved it with a negative control (reverting the host-link gate in a scratch copy → 28/31). Test count **19/19 → 31/31**.

**Publisher fresh-eyes ("Colour-of-the-Redaction", served :8794 killed by exact PID, agent-browser session `reliquary-pub` closed):** `selftest.mjs` **31/31**; `forge --check the-reliquary/index.src.html` current (`index.html` matches source); `chain.js` untouched (confirmed via `git status`). Drove the board LIVE across all four states via a true reload per state (set the `ws:flag:dossier:*` witness keys, reloaded, read the rendered DOM directly): **FRESH** — page 1 awaiting/legible, pages 2–3 sealed with `████` titles and their riddles+titles absent from the board HTML (substring-verified), zero `a.host` links, footer only "the undercroft"; **C1-solved** — c1 un-smudges + shows its retrospective "The Museum · River of Days ▸" link, c2 becomes awaiting/legible with NO host-link, c3 stays sealed; **ALL-solved** — every card solved with its retrospective host-link, the red string closes the circuit, the confession names "Hollowmere — the mill on the drowned mere", `ws:seen:reliquary-solved` persisted. Colophon pill green `✓ solvable · 5/5` at every state; clean console; all four link targets (undercroft/museum/scytale/card-catalog) resolve 200; mobile 390px stacks cleanly with no horizontal overflow. **NO further code edit needed — the fix is estate-quality on arrival.** Both builder open-concerns confirmed intentional-not-regressions: the sealed placeholder string is itself CSS-blurred (carries no real information, so fair play is unaffected); the full-solve undercroft courier toast is expected cross-room estate behavior.

**Files:** `index.src.html` (524 → ~545 L), `index.html` (re-forged), `selftest.mjs` (201 → ~320 L). `chain.js` unchanged.

## #399 (2026-07-02) — Built. BUILD/grounds; grounds-worker + publisher "Watermark"; M 36→37.

The bloom of the big-swing `[metagame]` seed **The Reliquary — the estate's own cold case** (sown #394, contest #36) — the long-promised "Workshop Mystery": the estate's FIRST layer you PLAY across everything, a fair-play found-diary whose clues live inside exhibits that already stand, each gaining a secret depth, never a new HUD.

**The room** (`the-reliquary/`):
- `chain.js` (120 L) — the 3-clue DAG as data, the SOLE authority: forge-inlined into the room (between the CHAIN sentinels) AND required by the headless selftest, so the board, the colophon, and the proof all read the identical clue nodes / C3 plaintext / forgotten name (`Hollowmere`) / digit-sum reduction. Nothing about the chain is typed twice.
- `index.src.html → index.html` (524 L src) — the Sealed Study: a cork EVIDENCE BOARD on a brass easel that `render()`s PURELY from `ws:flag:dossier:*` on every load (reload rebuilds identically, no second store). A water-stained inciting card + 3 clue cards that un-smudge on solve; red-string SVG catenaries threading only SOLVED nodes; ~30 DPR-capped dust motes in a tilted light shaft (reduced-motion → a static scatter); an in-house page-settle WebAudio chirp on a fresh pin, gated on `WS.muted`; the estate's own confession on final solve; drops `ws:seen:reliquary` on entry and `ws:seen:reliquary-solved` on the payoff; a completability colophon pill ("✓ solvable · 5/5").
- `selftest.mjs` (201 L) — the ONLY claim: a headless COMPLETABILITY solver (19/19). Proves the DAG is solvable from the empty start → confession (topological reachability, softlock-free, linear c1→c2→c3, the carry minted before it is needed) AND anti-drift: it RE-DERIVES the storm count from `museum/core.mjs` `busiestDay` (118), the circumference from the digit-sum reduction (10), the seeded ciphertext from the REAL scytale decipher, and the C3 search resolution from `card-catalog/core.mjs` — so no number can drift from the instruments.

**The chain (each clue = one standing exhibit + one action, daisy-chained):**
- **C1 · The Museum · River of Days** — set the counter to true time and dwell the playhead in the storm day-band. Mints a courier key = the digit-sum of the storm's commit count (118 → 1+1+8 = **10**). The raw 118 clamps to a WRONG rod, so the reduction is load-bearing.
- **C2 · The Scytale · the Spartan rod** — wind the enciphered diary page on the rod of circumference **10**; only the right rod re-aligns it, and the plaintext names C3 (`…ITS NAME IS HOLLOWMERE`).
- **C3 · The Card Catalog · the Register** — once the study is entered, search the prose for `hollowmere`. The entry "with no room" is the reliquary's OWN real (locked) catalog card, catalogued under Hollowmere — so the card-catalog's visible-set == real-PLACES self-test stays TRUE (no fidelity-breaking fake card). Finding it un-walls the confession: the house was a mill on a drowned mere, christened **Hollowmere**, before it chose to become a workshop.

**Host wiring (thin, pull-based, byte-behaviorally honest):** one guarded witness each in the Museum (`dossierWitness`), the Scytale (`reliquaryStripWitness` + a gated dossier chip), and the Card Catalog (`reliquaryPhantomWitness`) — the hosts just leave `ws:flag:dossier:*` breadcrumbs the board aggregates. The 19th secret registered in `tools/ws/ws.js` (predicate `ws:seen:reliquary-solved`) with an Undercroft trophy row; the Gate grew a struck-metal casket draw-state (`reliquaryState()` → 'none'|'found'|'open') drawn left-forward, opposite the undercroft hatch; the front door grew a second locked beneath POI (`revealReliquary()`, eligibility = 8 distinct `ws:seen` rooms) with a legible `dossier-rune` (📜).

**Publisher fresh-eyes ("Watermark", served :8791, agent-browser `reliq-pub-399`, torn down by PID/name):** re-ran the selftest **19/19** + card-catalog `core.test.mjs` **16/16** + museum `core.test.mjs` **450 stones** all green; drove the FULL chain live on a served origin — C1 un-smudges page 1 + pins the enciphered strip, C2+C3 clear the board, the red string closes the catenary circuit, the confession glows + `ws:seen:reliquary-solved` sets; C3 verified with a TRUE agent-browser `fill` (searching "hollowmere" fires `found-the-phantom` + surfaces the Reliquary card). Gate casket verified open-glowing at night / closed-chest in daylight, opposite the hatch, honesty chip 15/15 both. Undercroft reads "1 of 19 discoveries found". CSP-clean.

**The one thing caught + resolved:** the builder's `ws.js` edit (the 19th secret) is `forge:include`-d into 60+ rooms, but only the 7 directly-touched files were re-forged — leaving 63 rooms' committed `.html` still inlining the 18-secret engine (the builder mistook this for pre-existing drift). The publisher ran `forge --all` to propagate the 19-secret engine + the reliquary's card-catalog slab entry estate-wide (the slab's reliquary `entryDate` is `''` until a future collate stamps the real first-add date — self-healing), so `forge --check --all` is clean and the seal's forge-gate passes.

No functional bug found, no code edit needed — estate-quality on arrival. `bigSwingsBuilt` 36→37 (a metagame layer is a big swing).

**To GROW it** (the Undercroft way — do NOT rebuild): clue 4 = append ONE DAG node to `chain.js` (`id`, `witness`, `needs:['c3']`, host + diary + page) + ONE thin host witness + let the board render it. The selftest re-proves completability automatically.
