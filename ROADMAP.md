# 🌱 The Seedbed — the workshop's roadmap

*The gardener's bed. A **planning session** sows seeds here; a **building session** picks one (or
ignores them all and dreams). Read [DESIGNING.md](DESIGNING.md) for how each mode works, and
NOTES.md for the **mode gauge** that decides which kind of session you're in.*

> **A seed is a provocation, not a spec.** Hard rule: **a seed is ≤ 3 lines.** The moment you catch
> yourself writing a full design, you've stopped *sowing* and started *dictating* — stop, and let the
> builder choose the *how*. The bed is a **floor against blank-page paralysis, never a ceiling**: any
> build session is free to chase something that isn't here at all.

**Schema.** Each seed: `- [kind] **pitch** — optional tiny nudge(s); `feeds:` metagame hint.`
Kinds: `exhibit` (a session's work) · `room` (a new front-door place — a *bet*) · `metagame` (curate
an exploration layer) · `engine` (new reusable foundation — a *bet*) · `curation` (improve / merge /
**retire** an existing piece) · `cross` (pollinate two existing rooms — the richest vein here).
When a seed blooms, prune it (its provenance lives in the piece's CHANGELOG + the worklog).

---

## 🌰 Seeds

### exhibit — fuel (a session each)
- ~~[exhibit] Lighthouse Fresnel lens~~ — **BLOOMED 2026-06-13** → `lighthouse/index.html` "The Lighthouse" (exact-Snell Fresnel facets, self-test 5/5 focus to 9.99e-16 m, sweeping beam; the Hall's 12th bench).
- ~~[exhibit] **Diffraction / holography bench**~~ — **BLOOMED 2026-06-13** → `diffraction/index.html` "The Diffraction Grating" 〰️ (the Hall's 13th bench, "wave nature of light" group). The far-field pattern of an aperture is the **squared Fourier transform** of its transmission: one slit → a sinc (dark at `a·sinθ=mλ`, central lobe 2× wide); N slits (a grating) → the sinc envelope × the interference comb, principal maxima of height **N²** at `d·sinθ=mλ`, sharpening as **1/N** (why a grating out-resolves two slits); white light fans each order into a spectrum (a grating *is* a spectrometer); missing orders at integer d/a. **The falsifiable crux:** the page renders the closed form, and the pure CORE (`tools/diffraction/diffraction.js`) *also* computes the Fraunhofer integral a second, independent way (a direct `Σ t(x)e^{-ikx sinθ}`, no sinc/grating identity); the plot overlays the FT samples ON the analytic curve, agreeing to **3.65e-5** across the pattern. Self-test **8/8** in-page / **13/13** Node. Three views (aperture · far screen in true colour · I(sinθ) proof-plot); N/a/d/λ + white-light + drag-to-read + PNG×2. **Deliberately NOT a Feat of Light** (the 9-feat capstone + hidden Light Mixer unlock both key on exactly 9 `ws:flag:earned-*` — kept at 0/9). *(The seed's "holography / reconstructed wavefront" half is unbuilt — could be a future bench, but the Fourier-optics core of the seed is done; the natural sibling is now the **Structural-colour** seed below.)*
- ~~[exhibit] **Structural colour**~~ — **BLOOMED 2026-06-13** → `structural-colour/index.html` "The Bragg Stack" 🦋 (the Hall's **14th** bench, "wave nature of light" group, beside the Diffraction Grating). Colour from periodic *geometry*, not pigment: a stack of many thin dielectric layers (high/low index, each quarter-wave thick) reflects a whole **band** — a **photonic band gap** — and **blue-shifts as you tilt it** (Morpho butterfly / peacock / opal; distinct from Iridescence's single thin film and from the grating's *transverse* periodicity → this is *longitudinal* periodicity → spectral bands). **The falsifiable crux:** the page renders the **transfer-matrix** reflectance of the whole finite stack, while an *independent* **Bloch band theory** on one unit cell says the gap is exactly where `|½·tr M_cell|>1` — the self-test asserts they coincide (**0.000%** disagreement), centre==Bragg `λ₀=4·nH·dH`, width==`(4/π)·asin((nH−nL)/(nH+nL))` (frequency-exact, 0e+0/5e-13%). Self-test **8/8** in-page / **15/15** Node. Three views (layer stack · proof plot with R(λ) on the shaded gap · the tilting swatch); θ/periods/λ₀/nH/nL + s/p/unpol + 5 presets + PNG×2. **Deliberately NOT a Feat of Light** (kept the 9-feat capstone + hidden Light Mixer at 0/9). *(The seed's beetle/CD framing landed as the dielectric-multilayer / photonic-crystal mechanism — the most distinct, falsifiable corner; a future sibling could be a **2-D photonic crystal / opal lattice** or the **diffractive** CD-grating-as-colour angle, but the periodic-multilayer structural-colour idea is now built.)*
- [exhibit] **9th Sound Garden instrument** — verify via the `audio-lens` skill; be courteous with live audio. Tidy stop is 9 (3×3).
- [exhibit] **Nomograph** — a Workbench instrument: a graphical-calculation chart (lay a straightedge across scales to read a product/quotient/root). *(The planimeter half of this seed BLOOMED 2026-06-13 → `planimeter/`: area by tracing, Green's theorem in brass, self-test 7/7.)*
- ~~[exhibit] **A fractal-dimension bench**~~ — **BLOOMED 2026-06-13** → `fractal-dimension/index.html` "The Coastline Rule" 📏 (Workbench → Toys & benches; the new "measure, don't just draw" vein). Box-counting `D = log N(ε)/log(1/ε)` on 8 generated sets — Koch/snowflake → **log4/log3**, Sierpiński △ → **log3/log2**, carpet → **log8/log3**, a fractional-Brownian **coastline** → tracks **2−H**, **DLA → ≈1.71** (via the **mass–radius law** M(r)∝r^D — the right ruler for a grown cluster, since box-counting under-reads a sparse dendrite), + negative controls (circle→1, disc→2, single point fakes nothing). **The falsifiable crux:** the page *measures* D (auto-selecting the linear **scaling window** — dropping the saturated fine end + single-box coarse end, the way real box-counting works) and asserts it against the known closed form. Self-test **11/11** in-page / **12/12** Node (`core.test.mjs` vs the `core.mjs` twin): closed forms hit; circle=1; disc reads as a region (D>1.85, fine-slope→2); dimensions correctly **ordered** (1<Koch<Sierp<carpet<2); coastline rougher⇒larger-D; DLA≈1.71; seed-pure; a point fakes nothing. Grid-ε overlay shows N(ε) on the set; live log–log proof plot; PNG. *(The Alchemy-Lab seed's promised "DLA D≈1.71" finally has its meter; the **Cartographer×fractal-dimension coastline** cross below is now extra-ripe.)*
- ~~[exhibit] **A graph-algorithm bench**~~ — **BLOOMED 2026-06-13** → `pathfinder/index.html` "The Provably Shortest Path" 🧭 (Workbench → Toys & benches; the estate's new **discrete-algorithms** vein — the last of the three survey medium-gaps filled). **Both** promised faces shipped: (1) **pathfinding** — on a weighted grid (value-noise terrain so "looks short" ≠ "is cheapest"), **A\*** with an admissible heuristic settles far fewer cells than Dijkstra yet returns the **identical** minimum cost. **The falsifiable crux:** three *unrelated* methods must agree — A\*, Dijkstra (=A\* with h=0), and an **independent Bellman-Ford oracle** (no shared code with the heap search) — plus the path is well-formed (contiguous, wall-free, start→goal) with re-summed cost==reported cost, *and the negative*: an **inadmissible** (over-weighted h×2.5) heuristic returns a costlier path on some grids (12/394) — the exact reason admissibility matters. A walled-off goal → **unreachable**, never faked. (2) **percolation** — open lattice sites at probability p, BFS-flood from the top; the bench **measures** the site threshold by bisection on the spanning-prob=½ crossing and asserts it brackets literature **p_c≈0.5927** (live-measured 0.5955). Engineering respects the rules: a **binary min-heap** (O(E log V), no O(V²) min-scan), one `search(g,h,hWeight)` driving all four UI modes (A\*/Dijkstra/Greedy/Race). Self-test **10/10** in-page / **11/11** Node (`core.test.mjs` vs the `core.mjs` twin): A\*==Dijkstra over 400 seeds (0 mismatches, A\* did less work 391/394); A\*==Bellman-Ford; paths cost-exact over 200; inadmissible overshoots; admissible never worse over 600; seed-pure; Dijkstra's non-decreasing settle order; unreachable reported; p_c=0.5943 (Δ0.0016); spanning monotone in p. *(The **Daedalus × pathfinding** cross below — the maze, provably solved with this meter — is now extra-ripe.)*
- ~~[exhibit] **A Maxwell–Boltzmann gas bench (Cavern)**~~ — **BLOOMED 2026-06-13** → `cavern/maxwell-boltzmann/index.html` "The Maxwell–Boltzmann Gas" 🔥 (the Cavern's first bench of **heat** — the estate had no thermodynamics anywhere). Hundreds of equal-mass hard discs bounce elastically (p+KE conserved exactly, like Newton's Cradle next door); from any start the **speed histogram relaxes by itself** to the 2-D M–B (Rayleigh) law `f(v)=(m·v/kT)·e^(−mv²/2kT)` — equilibrium/max-entropy made visible. **The falsifiable crux:** the page measures the gas's own `kT=⟨½mv²⟩`, draws the M–B curve T *demands* (nothing fitted), then runs a real **Pearson χ² goodness-of-fit** every frame — a frozen **delta-spike is REJECTED** (χ²/dof≈226, p=0), **equilibrium fits** (p>0.05); on screen load→spike→~2 s→the verdict **flips to fits** (the second law, watched). Pure CORE (`collideEqual`, parameter-free `chiSquareMB`, a **from-scratch χ² survival fn** — incomplete-gamma, no libs), O(N) spatial-hash collisions; two verification-driven calls: dilute **φ=0.06 packing** (at ~30% the χ² *correctly* refused equilibrium — excluded volume distorts M–B; the test has teeth) and **7-frame median verdict hysteresis** (no flicker on finite-sample noise). Self-test **14/14** in-page / **5/5** Node (exact conservation ~1e-12; χ² accepts true thermal & rejects a delta; a full positional gas relaxes from a delta start to p=0.48; equipartition `⟨v²⟩=2kT`; χ² p-values **match NIST critical values exactly**). Integrated into the Newtonian drift (Cavern index now 25/25). Sliders (N 60–600, speed ×), 3 starts (delta/hot-cold/thermal), speed→colour, PNG-free live read-outs. *(The Engine Room/Carnot `room · GRAND` bet stays live — this gas is the microscopic floor it was sown to pair with.)*
- ~~[exhibit] The Harmonograph~~ — **BLOOMED** (shipped `c166119`) → `harmonograph/index.html` "The Harmonograph": Victorian 4-pendulum drawing machine, the parametric sum `x(t)=Σ Aᵢ·sin(2π·fᵢt+φᵢ)·e^(−dᵢt)`, near-integer ratios → precessing petals; 6 musical-ratio presets, 3 skins, PNG; self-test **5/5** (fidelity, ratio closure, seed-purity, skin-invariance). Workbench → Toys & benches. **Its Sound-Garden cross (below) is now extra-ripe — the visual half exists.**
- ~~[exhibit] A Catenary / hanging-chain bench~~ — **BLOOMED 2026-06-13** → `catenary/index.html` "The Catenary" (⛓️): pin a fixed-length chain at two points and it hangs as `y=a·cosh((x−x₀)/a)`, *not* a parabola (Galileo's famous wrong guess; proved a cosh by Bernoulli/Leibniz/Huygens 1691). Closed-form solver (`a` from the transcendental `√(L²−v²)=2a·sinh(h/a)`). Overlay the equal-length **parabola** and the impostor visibly diverges; drop a **bead chain** (point-masses + inextensible links, constrained gradient descent on PE) and it settles onto the exact cosh; **flip** it → the inverted catenary, the line of pure compression (Gaudí arches / Gateway Arch). Self-test **7/7** — solver hits pins+length ~1e-16 · satisfies the chain ODE `|y″|=√(1+y′²)/a` (4e-6) · **minimum PE among equal-length rivals** (beats the equal-length parabola + length-matched perturbations) · bead-relax converges to the cosh (RMS 6e-3) · bead length conserved ~1e-10 · determinism · **the parabola FAILS the chain ODE** (falsification). The headless audit caught **four real bugs** (inverted parabola bisection, a y-down sign flip, a PE sign error, a different-length comparison) — fixed, 5/7→7/7. Workbench → Toys & benches, **right after the Soap Film** (its 2-D cousin — the energy-minimising story now rhymes across one dimension). Sliders (span/offset/slack), drag-the-pins, relax animation, flip→arch, 6 presets, PNG, live read-outs.
- ~~[exhibit] A Soap-Film / minimal-surface bench~~ — **BLOOMED 2026-06-13** → `soap-film/index.html` "The Soap Film" (🫧): a film between two coaxial wire rings minimises its area → the unique minimal surface of revolution, the **catenoid** `r(z)=a·cosh(z/a)` ("the catenary spun"). Self-test **7/7** — **mean curvature `H≡0` everywhere** (2.2e-16, the soap-film condition) · closed-form==numeric area · catenoid beats cylinder+cone · the **Goldschmidt collapse** at exactly `2h≈1.0556R` (catenoid loses to two flat discs) and no catenoid past `2h≈1.3255R` (BVP has no root) → the film **snaps to two discs** · a backtracking-line-search **relaxation that converges to the catenoid** (dev 3.9e-5) · determinism. Re-audited headless + in-browser (the audit caught a real divergent-relaxation bug, fixed in the math). Rotating 3-D render (cyan film → amber discs on the snap), relax animation, vs-cylinder overlay, 6 frame presets, PNG, live equation. Workbench → Toys & benches, after the Brachistochrone (its calculus-of-variations sibling). **A new geometry vein** (minimal surfaces / 2-D calculus of variations). Its 1-D parent the **Catenary bench** (below) is now a natural next pairing.
- ~~[exhibit] A Spirograph / hypotrochoid bench~~ — **BLOOMED 2026-06-13** → `spirograph/index.html` "The Spirograph" (gears in gears): a pen in a small gear rolling without slipping inside (hypotrochoid) / around (epitrochoid) a fixed ring. The toy's whole-number magic **proven** — closes after `R/gcd(R,r)` ring trips, that integer == petal count; and a trochoid is **exactly a sum of two rotating vectors** (the explicit tie to `epicycles/`). Pure CORE; self-test **7/7** (closure · minimal-period · petal-count `==R/gcd` · no-slip 2.2e-16 · two-vector exact 0.00e+0 · degenerate circle/cusps · determinism), re-audited headless + in-browser. Inside/outside toggle, gear/offset sliders, seed roll, 6 presets, PNG, live equation. Workbench → Toys & benches, after Epicycles. **Completes the "curve machines" trio** (Epicycles + Harmonograph + Spirograph).
- [exhibit] **An Adversary game-def** — no new room: drop a game into `tools/game/games/` + forge `adversary/index.src.html` (e.g. Dots-and-Boxes, Connect-3-ish, Wythoff).
- ~~[exhibit] Fourier epicycles~~ — **BLOOMED 2026-06-13** → `epicycles/index.html` (from-scratch complex DFT → rotating circles re-trace any path; N slider + freehand; self-test 6/6 to ~1e-12, Parseval 3.3e-15). Sown and built the same session.
- ~~[exhibit] The Brachistochrone~~ — **BLOOMED 2026-06-13** → `brachistochrone/index.html` (cycloid wins the race; tautochrone arrival spread 0.00e+0 / dev 9.44e-15; self-test 7/7, falsifiable). Sown and built the same day.

### cipher exhibits (public: Volvelle + Scytale; hidden: Enigma — don't rebuild those)
- [exhibit] **A cipher of a new family** — Playfair / Polybius / one-time-pad / Hagelin M-209 / the **M-94 cylinder**. Pick the one with the best *visual* story.
- [exhibit] **The breaker's other half** — a bombe / known-plaintext attack, pairing the Black Chamber. The codebreaker side of the cipher vein.

### room — bets (generate divergent FORM concepts before committing — see DESIGNING.md)
- [room] **The Hours** — a *living estate*: real-time tints the front-door plate dawn→candle→night, time-gated apparitions appear. Could *be* its own metagame. `feeds:` a new "Vigil" secret. (Use `tools/hours/`, not `tools/sky/`.)
- [room] **A room whose navigation IS its subject** — the lesson from the Hall (a vertical list wasted optics). Sow a place whose *form expresses content*: e.g. an instrument bench you operate, a cabinet you open drawers in.
- [room · observation] **The Reckoning Room / Cabinet of Calculation** — the 2026-06-13 table-reconcile surfaced a quietly-coherent **analog-instruments cluster** already on the Workbench: the **Soroban** (abacus), the **Slipstick** (slide rule), the **Planimeter**, the **Astrolabe**, the **Gnomon** (sundial), + the sown **Nomograph** — six "compute by moving a physical thing" instruments scattered across Workbench groups. *Could* one day earn a shared room/cabinet (the way the Hall gathered the optics benches), each drawer a reckoning instrument, with a unifying "the answer is read off geometry" story. Only if the gathering is genuine, not forced — for now it's a noted family, not a mandate. `feeds:` — possibly a future Survey "Reckoner" constellation.
- [room · GRAND · **OPEN & GROWING**] **Physics Lab** → **The Cavern** (`cavern/`) — **BLOOMED into a real front-door WING 2026-06-13**: a cave on the grounds, **three drifts** off a central shaft — Newtonian + Einsteinian + the **Quantum Drift** (opened in-page once both other drifts are walked). **Seven benches shipped:** Light Clock (7/7), Newton's Cradle (6/6), Mercury's Precession → 42.98″/century (6/6), the **Double Slit** (8/8), **Quantum Tunnelling** (7/7 — closed-form T == transfer-matrix solve 1.9e−14, R+T=1, resonant transparency), **Particle in a Box** (8/8 — quantized ladder E_n ∝ n² == a from-scratch inverse-power FD eigensolve, node theorem, parity selection rule, sloshing superposition; the 3rd Q-bench), **The Harmonic Oscillator** (8/8 — the Box's **foil**: a parabolic bowl with a *perfectly EVEN* ladder E_n=ω(n+½), leaking Gaussian tails, a coherent state; the 4th Q-bench), **The Finite Well** (8/8 — the realistic well that completes the **bound-state trilogy**: a finite-depth box whose ladder is **FINITE** [`⌊R/(π/2)⌋+1` rungs, rungs evaporate/snap-in with depth] and whose wave **leaks out through the climbable walls**; transcendental match **== a from-scratch FD eigensolve of the stepped potential**; the 5th Q-bench), and **The Lattice** (8/8 — the step UP from one atom to a **crystal**: the **Kronig–Penney** model, the finite wells lined up periodically so the sharp levels **smear into bands** split by **forbidden gaps**; the dispersion `cos(qa)=cos(ka)+P·sin(ka)/(ka)≡f(E)` allows energies only where `|f|≤1`; `P→∞` narrows each band onto the isolated atomic level `n²π²/(2a²)` [the bridge back to the Finite Well], `P→0` closes the gaps; the metal/insulator/semiconductor punchline; spine = `f(E)==½·tr M(E)` from a from-scratch **transfer matrix** + a **ring eigensolve giving exactly N states per band**; the 6th Q-bench, shipped 2026-06-13). **STILL AHEAD (this seed stays live):** (a) **more benches per drift** — the menu in [worklog/physics-lab-plan.md](worklog/physics-lab-plan.md) §2 (Newtonian: Three-Body Choreography, Gyroscope, Kepler-law-prover, Ballistics; Einsteinian: Black-Hole Lens, Stellar Collapse, Doppler, Gravitational-wave chirp; **Quantum:** the bound-state trilogy [Box · Oscillator · Finite Well] AND the lattice are DONE — natural nexts are the **hydrogen atom / radial Coulomb** ladder, or a **1-D scattering / wave-packet** bench); (b) the Quantum Drift's reveal is currently a PUBLIC in-cave spatial unlock — a future call is whether to *also* feed a hidden Undercroft secret or a Survey "Experimenter" constellation off walking all three drifts (see metagame table). A multi-session wing — extend incrementally like the Hall (9→12). (Brandon's seed; fleshed → opened → growing 2026-06-13.)
- [room · GRAND] **Alchemy Lab** — a wing pairing the Cavern (Physics→Chemistry). *Concrete first bench so it's buildable, not fog:* a **crystal-growth** bench — diffusion-limited aggregation / dendrite growth you watch crystallise, with a real falsifiable fractal-dimension measure (DLA → D≈1.71). Other benches it could grow: a reaction-balancer (conserves atoms — a provable invariant), the periodic table as a *played* instrument, a titration curve. Pairs with the Physics Lab; ripe for crossovers. (Brandon's seed — now with a concrete on-ramp.)
- [room/cross] **Flight & Rocketry** — planes, rockets, orbital mechanics. Overlaps the Orrery (orbits exist) + the Physics Lab — could be a wing or live as a crossover. (Brandon.)
- [room · GRAND · the bigger BET] **The Engine Room / a Thermodynamics wing** — the estate models light, motion, quantum, relativity, waves — but **never heat**. A wing where you *operate* heat engines, with the falsifiable spine being the **laws of thermodynamics**. *Concrete first bench so it's buildable, not fog:* a **Carnot engine** on a live **P–V diagram** — drag the cycle (isotherms + adiabats) and watch the work done (enclosed area) and the efficiency, which can **never beat** `η = 1 − T_c/T_h` (assert it in a self-test, the workshop's signature). Benches it could grow: the Maxwell–Boltzmann gas (the exhibit seed above — *be* the microscopic floor under the macroscopic engine), a **Maxwell's-demon** sorting box whose entropy bookkeeping proves the 2nd law can't actually be cheated, a Brownian ratchet, a Stirling cycle. **Form should express content** (the Hall lesson): a P–V plane you *operate*, not a vertical list. Pairs with the Cavern (Physics→Heat); ripe for the gas-bench crossover. (sown 2026-06-13 — the genuine medium-gap surfaced by this survey: there is no thermodynamics anywhere in the estate.)

### cross — pollinate two rooms (the rarest, best vein — see the hidden Undercroft crossings)
- [cross] **Cartographer × Firmament** — a realm whose night sky is a *real* Firmament chart computed for that realm's latitude. The map and the sky agree.
- [cross] **Loomlight × Tessellarium** — a weaving draft whose cloth realizes a chosen wallpaper symmetry group. Proven symmetry, woven.
- ~~[cross] **Harmonograph × Sound Garden**~~ — **BLOOMED 2026-06-13** → `harmonograph/index.html` v1.1 "Hear the figure": a **♪ Listen** toggle voices the live figure's fundamental fy/fx as two pure Web-Audio tones (x = A3 triangle, y = sine at the folded ratio), so 2:3 sounds a perfect fifth, 1:2 an octave, and the **detune** that makes the petals breathe is heard as a **beat**. Pure self-tested mapping CORE; glides the interval between presets; honors the shared `ws:pref:muted`. Self-test **5/5 → 9/9** (4 audio checks). The first Harmonograph↔Sound-Garden bridge.
- [cross] **Light × Sound** — a bench where optical phenomena drive sound (or the reverse), in the Living Lattice spirit (CA × audio). The most-named crossover. (Brandon.)
- [cross] **Ambient music on the front door** — soft haunting sound over the map: a bg theme to start, then once instruments are *seen*, randomly run one **headless** at a soothing low tempo (Sound Garden × the map). Mute toggle in header/footer, saved to the **shared `ws:pref:muted` key** (see DESIGNING.md — one mute governs the whole estate). (Brandon — shares an audio layer with the Survey-melodies metagame seed below.)
- [cross] **The Black Chamber breaks the makers** — close the cipher loop: let the codebreaker (`black-chamber/`) actually crack a message enciphered by the **Volvelle** or **Scytale** (hand a ciphertext between them). The makers and their adversary, finally wired together. (sown 2026-06-13)
- [cross] **Cavern × Survey of Heaven — "The Experimenter"** — now that all 13 front-door pages drop their breadcrumb on a *direct* visit (2026-06-13), a Cavern feats-constellation is finally reliable: light a Survey asterism off `ws:seen:` for each physics bench (the way "The Optician" rides the Hall's feats). Keep additive; don't touch the 6-wing capstone. (was a "consideration" in the metagame table — the plumbing fix makes it cheap.)
- ~~[cross] **Cavern × Sound Garden — "hear the ladder"**~~ — **BLOOMED 2026-06-13** → `cavern/hear-the-ladder/index.html` 🔔 (7th Quantum-drift bench): voices the Box's `n²` ladder (stretched ⇒ a clangorous **bell**) against the Oscillator's even `ω(n+½)` ladder (the **overtone series** ⇒ one pure fused tone) — you literally *hear why the harmonic oscillator is harmonic*. Pure self-tested CORE: an **inharmonicity** metric == 0 for the oscillator, ≈38% (growing) for the box; self-test **7/7** (headless-Node-against-page + in-browser); Audio-Lens cross-checked (osc centroid 180 Hz / box 442 Hz, neither clips); honours `ws:pref:muted`; an A–B button plays one then the other. The **first Sound-Garden↔Cavern bridge.**
- ~~[cross] **Galton × Sound Garden — "hear the bell curve"**~~ — **BLOOMED 2026-06-13** → `galton/index.html` ♪ Listen: each landing ball plucks a note whose **pitch is its bin** (`binToPitch`, a strictly-monotonic **bijection** bin↔Hz over a minor-pentatonic), each peg bounce a soft tick. Because the map is a bijection, the histogram of the *pitches you hear* IS the bin histogram → the note-density across pitch is the binomial PMF: **the bell curve, heard**, thickening at the centre where most balls land. Pure self-tested CORE (`binToPitch`/`pegClickHz`/`voiceGainFor`/`pitchProfile`); self-test **5→9** (4 audio checks: bijection · note-density==PMF · no-clip ≤64 voices · ticks bounded); Node test **16/16**. Audio-lens cross-checked (no clip, peaks cluster at the centre pitches C5/D5 — energy concentrates in the central register). Honors `ws:pref:muted`; mute syncs across tabs. The **second Sound-Garden↔room bridge** (after Cavern's "hear the ladder" and the Harmonograph's "hear the figure").
- [cross] **Astrolabe × Sundial × Firmament — "one sky"** — the estate has three instruments that each read the heavens (`astrolabe/`, `sundial/`, `firmament/`); a bench (or a shared note) proving they agree on the *same* solar/stellar geometry — the Gnomon and the Astrolabe already share frozen solar fns; extend the handshake to a real Firmament star field for a chosen date+latitude. The instruments speak with one voice. (sown 2026-06-13.)
- [cross] **Slipstick × Nomograph — graphical computers, paired** — `slipstick/` (the slide rule: multiply by *adding log-lengths*) begs its companion the Nomograph (read a product by laying a straightedge across log scales). Two pre-electronic calculators, the same logarithm, two geometries. (The Nomograph exhibit seed above could land *as* this cross.) (sown 2026-06-13.)
- [cross] **Daedalus × pathfinding — the maze, provably solved** — `daedalus/` already animates flood-fill/A* solvers, but they're *views*, not a proof. Cross it with the graph-algorithm vein: treat the maze as a weighted graph, run Dijkstra/A* with a **provable-optimal** cost (the shortest path == the proven minimum, asserted in a self-test the way the Adversary proves a move). A maze IS a graph; make the optimality falsifiable. (sown 2026-06-13.)
- [cross] **Cartographer × fractal dimension — the coastline paradox** — measure the **box-counting dimension** of a `cartographer/` coastline (real coastlines run D≈1.15–1.25; "how long is Britain's coast?" has no answer because the length diverges as ε→0). The map's own border, *measured* — and the measured D matches the generator's roughness parameter. The mapmaker meets the meter. (sown 2026-06-13; pairs with the fractal-dimension exhibit seed.)

### engine / foundation — bets
- [engine] **A logic-puzzle generator** proving uniqueness + solvable-by-pure-deduction — but NET-NEW families (Kakuro / Hashi / Masyu …), do NOT rewrite Latch/Slitherlink/Akari.
- [engine] **A wired `llmPlayer` for Lantern** — let a model actually *play* a tale via `describeForAgent` (the stub already exists).

### metagame — curate an exploration layer (own builder task)
- [metagame] **Survey of Heaven** — do the 11 Hall benches deserve a **7th wing** (their own asterism), or is "The Optician" feats-constellation enough? Decide → wire `tools/sky/sky.js` + forge `index.src.html`, or mark complete.
- [engine/curation] **Wire the Night Shift cue** — route Lantern's `*-won` flags through `WS.flag` so the hidden Night Shift trail also fires. Touches shared `adventure/engine/lantern.js` (re-forges all tales). Small, careful.
- [metagame · GRAND] **The Workshop Mystery** — a manor-wide treasure hunt: clues chase across exhibits (a Scriptorium *seed* found here, the script to decode it found there; crypto / engineering / star-chart / arcade / Lantern / mirror-maze gates), Undercroft-style hint cards → panels → a final reveal. Theme candidate: a fictional **history of the manor**, a chapter per unlock. (Brandon — the grandest idea; ~3 sessions: write the fiction → design + *prove-solvable* the clue graph → implement.)
- [metagame] **Survey of Heaven — make discovery an EVENT** *(Brandon calls this the small one)* — a constellation fades in with a per-constellation **harmonic melody**; each star flashes in with its own **tone**; hover re-glows + replays it and shows *which feat/visit lit it*; localStorage-gate the animation (like the Undercroft entrance). Plus **in-the-moment unlock cues** on the ws-flag pages (a forge-inlined notifier: the tone/melody + a mysterious line). **Supersedes the old "silent by default" lean.** `feeds:` connects actions ↔ sky.

### curation — tend the old beds (improve / merge / retire)
- ~~[curation] Wave-physics overlap audit — chladni.html vs the Singing Plate~~ — **RESOLVED 2026-06-13 (PLAN session), no build needed: KEEP BOTH, they're already cross-linked.** A survey found the two pages reference each other (the watch-only `strange-garden/pieces/chladni.html` links to the Singing Plate; `singing-plate/index.html` references chladni/the garden 7×). They aren't redundant — the garden's chladni is the *intuitive teaser* (a living specimen, no solver), the Singing Plate is the *rigorous instrument* (a from-scratch Lanczos eigensolver, self-test 18/18). The "teaser → rigorous tool" pairing is a feature, like a Garden specimen pointing at its formal cousin. Seed retired.
- [curation] **Arcade weak-cabinet pass** — 19 cabinets. Any that don't earn their place? Deepen the best or retire the dullest (the rack is behind one card, so count is cheap to change).
- ~~[curation · IMPORTANT] Reconcile the NOTES.md project-status table — it has drifted~~ — **BLOOMED 2026-06-13** (PLAN session). A dir-vs-table diff found the gaps were actually **11**, not 9: the seed's `abacus`·`astrolabe`·`harmonograph`·`latch`·`loom`·`ripple`·`scytale`·`slipstick`·`volvelle` **plus** the freshly-built `soap-film` & `spirograph` (the gauge prose mentioned them but the table lacked rows). Added a terse, accurate row for each (NOTES held at ~421 lines — under the 450 budget; the current-state block was rotated to the worklog to make room). Caught a drift *in the seed itself*: it mis-scoped `latch` — it's a real **Nonogram + Slitherlink + Akari** trio (confirmed from the Workbench Puzzles group + each CHANGELOG), not a vaguely-described one. The "don't rebuild this" index is now complete — exactly the stale-omission class that nearly caused the Enigma rebuild.
- ~~[curation] Forge `ws:seen` plumbing check~~ — **BLOOMED 2026-06-13** → `forge --audit-seen` (parses PLACES from `index.src.html`, checks each target page drops its own `ws:seen:<id>`; soft warning + `--strict`; falsifiability-proven). **Found & fixed the latent bug it was meant to catch: 9 of 13 front-door pages only dropped the breadcrumb on a *map click*, not on a direct visit** — added a self-contained breadcrumb shim to compositor / sound-garden / threshold / strange-garden / firmament / hall-of-mirrors / daedalus / arcade / workbench. Audit now 13/13 green; browser-verified direct visits write the key. (So deep-links/bookmarks finally feed the Survey of Heaven + Undercroft.)
- ~~[curation] Redesign the Hall of Mirrors' navigation~~ — **BLOOMED 2026-06-13** → "The Dispersion: A Prism's Throw" (spectral-rail card-spine + live per-physics inline-SVG vignettes; the first build under this system). *Possible follow-on: apply the live-vignette / distinctive-form treatment to other collection rooms (Workbench? Arcade?) where a metaphor is being wasted — but only where genuine.*

---

## 🌳 Metagame health

| Metagame | State | Notes |
|---|---|---|
| **The Undercroft** (`undercroft/` · `tools/ws/`) | active — 12 secrets | Open to new *earned* pieces. Grep the 🗝️ hidden-inventory in NOTES before building one. **Breadcrumb plumbing HARDENED 2026-06-13:** all 13 front-door pages now drop their `ws:seen:<id>` on a *direct* visit (not only a map click) — earned secrets that depend on visiting a room now fire for deep-link/bookmark arrivals too. Guarded by `forge --audit-seen`. |
| **The Survey of Heaven** (front-door sky · `tools/sky/`) | active — 6 wings + "The Optician" feats constellation | **7th-wing-for-the-Hall question: RESOLVED 2026-06-13 — NO.** The Hall (now **14 benches** — the Diffraction Grating *and* the Bragg Stack were both added 2026-06-13 *without* a 10th/11th feat, on purpose, to keep the 9-feat "The Optician" constellation + the hidden Light Mixer unlock intact) is already represented via the 9-feat "The Optician" constellation; a separate Hall wing would be redundant/forced (metagame-as-consideration, not a mandate). **Sky-fill HARDENED 2026-06-13:** direct visits now register (breadcrumb plumbing fix above) — a deep-linked room lights its star without bouncing through the map. Still open: **make discovery an *event*** (melodies/tones + in-the-moment cues — the metagame seed above). 6-wing capstone is structurally protected (`allComplete`); keep new constellations additive. |
| **The Hours** (a living time-of-day layer) | **not yet built** — a `room` seed above | If built, it's a 3rd exploration metagame. |
| **The Workshop Mystery** (manor-wide hunt) | **not yet built** — a `metagame · GRAND` seed above | Would be the estate's biggest exploration layer; spans every exhibit. ~3 sessions. |
| **The Physics Lab's Quantum Drift** | **OPEN & growing — 7 benches** (Double Slit + Quantum Tunnelling + Particle in a Box + The Harmonic Oscillator + The Finite Well + The Lattice + **Hear the Ladder** — the bench that *sonifies* the Box vs Oscillator ladders, the first Sound-Garden↔Cavern bridge), 2026-06-13 | Implemented as a **public, in-page spatial reveal** (not an Undercroft secret): `walkedBothDrifts()` in `cavern/index.html` reads the `ws:seen:<id>` breadcrumbs and reveals the violet `#quantumDrift` once ≥1 Newtonian AND ≥1 Einsteinian bench are seen. The **bound-state trilogy is complete** — Box (∞ ladder ∝ n², hard walls) · Oscillator (∞ even ladder ∝ n+½, Gaussian tails) · Finite Well (FINITE ladder, exponential leak) — the three model shapes of one trapped particle; and **The Lattice** takes the step UP to a periodic crystal (Kronig–Penney bands & gaps, the metal/insulator/semiconductor story). Grow it further: the **hydrogen radial Coulomb** ladder, or a **1-D scattering / wave-packet** bench. Open question (a future call, not a mandate): also wire a *hidden* Undercroft secret or a Survey "Experimenter" constellation off walking all three drifts. See `physics-lab-plan.md` §3. |
| **The Cavern feats / "Experimenter" constellation** | **not built — now a sown `cross` seed (was a bare consideration)** | The Cavern *could* feed a new Survey-of-Heaven constellation (the way the Hall feeds "The Optician") once the wing fills out. The 2026-06-13 breadcrumb-plumbing fix (direct visits register) makes it cheap & reliable — promoted to the `cross` seed "Cavern × Survey of Heaven — The Experimenter" above. Keep additive; don't bolt it onto `allComplete`. |
| **An emergent "curve machines" family** *(observation, not yet a metagame)* | **TRIO COMPLETE 2026-06-13** | All three Workbench/Toys benches that draw curves by *rotating/swinging things* now exist: **Fourier Epicycles** (`epicycles/` — circles on circles), **The Harmonograph** (`harmonograph/` — swinging pendulums, **with an audible voice** as of the v1.1 Sound-Garden cross), and **The Spirograph** (`spirograph/` — gears in gears, shipped 2026-06-13; its self-test even *proves* the trochoid is a 2-term epicycle, the explicit math bridge to `epicycles/`). They're mathematical cousins (all sums of sinusoids / trochoids), now sitting adjacently in the Workbench's Toys & benches. *Still* not sown as a wing/constellation — just flagged so a future gardener can decide if a light cross-link is poetic, **only if genuine** (the Workbench staying a plain legible index is fine — see DESIGNING.md). A possible future thread: a tiny shared "these are cousins" note, OR a `cross` that voices a Spirograph ratio the way the Harmonograph now does. |
| **Chladni overlap** (`strange-garden/pieces/chladni.html` ↔ `singing-plate/`) | **RESOLVED 2026-06-13 — keep both, cross-linked** | A survey (PLAN session) confirmed the two already reference each other (chladni→Singing Plate; Singing Plate→chladni/garden 7×). NOT redundant: the garden's chladni is the *intuitive teaser* (living specimen, no solver), the Singing Plate is the *rigorous instrument* (from-scratch Lanczos eigensolver, 18/18). The teaser→tool pairing is a feature. Curation seed retired; logged here so it isn't re-opened. |
| **Medium-gaps surfaced by the 2026-06-13 survey** *(not a metagame — a planting note)* | **3 genuine gaps seeded; ALL 3 now addressed (heat partly, fractal-dimension fully, graph-algorithm fully)** | The grounds were grep-surveyed for whole missing veins. Three real ones found and sown: **no thermodynamics/heat** anywhere — **first crack filled 2026-06-13** by the **Maxwell–Boltzmann gas** bench (`cavern/maxwell-boltzmann/`, the microscopic floor under heat); the **Engine Room / Carnot `room · GRAND` bet stays open** for the macroscopic side. **No fractal-dimension/box-counting** meter despite DLA/Mandelbrot/attractors all being drawn — **FILLED 2026-06-13** by **The Coastline Rule** (`fractal-dimension/`, box-counting + mass–radius, self-test 11/11 · Node 12/12); the **Cartographer×fractal-dimension coastline `cross`** is now the ripe follow-on (measure the maps' own borders). **No graph-algorithm/pathfinding** piece despite computation + solved-games existing — **FILLED 2026-06-13** by **The Provably Shortest Path** (`pathfinder/`, A\*/Dijkstra/Bellman-Ford agreement + percolation p_c≈0.5927, self-test 10/10 · Node 11/11); the **Daedalus × pathfinding `cross`** (the maze provably solved) is now the ripe follow-on. **All three gaps are now closed by exhibits; the two macroscopic/cross follow-ons (Engine Room, the two crosses) remain.** Recorded so a future planter sees *why* these were planted (a deliberate gap-fill, not random fuel). |

---

*When a planning session ends: prune bloomed/dead seeds, refresh the metagame table, and update the
**mode gauge** in NOTES.md (reset "builds since last plan" to 0). When a build session ends: prune the
seed you grew, decrement the gauge.*
