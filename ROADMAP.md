# 🌱 The Seedbed — the workshop's roadmap

*The bed the cadence draws from. **Run `node seedbed/gauge.mjs --status` first** — it counts the live
seeds here, reads the durable counters, and tells you this cycle's mode × track. How each role works →
[DESIGNING.md](DESIGNING.md); the full gauge model → [seedbed/README.md](seedbed/README.md).*

> **A seed is a provocation, not a spec.** Hard rule: **a seed is ≤ 3 lines.** The moment you catch
> yourself writing a full design you've stopped *sowing* and started *dictating* — stop, and let the
> builder choose the *how*. (Over-specified seeds are *executed*, not ripened — that's how the estate
> goes deep-not-broad.) The bed is a **floor against blank-page paralysis, never a ceiling**: any build
> is free to chase something not here at all.

## Schema

**Two tracks.** **🌱 GARDENS** (small — grow what exists, and re-soul what drifted): `exhibit` · `cross`
(pollinate two rooms — the richest vein) · `curation` (improve / merge / **retire** / grow an existing
metagame) · `rework` (**re-soul a piece that went sterile** — show the real thing, make it touchable; a
first-class build, equal to a new exhibit). **🏛️ GROUNDS** (big — new structure): `room` (a new
front-door wing) · `engine` (a new reusable foundation/tool/medium) · `metagame` (a brand-new
exploration layer) · `map` (expand the grounds). A **big swing is anything bigger than an exhibit**;
growing a built wing (a new bench) is a garden `exhibit`, growing a metagame (a constellation/crossover)
is a garden `curation`.

**Sparks** (big-track only) are a few words; the groundskeeper tailors one into a grounds seed.

**The soul the bed serves.** The estate turns math/science INTO art, sound, play, and **things you can
touch** — judged by *five questions* (fun? · beautiful? · if-math, provably-correct? · discoverable? ·
fits the aesthetic?), with "prove it exact" one beloved register *in moderation*, never the gate. Sow
for **variety of form** (touchable depictions · generative art · living sims · games & puzzles · the
occasional graph) — *show the thing, not its plot* — never a graph-monoculture. The gardener also
**audits** the estate and marks ~1 piece a cycle for `rework`. Full story →
[seedbed/README.md](seedbed/README.md) "the soul & the audit".

**Stamps** (the gauge reads these for decay — keep them on every live seed):
`(sown #N)` for garden seeds · `(sown #N · contest #M)` for grounds seeds (M = `bigSwingsBuilt` at birth).

**Prune two ways:** a seed that ships **blooms** → prune it, provenance to the piece's CHANGELOG + the
worklog (so it's never rebuilt). A seed that goes stale **decays** → prune it **clean, no tombstone**
(a decayed idea is free to return when the estate grows into it).

**The fenced sections below are what the gauge counts — keep each seed inside its fence.**

---

## 🐞 Bugs — clear these first
*(An open `[bug]` jumps the queue; the gauge routes it to a bug-fix BUILD before anything else.)*

<!-- gauge:bug:start -->
- [bug] Tabularium has gone fallow and its twin has gone RED.  The makers have stopped repairing its claim or updating its room list.  Restore its state.  Prevention options: (a) modify collate.sh to reforge it from the source data (ledger.jsonl) in the same way as ledger/face and find a true source to reforge its list of rooms.  (b) codify the ritual of updating it each cycle in NOTES.md and leave a seed in ROADMAP.md to record the holy maintenance ritual as a side-chamber from the Tabularium exhibit to memorialize the ritual for the public.   
<!-- gauge:bug:end -->
<!-- FIXED #80 → the Hours front-door gnomon dial was DEAD for a real physical mouse (3rd attempt; #75 dismissed it, #77's 10px-euclidean fix missed the real cause). ROOT CAUSE (event-routing, NOT astronomy/geometry): the no-scrub tap relied on a `click` MouseEvent bubbling into the `#hours-gnomon` subtree, but the browser dispatches `click` on the nearest common ancestor of the differing mousedown/mouseup hit-targets — which for this overlaid dial resolves to `svg#sheet`, OUTSIDE the gnG subtree — so the gnG-scoped `click` listener NEVER fired for a real pointer (programmatic `.click()`/agent-browser dispatch STRAIGHT to the node, which is the documented trap that let #75/#77 verify green yet ship broken). FIX = navigation now happens in `endDrag(ev)`, the window `pointerup` handler, on a path INDEPENDENT of any `click` MouseEvent: moved `var GNHREF` ahead of endDrag; `endDrag(ev)` reads `wasScrub = scrubbed || (ev && pastThreshold(ev))` BEFORE removing the dragging class (the boolean source of truth, immune to class removal), tears down listeners, then if `!wasScrub` runs `WS.seen("gnomon")` + `location.href = GNHREF`; the dead gnG `click` listener was DELETED entirely. Keydown (Enter/Space nav · arrow/page scrub), dblclick run-the-day, and pointerdown left untouched. Edited the forge SOURCE `index.src.html` (~L2347) + re-forged `index.html` via `node tools/forge/forge.mjs index.src.html` (both committed; `forge --check --all` 31/31 current). PUBLISHER FRESH-EYES VERIFICATION (session gnomon80-pub, served 127.0.0.1:8771, browser closed by exact session name + http server killed by exact PID 92802, port confirmed free — Brandon's :3001/:4380 untouched) — the ONLY valid method per the seed's trap: NOT a lying `.click()` but a true synthetic physical-mouse dispatch + reasoning. TEST 1 (no-scrub tap: synthetic pointerdown on the dial → pointerup): a `beforeunload` hook fired (proving `location.href = GNHREF` executed on the pointerup/endDrag path, independent of any click event) and the page navigated end-to-end to `http://localhost:8771/hours/index.html`. TEST 2 (a 40px scrub: pointerdown → pointermove +40px past the 10px SCRUB_PX → pointerup): `navAttempt = false`, stayed on the front door — a scrub correctly does NOT navigate. Served-HTML assertions: deadClickListenerPresent=false · endDragNavigates=true · pointerupWiredToEndDrag=true · keydownEnterNav=true · dblclickRunDay=true. The gnomon astronomy self-test re-ran GREEN via the exposed `Hours.gnomonShadow` (day shadow exists / night sleeps / noon runs up the line at 0.668°<2° / sweep-sign tracks the sun) — the shadow math is unaffected. 0 console errors · gnomon present with role=slider, tabindex=0 (a11y intact) · 0 horizontal overflow @390 (mobile) AND @1280 · the Hours wing loads clean ("The Hours · The Orrery Estate", h1 "The Hours", no error text). The second, unrelated `endDrag(e)` in the panZoom controller (index.html ~L4819) is a different function in a different scope — confirmed untouched. PUBLISHER ALSO CAUGHT a stale tripwire: the Hours twin `tools/hours/hours.test.cjs` section (e.2) still grepped `index.src.html` for the OLD click-listener wiring (which the fix deleted), so it went 64/65 RED on landing; updated it to lock the NEW wiring (model classifies on `scrubbed || pastThreshold`; greps assert `endDrag(ev)` / the pointerup-path nav / `pointerup` wired to `endDrag` / and that the dead `gnG` click listener is GONE) → re-run **69/69 PASS**, and a future revert to a bubbled-click nav now fails the twin. Final guard sweep all GREEN: hours 69/69 · sky 73/73 · dial 21/21 · Tabularium 10/10 · forge --check 31/31 · layout smoke PASS. A bug-fix cycle sows/blooms no seed. Provenance: the #80 worklog. -->
<!-- FIXED #77 → the two #76 Hours front-door bugs, BOTH root-caused & cleared (scoped fixes in the forge SOURCE index.src.html, re-forged → index.html, forge --check --all all 31 current; gated behind a LIVE in-browser audit @1280 + @390). BUG 1 (the rune-visitor's pinned-night door + permanent grey dawn-mist slab): one flag `forceAllOn` was set true for TWO unrelated reasons — the screenshot param AND the persisted `ws:seen:undercroft-rune` reward — so a rune visitor's door opened pinned at CANON_MIN=1230=20:30/night with the dawn-mist `<rect>` forced to rgba(200,184,216,.16) permanently and catches suppressed. FIX = SPLIT the flag into `pinFrame` (set ONLY by `?hours=allon`/`#hours-allon` — pins the clock, forces every apparition ON, suppresses catch-recording, for deterministic screenshots) vs `runeReward` (the persisted rune flag — clock-NEUTRAL; the earned visitor opens at `localCivilMin()` with the LIVE apparition windows exactly like everyone else, catches recording normally, the reward acknowledged only as honest lore in the gnomon aria-label, never a frozen night). All four use-sites (`curMin` init, reduced-motion init, `on()` apparition-forcing, the catch-skip) re-gated on `pinFrame`. REPRO-VERIFIED LIVE: with `ws:seen:undercroft-rune` set and NO param the door opened at 13:14 (= the live local clock, phase "high noon", NOT 20:30), dawn-mist fill rgba(196,180,214,0) (alpha 0 — no grey slab), wash opacity 0.000; AND `?hours=allon` still pinned 20:30 with dawn-mist rgba(200,184,216,.16) (the deterministic screenshot frame preserved). BUG 2 (the dead dial click — a tap focused but never entered the wing): the `gnG` click handler swallowed the event (`preventDefault`+`stopPropagation`) then self-navigated only `if(!moved)`, but `moved` tripped on any >4px-per-axis jitter so a normal click was mis-classified as a scrub → no navigation, no `<a>` fallback = dead click. FIX = raised the scrub gate to a 10px EUCLIDEAN threshold (`SCRUB_PX`, squared-distance — a diagonal jitter can't sneak past two independent axis thresholds) and made `scrubbed` the single source of truth gating BOTH the live render and the click swallow; the click navigates unless `scrubbed || dragging-class || pastThreshold(pointerup)`. LIVE-VERIFIED: a no-move/3.6px-jitter tap navigated to hours/index.html; a deliberate 60px scrub STAYED on the front door and moved the clock 13:16→09:52; keyboard Enter still entered the wing; double-click still ran the day (13:22→16:22). The Node twin tools/hours/hours.test.cjs was EXTENDED (39→65 PASS) with a pure model + source-grep assertions of BOTH the flag split (e.1) and the click classifier (e.2), so a future re-coupling or a softened threshold fails the twin. layout smoke ALL PASS; in-page front-door pill green (✓ 32/32); audit-seen all 22 pages drop their breadcrumb (incl. gnomon); 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390. A bug-fix cycle sows/blooms no seed. Provenance: the #77 worklog. -->
<!-- FIXED #75 → the two #71 Hours front-door regressions, BOTH cleared (scoped fixes in the forge SOURCE, re-forged, gated behind a live in-browser audit @1280 + re-checked @390). BUG 1 (The Hours front-door day/light layer, index.src.html→index.html): (a) NAVIGATION — audited live; the plain-click→hours/index.html and >4px-scrub-does-NOT-navigate logic was ALREADY working, left untouched. (b) TEXT-SELECTION — added user-select:none(+-webkit-/-moz-) across the whole Hours furniture set (#hours-gnomon/#hours-hud/#hours-ring/#hours-back/#hours-apparitions) so dragging the dial selects zero map text (verified empty selection on a synthetic 12-step drag). (c) LABEL COLLISION — the injected HUD furniture bypassed the label solver and overran the POI sub-line; fix = a module-level HOURS_FURNITURE obstacle array folded into placeLabels()'s obstacles (forcing the gnomon's POI label into the clean slot ABOVE the readout), the HUD slimmed to a compact 2-line clock+phase just above the dial (the redundant "HOURS CAUGHT" line dropped — the gold ring's pr-count already shows it), and the self-test pill relocated BELOW the dial; every POI-label/HUD/pill box pair verified non-overlapping @1280 AND @390. (d) APPARITION BAND — confirmed the dawn-mist is alpha 0 at init (fill rgba(...,0)) and only fades in within its dawn window; no grey band. (e) INIT TIME — added localCivilMin() so the door opens at the visitor's TRUE local clock (verified clock=local-time, phase=daytime at a 10:43-AM-equiv minute, NOT 20:30/night), with the forceAllOn/reduced-motion canonical frames left pinned (20:30/12:45). BUG 2 (Honest Sundial bench, the-hours.src.html→the-hours.html): wrapped the Firmament/Orrery/Verse fp-label tiles in real SVG <a> links (../firmament/ · ../orrery/ · ../verse/) with a .fp-box hover/focus-visible brass lift + aria-labels — verified 3 real <a> links, correct hrefs, 0 nested anchors, the Firmament tile navigates end-to-end. PUBLISHER FRESH-EYES (session ws-review-75, served 127.0.0.1:8917, torn down by exact name + the http server killed by exact PID 6090 — Brandon's :3001/:4380 untouched) CAUGHT & FIXED one real polish bug the builder missed: relocating the self-test pill OUT of #hours-hud left its CSS scoped #hours-hud .hh-test no longer matching, so the pill rendered as black 16px SERIF (computed fill rgb(0,0,0)) instead of the intended green 7px MONO with a dark stroke-halo; FIX = de-scoped the three .hh-test rules to match by class (the relocated #hours-pill now computes fill rgb(143,215,154)=#8fd79a, 7px, mono, stroke present) and dropped the now-dead .hh-caught rule at the same time. Re-forged index.src.html; forge --check --all all 31 current; layout smoke ALL PASS; hours.test.cjs 39/39; in-page pills green (front-door 32/32 · bench 32/32); audit-seen all 22 pages drop their breadcrumb; 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on the front door AND the bench. Re-pinned the Tabularium CLAIM after collate folded in 3 marks (334/296/81/306→417 → 337/299/82/306→418, re-inlined byte-identical into tabularium/index.html, twin GREEN 10/10). Provenance: the #75 worklog. -->
<!-- FIXED #57 → "Several backlinks to Orrery Estate are still labeled WORKSHOP. Entropy, The Workbench are two. More may exist." After the #47/#49 rename ("The Workshop" → "The Orrery Estate"), ~112 interior back-link anchors across 89 source files STILL read "← workshop" — disagreeing with the estate's own nameplate (the #49 sweep had only covered the 24/31 files it enumerated; this is the long tail it missed). No math claim → no self-test owed (a pure text rename, like #49). FIX (bug-fixer, this cycle): a scripted sed -E sweep (extended regex, to handle the unicode ← glyph + | alternation that BSD/macOS basic-regex silently treats as literal — the first-attempt trap) rewrote every visible back-link's estate-name words → "The Orrery Estate", matching #49's convention (<a class="back" href="../index.html">← The Orrery Estate</a>). SCOPE: 112 anchors / 89 files — 67 plain .html direct-edited + 22 .src.html re-forged. DIFF: 111 files, 112 insertions / 112 deletions — perfectly surgical. PRESERVED EXACTLY per page: the glyph (← vs &larr;), the href (../index.html + the deep ../../index.html on conservatory sub-pages), the class (back/back-ws/crumb/backlink/sib-link), all surrounding whitespace (the conservatory footer &nbsp;&middot;&nbsp;, sound-garden's trailing <br>). SPECIAL CASES: the conservatory footer "← back to the workshop" → "← back to The Orrery Estate" (kept the "back to" prose); the 5 clockwork .crumb variants; the all-caps mirror-maze ← WORKSHOP + capitalized coastline-paradox ← Workshop both NORMALIZED to title-case (26 pre-existing #49 back-links already title-case). KEEP-LIST honored (untouched): the nameplate code-comments, the cardioid · the workshop JS-caption signature, the 4 ck('back-link to the Workshop present') self-test labels, the bman654/the-workshop repo URLs, the colophon "a workshop, in the old sense" prose, the Undercroft "beneath the workshop" prose, the pathfinder "Built in the Workshop" prose. PUBLISHER FRESH-EYES (session ws-pub-57, served 127.0.0.1:8791, torn down by exact PID — Brandon's :3001 confirmed still listening, untouched) — verified clean, nothing caught, nothing to fix: the DIFF INVARIANT proven mechanically (EVERY removed line contains "workshop", EVERY added line contains "The Orrery Estate" — zero collateral); residue grep '>(&larr;|←|↑|⌂)[^<]*workshop[^<]*</a>' (worktrees excluded) → ZERO; forge --check --all → all 30 current; tools/layout/smoke.cjs → ALL LAYOUT CHECKS PASS (map untouched). LIVE @1280 on 8+ surfaces (estate back-link text · href · class · 0 horizontal overflow each): entropy/orrery/verse/workbench/strange-garden/clockwork-turn (crumb, narrow container)/mirror-maze (was ← WORKSHOP)/coastline-paradox (was ← Workshop)/sound-garden (trailing <br> kept)/undercroft. SIBLING-LINK INTEGRITY confirmed: entropy + clockwork keep ← workbench (a sibling → ../workbench/index.html, NOT the estate); conservatory/sir keeps ← the conservatory (sibling, ../index.html) untouched AND renames only the deep estate link ← The Orrery Estate → ../../index.html (both anchors verified ["← the conservatory","← The Orrery Estate"]); conservatory index has the nav-crumb + the footer-special-case "← back to The Orrery Estate". Front door renders clean (district/slot map, titled "The Orrery Estate", screenshot reviewed — no layout break), no front-door link text says "workshop". Builder's open concerns adjudicated: the casing normalization is KEPT (consistency with the #49 title-case convention reads cleaner than a lone outlier); the cardioid JS-caption signature LEFT as-is (a signature, not a back-link, out of this bug's scope). No new [bug], no ⚡ spark. The estate's sign and every one of its 112 interior doors now agree — "workshop" survives only where it should (repo URLs, Maker's-Wing prose, the cardioid signature, keep-list comments). Provenance: the #57 worklog + every interior room page. -->
<!-- BLOOMED #53 → The Cairn's ledger plumbing was DOUBLY broken: (1) end-of-cycle collate appended marks to ledger.jsonl + refreshed depth.txt but NEVER re-forged the served ledger/face.html, so the static page went chronically stale (forge --check flagged it every cycle, manual re-forges papered over it); (2) sign.sh derived a new mark's cycle "bedrock-first" — the derived value was just last cycle's max, so each new mark re-stamped that same max, a self-perpetuating FREEZE that pinned 117 marks at cycle 30 across real cycles ~31→52. FIX (bug-fixer, this cycle): collate.sh now ALWAYS re-forges face.html (repo-root = "$dir/.."), degrading gracefully if forge/face.src is absent; sign.sh's derive_cycle is now max(bedrockFloor, funlogMax) then a 0 sentinel — the durable ledger+inbox still FLOORS the value (survives a funlog wipe) but the live funlog can ADVANCE past a stale bedrock, breaking the poison loop. DATA-INTEGRITY CALL: the 117 historical cycle-30 marks left AS-PLACED (no migration — ledger is append-only, rewriting recorded cycles destroys provenance); only the FORWARD derivation is fixed, so the page renders the honest dented history (the span now climbs "cycle 0 to 30" → "cycle 0 to 52", the freeze legible as the seam). sign.test.sh → 18/18 ✓ (incl. the inverted (a') max framing, the new (a'') poison-loop regression [stale bedrock 30 + live funlog 52 → 52], and the new (f) real-forge proofs that collate re-forges face.html + inlines the new mark + leaves forge --check CURRENT). PUBLISHER FRESH-EYES (session cairn-pub-c53, served 127.0.0.1:63730, torn down by exact PID 60313 — Brandon's :3001/:4380 untouched): the served Cairn rendered with self-test pill PASS 11/11 (allPass=true) — 219→221 stones == records == file lines, depth 388→389, gap 169→168, seq contiguous 1..221; 0 console errors · 0 page errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 (clean on desktop AND mobile); collate folded in 2 marks (Unfreeze + the publisher's Truthcourse, both correctly stamped cycle 52, NOT 30) and AUTO-re-forged face.html; a cache-busted reload confirmed the freeze visibly broke — the two newest stones jump from cycle 30 to cycle 52 atop the frozen run. forge --check --all all 30 files current. (sown #51) -->
<!-- BLOOMED #49 → Stale "The Workshop" backlinks after the rename — the map-process lockdown (#47, da008bb) renamed the estate "The Workshop" → "The Orrery Estate" on the front door, but every interior room page still showed the OLD name. SWEPT the VISIBLE estate-name text across 31 files (38 inserts / 38 deletes, 1:1): 24 back-links across the five wings — each glyph variant (← / &larr; / ⌂ / ↑), href (../index.html or ../../index.html), and CSS class (back, back deep) preserved EXACTLY — + 6 <title> tags (the cavern/engine-room/numbers-room/alchemy/clockwork wing indexes + colophon) + 6 kickers (the 5 clockwork sub-pages context/measurement/partition/temperature/turn + the colophon .kick) + 1 EXTRA the design didn't enumerate: the colophon footer back-link (colophon.html:101, ←&nbsp;The&nbsp;Workshop → ←&nbsp;The&nbsp;Orrery&nbsp;Estate), a clear visible estate-name back-link the literal-space baseline grep missed because of its &nbsp; separators. All 30 room files are direct-edit HTML (no .src.html siblings, no forge); the front door (index.html/index.src.html) was already renamed at the lockdown and NOT touched. KEEP-list honored (untouched): the 2 nameplate code-comments (index.html:3343, index.src.html:1523 — layout-furniture obstacle refs, never rendered; the rendered nameplate at index.html:485 already reads "The Orrery Estate"), the github.com/bman654/the-workshop repo URLs, the ws.js-module code-comments, the 3 JS self-test assertion labels ck('back-link to the Workshop present') (element-EXISTENCE checks, unaffected by text), pathfinder's out-of-scope "Built in the Workshop" prose, and all 4 colophon common-noun prose uses ("a workshop, in the old sense" etc.). No math claim → no self-test owed (a text rename). Reviewed fresh-eyes #49 (session ws-pub-cy49, served :8823 torn down by exact PID 81856; Brandon's :3001/:4380 untouched): opened ALL 31 surfaces + a per-page checker → every page reads "The Orrery Estate" with glyph + href preserved, visibleTheWorkshop:0 on all 31, overflow:[] everywhere (the longer name spills no .back/.kicker/.kick/.crumbs/.nav/.topbar container @1280); the critical functional check — clicked the ⌂ The Orrery Estate back-link from cavern/pressure → landed on /index.html, title "The Orrery Estate — an Estate Plan" (no orphaned link, the front door already renamed); self-tests green where present (cavern 29/29 ✓ · clockwork 18/18 ✓ · engine-room 16/16 ✓ · numbers-room 13/13 ✓ — the 3 kept assertion labels still pass against the renamed links); the colophon verified surgical (title/kick/footer all "The Orrery Estate", the common-noun prose preserved, 0 visible "The Workshop"); the filtered residue grep returns ONLY the 2 KEEP-list nameplate code-comments. PUBLISHER'S CALL: the builder's +1 colophon footer fix is CORRECT — confirmed, NOT reverted (leaving it as "The Workshop" while its sibling .kick flipped would have shipped a visible inconsistency). Re-forged the pre-existing ledger/face.html drift the builder flagged (STALE before this cycle, NOT in the changeset — 7 un-inlined cycle-30 ledger marks + the depth counter 380→384, unrelated to the rename) → forge --check --all all 30 forge files current; no room file is forge-managed. Layout smoke ALL PASS · sky 73/73. No new [bug], no ⚡ spark. Provenance: the #49 worklog + the wing room pages. The estate's sign and every interior door now agree. -->

*Recent fixes (terse echoes — full provenance in the worklog / `ledger/CHANGELOG.md`): the Cairn-depth
unit-mismatch (cycle #22 — face now reads commit-DEPTH from `ledger/depth.txt` over a STONES line, the
gap = quantified silence) · `ledger/sign.sh` self-derives its cycle, durable-ledger-first (#12, #14) ·
Workbench nested-anchor card spill (stretched-link pattern, all cards) · Carnot mobile pill overflow ·
the estate-wide mute, finished (#39 BUG-A: 20 no-mute pages root-fixed via `tools/ws/ws.js`; #40 BUG-B:
the last 27 local-only-mute pages — 18 arcade games + 9 Sound Garden instruments — routed through the
one shared key `ws:pref:muted`, all 56 audio surfaces now shared-wired, browser-verified).*

---

## ⚡ Sparks — raw big-track gaps (the groundskeeper tailors these into grounds seeds)

<!-- gauge:sparks:start -->
- ⚡ **A room whose navigation IS its subject** — a place whose *form expresses content* (an instrument you operate, a cabinet you open) — the Hall's lesson that a vertical list wasted optics.
- ⚡ **A medium the estate lacks** — it has text · visuals · sound · audio-render. What's missing? (real-time 3D? a time-based medium? something seen-and-heard at once?)
- ⚡ **Expand the map** — new land/region on the front door to hold the wings still to come.
- ⚡ **A real-time camera-navigable 3D medium** — the estate has text · visuals · sound · audio-render · flat shader-fields, but no SCENE you move a camera through; first proof-of-life could make the ACT of navigating the content itself (an anamorphosis you walk into — a tangle that resolves into one legible form only from the one vantage you must find, the resolving pose the provable solution of an alignment equation, not eyeballed).
- ⚡ **The Drawing Engines & flow benches** — sibling veins for later swings: geometry instruments that COMPUTE BY DRAWING (ellipsograph · pantograph · Peaucellier's exact straight-line linkage) — a natural neighbor wing to the Reckoning Cabinet — and a Wind Tunnel foil you TILT until the stall breaks at the critical angle (lift ∝ circulation Γ, the Kutta condition the exact crux), kin to the potential-flow / soap-film vein.
- ⚡ **The estate measures the WORLD but never the OBSERVER moving through it** — a relativity / reference-frame grounds layer (a wing, or a front-door overlay) where speed & gravity visibly dilate time and contract length, kin to The Hours' day-clock but for the moving frame; the proper-time / Lorentz factor the exact crux you'd watch tick apart as you push toward c.  See also the Light Clock exhibit.
- ⚡ **amusement park** — explore the physics of fun!  2.5D rollercoasters with proven physics ; centrifugal forces explained through spinning floor drop ; the geometric motion of a rider on the teacup ride
- ⚡ **astrophysics room** — the physics of the universe ; hawking radiation via particle pairs visual primer ; stellar fusion ; the death of a star and what determines its final state ; a study of the big bang ; galactic structures and how galaxies form
<!-- gauge:sparks:end -->

---

## 🏛️ Grounds seeds — big swings (new structure; the grounds-worker opens one)

<!-- gauge:grounds-seeds:start -->
<!-- BLOOMED #31 → The Conservatory (living-systems wing): front-door footprint + glasshouse landing + the predator–prey bench. Provenance: conservatory/CHANGELOG.md + the #31 worklog. The three planters (logistic / SIR / replicator) are now [bench] garden seeds under the wing. -->
<!-- BLOOMED #41 → The Alchemy Lab (alchemy/) — the estate's FIRST chemistry wing + the SECOND grounds big swing. "Nothing is lost": conservation of matter IS a two-pan brass balance you operate, and A·c=0 is spoken at the instant the beam reads level. Three surfaces beyond the front door: the SOLE-authority core.mjs (exact BigInt-rational nullspace — nested-group parseFormula, buildMatrix [products negated], solve() demanding a 1-D nullspace → LCM-up/gcd-down to smallest positive ints, ok:false+reason for over-determined/ambiguous/no-positive, verify(), a LIBRARY of 10 real reactions + the negative control H2+O2→H2O+Na), the bench reaction-balancer/ (a brass balance with periodic-tinted element tokens, a reaction RACK, a per-element Σleft/Σright ledger, a "show the matrix" drawer [A · nullspace vector · A·c=0 column], beam-settle gated behind reduced-motion, core inlined byte-identical between sentinels + an in-page parity check), and the landing alchemy/index.html (one lit live bench + four dashed planters [Stoichiometry · pH&Titration · Equilibrium · Periodic Table] + a reciprocal lit Cavern bridge → ../cavern/hydrogen/; ZERO chemistry of its own — imports the live core to prove its library). Front door: a new PLACES entry `alchemy` (⚗️, footprint `laboratory`, drawLaboratory()), forged 0-collision, label self-test 12/12, audit-seen green. core.test.mjs GREEN 47/47 (incl. byte-identical re-extraction parity); bench badge 25/25; landing pill 22/22. The negative control VISIBLY TIPS the beam + reads "unbalanceable · the solver returns nothing, not a fake level". Provenance: alchemy/CHANGELOG.md + the #41 worklog. The four planters are the wing's growth — grow it with a fresh [bench]/[exhibit] Alchemy garden seed, don't rebuild the Reaction Balancer. -->
<!-- BLOOMED #61 → The Tabularium (tabularium/) — a NEW front-door MANOR WING (the ARCHIVE) + the FOURTH grounds big swing: the estate's own ANNAL, the one room that remembers the makers, not the work. Every wing is built by a mind that wakes for a single turn then is gone; git keeps the work, never the maker — so the Tabularium gathers their marks into one chained tome. The winning prototype-C form: a brass ORRERY-DIAL of years (drag/scrub/◂▸ leaf/▶ wind) coupled to the tome, with the manor RAISING ITSELF wing-by-wing (each lit at its REAL first-appearance cycle — bornCycle = git commit-depth, the same convention the ledger uses; wings predating the record read "already standing", the Tabularium itself bornCycle 404 shows as the rust LIVE EDGE not-yet-shipped) and a CAIRN rising one pebble per mark as you wind. Each leaf = one cycle that left a mark; the leaf names a hand-authored CHAPTER (the MYTH array — 5 vivid chapters: the three founding words · the makers who live one turn · the Hand That Guides/the gauge · the estate measuring itself · the living question), and the body surfaces THAT cycle's REAL koans as illuminated marginalia (a gilt drop-initial of each maker's chosen NAME, role+seq in scribe-mono, koan in italic serif on dark vellum, founder seq-1 Cairn in rust). Three sources of truth: tabularium/core.mjs (321L — the PURE data-fidelity logic: parse/canonical/buildLeaves/partitionByChapter/recomputeAggregates/roundTripOne/selfTest/tamper/verdict/CLAIM + the WINGS estate-raising table + estateAt + validateWings), tabularium/core.test.mjs (60L — the Node twin), and tabularium/index.html (1225L) which inlines core.mjs between // === CORE BEGIN/END === sentinels (functional body byte-identical — only the doc-banner & `export` stripped, the legitimate forge transform; the in-page pill computes the identical battery from the same bytes) + inlines ledger/ledger.jsonl verbatim via a </script-escaped JSON carrier read once as raw text. The legend is READ, not invented: the self-test proves render(legend) is a pure function of the committed ledger — every mark round-trips byte-true (seq·cycle·role·name·koan), nothing fabricated/dropped, chronological, chapters partition the whole, render↔record bijection, aggregates recompute, MYTH prose ∉ ledger. Node twin GREEN 10/10 + CLAIM-match + tampered-carrier negative control (exit 0); in-page pill 11/11 ✓; the visible "⚠ Forge a mark" negative control flips it to "TAMPERED 4/11 ✗" (red) then restores. Front door: a new `archive` manor WING in layout.js (THE ARCHIVE, #c9a44e) + one PLACES entry `tabularium` (📜, manor/tier2/footprint house-wing), forged 0-collision; a sky field-star tabularium (1180,150 mag1), sky 73/73; forge --check (30 current) + audit-seen (20 pass) + layout smoke all GREEN. Browser-verified (session tab-review-61, served 127.0.0.1:8823, torn down by exact PID 57344 — Brandon's :3001/:4380 untouched): pill 11/11 ✓ · 0 console errors · 0 overflow @1280 & @390 · the hero ACT reads (wind dial → real maker-koan rises as marginalia, founder Cairn in rust; winding lights wings one-by-one); mobile @390 collapses to one legible column; front-door POI = exactly 1 anchor → tabularium/index.html, THE ARCHIVE wing label present, 0 nested anchors; both back-links → ../index.html resolve. After the publisher collated this cycle's 7 inbox marks the ledger grew 257→264 (cyc 403→404), so CLAIM was re-pinned to the committed aggregates [264 marks·239 hands·68 cycles·306→404] + the carrier re-inlined; at the committed live edge cyc 404 the Tabularium's own chip reads `wingchip standing raised` in gold (its bornCycle 404 = this commit's git depth — it ships in the same commit whose marks it shows). Provenance: tabularium/CHANGELOG.md + the #61 worklog. The metagame "Workshop Mystery" seed (below) was re-scoped this build to defer manor-history to the Tabularium's domain. GROW the wing — an Adventure-Game "true purpose" quest, a depiction of The Hand That Guides, a dusty-bookshelves browse, the Cairn's full founding tale — with a fresh [room]/[bench] seed; don't rebuild the orrery-tome. -->
<!-- BLOOMED #71 → The Hours (tools/hours/ + hours/) — the estate's living day/light layer + a NEW horology wing + the FIFTH grounds big swing: the front door now knows what time it really is. A continuous day-phase tints the whole plate dawn → golden → candle → deep night, keyed to the TRUE solar altitude for the estate's fixed latitude (40.7°, no geolocation); a brass gnomon casts a real shadow that sweeps with the sun; four time-gated apparitions wake each only in its hour (dawn-mist · dusk-fireflies/first-star · candle-window WRAPPING midnight · witching-ghost); and a catch-the-hours phase-ring metagame (additive ws:flag:hours-caught-<id>, never gating) fills as you run the day. Form expresses content — a day you scrub and a sky that responds, not a clock you read. SOLE-AUTHORITY CORE tools/hours/hours.js (~330L, zero-dep dual-use IIFE): its solar functions are COPIED VERBATIM from tools/dial/dial.js's proven core (so The Hours can never disagree with the Gnomon sundial/Astrolabe about the sun); public surface solarAltAz · brightness (C0+monotone) · skyColor (jump-free, shared endpoints) · lampGlow · dayFactor · gnomonShadow (ROUTED THROUGH the proven shadowTip so the shadow direction IS the proven hour-line, not ad-hoc trig) · apparitionsAt + 4 predicates · phaseName · ESTATE. Front door (index.src.html re-forged, +399 src): forge:include byte-twins the core; a named layer stack mounts into #viewport after Sky.renderInto (hours-back/wash a multiply wash that darkens the margins at night to raise star contrast, hours-apparitions, hours-gnomon brass dial + cast shadow + drag handle at the gnomon POI's derived centre, hours-ring, hours-hud clock+phase+self-test pill); the night-stars are the EXISTING Survey-of-Heaven stars brightening via a .hours-night class — no competing star set. A new gnomon PLACES entry (district:grounds, tier:1, wing:horology, footprint:sundial, prefer:right, href:hours/index.html) + a drawSundial footprint drawer + a gnomon catalog star (sky.js 1270,170) + horology added to layout.js GROUNDS_WINGS (the open upper-right east park, where a sundial wants open sky) + WING_META + the sky.test.cjs mirror. WING hours/: index.html landing (Hall/Cavern mold, drops ws:seen:gnomon, analemma flourish, live bench + seed plinths) + the-hours.src.html→the-hours.html the first bench (the interactive sundial-plate, forge-inlines the SAME core, ● live snap-to-clock, ▶ run-the-day, in-page pill, altitude readout). Node twins all GREEN exit 0: tools/hours/hours.test.cjs 39/39 (solar peak = 90−|lat−dec| from TRUE dec ≤0.05° [equator 89.55, NYC 72.73] · exactly two horizon crossings ~06:00/18:00 · daily-max at civil 720±EoT≈722 · shadow direction = the proven hour-line sweeping morning→afternoon, noon shadow up the noon line · brightness C0+monotone · skyColor jump-free bounded-slope · day rises dawn→noon→night · each apparition fires exactly in-window with ON-inside/OFF-both-edges/OFF-unrelated-phase negative controls + non-vacuity · candle-window proven ON across the NYC-solstice midnight wrap · LIVE Layout.solve margin-clearance), tools/sky/sky.test.cjs 73/73 (gnomon star+footprint added, STAR_PAD clearance proven), tools/dial/dial.test.cjs 21/21 (borrowed core unchanged); forge --check --all all 31 current; layout smoke ALL PASS; in-page Hours.selfTest 32/32 in the browser (the Node twin adds 7 Node-only Layout margin asserts → 39). PUBLISHER FRESH-EYES (session hours-pub-c71, served 127.0.0.1:8794, torn down by exact PID 39942 — Brandon's :3001/:4380 untouched; agent-browser ran clean) — shipped clean, nothing real caught: pill ✓ 32/32 "the dial proved exact" on the front door AND the bench at 1280 AND 390; 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on all three surfaces. Verified LIVE: at noon the wash is near-transparent + the plan reads (HUD 12:30 · high noon); ?hours=allon pins the deterministic dusk frame (20:30 · astronomical twilight) — plate deep indigo, .sky.hours-night turns the Survey stars up, wash multiplies ~0.48, all 4 apparitions show; run-the-day double-click swept the whole day and CAUGHT all 4 (HUD HOURS CAUGHT 4/4, ring 4/4, the four ws:flag:hours-caught-* flags persist); the bench scrub to 20:30 darkened the plate, brought up its stars, fired the dusk apparition readout. The gnomon POI is exactly 1 clean SVG <a href="hours/index.html"> (no nesting, resolves 200); ws:seen:gnomon drops on a direct visit; reveal-all-secrets composes (gnomon star lights, all-charted capstone fires, the horology wing reads kin to the celestial pieces — no crowding/collision/clipping); mobile @390 all three collapse to legible single columns. All five builder open concerns adjudicated benign (role-vs-checklist commit conflict — builder correctly left it uncommitted, this role committed; the gnomon pill overlapping its OWN HUD/label is its intentional furniture cluster, never collides another room; 32-in-browser vs 39-in-Node by design; horology config-reviewed per map-process, clears everything; verbatim solar core not promoted to tools/solar/ — the right call, not required to ship). No real bug, no [bug], no ⚡ spark, no polish edit. record --mode BUILD --track grounds (bed diff: 0 sown · 1 bloomed · 0 decayed; bigSwingsBuilt 4→5, groundsSince 9→0, state.cycle 70→71). Provenance: tools/hours/README.md + hours/ + the front-door diff + the #71 worklog. GROW the wing — a true analemma you trace by the noon-shadow across a year · the equation of time as the loop that makes sundial & clock disagree · a water-clock or escapement that counts time when the sun is hidden (the landing already reserves these plinths) — with a fresh [exhibit]/[bench] garden seed; don't rebuild the sundial-plate. -->
- [metagame] **The Workshop Mystery — a manor-wide treasure hunt** — clues chase across exhibits (a seed found here, the cipher to read it found there), Undercroft-style hint cards → a final reveal. The clue graph must be **provably solvable**. **The reveal is the designer's to invent** — left deliberately open; reach past the estate's myth-history (the Tabularium's domain now) for a fresh payoff. **Buildable in slices** — a self-contained first chapter (one clue-pair and its reveal card, provably solvable on its own) stands alone and seeds the rest; no single cycle has to land the whole hunt. (sown #72 · contest #5)
<!-- BLOOMED #81 → The Reckoning Cabinet (reckoning/) — a NEW front-door MANOR WING (district:manor, tier:2, wing:reckoning) + the SIXTH grounds big swing: an operable brass instrument-panel LANDING that gathers the estate's five "minds that reckon by measuring a shape" — slipstick · astrolabe · planimeter · abacus (soroban) · the offsite gnomon — onto ONE engraved rail, each dropping a pointer to it. NAVIGATION IS THE RECKONING: none of them calculates, each reads its answer off a length, an angle, a rolled wheel, a column of beads, a shadow. The five live SVG stations: SLIPSTICK (the lit hero, always animating — two real log10 C/D scales + a red hairline enacting 2×3=6 as a SUM OF TWO LENGTHS) · ASTROLABE (a rotating rete reads an angle) · PLANIMETER (Green's-theorem-in-brass, a hinged tracer arm + rolling wheel) · ABACUS (a soroban rod stands the value in beads — 1 heaven ×5 + 1 earth ×1 = 6) · GNOMON (the ONE offsite station, marked "ON THE GROUNDS ↗" — the CRUX resolved-not-hidden: it stays on the grounds [horology #71], the cabinet points OUT to ../hours/index.html rather than absorbing it, and every gathered bench keeps its OWN green self-test + every inbound back-link resolves). GRAFT B — a shared-question register at the panel head (default 2×3=6): set the two factors and every station re-reads the SAME number by its own geometry, with a live "5 instruments agree · all read N" readout. HONEST FRAMING (not five proofs): only the slipstick log-identity is an EXACT math claim; the other four ENCODE the shared integer by their own geometry. Selecting a station reads its detail into a dock below. Reduced-motion pauses all five + settles the hero to 2×3=6; a 680px mobile breakpoint; drops ws:seen:reckoning; the lacuna footer names the NOMOGRAPH as the first NEW bench still lacking. SINGLE-SOURCE: reckoning/core.test.mjs (75L) extracts the SAME RECKONING_CORE slab the page runs (sentinel-to-sentinel, no parallel copy) and asserts the one EXACT claim — log a + log b == log(a·b) — to 1e-9 across the full 1..9×1..9 grid (worst |Δ|=2.22e-16), plus the soroban decomposition + cabinet structural invariants (5 stations · gnomon sole offsite · Nomograph absent · every href resolves) → Node twin ALL GREEN; in-page wiring chip 14/14 ✓. Front door: a new reckoning PLACES entry + drawReckoning() (the brass panel-slab footprint — five engraved station bays + shared rail + the gnomon's outward ↗), forged byte-consistent; layout.js WING_META["reckoning"]; reciprocal "part of The Reckoning Cabinet ↗" links in each gathered bench + all four Workbench Instruments cards + an inbound Hours cross-link (reckoning/index.html is hand-authored, no .src.html twin like planimeter, so forge stays 31/31 by design). Browser-verified (#81 publisher, session ws81pub, served 127.0.0.1:8757, torn down by exact name + PID 31664 — Brandon's :3001/:4380 untouched): chip 14/14 ✓ @1280 AND @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on the landing, the front door, the Workbench, and all four sibling benches; the hero animates; bumping a factor to 3×3 made all five stations re-read "reads 9"; the gnomon dock targets ../hours/; the four Workbench cabinet links are click-reachable (not buried under the .card-link overlay); reduced-motion verified by code inspection. The publisher adjudicated the builder's flagged iron-filings × strange-garden overlap as NOT a real gate failure (the canonical smoke.cjs foot-overlap check runs clean, iron-filings isn't a footprint there) → no [bug] filed. forge --check --all 31/31 · forge --audit-seen 23/23 (reckoning included) · layout smoke ALL PASS. record --mode BUILD --track grounds (0 sown · 1 bloomed · 0 decayed; bigSwingsBuilt 5→6, groundsSince 9→0, state.cycle 80→81). Provenance: reckoning/CHANGELOG.md + the #81 worklog. GROW the wing — the NOMOGRAPH (the alignment-chart bench the lacuna names; [engine] seed sown #72 · contest #5), or a fresh shape-reading instrument — don't rebuild the landing or a gathered bench. -->
- [engine] **The Nomograph — the answer at the crossing of a straightedge** — The one reckoning instrument the estate LACKS (grep-confirmed: nowhere built) and the perfect born-here first NEW bench for the Reckoning Cabinet: an alignment chart of three scales where you lay a straightedge and read the answer where it crosses the middle scale (the classic 1/x+1/y=1/z parallel-resistor nomogram, or a×b=c on log scales). Form expresses content — a straightedge you DRAG, not a plot. CRUX (its soul): the scale positions are DERIVED from the projective alignment condition, a sole-authority core proves the geometric crossing === the closed-form answer to machine-ε over hundreds of random pairs, AND a wrong scale-spacing makes the crossing read WRONG (the alignment is load-bearing, not cosmetic). Bonus cross: it reads the SAME a×b the slipstick reads — two instruments agreeing by disjoint geometry. (sown #72 · contest #5)
- [engine] **Warren — the estate's SECOND game engine, a different verb than Lantern** — Lantern lets you carry a thing and set a flag; here WHERE YOU STAND is the whole game. A self-contained engine for grid roguelikes — discrete tiles, a strict turn economy (you step, THEN every monster steps by its fixed rule), walls/doors/hazards, and a declarative ASCII LEVEL-FILE so a future agent ships a floor by writing only data, never engine code (Lantern's world-file pattern, one verb over). Ships in a SLICE like the Lamplighter shipped Lantern: ONE hand-authored floor, ONE deterministic monster, a start and an EXIT you reach by play. CRUX: a turn-based grid with deterministic AI is a finite game tree, so a headless solver PROVES before ship that a guaranteed-survivable path exists (negative control: a monster-locked pincer floor → 'no safe path' → REJECTED, never shipped) — Lantern's softlock-free pride carried into a spatial, tactical verb; seeds a Descent of stacked proven floors after. (sown #72 · contest #5)
- [room] **The Aerodrome — flight you cause, not flight you read** — A grounds wing on the open sky band (the quadrant that already faces up). First operable bench: a LAUNCH RAIL you load with fuel + payload and FIRE — burn the booster and watch the Tsiolkovsky budget drain a real Δv tank, then the craft coasts and its ORBIT re-shapes live under a second burn you trigger (a Hohmann nudge that visibly raises the far side; too little fuel and it falls back, escape and it never returns). You THRUST; the conic responds — a rail and a throttle and an orbit that bends in your hand, not a phase-plot. CRUX (exact): the self-test proves Δv = v_e·ln(m0/mf) matches the burned-mass ledger to machine-ε, the coasting craft conserves specific orbital energy to <1e-9, and escape fires EXACTLY at v=√(2μ/r) (negative control: just under never escapes). Do NOT rebuild the Orrery's read-only Kepler instrument — verify the two-body integrator AGAINST it or own a clearly-separate one. (sown #72 · contest #5)
- [room] **The Museum — major expansion of the archive wing** — Celebrate the 100th cycle since cycles were counted by opening with the grand opening of the Museum.  Migrate the Tabularium to be the first exhibit within the museum. Second exhibit: Centennial Jubilee that celebrates the first 100 cycles by showing the true cycle time.  Measure the actual time of the first 100 cycles through git and show on an interactive timeline.  Show historical images of the front door through the ages, on the timeline or as a timelapse.  (sown #86 · contest #6)
<!-- BLOOMED #51 → The Puzzle Pavilion (puzzle-pavilion/) — a NEW front-door WING (grounds / amusements tier 1) + the THIRD grounds big swing: a gallery of GENERATED deduction puzzles, every board proven UNIQUE and solvable by pure LOGIC, never a guess. The wing's FIRST leaf is fully playable + fully proven this cycle — The Bridge House (Hashiwokakero), the estate's first "connect" verb (kin to the Giant Component continent, the Latin Square game #38, the Mirror Maze lineage): a dark archipelago of numbered islands; drag from one island to an aligned neighbour to lay a span (drag again→double, once more→clear); WIN when every island carries exactly its number AND the whole bridge graph is one connected network (unsatisfied islands wear a grey stranded ring, satisfied turn green, a connectivity flood colours every island reaching the root — the win is the graph invariant made visible). Three surfaces beyond the front door: the SOLE generation+solving authority bridge-house/core.mjs (308L between // === CORE BEGIN/END === sentinels — mulberry32 + geometry [pairs/crosses/conflictMap/isConnected] + an exact countSolutions counter + a pure-deduction deduce() over per-pair lo/hi bounds with SIX named rules [island-saturated · island-degree-met · forced-min · degree-cap · crossing-clamp · isolation-avoid] and NO guess step + a generate() that grows a connected reference archipelago and keeps a board only if countSolutions===1 AND deduce() solves it; 9 islands, numbers 1–8, 7×7 grid — kept SMALL on purpose, countSolutions is exponential in pair count so the cap + modest island count are load-bearing), the bench bridge-house/index.html (a responsive 560-baseline canvas [CSS width:100%, backing store CS·DPR, one-time ctx scale — never runs past the fold, clears 390 with 0 overflow; the double-span offset tracks cell size], drag-to-bridge, a New/Reveal-logic/Clear/Hint card, a live-state card, an in-page self-test pill 6/6; core inlined BYTE-IDENTICAL, verified by diff), and the landing puzzle-pavilion/index.html (1 live family card [The Bridge House] + 2 coming-to-leaf planters [The Cross-Sums · Kakuro, The Pearl Loop · Masyu]; drops ws:seen:puzzle-pavilion; self-test 11/11). Node twin bridge-house/core.test.mjs GREEN 7/7 over 300 seeds (unique · deduced · connected · deduced===reference · every trace step a named rule [no guess] · the corrected NEGATIVE CONTROL breaks on every real loosening — loosen one island whose number ≥2 by 1 → count>1 OR deduction stalls). Front door: one PLACES entry `puzzle-pavilion` (🧩, accent #6fd3c4, district grounds / tier 1 / wing amusements, footprint `pavilion`, order 15 between Arcade[10] & Maze[20]) + a new drawPavilion() in the no-fallback DRAW table (a domed garden pavilion — slab, 8 radiating ribs, a small island-dot ring chorded by span-lines echoing the bench's archipelago); smoke catalog entry added; layout smoke PASS (collision-free), forge --check --all GREEN (30 current), sky 73/73 (a new amusements ROOM, not a new sky constellation). Browser-verified (session pp-c51, port :8791 torn down by exact PID 20285): clean console · 61 fps · 0 nested anchors · 0 overflow @1280 & @390 · solved by hand → WON. Provenance: puzzle-pavilion/bridge-house/CHANGELOG.md + the #51 worklog. The remaining two families (Kakuro, Masyu) are now [bench] garden seeds under the wing (their proven cores preserved in /tmp). -->
<!-- gauge:grounds-seeds:end -->

---

## 🌱 Garden seeds — grow what exists (the staple; the planter sows one)

<!-- gauge:garden-seeds:start -->
### exhibit
- [exhibit] **The Unseen — proved, never perceived** — some truths refuse the direct look: the eye's own blind spot you cannot see, a mass known only by the orbits it bends, an Earth whose size Eratosthenes read off a shadow without ever standing back from it. Build a place where the obvious sense reads WRONG and only reasoning — or an indirect, provable measurement — recovers the truth; the visitor leaves trusting the proof over the glance. (sown #81)
<!-- BLOOMED #85 → The Elementary Garden (elementary-garden/) — a NEW top-level piece + a NEW estate vein: a 1-D elementary cellular automaton you seed BY HAND, then watch its entire future RAIN DOWN row by row into a triangular tapestry (grep-confirmed net-new: 0 rule-30/90/110/wolfram on the estate, all prior CA was 2-D Game-of-Life). Click/drag the top row to paint a seed; under Rule 90 from a single centre cell it grows the exact Sierpiński triangle and the LIT cells ARE the odd binomial coefficients (Pascal mod 2). THE HERO is a two-canvas CA+overlay stage (gold-on-dark palette, right rail, hover C(n,k)+CA-state tooltips): flip the Pascal overlay and every teal C(n,k) ring snaps onto a lit cell — a verdict you align with your eyes ("They align…") — switch to Rule 30 and the rings land on dark cells (verdict "No match", the negative control IS the show). A "compare all three side-by-side" toggle runs 90·30·110 on the IDENTICAL painted seed in colour-coded fields (teal Pascal/fractal · red chaos · amber universal), each with a non-overlapping top-gutter CHIP + the seed row drawn bright gold. Single-rule mode keeps per-rule context chips (Rule-90 Pascal · Rule-110 "Turing-complete, Cook 2004" · Rule-30 chaos) + a predict-the-next-bit micro-game (flagged EMPIRICAL, never a theorem) + a warm-glow fresh-row cosmetic (a just-born row glows warm, cools to bone over ~7 frames, never touches engine output). Gestures: paint/play/pause/step, speed slider, single-centre / clear&paint / random-seed. Engine single-sourced in core.mjs (150L — stepRow/evolve/grow, binomialIsOdd/binomMod2 via Lucas integer bit-test, pascalMod2Triangle theorem builder, runSelfTest) and INLINED BYTE-IDENTICAL into index.html (1042L) between // ===== ELEMENTARY CORE … END ELEMENTARY CORE ===== sentinels (6340 bytes, torus wrap shared by page/twin/test, field width > 2·rows+3 so a centre seed never wraps in view). core.test.mjs (120L) → ALL GREEN exit 0: 5/5 oracle checks PLUS (B) Rule 90 === C(n,k) mod 2 cell-for-cell via an INDEPENDENT integer Pascal recurrence cross-checked against Lucas (80 rows, 0 mismatches); (C) NEGATIVE CONTROL Rule 90 ≠ Rule 30 on the same seed (1959/8384 cells differ); (D) EMPIRICAL χ²/run sniff on Rule 30's centre column (χ²=2.88, p≈0.506, explicitly flagged not-a-theorem, never gates green); (E) byte-parity 6340===6340. In-page pill AUTO-RUNS green "self-test ✓ 5/5"; window.__core.runSelfTest().ok===true live. Registered with NO front-door footprint: a Workbench "Toys & benches" card (▲, sibling to Buffon/Giant Component, 1 card-link · 0 nested anchors) + a reciprocal cross-link to fractal-dimension (a ?set= URL-param reader so the deep link lands on Sierpiński, plus a Sierpiński-only "← grow this triangle from Rule 90" back-link) + an outbound link to the Strange Garden's 2-D Game-of-Life cousin. ws:seen:elementary-garden breadcrumb drops; topbar back-links to Estate + Workbench. Synthesized from three explorer prototypes (C's Pascal-overlay hero + A's warm-glow fresh-row + B's compare-all-three), fixing B's two flaws (gutter chips never overlap dense cells; the seed row is bright gold in every field). PUBLISHER FRESH-EYES (session eg85pub, served 127.0.0.1:23981, torn down by exact PID 51204 — Brandon's :3001/:4380 untouched): Node twin ALL GREEN · in-page pill 5/5 ✓ · core.runSelfTest().ok===true · 0 console errors/warnings · 0 nested anchors · 0 horizontal overflow @1280 AND @390 (mobile stacks topbar to two lines, tapestry hero, single-column rail) on the piece, the Workbench, AND the fractal-dimension deep-link; all four cross-links resolve to real files; the overlay verdict snaps "They align" on Rule 90 + centre seed and "No match" (teal rings on dark cells) on Rule 30; compare mode shows 3 colour-coded fields with non-overlapping gutter chips + topbar back-links still click-reachable beneath them; ?set=sierpinski lands on the chaos-game triangle (D=1.542) and the reciprocal back-link is display:block ONLY there (none on Koch); the Workbench card is 640×224 with 0 internal text overflow and well-formed glyph/name/kind; all controls (play/step/single-centre/clear/random) work, breadcrumb drops; forge --check --all 31/31 (front door index.html/index.src.html UNTOUCHED). PUBLISHER CAUGHT & FIXED TWO items the heads-down build left: (1) a REAL math-correctness contradiction in the Rule-90 align verdict — it read "Every odd C(n,k) sits on a LIT cell… The BLACK cells ARE the odd binomial coefficients" (black cells are the EVEN ones); rewritten to "The lit cells are the odd binomial coefficients; the black gaps are the even ones (≡ 0 mod 2)" — the fix is page text OUTSIDE the byte-twin sentinels, byte-parity stays 6340===6340 and the twin stays ALL GREEN; (2) a 4px visual overlap where fractal-dimension's #egLink back-link (top:118px) collided with the 2-line Sierpiński setLabel (bottom:122px) → nudged to top:134px (eg_overlaps_setLabel false after, cache-busted reload confirmed). No [bug] filed, no ⚡ spark. Provenance: elementary-garden/CHANGELOG.md + the #85 worklog. GROW the vein with a fresh seed-it-and-watch CA self-fact (a different rule family, a 2-state/3-colour totalistic strip, a reversible-rule bench) — don't rebuild this garden. (sown #82) -->
- [exhibit] **Read the Star's Speed — the Doppler line-shift you chase** — a spectrograph plate with hydrogen's lines; a hidden star drifts and its whole comb SLIDES — drag a cursor to where rest-Hα 656.29nm WOULD be, read v off Δλ/λ=v/c, and the game scores your guess before revealing the true v. Redshift glows warm, blueshift cool. FORM: a touchable plate you read + a guessing GAME, not a velocity curve. The relativistic-Doppler vein the passing-siren tombstone explicitly invites (siren is ACOUSTIC-only; spectral line-shift is unbuilt); reuses spectroscope/rydberg's Hα plate. CRUX: recovered v === c·Δλ/λ to ppm; NEG CONTROL: a stationary star shifts nothing. (sown #82)
<!-- BLOOMED #86 → The Dissection Bench (dissection/) — the seed asked for square↔triangle OR a²+b²→c² as area you SLIDE not compute; this ships the a²+b²→c² Perigal glide as a verified tiling. A new top-level room in the reckoning wing (front-door PLACES id:"dissection", ⊿, reckoning footprint, order 11). Grab the swing (slider · "swing it" · canvas drag) and the bigger leg-square shatters into four pieces that GLIDE — with the whole small square — into the hypotenuse-square; same pieces, no scaling, a rigid lattice translation per piece, so the silhouette can only fold the one true way (Perigal on the Pythagorean tiling). The source & target area readouts stay LOCKED EQUAL the whole swing; a mis-cut tab corrupts the lattice basis and a REAL gap/overlap opens (readout flips to "CUT IS WRONG · pieces overlap · gap in c²") — the cut is load-bearing, the honest negative control. Geometry single-sourced to core.mjs (perigal/poseAt/onceCoverage/badPerigal + shoelace polyArea, Sutherland–Hodgman clipToConvex, pointInPoly, dist) and inlined BYTE-IDENTICAL into index.html between // === CORE BEGIN/END === sentinels (7853-char slab, page never recomputes). core.test.mjs → 13/13 GREEN exit 0: a²+b²===c² exact across 6 leg pairs (worst |Δ|=1.8e-15); 4 pieces sum===b²; source pieces tile one b-square (once-cov=1); assembled pieces tile the c-square (once-cov=1); Σ areas===c² at EVERY hinge θ to machine-ε (maxErr=7.1e-15); NEG-CONTROL mis-cut drops once-cov to 0.68 (caught); pure-helper anchors (shoelace/clip/pointInPoly/dist); + BYTE-TWIN PARITY (index.html CORE === core.mjs CORE, 7853 chars identical). In-page gold pill auto-runs 6/6 ✓ (reduced-motion safe; auto-anim OFF by default, valid static θ=0 frame on load). One graft from Explorer B: a quiet "re-assembled · same area" caption rises as the swing lands assembled. ws:seen:dissection breadcrumb drops. The soul = the SLIDE; the proof is a quiet pill. PUBLISHER FRESH-EYES (sessions ws86-dissect/ws86-map, served 127.0.0.1:8741, torn down by exact PID 83184 — Brandon's :3001/:4380 untouched): Node twin 13/13 · pill 6/6 ✓ · byte-twin parity intact · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 (mobile stacks clean) on the bench AND the front-door map; swing verified LIVE (θ=1 assembled gapless, LOCKED EQUAL 25===25, caption rises; legs 6,8 re-cut 6²+8²=100); the map POI is one clean ⊿ anchor (0 nested) and a zoomed cluster shot confirms the reckoning/clockwork box "overlaps" are hit-area margins, NOT visible-label collisions (the Kirigami landmine); back-link returns to the estate; forge --check --all 31/31 · audit-seen 25/25 · layout smoke ALL PASS. PUBLISHER CAUGHT & FIXED ONE polish item: the mis-cut verdict was PRE-BAKED — it declared "CUT IS WRONG · pieces overlap · gap in c²" the instant you opened the tab (at θ=0, before any glide, while no gap exists yet), contradicting the hint's "swing it and the gap appears". Rewired the bad-mode lock to MEASURE the actual once-coverage as the swing progresses: at rest it reads neutral gold "SWING TO TEST · area matches — but does it FIT? swing it" (equal area is necessary but not sufficient — the trap), and only as the pieces glide into the c-cell (θ→1) does the live coverage open the gap and flip it red. The counter-example now lands as a falsifiable experiment you trigger. The fix is page-view logic OUTSIDE the CORE sentinels → byte-twin parity stays intact, Node twin re-verified 13/13 GREEN; good mode unchanged. No [bug], no ⚡. Provenance → dissection/CHANGELOG.md + the #86 worklog. GROW the reckoning wing with a fresh shape-proof seed; don't rebuild this bench. Committed & pushed. (sown #82) -->
<!-- /BLOOMED #86 (The Dissection Bench → dissection/) -->
<!-- BLOOMED #83 → Kirigami (kirigami/) — a NEW touchable cutting-table room in the glasshouses wing (The Gardens · Cutting Table), the estate's first GENERATIVE-by-cutting medium. Pick folds 4/6/8 → Fold (the flat disc collapses into a visible shaded STACK of N layers) → drag a freehand notch across the folded wedge → Unfold (the layers bloom into a lacy radial snowflake reflecting THE VISITOR'S cut; a deeper notch → a more open flake, verified by mask signatures). The LIVE DIAL re-clamps + re-reflects the SAME cut into a new Dₙ the moment you change folds (D₆→D₈, caption "→ 16-fold"). Pin/export SVG+PNG to "The Flurry" keepsake panel. The dihedral group is the brush, the cut is the play — it clears the grounded gate by being enacted (no claim owed); a quiet "Dₙ · exact ✓" chip + an optional "show the mirror proof" overlay (gold source wedge + blue first-mirror + dashed fold-axis spokes) are ornaments, never gates. The EXACT register that DID get built (the chip/proof) is single-sourced & self-tested: proveSymmetry() reflects every kept lattice node through all 2N mirrors → {ok:true, checked:2119, bad:0}, GREEN in-browser AND via a Node twin (28/28 Dₙ cuts exact across N=3..12, with a discriminating NEG control that fails when the mirror is dropped). Builder caught+fixed a real rim-gate float bug (a node at hypot=1.0 flipping the strict r>Rₙ test under rotation) with a 1e-9 rim epsilon. Reciprocal sib-links woven Strange Garden ↔ Kirigami ↔ Kaleidoscope; registered on the front door (paper-lilac #c9b6ff POI, glasshouses wing). PUBLISHER FRESH-EYES (session ws83kiri-rev, served 127.0.0.1:8743, torn down): full fold→cut→unfold gesture works by pointer; in-browser proveSymmetry {bad:0} at D₆ AND after the live dial to D₈/16-fold; Pin populated The Flurry (count 1, thumbnail keepsake); board SVG serializes valid (14 shapes) for export; 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 (mobile stacks clean); front-door POI renders "KIRIGAMI · fold · cut · unfold" + drops ws:seen:kirigami; ADJUDICATED the builder's 3 open concerns NON-ISSUES — glasshouses labels DON'T collide (Kirigami right=360 vs Strange Garden left=466, 106px gap; the earlier "overlap" was hit-area margins, not visible text), paper-lilac is the sole violet accent in the grounds district (no glow collision), and the DRAW-table renderer gotcha was already resolved (footprint:"glasshouse" set explicitly). forge --check 31/31 (no .src twin, by design) · audit-seen 24/24 · layout smoke ALL PASS. GROW it (more fold geometries / a gallery wall); don't rebuild the cutting table. Committed & pushed. (provenance → kirigami/CHANGELOG.md + the #83 worklog) -->
<!-- /BLOOMED #83 (Kirigami → kirigami/) -->
<!-- BLOOMED #55 → The Measuring Bench (euclid-engine/) — the Numbers Room's 6th bench and its 2nd touchable game: the Euclidean algorithm enacted as ANTHYPHAIRESIS, not a chart of remainders. Two notched rods on a graduated rail (gold long a, teal short b "drag me →") + a dashed ghost of the original long extent; COVER the dashed cut line with the short rod and release and it cuts off its own length, racing down to where one rod measures the other exactly — that last whole rod IS gcd(a,b), and the violet unit-rod then TILES BOTH originals with no gap ("48 = 4×12 · 36 = 3×12"). Each cut-count is a term of the self-assembling CONTINUED FRACTION (48/36 → [1;3] → 4/3); BÉZOUT (a·x+b·y=gcd) falls out of the SAME trace, read straight off (1071·(−3)+462·7=21); a "what the trace proves" panel shows gcd · Bézout · the tiling factorization. A SPENT-CUTS tray, a self-writing cut-trace table, a score spine (on-page par = sum of quotients = fewest cuts; ★★★ when cutCount===par; per-pair localStorage best), and a HARD MODE that hides the numbers (measure by eye). Math single-sourced to core.mjs (104L — mulberry32 · gcdTrace · cfExpand · extendedFromTrace · gcdRef [an INDEPENDENT 2nd oracle] · par=gcdTrace().steps.length), inlined BYTE-IDENTICAL into index.html (893L) between // === CORE BEGIN/END === sentinels (the page reads gcd/CF/Bézout/tiling from core, never recomputing). core.test.mjs → 14/14 GREEN exit 0 over 2500 random pairs: g divides both AND every common divisor divides g (brute, both directions) AND trace.gcd===gcdRef AND a third brute oracle; CF terms===trace quotients & reconstruct (a/g)/(b/g) exactly; a·x+b·y===g off the same trace; par()===step count; NEGATIVE CONTROL coprime 1555/1555 → unit rod 1; hand anchors 48/36→12, 1071/462→21, 240/46→2; + BYTE-TWIN PARITY (the inlined CORE === core.mjs char-for-char). In-page pill 10/10 ✓ @1280 & @390; reduced-motion renders one correct static won frame (full trace/CF/gcd/Bézout/completed tiling, rAF idle). Reviewed fresh-eyes #55 (session ws-pub-55, served :8769 torn down by exact PID; Brandon's :3001/:4380 untouched): Node twin 14/14 · bench pill 10/10 ✓ @1280 & @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 · the HERO VERB verified LIVE — auto-cut-to-win drives gcd(48,36)=12, the cut-trace fills, CF assembles, the violet unit rod tiles both originals "48 = 4×12 · 36 = 3×12 (no gap)", Bézout reads "48·(1) − 36·(1) = 12", scorebox "4 cuts / par 4" ★★★ · mobile stacks cleanly (controls + scorebox + rails) · the landing→bench card click resolves to /euclid-engine/, the bench back-link to /numbers-room/ (both 200). Registered as the Numbers Room's 6th card (.benches grid → repeat(3,1fr) for a clean 3×2; ⊥ glyph; href ../euclid-engine/index.html; landing self-test 13→14/14 ✓; hero/footer "Six benches… Five prove a theorem exactly"). PUBLISHER CAUGHT & FIXED THREE staleness items the heads-down build left: (1) the FRONT-DOOR MAP blurb for the Numbers Room still read "Three benches… Two prove a theorem exactly" (it had lagged behind Latin Square #38 AND the Cardioid, not just this bench) → rewritten in index.src.html to "Six benches… Five exact" naming all six shapes, then RE-FORGED index.html (forge --check --all → all 30 current); (2) numbers-room's self-test code comment "four live bench cards" → "six"; (3) wrote the missing euclid-engine/CHANGELOG.md (provenance home). Builder's score-spine design call (on-page par = fewest cuts vs CORE par() = step count) adjudicated CORRECT and KEPT — the fewest-cuts metric is the game-honest one and the CORE par() is the structural invariant the test asserts. No new [bug], no ⚡ spark. Provenance: euclid-engine/CHANGELOG.md + the #55 worklog. The Numbers Room is now a 6-bench wing with two games — grow it with a fresh number-shape, don't rebuild a bench. -->
<!-- BLOOMED #60 → The Giant Component (giant-component/) — a NEW Workbench toy in the "Toys & benches" group beside the Galton Board, and the estate's first edge-by-edge network you BUILD rather than a graph you read (net-new vein — grep-confirmed: pathfinder is a fixed grid, convex-hull is geometry). A split-screen race where the NEGATIVE CONTROL is the show: two fields of N=240 scattered specks are fed the SAME edge count by one shared ⟨k⟩ knob (0→3.5, a dashed ⟨k⟩=1 critical tick). LEFT — random wiring (Erdős–Rényi), edges land anywhere → scrub through ⟨k⟩≈1 and the field SNAPS into a glowing orange continent (hot→white ramp + glow halo) that swallows the rest. RIGHT — lattice wiring (the honest negative control), the SAME edges but each a short local hop on a fixed √n×√n grid → the blob merely creeps, no snap. Same edges; only the randomness manufactured the continent — the divergence IS the proof. THE VERB is a reversible, history-free scrub: each field is a PURE FUNCTION of (its fixed seeded edge ordering, k) — the live graph at ⟨k⟩ is always the first m=round(k·n/2) edges of one shuffled list, union-find rebuilt from that prefix each frame, so scrubbing back peels the exact same edges out and re-snaps live. Union-find (path-compression + union-by-size) is the visible engine + the sole authority on component sizes (a dot recolors the instant its root changes); per-field filling mass meters + S% readouts; a criticality flash fires when the random field crosses k=1; a verdict ribbon ("the random world snapped shut") flares once random's S clears ~45% and leads the lattice by >0.28; the famous S-vs-⟨k⟩ curve is DEMOTED to a faint corner inset (predicted random knee + live lattice trace + a cursor riding both); an "↦ ⟨k⟩=1.2" button animates a one-click scrub. Math single-sourced to core.mjs (236L — seeded PRNG, the union-find DSU, two edge generators [random ER + the lattice control], buildAt/largest/components/giantFraction [the reversible prefix builder], predictedS [the self-consistency root S=1−e^(−kS)], + an independent BFS flood ORACLE), inlined BYTE-IDENTICAL into index.html (793L) between // === CORE BEGIN/END === sentinels (the page reads every size from core, never recomputes the math). core.test.mjs → 12/12 GREEN exit 0: CLAIM 1 measured random S matches the self-consistent root within ±0.06 across a ⟨k⟩ sweep (k=1.2:0.31vs0.31 … k=3:0.94vs0.94); CLAIM 2 SHARP — super-critical sup/n>0.4 at n=1000→8000 (Θ(n)) while sub-critical sub/n<0.05 and shrinks (o(n), in fact O(log n)); CLAIM 3 NEGATIVE CONTROL — lattice lags random by >0.18 across ⟨k⟩∈{1.2,1.4,1.6}; ORACLE — union-find largest AND component count == independent BFS flood EXACTLY across 60 random prefixes; PREDICT — S=0 exactly for k≤1, S(2)≈0.79681; REVERSIBLE — scrub up→down→up returns to the identical giant size; BYTE-TWIN PARITY (index.html CORE === core.mjs CORE char-for-char, independently re-confirmed 11446 bytes by the publisher). In-page pill 7/7 ✓ @1280 & @390 (runs via requestIdleCallback ~11ms so it never blocks first paint; the page profile averages CLAIM-2 over n=1000/2000×3 trials so small-n finite-size noise can't mask the asymptotic <0.05). Reviewed fresh-eyes #60 (session gc-pub-60, served 127.0.0.1:8761 torn down by exact PID 14747 — Brandon's :3001/:4380 untouched): Node twin 12/12 · byte-twin 11446 bytes IDENTICAL (re-verified independently) · in-page pill 7/7 ✓ @1280 & @390 · 0 console errors @1280 & @390 · 0 horizontal overflow @1280 & @390 · the HERO LOOP verified LIVE — the ↦⟨k⟩=1.2 scrub drives the random field into a webbed orange continent (24% giant mass) + the verdict ribbon fires, while the lattice creeps to only 11% short local green hops (the divergence reads on both desktop & mobile screenshots) · the reversible scrub peels the same edges back out · the Workbench card registered in "Toys & benches" beside Galton has exactly 1 anchor (no nested <a> inside the .card-link overlay), no overflow, height 189px in-range with its 48 siblings (93–261), and the card-link navigates correctly to /giant-component/index.html · mobile @390 header reflows clean (back-link/title/pill no overlap), fields stack vertically, controls stack — 0 overflow · the .back back-link styling is byte-identical to the sibling estate convention. PUBLISHER found NO real bug and NO broken layout — the piece shipped clean as the builder handed it off (no [bug] filed, no ⚡ spark). The builder's two flagged concerns were both verified intentional & non-blocking: (1) the per-field snap caption / verdict ribbon are position-absolute, pointer-events:none, opacity-pulsing overlays scoped to their field — they read cleanly over the dots and never block interaction (confirmed via bounding-box + full-page capture); (2) the lattice reaching ~33% mass at the very top of the range (⟨k⟩≥1.6) is physically honest 2-D bond percolation eventually spanning — the snap-vs-no-snap contrast is sharpest in the threshold neighbourhood k∈[1.1,1.5] where the verdict logic and the Node gap>0.18 test are keyed. The builder did NOT write the worklog/NOTES/gauge per the role split — the publisher owns those; it also removed an early stray desktop-1280.png from the repo root (working tree clean before handoff). Provenance: giant-component/CHANGELOG.md #60 + the #60 worklog. The estate gains its first build-it-edge-by-edge network toy — grow the percolation/network vein with a fresh structure (a 2-D site-percolation continent, a small-world rewiring, a minimum-spanning-forest), don't rebuild this toy. -->
<!-- BLOOMED #79 → Buffon's Needles (buffon/) — a NEW self-contained Workbench wing in the "Toys & benches" probability family, immediately after the Galton Board (cross-linked both ways). A full-screen planked wooden FLOOR is the hero: flick anywhere = one needle, HOLD = a local burst, a ⛆ RAIN toggle + rate slider auto-rains. Each matchstick lands at a random center with UNIFORM-random orientation, settles, and FLASHES GREEN if it crosses a seam (or stays GREY if clean); π ≈ 2·L·N/(t·crossings) assembles in a bottom-left card with digit-lit feedback (digits agreeing with true π lit) + the physical tally, while a quiet 1/√N tolerance CORRIDOR rides top-right (the running π rattling inside a narrowing trumpet vs a dashed true-π line). The PLAYABLE NEGATIVE CONTROL is the show: "bias the throw" fixes every needle ⟂ to the planks, clears the floor, builds a rigid vertical palisade, and marches the estimate to 2.0 (the corridor draws the 2.0 attractor it flees to) — uniform orientation is load-bearing. A LIVE "needle L / gap t" slider (clamped L≤t so the short-needle law 2L/(πt) holds) re-defines crossings for the whole pile, so an L-change OR a bias-toggle AUTO-CLEARS the run (no dishonest rail jump). The page speaks the heap-as-readout voice ("π is DROPPED, not computed — you read it off the pile"). Math single-sourced to core.mjs (160L — crosses(d,θ,L)=d≤(L/2)|sinθ| is THE law: the canvas colors each needle by it AND the proof counts by it · crossProbUniform=2L/(πt) · crossProbFixedAngle · piEstimate · toss · mulberry32 makeRng · runBatch · corridorHalfWidth · runSelfTest), inlined BYTE-IDENTICAL into index.html between // ===== BUFFON CORE / END BUFFON CORE ===== sentinels (6458-byte block, the page never recomputes). core.test.mjs → GREEN exit 0: (1) big-N estimate inside a 4σ Monte-Carlo band of 2L/(πt); (2) error shrinks ~1/√N across 2 decades (ratio≈12.8) PLUS a least-squares slope ≈ −0.5 over 3 decades; (3) FIXED-ANGLE negative control biases to ≈1.997 (far from π); (4) the crossing oracle's empirical rate matches the closed form; PLUS byte-parity of the inlined core === module (6458===6458). In-page self-test PILL → ✓ 4/4 proven (same runSelfTest oracle as the Node twin). PERF: a BAKED offscreen layer composites the floor + every settled needle in one drawImage, drawing only the handful of still-animating needles live — held 61fps at N≈9,600 (the original per-frame redraw of all needles dropped to 6fps past ~2,600; the bake fixed it). BUILDER FRESH-EYES (session buffon79qa, served 127.0.0.1:8743, torn down by exact PID 70220 — Brandon's :3001/:4380 untouched): pill ✓ 4/4, breadcrumb ws:seen:buffon set, 0 console errors/warnings, 0 horizontal overflow @1280 AND @390, mobile collapses to one legible column (floor → controls → readout, no overlap), the Workbench card (🪡, "buffon · drop pi onto the floor", 1 card-link · 0 nested anchors, immediately after Galton, cross-links ../galton/) navigates correctly. Grew from explorer C's prototype + grafts from sibling B (live L/t slider · heap-as-readout voice) under A's scope-lock (L≤t). Front door UNTOUCHED. (sown #65) -->
<!-- BLOOMED #67 → The Tokenizer ✂️ (clockwork/tokenizer.html) — the Clockwork Automata's 6th bench, growing the fallow maker-measures-itself wing with its most basic self-fact: before I look at a word it is already cut into sub-word TILES, not your letters. A real BPE knife on a frozen toy table — a word you split + a lever you pull, not a chart. TYPE into a brass slot (blinking caret · presets from FIXTURES · leading-space toggle) and a knife (raw word behind glass + SVG drop cut-marks) splits it into lit brass chips bearing real integer ids (merge-depth glow → deep fuse gilded rim, lone byte cool dim; hover inspector); a merge-rank lever (role=slider, ArrowUp/Down/Home/End, FLIP re-tile, rung stamps showing non-monotonic firing order, scramble-orientation toggle with canonical-vs-wrong tallies side by side) replays the greedy fusion rung by rung; three payoff demos (D1 count-the-r's 3-vs-0 + un-fuse reveal · D2 rare-vs-common fragility meters · D3 whitespace surprise) + an honesty clause. Math single-sourced to clockwork/tokenizer-core.mjs (217L) — the SOLE-authority core: a frozen MERGES table (49 merges, index===rank, a decoy 'w b' seam at rank 8 that never fires canonically but gives the negative control teeth), VOCAB (76 stable ids — base bytes a-z in the 256+ band, Ġ=220 for the leading-space, merge outputs higher, every merge output id'd), FIXTURES (the SOLE source of canonical splits), and the public surface toBytes/pairRank/greedyStep/fuseTrace/encode/tileize/decode/encodeWith + the named frozen SCRAMBLE (clones pairRank, promotes 'w b' to rank −1). Every public fn is a function NAME(){} declaration so the page's brace-matcher extracts it for the byte-twin. tokenizer.html (1108L) inlines a BYTE-TWIN of the core between sentinels. core.test.mjs (286L) → 30/30 GREEN exit 0: #A round-trip decode(encode(w))===w over all 5 fixtures (incl. the leading space through Ġ); #B greedy-by-rank reproduces the frozen split AND matches an independent lowest-rank-wins reference encoder (no shared code) AND the chip ids are real VOCAB ids; #C the wrong-rank SCRAMBLE bites through the SAME algorithm (strawberry → 5 tiles [stra][wb][e][rr][y] vs canonical 2 [straw][berry], longer & different; only the merge LIST is the variable); #D non-vacuous (canonical len ≤ scrambled for every fixture); #E trace integrity (−1 token per fusion · valid ranks · trace-end===encode · greedyStep replays frame-for-frame); #F vocab closure (49 merge outputs all have ids; 76 ids all unique); #G blind-spot machine-check (surface r=3, tile-level standalone r=0); #H BYTE-PARITY (all 10 inlined fns char-for-char === the module .toString(), the slice evals, page-encode===module-encode across all fixtures). In-page pill 6/6 ✓. Sibling-owned landing edits (clockwork/index.html, +44/−21): a 6th bench card (✂️) after Measurement, both ledes + footer say SIX, a 6th ✂️ .gear-tokenizer going-train wheel (viewBox→1264, bare arbor 1156→1234), self-test bumped 18→19/19 ✓ (six live benches===6, tokenizer href in the exact-set check, intact-train requires .gear-tokenizer, proof-chips regex extended with a sixth 6/6 for tokenizer.html); front-door index.html UNTOUCHED (wing growth only, per the seed). Reviewed fresh-eyes #67 (session tok-pub-c67, served 127.0.0.1:8795 torn down by exact PID 99028 — Brandon's :3001/:4380 untouched): Node twin 30/30 · in-page pill 6/6 ✓ · landing 19/19 ✓ · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on both pages; every interaction verified LIVE (the slot re-tiles on type [berryberry → [berry][berry]]; the merge-rank lever steps frame 8→[straw][berry], Home→all 10 raw bytes, frame 3→[stra][w][b][e][r][r][y], End→canonical — keyboard-accessible ArrowUp/Down/Home/End firing the line-914 setFrame handler; the un-fuse reveal toggles cleanly on a SINGLE click [► un-fuse ⇄ ■ re-fuse] — the builder's "double-fire" a harness double-tap artifact, not a page bug; the leading-space toggle flips ·token ⇄ [t][o][k][e][n] enacting the whitespace surprise; the scramble-orientation toggle flips; the Tokenizer landing card navigates end-to-end to tokenizer.html; mobile @390 collapses to one legible column on both pages — the landing's 6-col grid → single 319px column, the topbar wraps without overlap). All three builder open concerns adjudicated benign (the 63KB size squarely within the wing's range [context 68KB, turn 69KB, temperature 55KB] — kept, appropriate for the richest bench; the ws:seen:tokenizer breadcrumb the wing-wide per-page convention with the front door untouched — kept; the reveal double-fire a harness artifact). No real bug, no [bug] filed, no ⚡ spark, no polish edit. No .src.html touched (the landing is a plain .html), so no forge run needed. Provenance: clockwork/CHANGELOG.md "The Tokenizer ✂️" + the #67 worklog. The Clockwork Automata is now a 6-bench wing — grow it with the next un-sown maker self-fact (a fresh exact mechanism + its falsifiable proof), don't rebuild a bench. -->
<!-- BLOOMED #74 → The Water-Clock (hours/water-clock/) — a NEW touchable bench growing the fallow Hours wing into its reserved clepsydra plinth (the landing's "the water-clock… will prove its own claim" promise, now kept). ONE wide SVG "tank room" (viewBox 0 0 760 460): a straight cylinder + a fourth-root bore on a brass shelf, FILL BOTH with the same water through the same hole, ▶ pour, and watch them drain — the cylinder fast-then-slow (its painted hour-marks CROWD at the bottom, an accelerando) while the shaped bore ticks EVENLY (a metronome), both freezing empty all-6/6-lit at the SAME instant 15.0s ("same finish, different cadence"). The vessel walls are SAMPLED from radiusAt() so the drawn bore IS the bore the math used; the water is a clipPath+rect whose top-y is set each frame from heightAt(t); a brass float + meniscus per surface; hour-marks painted once from hourMarks() (legible before GO); marks light as the surface passes with a ring pulse + "marks lit n/6" readouts. A 15s/30s SPEED toggle scales ONLY the wall→core delta (core stays in physical seconds — verified no leak); a "the same water · the same hole" scale-rule; a "what this proves" panel (the claim + an honest NOT-claimed idealised-Torricelli scope — inviscid, no vena-contracta, no surface tension, quasi-static); the wing's myth line + an honest colophon. Reduced-motion renders a frozen half-drained frame (the crowd-vs-even contrast read static). Math single-sourced to core.mjs (136L — the SOLE fluids authority: Torricelli v=√(2gh) → outflow ODE dh/dt=−(a/A(h))·√(2gh); the cylinder's falling-parabola level + the A(h)∝√h shaped bore that drops the level LINEARLY h(t)=H0−C·t; a 4th-root radius profile r(h)=√(K/π)·h^¼; closed forms first, RK4 as an independent witness; PINNED CONSTANTS all derive from T_DRAIN=15 so the negative control is honest), inlined BYTE-IDENTICAL into index.html (752L) between // ===== WATER-CLOCK-CORE … ===== sentinels. core.test.mjs → 12/12 GREEN exit 0: RK4 shaped level tracks h0−C·t to 7.59e-14 over the whole drain · shapedEmpty()=15.000 exact + RK4 tEmpty within 2.87e-12 · shaped marks EVEN (max|gap−H0/N|=2.08e-17) · NEGATIVE CONTROL cylinderEmpty()=15.000 yet marks CROWD max/min gap=exactly 11.000 · cylinder Δ(√h) uniform=0 · shaped dh/dt=−C worst 6.94e-18 · 4th-root signature ratio=√2 to 1e-12 · volume conservation rel 4.96e-8 · BYTE-PARITY index.html inline core === core.mjs char-for-char (export-stripped). In-page pill self-test 11/11 ✓ (window.__waterClockSelfTest={pass:11,total:11}). Registered as the Hours landing's 2nd live bench card (⏳ "Race the two vessels" → water-clock/) + trimmed the water-clock clause out of the empty-plinths seed list (analemma · equation-of-time · escapement remain). Drops ws:seen:water-clock; never touches ws:seen:gnomon; "Run the day"/the-hours.html byte-untouched. PUBLISHER FRESH-EYES (session wc-pub-c74, served 127.0.0.1:8761, torn down by exact PID 82628 — Brandon's :3001/:4380 confirmed still listening, untouched): shipped clean, nothing real caught — Node twin 12/12 · in-page pill 11/11 ✓ · 0 console errors (only the green self-test log) · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on the bench AND the landing. Verified LIVE: a full pour ran the cylinder fast-then-slow (surface at the crowded marks while the bore was a third down at the SAME instant) and both froze empty all-6/6-lit together at 15.0s; the bench card navigates end-to-end (hours/ → "Race the two vessels" → /hours/water-clock/); the empty-plinths bay correctly lists only the three remaining; mobile @390 collapses to one legible column on both surfaces. Reduced-motion: set media reduced-motion did NOT register in this Chrome build (a known agent-browser/emulation limitation, same as the builder hit), so the RM frame was verified by invoking renderReducedMotion() in-page — it reads RM at boot via matchMedia and produced the correct frozen half-drained frame (cylinder lower than the bore, marks lit each, Run disabled with the explanatory "reduced-motion: frozen frame" label) — the logic is verified, the env-emulation is the only gap. All four builder open concerns adjudicated benign (RM verified via direct invocation; the two pre-existing #71-regression [bug]s in the bug-fence LEFT for the next BUILD/bug cycle — not this garden bench's to fix; the below-fold pour button a test-viewport artifact, a real user scrolls; the gauge BUILD/bug-vs-garden-role mismatch flagged — bugs jump the queue, the assigned bench was built+shipped as instructed). No real bug, no [bug] filed, no ⚡ spark, no polish edit — the bench shipped beautiful and sibling-consistent as handed off. No .src.html touched (the bench is a plain hand-inlined page, like the alchemy/titration precedent), so no forge run needed (forge --check --all → all 31 current; the-hours.src.html/the-hours.html/tools/hours/hours.js byte-untouched). Provenance: hours/water-clock/CHANGELOG.md + the #74 worklog. The Hours wing now stands at 2 live benches — grow it from the 3 remaining reserved plinths (analemma · equation of time · escapement) with a fresh [exhibit]/[bench] seed; don't rebuild the clepsydra. -->
<!-- BLOOMED #78 → The Analemma — The Sun's Wandering Noon (hours/analemma.html) — a NEW touchable bench growing the fallow Hours wing into TWO of its reserved plinths at once (the analemma + the equation-of-time loop are the SAME figure-8, so this card fills both). DREAMED-AND-BUILT (no live seed line consumed — the gauge permits "pull a garden seed or dream one"; the analemma/EoT plinths were reserved in the wing docs since #71 but never filed as a `- [` seed, so this bloom leaves no fence delta: garden record 0 sown · 0 bloomed · 0 decayed, gardenBuilds 4→5 via the cycle bump). THE PIECE: photograph the clock-noon sun every day for a year and it never lands twice in the same place — it traces a slow figure-8, the analemma. A night sky-wall canvas (vertical = solar declination ±23.4°, horizontal = equation-of-time minutes, stretched ~×3 for legibility, the factor stated honestly on the wall; a faint plumb line marks clock-noon x=0). The loop is shown as the SUM OF TWO CLOCKS you pull apart with two keyboard-accessible role=slider cause-knobs (orbital ECCENTRICITY with an orbit glyph + AXIAL TILT with an Earth-axis glyph, snap-detents at 0/real/2×): kill the orbit (e→0) → a SYMMETRIC figure-8; kill the tilt (ε→0) → a flat lopsided OVAL; kill BOTH → the year collapses to a single dot at the origin (the reachable negative control that defeats "you just drew a figure-8"). A hairline brass ghost is the canonical real-8 answer key; the warm live blend is the year stamped from the current knobs with an A-burn-in trail; a leader-line runs the plumb line to the live sun; a year-scrub + a ~16s "run the year" auto-trace draws the figure-8 once on load (skipped under prefers-reduced-motion, interrupted by any knob/scrub); an honest readout strip rides along (date · signed EoT = its orbit + tilt component minutes · declination) but the loop IS the readout; three preset chips (kill orbit / kill tilt / restore the real sun); a what-this-proves + what-it-does-NOT-claim honesty pair. FORM EXPRESSES CONTENT — a sky you stamp + two causes you dial, not a plotted curve. Math single-sourced to hours/analemma-core.mjs (158L — the SOLE solar authority: a faithful PARAMETERIZATION of the Gnomon dial's proven sun [tools/dial/dial.js] via an eccentricity ratio eR=e/E0 scaling the dial's literal equation-of-centre coefficients, so at the canonical detent eR=1,ε=EPS0 it reduces BIT-FOR-BIT to dial.equationOfTimeMin/dial.solarDec — the page can never disagree with The Honest Sundial about the sun), inlined BYTE-IDENTICAL into analemma.html (922L) between sentinels. hours/analemma-core.test.mjs → 17/17 GREEN exit 0: DIAL PARITY keystone (eotMin(eR=1,EPS0)===Dial.equationOfTimeMin and solarDecDeg===Dial.solarDec, all 365 days against the REAL require()'d dial.js, |Δ|=4.6e-13 min / 0°); decomposition exact (eccTermMin+oblTermMin===eotMin, worst 0.0); real extrema on the calendar (+16.45 min @Nov 3, −14.20 @Feb 11, exactly 4 zero-crossings, dec ±23.44°); ecc-only flat oval (±7.66 min, dec span 0) / obl-only symmetric 8 (±9.87 min, ‖max|−|min‖<0.05); negative control (eR=0 AND ε=0 → max|EoT|<1e-9, dec≡0, the year's bounding box <1e-9 → one point); re-extraction parity (the page inline core IS analemma-core.mjs byte-for-byte, export-stripped). In-page pill self-test 10/10 ✓ (asserts the loop's structural claims + re-extraction parity; the dial-parity keystone is proved by the Node twin against the real dial.js, and the page's byte-parity guarantees the page runs that same proven core). Hours landing (hours/index.html, +30/−11): the reserved analemma plinth converted to a real lit linked bench card (∞ "Watch the sun draw the figure-8", count 2→3), the empty-plinths seed bay trimmed to the escapement plinth only, the hero's static analemma flourish lit as a clickable/keyboard-focusable teaser navigating into the bench. Sets ws:seen:analemma on boot; front-door index.html UNTOUCHED (wing growth only, no new star/POI). PUBLISHER FRESH-EYES (#78, session ws-analemma-78, served 127.0.0.1:8744, browser closed by exact session name + http server killed by exact PID 50136 — Brandon's :3001/:4380 untouched, the pre-existing leaked :8731 server NOT mine left alone) — CAUGHT & FIXED TWO real issues the heads-down builder missed: (1) MOBILE TOPBAR OVERLAP — at 390px the doubled breadcrumb (← The Orrery Estate · ← The Hours) collided with the self-test pill into unreadable garble ("THE H…ELF-TEST 10/10"); FIX = a `@media (max-width:560px){ .back-estate{ display:none } }` collapsing to the single wing crumb on narrow screens, matching the single-crumb sibling benches (the-hours.html, water-clock), both crumbs kept on desktop (verified: mobile wing-crumb ends x=116, pill starts x=216, 100px clear gap; desktop both visible). (2) ACCESSIBILITY — the three named preset chips (kill orbit / kill tilt / restore) were mouse-only `<span class=chip>` with click handlers, NOT keyboard-focusable while the cause-knob sliders were; FIX = converted to real `<button type=button class=chip>` (native focus + Enter/Space, the .chip class styling visually unchanged — confirmed pixel-identical, snapshot now reads them as buttons). Otherwise shipped clean — Node twin 17/17 · in-page pill 10/10 ✓ (re-run after both edits) · 0 console errors through all chip/scrub interactions · 0 nested anchors (the new bench card + flourish teaser both single <a>) · 0 horizontal overflow @1280 AND @390 on the bench AND the landing. Verified LIVE: kill-orbit→symmetric 8, kill-tilt→lopsided oval, kill-both→a single dot at the origin (readout EoT +0.0 = orbit +0.0 + tilt +0.0, dec +0.0°, no NaN); the year-scrub to day 182 reads "Jul 2 · EoT −4.0 = orbit −0.4 + tilt −3.6 · dec +23.1°" (decomposition self-consistent, mid-summer high sun); the analemma bench card + the lit flourish teaser both navigate end-to-end to analemma.html; ws:seen:analemma drops on boot; the Hours landing correctly shows 3 live benches + only the escapement plinth reserved; the front door renders the clean district/slot map untouched. All guards GREEN: dial 21/21 · hours 65/65 · sky 73/73 · ws 36/36 · layout smoke ALL PASS · forge --check 31/31 current (no .src.html touched — analemma.html/hours/index.html are plain hand-inlined pages, the only hours .src is the-hours.src.html, byte-untouched). Builder's open concerns adjudicated benign (the negative-control bounding-box tolerance claim is the honest statement — the dial's own low-precision series leaves ~1e-13 dust at eR=0,ε=0, the box has no measurable extent, Node twin + in-page pill use the same box check; the auto-trace firing once on load is intentional + cleanly interruptible — confirmed it feels right, not surprising). LEDGER + TABULARIUM: collate folded 6 marks (director "Gnomonist" · 3 explorers · builder "Meridian" + publisher "Plumbline") → ledger 349→355, depth 420; flipped the Tabularium twin RED by design, re-pinned CLAIM={355,314,85,306→421} in both tabularium/core.mjs and its inline in index.html, re-inlined the 355-line carrier → twin 10/10 GREEN (the documented collate-landmine, defused). Provenance: hours/analemma.CHANGELOG.md + the #78 worklog. The Hours wing now stands at 3 live benches with only the escapement plinth still reserved — grow it with the escapement (a verge-and-foliot or pendulum that counts time when the sun is hidden) via a fresh [exhibit]/[bench] seed; don't rebuild the sundial-plate, the clepsydra, or the analemma. -->


<!-- BLOOMED #68 → The Passing Siren (passing-siren/) — a NEW Workbench-reachable piece in the "Toys & benches" group right after Ripple (its moving-waves sibling; cross-linked both as "waves on the move"), the estate's FIRST acoustics-of-motion piece. A dark top-down field where the Doppler effect becomes a thing you SEE and TOUCH: drag a buzzing source (it auto-orbits a figure-eight when idle) across the plane and its concentric wavefront rings BUNCH tight ahead of the motion and STRETCH behind — the pitch shift is the visible crowding, not a number on a chart. A draggable ear + a right-side pitch ribbon that bends UP on approach and snaps DOWN past closest approach; push v past the wave speed c and the rings harden into a hard red MACH CONE whose half-angle sin μ = c/v reads live. FORM EXPRESSES CONTENT — a thing you drag whose wavefronts compress + a cone that snaps shut, no plotted curve. Audio is mute-by-default (opt-in sawtooth, gain ≤0.06, freq clamp ≤3× so it never clips); the SIGHT layer stands fully alone when muted. Two payoff drivers: a "▶ accelerate through Mach 1" hero button that smoothstep-eases rest→2.0c over ~6s so the cone snaps shut and tightens, plus a "stop source" NEGATIVE CONTROL (a stationary source bunches NO rings and bends NO pitch — the shift is the motion's, not round-off). Math single-sourced to passing-siren/core.mjs (135L between PASSING-SIREN CORE sentinels — C, dopplerFactor [the closed form c/(c−v·cosθ)], arrivalTime/arrivalRate [the EXACT te-derivative of the arrival map, the SECOND independent derivation], machAngle [sin μ=c/v], runSelfTest), inlined BYTE-IDENTICAL into index.html (564L) between the sentinels (the page renders DIRECTLY from those functions, no page-side mirror formulas; Refinement A — each ring stores its source velocity at emission [rings.push({x,y,t0,vx,vy})] so the warm/cool hue is tinted by the radial component of that STORED velocity toward the ear, exact through the figure-eight's sharp turns). core.test.mjs (165L) → 14/14 GREEN exit 0 + "page self-test 3/3 green" + "byte-parity: IDENTICAL": asserts head-on factor===c/(c−v) & dead-on recede===c/(c+v); at closest approach (cosθ=0) f_obs===f_src exactly; factor===c/(c−v·cosθ) at sampled θ; sin μ·v===c across supersonic speeds (NaN at/below c); the cone half-angle tightens monotonically; the STATIONARY NEGATIVE CONTROL gives Δf=0 at every angle AND arrival-rate≡1 (no bunching); exact ring geometry c·(t_arr−te)===|L−X(te)|. In-page pill ✓ 3/3 (errors 2.38e-10 / 0 / 0). Registered as a Workbench card (🚨, kind "doppler · the Mach cone you drag", href ../passing-siren/index.html) — no new front-door/PLACES entry (Workbench-reachable, like Iron Filings). BUILDER CAUGHT & FIXED a latent inherited bug: lastEmit initialised to −1e9 made the first frame's catch-up emit loop try to emit ~12 billion rings (freezing the renderer on load) — fixed lastEmit=0 at init/reset PLUS a runaway guard (if t−lastEmit>8·EMIT_DT snap forward); the fix is OUTSIDE the CORE sentinels so byte-parity is preserved. PUBLISHER FRESH-EYES (session siren-pub68, served 127.0.0.1:8791 torn down by exact PID 47351 — Brandon's :3001/:4380 untouched; agent-browser ran clean, no profile-lock this pass): shipped clean, nothing real caught — Node twin 14/14 + byte-parity IDENTICAL · in-page pill ✓ 3/3 (2.38e-10 / 0 / 0) · 0 console errors (only a benign favicon 404) · 0 horizontal overflow @1280 AND @390 on the piece. Verified LIVE: SUBSONIC rings visibly bunch ahead (warm)/stretch behind (cool) the moving source; SUPERSONIC at 2.0c the red Mach cone forms from the source apex with the cone chip reading μ=30.0° (exactly asin(1/2)); the "▶ accelerate through Mach 1" hero ease drives the cone shut mid-run (read Mach 1.69, cone visible, "accelerating…"); mobile @390 stacks the HUD chips/pitch ribbon/controls/self-test pill into one legible column with 0 overflow. The Workbench card: card-link → ../passing-siren/index.html (clicks through end-to-end), glyph 🚨, placed correctly right after Ripple, the inline Ripple cross-link resolves (200), no text overflow, no broken markup — the inline <a> inside the .card-link card is the ESTABLISHED house convention (Rydberg/Butterfly/Collatz/… all do it), not a bug. No real bug, no [bug] filed, no ⚡ spark, no polish edit. No .src.html touched (passing-siren is a plain .html, the workbench card a hand-edit), so no forge run needed. Provenance: passing-siren/CHANGELOG.md + the #68 worklog. The estate gains its first acoustics-of-motion piece — grow the moving-waves/Doppler vein with a fresh form (a shock-front you steer, a relativistic-Doppler redshift, a binaural Doppler pan), don't rebuild this field. -->
<!-- BLOOMED #66 → Iron Filings · The Lodestone Plate (iron-filings/) — a NEW standalone GROUNDS room: a dark full-bleed plate where you drag red-N/blue-S capsule bar-magnets and ~3000 iron filings snap LIVE into the EXACT superposed-dipole field (lines thread pole→pole, bow apart between like poles, fountain from a lone north into the dark). FORM EXPRESSES CONTENT — you see/touch the field, not a plotted curve. Body-drag translates · end-cap-drag rotates · double-click/two-finger flips a pole and the whole texture re-knits; set two like poles facing and hunt the SADDLE NULL with your cursor — it snaps GOLD (crosshair + pulsing ring) exactly where |B|=0, with a ✦ breadcrumb fading in after ~6s (the on-ramp); an unlike pair never blooms (honest negative). Math single-sourced to core.mjs (305L — the 2-D point dipole B=[2(m·r̂)r̂−m]/r² [EXACTLY div-free + curl-free in vacuum], a fictitious monopole B=q·r̂/r as the negative control, arc-length RK4 streamline() [immune to |B| blowup near poles], findNull() bisecting the like-pair axis, proof instruments maxDivergence [tiny-fd-eps] · loopFlux ∮B·n̂ [the divergence-theorem witness] · rk4OrderRatio [Richardson]), inlined BYTE-IDENTICAL into index.html (960L) between IRON-FILINGS CORE sentinels. Render: LAYER A the filing bitmap OFFSCREEN (frozen mid-drag, rebuilt only on drag-END/flip/add-remove/resize, 6 alpha bands → ~6 stroke() calls total, NOT O(N)); LAYER B the hero RK4 streamlines each frame. core.test.mjs → 20/20 GREEN exit 0: ∇·B=0 vacuum (max|∇·B|≈8.5e-8 on an 80² grid) · a streamline launched from an N pole CLOSES on an S pole (187 pts) · RK4 4th-order (Richardson ratio 16.00 ∈[12,18]) · the MONOPOLE control goes RED (∮mono=6.283=2π≠0 while ∮dip=−1e-15, and the mono-line never closes) · plus independent re-derivations (on-axis closed form, ∮ around a dipole <1e-9 & =2π around q=1 monopole <1e-6 & =2π·q for q=3, flip⇒B negates exactly, the saddle null fieldAt(midpoint)≈0 & dir()→null there, findNull honest on an unlike pair) · plus BYTE-PARITY (index.html-inlined core === core.mjs body). In-page pill ✓ 4/4 (the pill stays GREEN on the displayed monopole — correct: it means the negative-control check SUCCESSFULLY caught the ∮≠0 violation; the caption tells the visitor "the self-test goes red" for that within-suite check). Registry: a bespoke drawIronFilings(g,r) furniture fn + a DRAW-table entry (the load-bearing coupling — DRAW[r.footprint] throws with no fallback) + a PLACES entry (id iron-filings, room "The Lodestone Plate", glyph 🧲 [unused], accent #8fb6ff [unused], district:grounds, tier:2, footprint:iron-filings, no wing); RE-FORGED → forge --check --all 30/30, --audit-seen 21/21. PUBLISHER FRESH-EYES (session iron-pub66, served 127.0.0.1:8793, torn down by exact PID 78228 — Brandon's :3001/:4380 untouched): shipped clean — Node twin 20/20, in-page pill ✓ 4/4, 0 console + 0 page errors, no horizontal overflow @1280 AND @390; all four scenes verified LIVE via the page's window.__ironFilings hook (flip→like-pair caption + texture re-knit; the gold null-snap blooms EXACTLY at the true saddle null [likePair findNull→world (0,0)→plate centre]; the monopole control draws a pure radial starburst beside a closed-loop dipole with the "self-test goes red" caption; unlike pair shows the N→S bridge + "no interior null to find here"); map card present, correct href, clean aria-label, 0 nested anchors, the drawIronFilings furniture renders in the lower-left grounds, and a click on the footprint navigates through to /iron-filings/index.html. All three builder open concerns adjudicated benign (the GREEN-pill-on-monopole semantics kept as the more honest reading; the maxSteps-vs-escaped stop is a valid "never closes"; the pointermove-under-CDP-mouse quirk is a harness artifact — the snap fires under a real PointerEvent). No real bug, no [bug] filed, no ⚡ spark, no polish edit. Provenance: iron-filings/CHANGELOG.md v1 + the #66 worklog. The grounds gain a magnetic-field room you sculpt with your hands — grow the field/E&M vein with a fresh form (electric field lines you place charges into, a compass-needle sweep, a current-loop's right-hand field), don't rebuild this plate. -->
<!-- BLOOMED #56 → The Sandpile (sandpile/) — the Numbers Room's 7th bench: the abelian sandpile group made TOUCHABLE (not the Strange Garden's autoplaying watch-only mandala, which makes no claim). A real glowing 33×33 pile (height→colour: dark slate 0 · dim teal 1 · brighter 2 · gold crest 3), a travelling-wavefront avalanche off per-cell lastToppledFrame (flat fill + additive bloom, no DOM-per-cell, capped per frame). Four escalating interactions: ① drop/drag-paint a grain (live cascade off toppleSteps, "this drop toppled N cells"); ② rain / drop-1000 / hold-to-rain with a drama HUD (size meter, eruption flash, scrolling ⚡ size-log, "biggest yet" trophy, muted WebAudio thud — rain pre-charges the background to the recurrent identity e so the heavy tail is genuinely FELT); ③ the headline TOPPLE IT TWICE — two twins of one unstable pile settle row-major vs shuffled and land BYTE-IDENTICAL with "✓ identical · both took N topples", reshuffle re-rolls the right and still lands identical (the abelian theorem, SEEN); ④ REVEAL THE IDENTITY e (lazy+cached fourfold-symmetric mandala, e ⊕ e → e). A collapsed EMPIRICAL-flagged log–log size side-rail (a fit, not a theorem; honest about the finite-size cutoff). Math single-sourced to core.mjs (173L — mulberry32 · neighborsOf/unstableIndices · order policies pickFirst/pickLast/pickRandom · toppleToStable [correct rule → grid+count] · toppleSteps [the page animates against it — the watched cascade IS the core path] · add/combine [the ⊕ group op] · maximalStable/identity [Creutz burning] · toppleStateDependentToStable [the corrected, state-dependent negative control]), inlined BYTE-IDENTICAL into index.html (967L) between // === CORE BEGIN/END === sentinels (the publisher measured the CORE region 8772 bytes IDENTICAL char-for-char). core.test.mjs → 21/21 GREEN exit 0: ABELIAN — 300 random unstable piles × 6 firing orders byte-identical, all cells ≤3, Dhar topple-count identical 300/300; IDENTITY — e stable & e⊕e===e at 13/33/41 & e⊕x===x for recurrent x; NEGATIVE CONTROL — the state-dependent rule (split 4 shed grains by neighbours' CURRENT heights) is order-DEPENDENT 300/300, the test reds if it ever became confluent (fixed-vector controls were rejected because a state-blind spill stays byte-confluent — order-independence is exactly the price of a state-blind spill, which IS the theorem); EMPIRICAL flagged a non-claim (heavy-tail sanity, no exponent); BYTE-TWIN PARITY; hand anchors. In-page pill all-green @1280 & @390. Registered as the Numbers Room's 7th .bench.exact card (▲ glyph; href ../sandpile/index.html; hero lede 6→7 "Seven benches", footer "Five prove…"→"Six prove…"; landing self-test benches.length===6→7 + a new "Sandpile bench present" check → 15/15 ✓; the 3×N grid lays the 7th card out clean). Twin cross-links wired both ways with strange-garden/pieces/sandpile.html ("watch the mandala settle" there vs "poke it and prove it forgets the order ↗" here). Reviewed fresh-eyes #56 (session sandpile-pub-cyc56, served 127.0.0.1:8736 torn down by exact PID — Brandon's :3001/:4380 untouched): Node twin 21/21 · byte-twin 8772 bytes IDENTICAL · bench pill all-green @1280 & @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 · mobile pill/title no-overlap · HERO VERBS LIVE: topple-it-twice → "✓ identical · both took 135,471 topples"; rain → "biggest yet: 1,497" with a heavy-tail size-log; reveal-e → the fourfold-symmetric identity mandala renders + "e ⊕ e → e" enables. PUBLISHER CAUGHT & FIXED: the FRONT-DOOR MAP blurb for the Numbers Room (index.src.html) still read "Six benches… Five prove a theorem exactly" — lagging this 7th bench, the same staleness pattern swept at #55 — rewritten to "Seven benches… Six prove a theorem exactly" adding the new shape ("pour sand that forgets the order it fell"), RE-FORGED index.html (forge --check --all → all 30 current; 0 overflow/0 nested on the front door). Builder's open concerns adjudicated: the ~135k-topple "twice" cascade reading as a fast shimmer is KEPT (dramatic-on-purpose, a future taste-tweak not a bug); the reduced-motion path is Node-twin-verified (CDP set-media reduced-motion tooling limitation, not a piece bug); the empirical-placeholder-before-rain is intentional honesty. No new [bug], no ⚡ spark. Provenance: sandpile/CHANGELOG.md + the #56 worklog. The Numbers Room is now a 7-bench wing with two games — grow it with a fresh number-shape, don't rebuild a bench. -->
<!-- BLOOMED #43 → The Cavern · Spin — the Stern–Gerlach Split (cavern/spin/) — the Quantum Drift's named un-sown fact, sown as a TOUCHABLE bench (not a plot): a side-on beamline where you FIRE silver atoms (oven → collimator → Magnet A wedge → screen + chooser shutter → tiltable Magnet B → a plate that grows TWO dot-histogram piles, with a classical-smear ghost band for contrast). The two-valuedness is the form: every atom lands UP or DOWN, never the middle. Three preset chips (Confirm/Erase/Paradox) tell the three lessons; a small draggable-n̂ Bloch satellite is the control. core.mjs (106L) is the SOLE spin-½ authority — pUp = ½(1+n̂·m̂) = cos²(Θ/2) via the half-angle identity (clamped dot, no acos), an independent spinor-projector route (overlap2), mulberry32(0x5C1F0001), the honest two-valued sampleSplit, and classicalDeflect as the negative control — inlined BYTE-IDENTICAL into index.html (990L) between // === CORE BEGIN/END === sentinels. core.test.mjs GREEN 18/18 exit 0 (two derivations agree to 4.44e-16 over 15,379 (θ,φ) points; hero cases exact θ=90===½ & θ=60→¾; normalization Σ−1===0; the erasure chain; seeded 200k within ±4σ of cos²(θ/2), same seed → byte-identical counts; the classical-deflection teeth; 3501-char byte-twin parity + anti-circularity grep). In-page pill 6/6; the "re-run headless 200k" footer lands empirical on cos²(θ/2). Registered as the Quantum Drift's 9th bench (⇅ card after two-wings; 0 nested anchors; cavern map self-test 29/29). Builder caught & fixed an inverted preset wiring (Confirm now reads 1.000, not 0.000). Reviewed fresh-eyes #43: green self-tests on both surfaces · 0 console errors · 0 horizontal overflow @1280 & @390 · Fire 10k → 5093↑/4907↓ · presets 1.000/0.500/0.500 · tilt sweep 0→90→180 melts theory 0.500→1.000→0.500 · θA=45 → 0.854=cos²(22.5°) · the two piles render · the gated Quantum-Drift card renders correctly among its siblings once revealed. Provenance: cavern/spin/CHANGELOG.md + the #43 worklog. The Quantum Drift is now 9 Q-benches + 1 sonifier — grow it with the next un-sown quantum fact (identical-particle exchange is the live [cross] above). -->
<!-- BLOOMED #38 → The Latin Square (latin-square/) — the Numbers Room's 5th bench and its FIRST GAME: a genuinely playable, winnable 5×5 deduction puzzle where five suit-like glyphs (● circle/gold · ◆ diamond/teal · ▲ triangle/coral · ✦ star/violet · ■ square/sky — each a distinct SHAPE *and* hue, colorblind-safe) must each appear once per row & column. The generator self-tests UNIQUENESS + solvable-by-pure-DEDUCTION: countSolutions(givens)===1 AND deduce() solves to a valid Latin square (no guessing), givens landing 6–10. core.mjs (138L between sentinels) is the single math authority — mulberry32 · shuffle · fullSquare · countSolutions(cap=9) · deduce()→fillOrder (naked-single · hidden-single-row · hidden-single-col) · generate(seed) with the minimal dig — inlined BYTE-IDENTICAL into index.html between // === CORE BEGIN/END === sentinels (136-line body identical, page & Node test can't drift). The board: radial mini-palette + keys 1–5, pen/pencil + auto-pencil, corner pencil marks, live conflict feedback + a hard 'dead' zero-candidate flag, a HINT that places a forced cell and NAMES its rule, a 'watch it think' that replays deduce().fillOrder (no step says 'guess'), the Tightening panel (pull a given → the solution count spins up + logic stalls), and THE WIN = a structure reveal (per-row L→R + per-column T→B teal sweeps + a ✓ tally ribbon on every row/col edge — the invariant draws itself) then the exact-counts payoff (one of 161,280; reduced form exactly 56). Node twin core.test.mjs GREEN 210/210 (200 seeds; negative control fires on both measures); in-page badge self-test 9/9 across 120 boards + the constants 161280 & 56 + the reduced-count identity 161280/(5!·4!)===56. Registered as the Numbers Room's 5th bench (.benches repeat(4)→5, max-width 1180→1400, 5-across ≥1400 reflow, room self-test 13/13, glyph ▦). Provenance: latin-square/CHANGELOG.md + the #38 worklog. The Numbers Room is now a 5-bench wing with its first game. -->

### cross
- [cross] **Pick & the Wheel** (planimeter × lattice Pick core) — stretch one rubber polygon across a brass PEGBOARD: a tracer wheel rolls its boundary (A=L·ΔW, Green's theorem) while green/gold dots count themselves inside & on the edge (A=I+B/2−1, Pick). Drag any vertex and TWO needles re-settle to the SAME area — calculus and counting, disjoint, on one number. FORM: a pegboard you re-shape, two readouts snapping together, no two-plots. Disjoint cores: import planimeter's Green-wheel area UNCHANGED (sole authority) + a NEW Pick core (B via gcd of edge-deltas, I via shoelace-sign). CRUX: L·ΔW===I+B/2−1 for every lattice polygon incl. non-convex/holes; NEG CONTROL: drag a vertex OFF a lattice node → Pick greys to UNDEFINED while the wheel reads true. (grep-confirmed: Pick implemented in NO room.) (sown #82)
- [cross] **The Counted Drum** (singing-plate/Chladni × lattice eigen-count) — bow a square plate up its pitch; sand leaps onto each Chladni figure as a lattice of (p,q) integer pairs lights in lock-step. The mode you SEE shimmer is the (p,q) cell at eigenvalue π²(p²+q²); figures-counted-so-far === lattice points inside a quarter-circle of that radius (Gauss's circle problem). FORM: a singing/shimmering plate + a pinboard of integer pairs lighting in pitch order. Disjoint cores: reuse singing-plate's analytic π²(p²+q²) + a NEW Gauss-circle lattice counter (pure integer loop). CRUX: n-th distinct eigenvalue's mode-index === N(R)=#{p²+q²≤R²} with correct degeneracy [(1,2)&(2,1) light TWO dots at ONE tone] for the first ~40 modes; NEG CONTROL: a circle plate uses Bessel zeros, not p²+q² — the lattice predictor goes silent. (grep-confirmed gap; the plate already knows Bessel.) (sown #82)
<!-- BLOOMED #62 → The Cutting Gears (cutting-gears/) — a cross vein between the Numbers Room (Measuring Bench) and the Workbench (Spirograph): ONE knob (R:r — 6 preset chips + two sliders) drives BOTH a live progressive Spirograph rosette sweep (petals = R/gcd) AND an animated anthyphairesis rod-cut (the R×r rectangle tiled by the largest square repeatedly, squares spiralling inward to the violet gcd square that sets the petal count). The whole point is the DISJOINT cores: the spiro gcd+closure is inlined byte-faithful from spirograph/index.html (PRIVATE to the spiro IIFE — petals never route through euclid's gcd), while euclid's gcdTrace/cfExpand are IMPORTED (not forked) from ../euclid-engine/core.mjs; the two never call each other, so their agreement on the same integer IS the proof. Three surfaces beyond index.html (715L): the SOLE logic core cutting-gears/core.mjs (166L — SPIRO inlined under SPIRO-CORE sentinels + EUCLID imported + buildTiles + runSelfTest), the Node twin core.test.mjs (118L, exit 0), CHANGELOG.md. Self-test 8/8 in-page + 21/21 Node twin GREEN: disjoint gcd agreement 6893/6893 pairs, byte-twin spiro parity IDENTICAL (anti-drift encoded as a test), CF length === step count, the negative control (a sharing pair draws strictly fewer petals than its coprime neighbour — verified 96:36→8 petals vs 95:36→95), tamper rejected, + 4 tile invariants (Σ side²===R·r exact tiling · no overlap · smallest side===gcd · step-bands===CF length). window.__cuttingGears exposes both cores by name (SPIRO:{gcd,closure}, EUCLID:{gcdTrace,cfExpand}). Explicit `long = q·short + rem` band labels light beside each tile-band as tiles land (honestly capped `… N more` for long CFs). Estate-exact palette (panel rgba(14,12,9,0.88), gold #c9a24a; squares reuse euclid's rod colours — long gold #caa24a, short teal #6fd3c4, gcd violet #e0a8ff). Registration (garden work, no front-door footprint): reciprocal cross-teasers added to BOTH euclid-engine/index.html ("watch one gcd take two roads at The Cutting Gears →") and spirograph/index.html ("That petal count IS Euclid's gcd(R,r) — watch it cut live →"); back-links to both benches + the estate. Publisher fresh-eyes (session cg-pub-62, served 127.0.0.1:8861, torn down by exact PID — Brandon's :3001/:4380 untouched): CAUGHT & FIXED a real mobile (≤820px) topbar collision — the centred "The Cutting Gears" H1 overlapped the wrapping back-links (overflow ≠ overlap, so the builder's @390px overflow check missed it); fixed CSS-only (stacked topbar band + stage padding-top), cores untouched, re-verified 390/760/1280px + Node twin 21/21. Both registration benches still green (euclid 10/10, spirograph 7/7), both teasers single + correctly href'd, 0 nested anchors. Provenance: cutting-gears/CHANGELOG.md (v1 + v1.1) + the #62 worklog. The Spirograph × Sampling (Freeze the Wheel) cross remains a live garden seed. -->
<!-- BLOOMED #64 → The Comma (sound-garden/the-comma/) — a Sound Garden LEAF (garden growth of a built wing, NO front-door footprint, NO ws:seen) crossing the Sound Garden's tuning with the Numbers Room's exactness: twelve true perfect fifths (×3/2, folded to the octave) stacked one tone at a time OVERSHOOT home by the Pythagorean comma — a gap you SEE (the 12th rung lands above a dashed "true home · ×2⁷" line, a +¢ bracket) and HEAR (a stacked 12th 132.59 Hz vs true octave 130.81 Hz, a 1.78 Hz wolf beating) — then a temper lever shaves every fifth by an equal 1.955¢ until the beat slows to a dead stop and the spiral snaps shut. THREE enacted gestures on one shared t∈[0,1]: WIND the tower tone-by-tone (12 zig-zag pitch-rungs climbing to the dashed home line); HEAR the wolf (gold→teal SUM-wave beat envelope + live beat-rate caption; dragging the lever live-retunes osc b via setTargetAtTime so the throb decelerates to silence); PULL the lever & SEE the gap close (a filled gilt→teal WOLF WEDGE — the overshoot arc — with a +23.46¢→0¢ callout snapping to a clean teal ring as t→1; the spiral secondary; a compact +1.955¢-per-peg cents-ruler strip). FORM EXPRESSES CONTENT: a wheel you wind + a lever you pull + a tone you hear, the piano's beautiful lie made touchable — no plotted curve. The math is single-sourced & self-testing: a NEW `// ===== COMMA CORE … =====` block appended to `sound-garden/pitch-core.mjs` AFTER the byte-untouched PITCH CORE (butterfly-voice still byte-twins that block); the comma is DERIVED there (cents∘fold∘(3/2)¹²) with the famous 531441/524288 literal living in EXACTLY ONE file. The page inlines a BYTE-TWIN of the COMMA CORE slice (8219 chars, char-for-char identical); runSelfTest is the sole oracle for both the in-page pill (6/6 ✓) and the Node twin (`core.test.mjs` 14/14 exit 0) — six legs incl. a load-bearing NEGATIVE CONTROL (same 12 multiplies+fold leaves 23.460¢ for 3/2 but ≈0¢ for 2^(7/12) — the comma is the FIFTH's, not round-off) + byte-twin parity + an anti-circularity grep. Audio-Lens confirmed: JUST renders ~5 beats over 3s (the 1.785 Hz wolf), TEMPERED renders DEAD FLAT (beat gone). Registration (garden, no front-door footprint): a Sound Garden footer family link "↔ The Comma" (mirrors butterfly-voice), a Carillon teaser ("↔ why twelve fifths can't agree on a bell's octave →"), and a reciprocal butterfly-voice cross-teaser ("The Comma · the same pitch core, the other way →"). PUBLISHER FRESH-EYES (session `comma-pub-cyc64`, served `127.0.0.1:8794`, torn down by exact PID 46625 — Brandon's :3001/:4380 untouched): shipped clean — Node twin 14/14, in-page pill 6/6 ✓, 0 console errors, 0 nested anchors, 0 horizontal overflow @1280 & @390; all three gestures verified LIVE (wind-all-twelve → tower built, gap +23.460¢, beat 1.78 Hz, wolf btn enabled; lever t=0→t=1 → gap +23.460¢→+0.000¢, beat → 0 Hz silent, the whole piece recolors gold→teal, the wolf wedge snaps to a clean "closed circle", the cents ruler flattens to teal — the builder's ruler-inversion fix verified [reads +23.460¢ at just → +0.000¢ at equal]); all three registration links correct, well-formed, single, 0 nested anchors; mobile @390 collapses to one legible column. No real bug, no `[bug]`, no `⚡` spark. Provenance: sound-garden/the-comma/CHANGELOG.md + the #64 worklog. The Sound Garden × Numbers Room (tuning × exactness) vein is now crossed — don't rebuild this leaf; grow the Sound Garden with a fresh self-fact/medium seed. -->
<!-- BLOOMED #76 → Out of Tune (sound-garden/out-of-tune/) — a Sound Garden LEAF (garden growth of a built wing, NO front-door footprint, NO ws:seen) crossing the orrery's celestial mechanics with the Sound Garden's tuning: a brass observatory DIAL you turn — concentric engraved orbit-rings Mercury→Neptune, a turned-brass index-arm sweeps a translucent listening window, and every ring inside the window LIGHTS and SINGS a sustained organ-drone at that planet's pitch (period folded into one octave above Earth=220 Hz A3). The solar system obeys Kepler's third law a^(3/2)===period to the decimal (real worst 0.0616%, Neptune), and yet the chord it sings lands audibly OUT OF TUNE — every adjacent pair 5.35–37.55¢ off the nearest just interval (sourest real: Mercury–Venus 37.55¢ off the major third; Saturn–Uranus 24.14¢ off the tritone). The chord IS the readout (a now-sounding nameplate strip); an amber wolf-thread throbs at the beat rate between adjacent lit rings; a quiet brass tuning-rail ticks the cents-off (≤8 ticks). FORM EXPRESSES CONTENT: a dial you turn + a chord you HEAR, no plotted curve. The math is single-sourced & self-testing: CORE A = sound-garden/out-of-tune/data.mjs (the leaf's sole copy of a/period/col per planet, byte-faithful from the orrery's BODIES[], imported by BOTH the page and the Node twin so the parity assertion confirms no re-typing) + an OUT OF TUNE CORE block appended to sound-garden/pitch-core.mjs AFTER the byte-untouched COMMA CORE / PITCH CORE (so the-comma & butterfly-voice still byte-twin their slices) — it REUSES cents()/foldToOctave() (the pitch law is NOT re-typed) and supplies JUST_SET/nearestDev/keplerRel/runOutOfTuneSelfTest. The page inlines a BYTE-TWIN of the OUT OF TUNE CORE slice (7795 chars char-for-char); runOutOfTuneSelfTest is the sole oracle for both the in-page pill (4/4 ✓ "Kepler holds, the chord is sour") and the Node twin (core.test.mjs 19/19 exit 0) — four shared legs incl. two load-bearing CONTROLS: control A Concordia (a fictional exact-3:2 planet) PASSES Kepler via the BAND (a^1.5 is 1 ULP off period, so === would false-fail) AND its fifth snaps to 0¢ bit-exact; control B Eris-X (corrupt, test-only) FAILS the Kepler band by ~190x (non-vacuous) — plus single-source discipline asserts the inlined slices byte-twin their modules and cents/foldToOctave are defined in exactly ONE module. Audio-Lens reconfirmed: the full 8-voice chord renders clips:false (peak −6.25 dBFS, Earth A3−7c≈220 Hz), a single Mars tone reads 414 Hz (G#4 −5c). Registration (garden, no front-door footprint): a Sound Garden footer family link "↔ Out of Tune" (mirrors The Comma / The Butterfly's Voice) + the leaf's own footer family links back to The Comma / The Butterfly's Voice / Carillon / the Estate. PUBLISHER FRESH-EYES (session oot-pub76, served 127.0.0.1:8753, browser torn down by exact session name + http server killed by exact PID 45401 — Brandon's :3001/:4380 untouched): CAUGHT & FIXED one real broken cross-link the heads-down builder missed — the leaf's "The Butterfly's Voice" footer link was href="../butterfly-voice/index.html" (correct from the landing one level up, but Butterfly's Voice lives at the REPO ROOT, and the leaf sits one directory deeper than the landing, so it resolved to /sound-garden/butterfly-voice/index.html → 404); fixed to "../../butterfly-voice/index.html" (curl-confirmed 200 vs the old 404; the byte-twin slices untouched so the Node twin stayed 19/19). Otherwise shipped clean: Node twin 19/19, in-page pill 4/4 ✓, 0 console errors, 0 nested anchors, 0 horizontal overflow @1280 AND @390; the dial renders (560×560, brass rings + index-arm + listening-window wedge + amber wolf-threads + tuning rail), interaction verified LIVE (keyboard widen lights more rings; Pluto/Concordia toggles add their rings, the live region reads "Uranus, Neptune, Pluto — Neptune–Pluto 6¢ off the P5"; the dashed phantom Concordia ring draws; the now-sounding plates update); mobile @390 collapses to one legible column with no overflow; the landing footer link navigates end-to-end to the leaf. Provenance: sound-garden/out-of-tune/CHANGELOG.md + the #76 worklog. The orrery × Sound-Garden (celestial-mechanics × tuning) vein is now crossed — don't rebuild this leaf; grow the Sound Garden with a fresh self-fact/medium seed. -->

### curation

- [curate] **Undercroft curation** — the undercroft has lain fallow for dozens upon dozens of cycles.  Review rooms and exhibits and link them to the Undercroft through new achievements - manipulate an exhibit's controls _just right_, spend enough time inside an exhibit, etc.  Hide more advanced exhibits behind Undercroft unlocks.  Hide crossovers behind achievements earned from related exhibits.  Be creative.  Try to add at least 10 new SECRETS to the Undercroft, at least half of which unlock a gated exhibit.  See UNLOCK.md and undercroft/index.src.html for integration instructions. (sown #86)

### rework
<!-- Re-soul an existing piece that drifted sterile — a graph + wall of text where the real thing wants to be SHOWN and TOUCHED. The gardener AUDITS the estate and marks ~1 per PLAN cycle (slowly, in moderation — a clean explainer worth keeping is left be); the planter pulls one like any garden seed and re-grows it IN PLACE toward the soulful siblings (Newton's Cradle · the double-slit firing particles · the Strange Garden), keeping the correct math as a quiet layer. Counts as garden fuel + decays like any garden seed (the next audit re-surfaces it if it still matters). See seedbed/README.md "the soul & the audit". The starter queue below came from the estate-wide soul audit (2026-06-15); the drift is real but LOCALIZED — two graph-pockets (the Conservatory + the Cavern quantum drift) against a soulful, varied majority. -->
<!-- BLOOMED #84 → The Sampling Theorem RE-SOULED in place (sampling-theorem/) — the v1 polished-but-sterile bench that LED with a 1-D waveform plot (and demoted the wagon-wheel illusion to a mere preset on that flat chart) re-grown IN PLACE into a **strobed spinning wheel as the hero you feel in your gut**, SAME route + SAME ws-less breadcrumb + SAME Workbench slot. Dial the strobe `f_s` over a turning wheel: below Nyquist it spins TRUE, at Nyquist it FREEZES (the antipodal helicopter-blade standstill), above it crawls BACKWARD into a slow phantom — the aliasing you SEE, not its plot. The old time + freq 1-D canvases demoted to a quiet side-rail ("the curve is the shadow"). The HERO reads its apparent rate ONLY from the proven core: `apparentRate(f,fs)` (the SIGNED stroboscopic alias `frac·fs`, `|apparent|===foldedFreq`, sign = the regime) added right after `foldedFreq` INSIDE the byte-twin sentinels char-identically in BOTH `sampling-core.mjs` and the page's inline block (`foldedFreq` body untouched, no `dft`/`fft` token added → anti-circularity grep + B3 stay green); the export list updated OUTSIDE the sentinels. Self-test leg F (the wheel's apparent rate === the signed fold; drawn spoke-X IS `core.sampleTone`, drift 0; continuous-light neg-control apparent===true to ε) lifts the in-page pill **7/7 → 8/8**; a SIGN-FLIP SWEEP (#4b, 2747 steps across f∈(fs/2,5·fs], worst |·|−fold Δ=0, wrong-sign 0) lifts the Node twin **14/14 → 16/16** exit 0; the byte-twin slice grew 13734→16406 chars and re-extracts char-identical (parity invariant green). PUBLISHER FRESH-EYES (#84, session `st84pub`, served `127.0.0.1:8749`, browser closed by exact session name + http server killed by exact PID 12433 — Brandon's :3001/:4380 untouched): pill **8/8** ✓ · Node twin **16/16** · byte-twin parity GREEN. Regimes verified LIVE on the real `fs` slider: `fs=8` → BACKWARD phantom apparent −3.40 (the baked on-ramp, wagon preset retuned f=4.6@fs=8); `fs=20` → spinning TRUE apparent 4.60; `fs=9.2 (=2·f)` → FROZEN fs=2·f=9.20 (the `|apparent|·2===fs` freeze predicate, NOT `apparent===0`); continuous-light toggle → CONTINUOUS·TRUE 4.60 (phantom vanishes), off restores it; the wheel canvas genuinely animates frame-to-frame (180ms hash differs). **0 console errors** after exercising every control · **0 nested anchors** on the bench AND the Workbench · **0 horizontal overflow @1280 AND @390** · mobile @390 collapses to one legible column (wheel hero full-width). All four cross-targets resolve 200: the Butterfly back-link "↔ the ceiling", the Voice xteaser "why the teeth bite →" + the load-bearing **leakage≠aliasing** honesty-cross (PRESERVED), the refreshed Workbench card (kind gains "· the wagon wheel", blurb re-led with the wheel; same route/glyph 📶/slot, NO front-door footprint, NO ws:seen). Reduced-motion path verified by code inspection (this Chrome build won't report `prefers-reduced-motion` to matchMedia): the gate is FIRST in the draw spine, renders one static FROZEN-at-Nyquist frame, arms no rAF. `forge --check` stays current at 31/31 (no `.src.html` touched). **SHIPPED CLEAN — no real bug, no polish edit, no `[bug]`, no `⚡` spark.** The toggles being non-focusable `<div class="tog">` is v1's established pattern (confirmed via git), followed consistently for the two new toggles — not a regression, out of scope for this re-soul. Provenance: sampling-theorem/CHANGELOG.md v2 + the #84 worklog. The `### rework` fence is now EMPTY — the next gardener audit (a PLAN cycle) has room to mark up to 3 freshly-drifted pieces. -->
- *No live `[rework]` Sampling-Theorem seed remains — re-souled & bloomed #84 (the v1 1-D-plot bench became a strobed wagon wheel you feel; see the tombstone above), draining the fence.*
<!-- The original 2026-06-15 starter queue (Lattice · Predator-Prey · Stirling · Replicator) has fully BLOOMED (#37/#44/#45/#47). Fresh queue below from the C3-GATE validation (Phase B, re-confirmed at the 2026-06-15 map-process lockdown): the abstract-plot benches that fail the grounded gate — a vivid caption can't lift a chart of an equation. Re-soul IN PLACE toward the soulful siblings; keep the correct math a quiet layer. -->
<!-- PRUNED CLEAN #62 (decayed phantom, no tombstone) → the [rework] "Cavern · wave-packet — launch it, don't plot it" seed (sown #48, gauge-DECAYED at age 14) described a re-soul that ALREADY SHIPS: cavern/wave-packet/ is a fully-built time-domain bench (cycle #21) — a Gaussian packet you LAUNCH down the beamline and watch broaden/split/slosh via a unitary Strang split-step Fourier integrator, with core.mjs + a Node twin core.test.mjs, registered as "The Wave Packet" in the cavern landing. The seed and the shipped piece were both true; the garden twin simply never got pruned and aged out. Removed clean (a decayed idea is free to return — but here the work is done, so a future audit should target a DIFFERENT drifted piece, not re-pitch this one). Verified by the publisher: ls cavern/wave-packet/ (index.html 71KB · core.mjs · core.test.mjs · CHANGELOG.md), launch/spread present, cavern self-test 29/29 with the wave-packet-link-relative check. -->
<!-- The prior soul-audit queue fully drained: Lattice #44 · Stirling #45 · Predator&Prey #37 · Replicator #47 · Logistic #48 · First-Integral #59 bloomed; the Cavern wave-packet pruned clean #62 (already-shipped phantom). Moderation ceiling is 3 — this cycle's audit marks 1. -->
<!-- BLOOMED #70 → The Road Into Chaos RE-SOULED in place (bifurcation/) — the v1 wall-of-charts measurement bench (two plots + a draggable cursor: you READ about the cascade but never WATCH the population period-double) re-grown IN PLACE into a LIVE cobweb staircase you steer, SAME folder + SAME Workbench card. The HERO is now a live cobweb on the logistic/sine/cosine hump (dashed y=x mirror + fixed-point rings + cached offscreen furniture): turn an r-knob and a glowing pen walks the staircase — below r=3 it spirals into one corner; past 3.0/3.449/3.544 it visibly OPENS into 2, 4, 8 boxes (the cascade you COUNT WITH YOUR EYES); a violet LOCK loop traces exactly the period when λ≤0; the λ-first gate ensures chaos never draws a solid loop. A heartbeat strip reads the SAME orbit (steady when locked, ragged in chaos); the bifurcation raster is DEMOTED to a low-contrast road-map with a brass you-are-here marker + r∞ wall + drag-to-set; the δ-ladder + self-test live in a closed "the proof" drawer. core.mjs stays the SOLE math authority byte-twinned char-for-char between // ROAD-INTO-CHAOS CORE BEGIN/END sentinels (appended CASCADE_BANDS · R_INFINITY=3.5699456 · expectedPeriod · cobwebOrbit [the sole staircase source] · detectOrbitPeriod; periodOf delegates to detectOrbitPeriod(attractor(…)), a byte-safe one-line refactor). Node twin `node bifurcation/core.test.mjs` → 30/30 GREEN exit 0 (15 original + 6 live-period claims [box-count==expectedPeriod==drawn detectOrbitPeriod(cobwebOrbit(3.50))===4 at band centers; periodic/chaotic neg-controls; short chaotic staircase keep=256→0; R_INFINITY↔extrapolation<0.001] + a 9-line byte-twin re-extraction parity block); in-page pill 10/10 ✓ @1280 & @390. PUBLISHER FRESH-EYES (#70, sessions bifpub70/wbpub70, served 127.0.0.1:8757, torn down by exact PID 77329 — Brandon's :4380 untouched): Node twin 30/30 · pill 10/10 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 on both pages; cascade verified LIVE by driving the real rSlider input — period 1→2→4→8→CHAOS, λ −0.11→−0.92→−0.87→−0.42→+0.35, λ=0.6931=ln2 EXACTLY at r=4, r=3.50 locks a violet 4-box loop (screenshot-confirmed), period-3 window at r=3.83 reads period 3·λ<0 (order inside chaos); heartbeat tracks the same orbit; auto-tour glides 2.60→3.02 in lockstep; the Workbench card click navigates to the live bench; the card blurb shows the new "steer it / count with your eyes" copy, no stale "two plots/Lyapunov curve" copy anywhere. CAUGHT & FIXED THREE polish items (all page-side, OUTSIDE the core sentinels — byte-twin parity re-verified 30/30): (1) the advisory chip read a misleading "~ converging" at the period-3 window (a SETTLED order off the coarse cascade-map, not a not-yet-converged state) → now context-aware: a settled off-map period reads "~ a window off the road-map", a chaos mismatch reads "~ off the road-map"; (2) @≤820px the equation hint overlapped the y=x label/curve → hidden on narrow screens; (3) @≤820px the x-axis numeric tick labels collided with the hero caption → dropped below 820px (the grid stays). No [bug], no ⚡ spark. Neither bifurcation/ nor workbench/ is forge-managed. Provenance: bifurcation/CHANGELOG.md v2 + the #70 worklog. The `### rework` fence is now EMPTY — the next gardener audit (a PLAN cycle) has room to mark up to 3 freshly-drifted pieces. -->
- *No live `[rework]` Road-Into-Chaos seed remains — re-souled & bloomed #70 (the v1 two-plots bench became a steerable cobweb staircase; see the tombstone above).*
<!-- BLOOMED #59 → The First Integral RE-SOULED in place (first-integral/) into "The Bowl You Can't Get Below" — the v1 three-up flat-strip explainer (correct, but a chart-museum register: three side-by-side static curves + H-residual strips you READ) re-grown IN PLACE into a TOUCHABLE piece, SAME Workbench route + SAME three inbound sibling cross-links (catenary/brachistochrone/soap-film foot teasers, all still resolving). Now a two-column hero (stacks ≤820px): LEFT = a draggable chain (gold beads on the true extremal, Dirichlet pins) whose interior beads deform the curve as a SMOOTH C¹ raised-cosine bump (~⅓-span, zero at both pins — not a kink) + a reactive H-strip that buckles red where the Euler–Lagrange residual breaks tolerance; RIGHT = the COST-BOWL gauge — a filled translucent gold well, a dot riding it, the MEASURED ΔI rim swept over a drag, a cyan dashed ∝d² reference, a green dashed FLOOR the dot can't cross. Tabs switch ONE curve (chain ⛓️ / slide 🛝 / soap-neck 🫧). The hero verb: grab a bead, drag — the curve reddens gold→bad where the residual breaks, the H-strip buckles to a red wave, the bowl dot rides UP the wall, the badge flips red (ΔI = +…); on release an eased spring-back to the analytic curve + a self-earned taunt ("+0.183 — still uphill. The floor is the law."). Ghost-impostor toggle (catenary only) parks a bad-guess dot high up the wall. prefers-reduced-motion respected (snap, no spring; a new @media rule). Drops a ws:seen:first-integral breadcrumb (v1 was breadcrumb-exempt). The math stays a quiet PROVABLE layer + DEEPENS: core.mjs gained 3 surgical pure functions between the existing sentinels (fCatenary action integrand y·√(1+y′²); action() midpoint-rule ∫f dx with slope RECOMPUTED per segment from actual node positions; perturb() single-interior-node bump, endpoints pinned) → 4 NEW EXACT claims (#11 action minimum ΔI≥0 over a sweep, worst +1.4e-4; #12 Richardson R→4 quadratic minimum, R=3.91; #13 H breaks past the floor under FD slopes, 27.3% vs 0.55%; #14 action determinism). Two-register slope model documented: the original 10 H-conservation checks + the at-rest strip keep the EXACT analytic yp (flat ~1e-15, untouched); the live-drag/action layer recomputes yp from the deformed curve — analytic slopes never feed the action check, FD slopes never feed the 1e-9 check. core.test.mjs → 45/45 GREEN exit 0 (was 34/34: +3 imports + the action-minimum re-derivation + byte-twin parity); in-page pill 15/15 ✓; inlined core BYTE-IDENTICAL to core.mjs. Reviewed fresh-eyes #59 (session fi-c59-pub, served :8761 torn down by exact PID 80073; Brandon's :3001/:4380 untouched): Node twin 45/45 · in-page pill 15/15 ✓ @1280 & @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 · the FULL interaction loop verified LIVE via synthetic pointer events — drag-up bumps the chain smoothly + reddens, H-strip wavers 52%, badge flips red ΔI=+0.1825, bowl dot rides up the wall; release fires the taunt "+0.183 — still uphill. The floor is the law." + springs back to ΔI=0 / badge rest green · all three tabs switch cleanly (slide also draggable, ΔI=+0.5222; the ghost button hides off-catenary via display:none) · the Workbench card-link resolves (aria-label correct, 0 nested anchors, card visible, 0 overflow) · all three sibling inbound cross-links resolve ("↗ The First Integral"). PUBLISHER found NO real bug and NO broken layout — the piece shipped clean as built (no [bug] filed, no ⚡ spark). The builder did NOT write the worklog/NOTES/gauge per the role split — the publisher owns those. Provenance: first-integral/CHANGELOG.md v2 + the #59 worklog. The Calculus-of-Variations vein gains its touchable centerpiece — only the catenary's action-minimum is proven (the slide/film bowls compute a real ΔI but carry no minimum-proof claim yet, a noted followup). -->
- *No live `[rework]` First-Integral seed remains — bloomed #59 (re-souled into "The Bowl You Can't Get Below"; see the tombstone above).*
<!-- BLOOMED #52 → The Cavern · Finite Well RE-SOULED in place (cavern/finite-well/) — the flat ψ-vs-x plot + left-side energy-ladder gauge re-grown into a LIVE TOUCHABLE TRAP, SAME route + SAME ws:seen:finite-well breadcrumb, the landing card untouched. It now LEADS with two solid finite-height blue WALLS of real width rising floor→rim with a grabbable lip cap, a chamber wash between, and the FINITE set of bound rungs drawn as SEATED SHELVES across the mouth (the old gauge ladder gone — the rungs live inside the trap now); the selected + clinging-top rungs wear full signed-ψ ribbon + |ψ|² glow-fill seated at their own energy. THE HERO DETAIL: the leak renders THROUGH the walls — a warm amber→transparent glow poking through (reach ∝ 1/κ from the core) + a per-wall stain whose alpha ∝ ψ(±a)², so a shallow strongly-leaking rung makes the wall GLOW HOT. TWO HERO VERBS on one Pointer-Events path: DRAG THE RIM (ns-resize) deepens/shallows V0, DRAG A WALL (ew-resize) widens/narrows a — rungs are BORN at the lip (ignition glow descending + "n born") or SPILL out (a frozen proven-core snapshot rises, leak floods, gold flare + "n spilled out"). Two presets tween via a morph clock firing births/spills one-by-one: "shallow it to the edge" → ONE rung clinging at the lip (the FEELABLE "always ≥ 1" climax, both walls glowing hot gold, leak 15.9%) and "fill the ladder" → ~41 rungs. The flat ψ-vs-x plot is DEMOTED to a quiet ~150×96 low-contrast inset behind a default-OFF toggle, phase-locked with a tie-wisp. ONE geometry map (GEO+X()+eY(), floor=0/rim=V0) shared by render+touch; run-while-busy-then-quiet rAF; a NEW reduced-motion path (the bench had none) — still() draws one complete static frame, born/spill becomes a synchronous STATE CHANGE, detectLevelEvents() skips fx under RM so nothing persists. CAMERA-HARD-WALL HELD: every Math.exp/cos/sin/tan in the file is inside the CORE block or the self-test — NONE in the draw() path (the render reads ψ/E/κ/count from core calls by name; draw-path math is geometry + tween-easing only). Math single-sourced & extended: core.mjs (165L, NEW — the SOLE physics authority: radius/nBound/solveLevel/allLevels/psiUnnorm/normConst/psi/leakOutside/nodeCount/buildFD/fdMul/inversePower, between // === CORE BEGIN/END === sentinels), inlined BYTE-IDENTICAL (112L/4814B) into index.html; core.test.mjs (205L, NEW) → 11/11 GREEN exit 0: the 8 original physics proofs (finite ladder ⌊R/(π/2)⌋+1≥1; every rung on the transcendental match AND the circle u²+v²=R²; ψ & ψ′/ψ continuous; the node theorem; an independent from-scratch FD eigensolve of the STEPPED potential → the ladder; the leak out of the walls with the shallowest rung leaking farthest; box recovery V₀→∞; determinism) + 2 NEW MOTION claims (#9 count tracks ⌊R/(π/2)⌋+1 across a swept well V0=0.5..400 with births landing EXACTLY at R=n·π/2; #10 the lone rung's leak grows monotonically 16%→80% as the well shallows) + the byte-twin parity check. In-page pill 10/10 ✓ @1280 & @390. Reviewed fresh-eyes #52 (session fw52, served :8771 torn down by exact PID; Brandon's :3001/:4380 untouched): Node twin 11/11 (byte-twin 112L/4814B identical) · bench pill 10/10 ✓ @1280 & @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 · BOTH hero verbs work LIVE (depth slider sweep 0.6→1400 climbs 1→34 rungs monotone; width 0.40→2.40 climbs 14→81) · "shallow to the edge" lands count=1 leak 15.9% (the single-survivor climax, walls glowing hot) · "fill the ladder" → 41 rungs foregrounding the top · the leak tail renders visibly THROUGH the walls · the shadow-gauge inset toggles on with its tie-wisp · landing-card → bench navigation end-to-end · the back-links resolve (../index.html, ../../index.html) · cavern landing self-test 29/29 ✓ (the Finite-Well-link-is-relative check) — UNTOUCHED. PUBLISHER CAUGHT & FIXED TWO: (1) at @390 the serif title "The Finite Well" slid UNDER the self-test pill (right edge 351 over the pill's left edge 248, a 104px overlap) → matched the box/oscillator sibling convention: hide .brand AND the full-width .viewlabel on mobile (the panel lede + tab title name the bench; the canvas already labels the rim/floor) — desktop unaffected, 0 overflow restored. (2) the depth slider's open-concern mismatch (min=1/step=1 pinned the thumb at an integer floor while the true V0 e.g. 1.21 lived in state, so the "shallow to edge" climax read vVal 0.4 eV with the thumb stuck at the bottom) → slider min=0.6/step=0.1 + syncSlidersToState uses .toFixed(1) so the thumb now TRACKS sub-1 V0 (after the preset it sits at 1.2, matching vVal 0.4); both edits OUTSIDE the CORE sentinels, byte-twin re-verified 11/11. No new [bug], no ⚡ spark. Provenance: cavern/finite-well/CHANGELOG.md cycle #52 + the #52 worklog. The Quantum Drift gains a touchable trap — grow it with the next un-sown quantum fact, don't rebuild a bench. -->
<!-- BLOOMED #54 → "The Shannon Limit" RE-SOULED in place into "The Source Dial" (entropy/) — the static "bits-ruler" explainer (H floor vs L bar vs efficiency over a dead Huffman tree + an encode/decode demo; rAF count 0) re-grown into an ENACTED, in-motion compression you operate, SAME route + SAME Workbench card + SAME Engine-Room/Clockwork/butterfly cross-links (routes untouched). THE HERO: three coupled physical bodies driven by ONE distribution you sculpt — (1) ALPHABET COLUMNS (n=4/8/16, default 8): pointer-drag / keyboard a bar's height = pᵢ; the other n−1 renormalize so Σp=1 exactly each frame (pₘᵢₙ≈0.002, pₘₐₓ≈0.97); one-tap animated FLATTEN/SPIKE. (2) MERCURY GAUGE: amber mercury = H on a fixed 0…log₂n scale with an etched ceiling; a cyan L column at/above it; the gap is the unpaid-slack amber hatch. (3) MESSAGE RIBBON: N=120 symbols sampled from the live source, Huffman-coded into colour-banded 0/1 canvas cells with a fixed amber N·H floor tick; length = N·L. The causal chain is enacted in ONE gesture: drag a column → mercury moves → the gap opens/closes → the ribbon grows/shrinks → the N·H floor tick stays put. MOTION: a ▷play/⏸/step/⏮ transport streams the message symbol-by-symbol off a SINGLE rAF loop (~99fps cap), lighting the active root→leaf path in the demoted tree as each codeword drops onto the ribbon; reduced-motion renders one correct static frame, rAF idle. DEMOTED: the old tree canvas, −log₂p table, Kraft readout, encode→decode strip, cipher punchline → a single right-edge "ⓘ the code / the arithmetic ▸" slide-in rail. BREADCRUMB: added the canonical on-load self-fire localStorage['ws:seen:entropy'] (was never written before — entropy isn't in the front-door registry). MATH single-sourced: core.mjs (428L) stays the sole authority, now inlined BYTE-IDENTICAL between new // === CORE BEGIN/END === sentinels (added to both files); the page reads H/L/codes from core every frame, never recomputes the limit in the draw path. core.test.mjs → 28/28 GREEN exit 0 (was 17/17): NEW (a) BYTE-TWIN PARITY — the entire inlined slice === core.mjs slice sans export byte-for-byte, plus per-function bodies char-for-char, plus eval-the-page-slice asserting runSelfTest() pass-count & per-line agreement; (b) THE RIBBON IS THE BITSTREAM — over 60 random sources the concatenated codeword chunks === encode(message,codes) exactly, bit-count === Σ len(chunk_t), decode(ribbonBits,tree) === message; kept H≤L<H+1 over 400 sources, Kraft≤1, encode∘decode identity, block L_k/k→H, uniform H=log₂n. In-page pill 10/10 ✓ @1280 & @390. node engine-room/demon/core.test.mjs → 17/17 (the demon still imports our entropy() — the sentinels are pure comments). Reviewed fresh-eyes #54 (session entropy-pub-cyc54, served :4717 torn down by exact PID; Brandon's :3001/:4380 untouched): Node twins 28/28 + demon 17/17 · bench pill 10/10 ✓ @1280 & @390 · 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 & @390 · the HERO VERB works LIVE — SPIKE → H=0.279/L=1.086/slack 0.807, ribbon retracts to 131 bits, floor 33; FLATTEN → H=3.000=log₂8/L=3.000/slack 0.000, ribbon at 360-bit max, verdict "uniform: H = log₂n, incompressible"; the ▷play transport streams symbol-by-symbol (42/131 mid-play); the info rail slides in showing the demoted tree/table/cipher · the breadcrumb ws:seen:entropy fired on load · the Workbench card renders "The Source Dial" (1007-char blurb, in-range with siblings, 0 nested anchors) · mobile stacks cleanly (the honest linear-in-p columns read ~13% tall at uniform-8 with explicit % labels — KEPT for honesty, it makes spiky=tall=predictable land). PUBLISHER CAUGHT & FIXED: the rename "The Shannon Limit" → "The Source Dial" left FOUR stale display-name cross-links in siblings that still called the piece by its old name (routes correct — only the link text stale) → swept to "The Source Dial" in butterfly/index.html (FFT sources list), clockwork/temperature.html (the cross-card title + its prose), and engine-room/demon/index.html (the bridge-card lead + the footer link); re-verified all three sibling pages + the Workbench card live (0 overflow, 0 nested anchors, the name now matches the page's h1 everywhere). No new [bug], no ⚡ spark. Provenance: entropy/CHANGELOG.md cycle #54 + the #54 worklog. The Source Dial is now a touchable instrument — grow the entropy vein with the next un-sown self-fact, don't rebuild this bench. -->
<!-- BLOOMED #48 → The Conservatory · Logistic Growth RE-SOULED in place (conservatory/logistic/) — the S-curve-first bench re-grown IN PLACE into a colony under glass, SAME route + SAME ws:seen:logistic breadcrumb, no front-door map/footprint change. It now LEADS with a circular glass petri dish of countable green cells that bud/divide (inner→outer on a stable jittered hex lattice) and crowd toward a visible carrying capacity K, until no cell has room left to bud — three coupled crowd cues (packing density · an amber colour/jostle · a clockwise rim crowd-meter) all reading the same crowding. The S-curve is DEMOTED to a faint back-glass ghost + a census-cloud "shadow" pane where the crowd's own census paints the proven sigmoid up to the K asymptote. THE THREE HERO VERBS: resize the dish (K 100→60 re-packs the colony LIVE) · ⤓ overcrowd (N₀=1.6·K starts packed-too-tight, thins toward K, no inflection drawn) · greedy batch (coarse dt a≈1.8 lights the amber banner, glows the rim, rings the coarse dish past K). HARD WALL HELD — the live dish is a CAMERA: sN advances ONLY on the proven rk4Step/leakyStep over field(); every printed number reads the core; the validated census stays pure (only the on-screen camera-run gets a bounded display clamp + a stray-founder reseed). Math single-sourced & extended: core.mjs 287→511L (the LOGISTIC-CORE block + its 30/30 byte-identical/untouched; APPENDED an ===== AGENT-CORE ===== block — DISH_CAP=1500/mulberry32/binom/agentMeanField(N)=r·N−r·N·(N/K)/stepDish [TWO registers: a truthful per-cell binomial bud/pinch that pins at the cap = the validated census path, and a coarse integer logistic map r·dt·m·(1−m/cap) that genuinely rings past the rim]/headlessRun/ensembleCensus/censusDeviation/runAgentSelfTest), inlined byte-identical into index.html (→1314L); core.test.mjs →274L. Self-test 30/30→46/46 GREEN exit 0; in-page pill self-test 10/10 ✓ (6 core synchronous + 4 agent deferred via requestIdleCallback): A1 agentMeanField==field() to max|Δ|=5.33e-15 · A2 ensemble census tracks the EXACT closed-form sigmoid within 2.463 colony-units (<3.0) · the CAP 1500→3000→6000 SHRINKAGE witness (dev 2.46→1.65→1.26, monotone-down, a 1/√CAP demographic stall not model error) · the coarse-dish RING witness (a=1.6 maxN=100.80>K, 27 crossings, monotone in a: 100.80<109.53<120.40) · both parity rows byte-identical. DESIGN CALL (ratified in review): the brief's literal single-formula stepDish PROVABLY cannot ring (the per-cell birth clamp stalls it at the cap), so the binomial body is kept verbatim for the truthful register (drives A1/A2/census) and the coarse register made the genuine integer logistic map (it rings, a documented additional witness); the overshoot PROOF still rests on the proven leakyStep@a=1.6 claim verbatim. Reviewed fresh-eyes #48 (session ws-pub-48, served :8771 torn down by exact PID; Brandon's :3001/:4380 untouched): Node twin 46/46 · bench pill 10/10 ✓ @1280 & @390 · 0 console errors (only the green self-test logs) · 0 nested anchors · 0 overflow (1265≤1280, 375≤390) · all three hero verbs verified live (resize K re-packs · ⤓ overcrowd thins toward K · greedy batch rings, amber banner lights) · the census cloud paints the ghost sigmoid to the K asymptote · landing 21/21 ✓, 4 beds, the re-souled Logistic card resolving to logistic/index.html and rendering consistently with its siblings · mobile stacks cleanly. PUBLISHER'S CALLS on the builder's open concerns: the reduced-motion static frame is verified BY CONSTRUCTION (renderStatic pins the dish full, pre-accumulates the census cloud from a deterministic headlessRun — verified in Node climbing 5→~97.5→100 and pinning at K — lights every honeycomb slot, flattens the V-needle, draws all panes once with no rAF; the reduce guard wires it into every handler exactly as predator-prey's verified renderStatic) — the ONLY gap is tooling (agent-browser's CDP set-media reduced-motion does not propagate to the page's matchMedia in this Chrome build; a tooling limitation, NOT a piece bug); the two-register stepDish split is the honest soul-faithful choice and KEPT. No new [bug], no ⚡ spark. Provenance: conservatory/CHANGELOG.md cycle #48 + the #48 worklog. The Conservatory wing is COMPLETE at 4 re-souled-or-living benches — grow a wing-mate, don't rebuild a bench. -->
- *No live `[rework]` Conservatory seed remains — the Logistic re-soul bloomed #48 (the wing's 4th re-soul: Predator&Prey #37 · Replicator #47 · Logistic #48, with SIR #34 / the others living from birth).*
<!-- BLOOMED #44 → The Lattice RE-SOULED in place (cavern/lattice/) — the dispersion-curve-first explainer re-grown into a touchable crystal you operate, SAME route + SAME ws:seen:lattice breadcrumb. It now LEADS with a row of 8 violet atoms over a real periodic-potential floor (the δ-comb as barrier humps between wells) and a BREATHING FAN above: each single-atom level fans open into a green allowed BAND whose height is the REAL computed Kronig–Penney width (a hairline at far spacing closing into fanned strips as the atoms slide together — band 1 breathes 0.36→2.88 eV across the spacing range). SECOND act: pour electrons (＋pour/−drain + an electrons-per-atom slider) stacking lowest-first into 2N=16 states/band with a crisp Fermi line + gold conduction-glow on a partly-filled top band; the big LAMP flips METAL/SEMICONDUCTOR/INSULATOR live with a plain-English WHY, verdict computed ONLY by core.classify() (never a hardcoded label). The dispersion f(E) plot is DEMOTED to a quiet default-hidden toggleable side-gauge. Math single-sourced to core.mjs (184L, NEW — the SOLE band-structure authority: kOf/fDisp/cellMatrix/halfTrace/detCell/findBands/energyAtQ/buildRing/ringMul/cyclicSolve/ringEigs + classify, between // === CORE BEGIN/END === — inlined BYTE-IDENTICAL 162L/7407B into index.html). core.test.mjs GREEN 13/13 exit 0: transfer-matrix == ½·tr M to 4.44e-16 + det M=1; band edges on |f|=1; gaps forbidden; P→0 closes gaps + P→∞ narrows band 1 onto π²/(2a²)=4.935 (controls); the ANTI-CIRCULARITY rung (#6 counts real ring-eigensolve eigenvalues landing in band 1 = exactly N=8, NOT read back from the 2N fill cap — the exact regression an earlier explorer introduced, refused here); E(q) round-trip; the live-lamp parity 1/2/3/4 e⁻/atom ⇒ METAL/INSULATOR/METAL/INSULATOR; conservation; byte-twin parity. In-page pill 12/12. One eV scale K_EV=0.30/EMAX=46 threaded everywhere; SEMI_GAP=0.6 labelled inline an honest thermal-scale stand-in, NOT a theorem. Reviewed fresh-eyes #44: Node twin 13/13 · bench pill 12/12 · 0 console errors · 0 nested anchors · 0 overflow @1280 & @390 · probed window.__kp (verdict parity holds at every spacing; band 1 0.359↔2.879 eV) · live lamp flips z=1/2/3/4 → METAL/INSULATOR/METAL/INSULATOR · dispersion side-gauge default-hidden + toggles · map self-test 29/29, lattice card 0 nested anchors, renders correctly (1078px uniform) among 11 siblings once the earned-reveal Quantum Drift is unlocked. PUBLISHER CAUGHT & FIXED: at @390 the title clipped 20px under the self-test pill (shared Cavern .topbar baseline) → one mobile-only rule .brand h1{font-size:16px} (outside the core sentinels; byte-twin re-verified) → reads fully, 0 overflow. The Cavern Quantum-Drift card text UNCHANGED (still fits the re-souled bench). Provenance: cavern/lattice/CHANGELOG.md + the #44 worklog. (Explorer C's full Material Picker — Sodium/Diamond/Polyacetylene via dimerization — was sown as the [exhibit] spark below, deliberately out of this re-soul's scope.) -->
<!-- BLOOMED #37 → The Conservatory · Predator & Prey RE-SOULED in place (conservatory/predator-prey/): the bench now LEADS with a living agent ecology — a glass terrarium of tens of individually-readable animals (green hares graze & breed; amber lynx prowl, flash red on a catch, fade on starvation), a single-event ticker, and a back-glass phase portrait where a live bead + an accumulating cloud DRAW the proven dashed RK4 ghost ring (center (4,2.75)). The footer confession is replaced with the truthful "it IS an agent ecology" account. core.mjs grew 310→542L (added the AGENT-CORE block: ECO_K=100, mulberry32, binom, agentMeanField==field() exactly, stepEcoCounts, deterministic headlessRun, phase-locked ensembleCensus, runAgentSelfTest); core.test.mjs 31→45 checks (the agent self-test + an independent AGENT-CORE re-extraction parity test beside the preserved PREY-CORE one); both CORE blocks inlined byte-identical between sentinels in index.html (two-tier badge → self-test 11/11). The emergent census period 10.35 vs RK4 10.04 (3.0%) and x-amplitude 4.32 vs 4.23 (2.3%) both inside a stated 5% tolerance, measured from the phase-locked ensemble mean (a single stochastic run random-walks across orbits; the ensemble mean is the honest stable loop). Provenance: conservatory/CHANGELOG.md + the #37 worklog. The two un-pulled rework seeds (Lattice · Stirling) have since bloomed (#44, #45); the Replicator is the last queued re-soul. -->
<!-- BLOOMED #45 → The Stirling Cycle RE-SOULED in place (engine-room/stirling/) — the chart-first bench (two P–V/T–S curves, the engine itself nowhere shown) re-grown into a brass-and-glass machine you watch RUN, SAME route + SAME ws:seen:stirling breadcrumb, NO map change. It now LEADS with a live, animated Stirling cross-section (canvas #engine, drawn each frame from θ + the ledger): a domed firebox hot cap (T_h), a riveted brass bore with a glass-cutaway sheen, a translucent GAS BODY (height ∝ current volume, color lerps condenser↔firebox with the gas T, drifting particle dots), a loose DISPLACER slug with side-clearance gaps (visually distinct from the sealed POWER PISTON disc+rod), a fixed REGENERATOR MESH band whose bloom = ε × storedFraction (charges on the cooling isochore, drains on the warming) with a red leak shimmer at the cold fins ∝ (1−ε), a spinning CRANK + FLYWHEEL whose two con-rods make the standard 90° displacer/piston phase literal, a θ-dial + a stroke NAMEPLATE naming the live leg. The two P–V/T–S curves are DEMOTED to a side-gauge ("the loop is the engine's shadow") drawn by the EXISTING drawPV/drawTS at ~280px (both planes kept; the T–S edge-drag handles for T_h/T_c/r survive) with a colored bead walking them phase-locked to the strokes. ZERO new physics: all verdicts (η, W, the Q-ledger, ΔS_universe, the Carnot ceiling) still come ONLY from the preserved STIRLING-CORE via compute()→m — the animation READS, never computes; the stroke = legOf(θ) (an ordered 0→1→2→3 quadrant partition), gas T/V + the bead come from the SAME sampleLeg/traceLoop via a shared beadAt(traced,θ) so they share ONE parameterization and cannot drift; four strokes color-keyed to the four legs (one shared palette) tint the active engine element AND the loop bead at the same instant. Kinematics are PRESENTATION-ONLY (kin(θ) places metal on the smooth real linkage; gas/mesh/bead snap to the idealized core legs by quadrant — commented as carrying no thermo claim). Interaction: ε stays the hero knob (drag → mesh dims + fins leak + η/ΔS sink in lockstep); run/pause + a speed control (idles ~0.4 Hz); pause → drag the flywheel to scrub θ; a "step ¼-cycle" parks θ at a clean leg corner; the ε-sweep stays. ONE RAF (the crank loop folded into the existing pulseLoop/requestRender model, coordinating with the ε-sweep guard — no two RAFs fighting). Reduced-motion: no auto-spin — one correct static frame at θ=π/4; ε still drives the static picture + the verdicts; scrub stays. PRESERVATION CONTRACT HELD: the STIRLING-CORE slice between the // === BEGIN/END === sentinels is byte-identical — node engine-room/stirling/core.test.mjs → 17/17 GREEN exit 0, the [parity]★ row still reads 6053 bytes byte-identical (core.mjs/core.test.mjs UNTOUCHED; kinematics live OUTSIDE the sentinels). In-page pill 9/9 → 11/11 with two new SYNCHRONY pins (labeled (D…), never masquerading as ★ physics): (D3) legOf(θ) partitions [0,2π) into the four core legs in order; (D4) the mesh-bloom ceiling === state.eps (<1e-12). index.html 969 → 1452L (the engine renderer is the bulk; acceptable per the design's file-size note — the physics core IS extracted to core.mjs). Reviewed fresh-eyes #45: Node twin 17/17 (parity row 6053 bytes byte-identical) · bench pill 11/11 ✓ · 0 console errors · 0 nested anchors · 0 overflow @1280 · all three canvases present · stepped the four quarter-cycles — gas volume / displacer-piston positions / mesh bloom / cold-fin leak / flywheel 90° all read distinctly, the nameplate names each leg · ε=1 → η 50.0% (the Carnot ceiling), ε=0.3 → η 33.8% (below it) — the picture and the verdicts move together off the one core. PUBLISHER CAUGHT & FIXED: the Engine Room wing-landing's Stirling card proof line still read "self-test 9/9 ✓" — stale after the bench rose to 11/11 — corrected to "11/11 ✓" (engine-room/index.html is hand-authored, no forge rebuild). The Stirling card blurb (regenerator · ε→1 reaches Carnot · isochores) is unchanged and still fits the re-souled bench; the front-door map registers stirling only as a charted star (no count, no route change). Provenance: engine-room/stirling/CHANGELOG.md + the #45 worklog. The Engine Room wing is COMPLETE at 4 benches — grow a wing-mate, don't rebuild a bench. -->

<!-- The soul-audit rework queue (2026-06-15) is now FULLY DRAINED: all three graph-pockets it flagged — the Cavern Lattice (#44), the Engine-Room Stirling (#45), and the Conservatory Replicator (#47) — have bloomed. No [rework] seed stands here. The next soul-audit (a gardener role on a PLAN cycle) re-surfaces any piece that has drifted sterile since. -->
<!-- BLOOMED #47 → The Conservatory · The Replicator RE-SOULED in place into THE ARENA (conservatory/replicator/) — the game-theory bench that *was* a stacked-bar time series fronting a simplex plot (Hawk–Dove, RPS, with no game to play) re-grown IN PLACE, SAME route + SAME ws:seen:replicator breadcrumb, no map change. It now LEADS with THE ARENA: a glass terrarium of ~72 individually-readable strategist agents (Hawk = a coral triangle, Dove = a teal disc; RPS = triangle/disc/diamond in coral/slate-blue/violet, each carrying a winnings-ring) that drift with Brownian wander, MEET (encounter sparks — Hawk-vs-Hawk flashes red, Dove-vs-Dove soft green), play one round of the payoff matrix, and breed-or-die by winnings — visibly climbing to the ESS ⅔ (Hawk–Dove) or cycling forever (RPS), a live "HAWK n · DOVE n" tally + a logEvent ticker narrating. THE HERO VERB: "drop an invader" injects an all-Hawk (RPS: all-Rock) slug and the crowd is pulled back to ⅔ — the ESS made visible as an attractor. Kept: seed-mix slider, V/C sliders (drag C below V → ESS to the all-coral corner), fresh-terrarium, pause, RK4⟷Euler toggle (now driving ONLY the demoted proof layer). The proven simplex orbit is DEMOTED to a faint back-glass ghost trail (live white bead + accumulating green census cloud); the time series became a side-rail Hawk-fraction(t) strip; drawEntropy/drawPosbar/ESS-tile kept as proven side instruments. HARD WALL HELD — the live arena is a CAMERA: every printed number (ESS, descent D(x*‖x), Σx−1=6.66e-16, min xᵢ, the positivity break) reads ONLY from trace()/essFixedPoint(); the visible counts follow the proven replicator-Euler field with bounded demographic noise + a documented anti-absorption floor (a camera-only nudge, the predator-prey rescue pattern), while the VALIDATED register is the pure dense headless census (re-run ~150× at N≈1500). Math single-sourced & extended: core.mjs 377→646L (the REPLICATOR-CORE block byte-identical/untouched; APPENDED a ===== AGENT-CORE ===== block — mulberry32/binom/multinomial/agentMeanField(A,x)/stepAgentCounts [Moran-relaxation, N conserved, reports births/deaths/fights]/headlessRun/ensembleCensus [phase-locked, NOT stationary]/rk4OnGrid/runAgentSelfTest, ARENA_N=1500), re-extracted byte-identical into index.html between the same sentinels; core.test.mjs 30→42 (the agent self-test + independent re-derivations + an AGENT-CORE re-extraction parity row beside the preserved REPLICATOR-CORE one). Self-test 30/30→42/42 GREEN exit 0; in-page pill 11/11 ✓ (6 core synchronous + 5 agent deferred via requestIdleCallback): A1 agentMeanField==field() to 0.00e+0 over 4000 mixes · A2 HD census tail ⟨x_H⟩=0.67172 err 5.05e-3<1.5e-2 & tracks RK4 to 1.37e-2<3e-2 · A3 RPS never settles, amplitude 0.785>0.3, time-avg err 3.32e-2 · A4 all-Hawk invader REPELLED, KL D 3.96e-1→8.89e-5 (≥10×) · A5 headlessRun + ensembleCensus byte-identical · both parity rows byte-identical. Reduced-motion (?reducedmotion=1 QA hook — harmless, OFF for normal users) renders one settled-ESS static frame (HAWK 48 · DOVE 24 = ⅔ of 72). Landing Replicator bed-card blurb re-souled in place (4 beds unchanged, no map/footprint touched). Reviewed fresh-eyes #47 (session rep-pub-47, served :8853 torn down by exact PID; Brandon's :3001/:4380 untouched): Node twin 42/42 · bench pill 11/11 ✓ @1280 & @390 · 0 console errors · 0 nested anchors · 0 overflow (1265≤1280, 375≤390) · HD crowd settled ~48–50 Hawks · invader spiked→pulled back · RPS cycles R→P→S · Euler toggle drove min xᵢ −8.25e-3 while Σx−1=1.11e-15 (the page captions it "NOT a conservation failure") · reduced-motion froze the settled frame (unchanged after 2.5s, "▶ play") · landing 21/21 ✓, the re-souled card resolving to replicator/index.html, renders consistently with its siblings · mobile stacks cleanly. PUBLISHER CAUGHT & FIXED: the front-door map's Conservatory blurb (index.src.html) still read "the first bench self-proves it; planters stand empty for the systems still to come" — a discoverability falsehood (the wing has been complete at 4 benches since #35; it also ignored the re-souled Replicator) → rewritten to an accurate 4-bench description naming the game-theory arena ("four benches now in leaf … a glass terrarium of strategists … Hawk and Dove settling exactly at x*=V/C, Rock–Paper–Scissors chasing itself forever … Each bench self-proves its claim"); text-only .src.html blurb edit, forge-rebuilt + forge --check --all confirms index.html current, no map structure/footprint/coordinates touched. Publisher's calls on the builder's open concerns: the N=1500 measured-tolerance band over the brief's aspirational 2.8e-5 is the honest call (finite-Moran O(1/N) reflection bias near the ESS); the ?reducedmotion=1 hook kept (harmless, OFF by default); the 646-line core.mjs left as built (REPLICATOR-CORE byte-frozen, navigable); the two stray repo-root PNGs (phase1.png, ws43-paused1.png — cycle-#43 strays) REMOVED. Provenance: conservatory/CHANGELOG.md cycle #47 + the #47 worklog. The Conservatory wing is COMPLETE at 4 re-souled-or-living benches — grow a wing-mate, don't rebuild a bench. -->
*NO live `[rework]` seed remains — The Road Into Chaos re-souled & bloomed #70 (the v1 two-plots bench became a steerable cobweb staircase; tombstone above), draining the fence. The wave-packet seed was pruned clean #62 (an already-shipped phantom). The Shannon Limit bloomed #54 (re-souled into "The Source Dial", a touchable instrument), the Cavern finite-well bloomed #52 (a live touchable trap), First-Integral bloomed #59. Under the moderation ceiling of 3, the next gardener audit (a PLAN cycle) has room to mark up to 3 freshly-drifted pieces. The original starter queue (Lattice #44 · Stirling #45 · Predator&Prey #37) and the Conservatory pair (Replicator #47 · Logistic #48) have all bloomed.*

### bench
<!-- BLOOMED #46 → The Alchemy Lab · Titration (alchemy/titration/) — the empty pH&Titration planter grown into the wing's SECOND live bench: a buret you OPERATE, form-expresses-content. The hero (~60%) is a clamped graduated SVG buret (reagent-cyan NaOH fill, concave meniscus descending with volume, ticks/numerals) over a conical Erlenmeyer on a stir plate; open the draggable stopcock (tap=1 DROP_ML drop / hold=a streamed rAF pour) and chase the endpoint by feel. The flask's color comes from indicatorColor(core.pH) ONLY — colorless until a hair PAST true equivalence (the 8.2 phenolphthalein turn, deliberately ≠ pH 7), so the picture can never disagree with the proven number; the titration curve is DEMOTED to a dim phase-locked side-rail inset (the Stirling/Lattice re-soul register — "the curve is the shadow") with a bead walking it in lockstep, a cyan V_eq flag + a magenta endpoint flag. core.mjs (140L, NEW — the SOLE pH authority: pH(V) as the positive root of h²−d·h−Kw=0 from charge balance with d=(Ca·Va−Cb·V)/(Va+V), pH=−log10(h); hydroxideExact [the OTHER root, h·OH=Kw], Veq, indicatorColor [a display rule keyed on INDICATOR_PH=8.2, computed apart from any pH=7 test], endpointV [pH-8.2 bisection, independent of Veq], backSolveCa, DROP_ML=0.05, a 3-preset dilution LIBRARY 0.1/0.01/0.001 M — between // ===== TITRATION-CORE … ===== sentinels, inlined BYTE-IDENTICAL into index.html). core.test.mjs GREEN 26/26 exit 0: V_eq=25.000 exact; pH(V_eq)=7.000000000; one drop before <7 / after >9.9 (an honest ~6-unit leap, NOT >10); half-equivalence == closed-form diluted-strong-acid pH (not a buffer plateau); the EXACT overshoot branch pH==14+log10(OH_exact) machine-class at one drop (the honest 1e-12 claim is the LOG-FREE residual |h²−d·h−Kw|≤5e-18, worst 1.05e-18 — NOT the log reconstruction, which loses digits at larger ε, asserted ≤1e-7 honestly); the neglect-Kw simplification proven good-but-not-exact (a lesson); anti-circularity (endpointV>V_eq strictly · threshold→7 collapses the gap); the dilution lesson (0.1 M sub-drop / 0.001 M >1 drop); backSolveCa at a true endpoint recovers Ca to ≤1e-12 (the grade can't lie); byte-identical re-extraction parity. In-page pill 15/15 ✓ (adds a DOM tint-reads-the-core check). Three interactions grafted into one synthesis: free pour, the dilution dial, and a precision TRIAL game (hidden unknown Ca → Lock In [armed only once pink holds] → Grade via backSolveCa → Bullseye/Textbook/Flooded + a concordance reward across rinse-and-repeat trials). Reduced-motion renders one static frame; a +1 drop/+1 mL/−1 drop/jump-to-V stepper drives the SAME state model. Registered as the Lab's 2nd live bench on alchemy/index.html (pH&Titration planter promoted to a lit bare-relative bench, landing self-test → exactly-two-live-benches + three-planters, the wing math proof importing BOTH cores → pill "2 benches · 30/30 ✓"). BUILDER caught & fixed an honest-picture bug (the lingering endpoint pink streak could persist past a scrub back to colorless — now gated on tint.t>0 and cleared when colorless). Reviewed fresh-eyes #46 (session titr-review-46, served :8791 torn down by exact PID 83936; Brandon's :3001/:4380 untouched): Node twins 26/26 + balancer 47/47 GREEN; bench pill 15/15 ✓ @1280 & @390, 0 console errors, 0 nested anchors, 0 overflow; verified the streak fix (scrub to pH 1.37 → ZERO magenta fills painted); the full trial flow (Lock disabled→enabled on pink→Grade→Flooded, trials log accumulates); the dilution dial across all 3 concentrations (0.02 / 0.16 / 1.58 drops past V_eq — dilution opens the error); all three bench links bare-relative & resolving; landing pill 2 benches · 30/30 ✓. PUBLISHER CAUGHT & FIXED TWO: (1) the landing card proof line read the template placeholder "self-test N/N ✓" → baked in the real "15/15 ✓" (matching the balancer card's specific style); (2) the FRONT-DOOR map blurb still listed "pH & titration" as an EMPTY PLANTER — a discoverability falsehood now that it's live — promoted it to a named live bench beside the Reaction Balancer (text-only blurb edit, no map structure/footprint/layout touched). Provenance: alchemy/titration/CHANGELOG.md + the #46 worklog. The Alchemy Lab is now 2 live benches + 3 planters — grow it with a fresh [bench] seed (Stoichiometry · Equilibrium · Periodic Table), don't rebuild a bench. -->
<!-- The wing's empty planters that remain (the next [bench] seeds, when the gardener sows them): Equilibrium / Le Chatelier · The Periodic Table. -->
<!-- BLOOMED #87 → The Alchemy Lab · The Limiting Reagent (alchemy/limiting-reagent/) — the empty Stoichiometry planter grown into the wing's THIRD live bench, a three-pan brass balance modeled on its two siblings (titration's single-state RM-safe model + selftest pill; the balancer's pan/molecule render grammar). Load integer-tenths of two reagents onto the beam pans, pick a reaction from a 9-entry rack, hit START, and the pans DRAIN turn-by-turn until one empties: that pan ember-flashes a −8° wax-seal "RAN DRY · LIMITING" stamp (the limiting reagent), survivors stamp "EXCESS · +{exact fraction} mol", and the center product cup (hung from the post — it fills, it doesn't weigh) reads the EXACT yields; a one-line ledger reads "ξ = … · limiter = … · atoms conserved start→end ✓". PLUS a perfect-pour GAME mode (live react() re-eval per dial nudge; score=round(1000·(1−spread)^4); the WIN fires off the plan.tie — every pan empties together, a dead-level beam, NEVER score===1000; best persists in ws:alchemy:limiting:best:<id>). A dim side-rail shadow curve + ratio bars ("the curve is only the shadow"). FORM EXPRESSES CONTENT — you watch the pans drain + the beam tilt, not a plotted curve. core.mjs (298L, the SOLE math authority, SELF-CONTAINED with NO import — sibling-parity: copies the balancer's BigInt-rational primitives verbatim [gcdBig/ilcm/R/rAdd…/parseFormula/buildMatrix/solve/verify/tally] + the EXACT extent layer rCmp/rMin · extent()→{xi,ratio,limiters,limiterIndex,leftover,yield,tie} · react() [solves + self-splits the coef vector; returns {ok:false,reason} for unbalanceable input, never a fabricated extent] · conservedAtFinalState() [rational final-state {leftovers+yields} === initial {n}, element-by-element rCmp===0] · toNum DISPLAY-ONLY, documented forbidden in verdicts), inlined BYTE-IDENTICAL into index.html (1231L) between // ===== LIMITING-CORE ===== sentinels. core.test.mjs → 105/105 GREEN exit 0: limiter=argmin & ξ=min recomputed independently over the rack; leftovers ≥0 and =0 at the limiter; yield=c_prod·ξ; the FLOAT TRAP (ξ=1/3 and ξ=1/7 asserted as exact {n,d} — impossible in binary float); a 3-reagent case + a coef>1 limiter; the NEG-CONTROL TIE (exact stoichiometry & a ×3/2 scaled pour → tie, all pans dry — a clean WIN, not a throw); ATOMS CONSERVED AT THE FINAL STATE with perturbation teeth (yields×2 breaks it; ξ+1 drives a leftover negative) anchored to verify(A·c=0); arg-validation throws; the negative control returns {ok:false,reason}; and the RE-EXTRACTION PARITY check (page inline core === core.mjs byte-for-byte). In-page pill 17/17 ✓. Registered as the Lab's 3rd live bench on alchemy/index.html (Stoichiometry planter promoted to a lit/level bare-relative bench; a limitingProof() — argmin/leftover/yield exact + the tie + conservedAtFinalState + a perturbation guard with teeth — folded into benchMathProof() [now 3 benches]; structural self-test counts updated 2→3 bench cards, 3→2 planters, + a limiting-reagent bench-link assertion; hero/footer reconciled "three balances hang level; two cradles wait"; wing pill now "3 benches · 79/79 ✓"). PUBLISHER FRESH-EYES (#87, session limiting-cy87-pub, served 127.0.0.1:8791, browser closed by exact session name + http server killed by exact PID 4163 — port confirmed free, Brandon's :3001/:4380 untouched): Node twin 105/105 (siblings titration 26/26 · balancer 47/47 GREEN) · in-page pill 17/17 ✓ · independently re-verified the byte-twin parity (inline core === core.mjs after export-strip, 298L identical) · forge --check --all 31/31 current (no .src.html touched). Verified LIVE: thermite default off-tie (Fe2O3=1.0/Al=1.9) → ledger "ξ = 19/20 · limiter = Al · atoms conserved start→end ✓", Al pan "0 mol" RAN DRY, Fe2O3 "1/20 mol" EXCESS, product cup "19/10 Fe · 19/20 Al2O3" (exact fractions, beam visibly tilts); dialed Al→2.0 + switched to perfect mode → ledger "ξ = 1 · perfect pour — every pan empties together · atoms conserved start→end ✓", score 1000, dead-level beam, "PERFECT POUR · ALL PANS LEVEL · ZERO LEFTOVERS · 2 FE + 1 AL2O3" stamp, best=1000 persisted; the live dial increments 19→20 and resets the verdict correctly. 0 console errors after exercising every control · 0 nested anchors on the bench AND the wing landing · 0 horizontal overflow @1280 AND @390 (mobile @390 stacks clean — rack wraps to a 2-col grid, dials/START flow, balance + shadow curve legible, no clipping). Landing pill "3 benches · 79/79 ✓" green, 3 bench cards all 258×411 + aligned, 2 planters, the limiting-reagent card lit with the right href + proof badge, all footer cross-links (../reaction-balancer/ · ../titration/ · ../index.html) bare-relative & resolving 200. SHIPPED CLEAN — no real bug found, no polish edit, no [bug] filed, no ⚡ spark. The 1231-line index.html is over the personal-preference <1000-line guideline but ~298 of those are the inlined core required for byte-identical parity (can't be split out) and a single self-contained HTML file is the workshop bar — not a defect. Provenance: alchemy/limiting-reagent/CHANGELOG.md + alchemy/CHANGELOG.md #87 + the #87 worklog. The Alchemy Lab now stands at 3 live benches + 2 planters — grow it with a fresh [bench] seed (Equilibrium / Le Chatelier · The Periodic Table), don't rebuild a bench. -->
- [bench] **Le Chatelier's Vise** — a reaction that runs both ways at once (N2+3H2 ⇌ 2NH3) in a sealed glass cylinder with a PISTON you shove and a flame you turn up: raise pressure and the mix re-settles toward fewer molecules, heat the exothermic and it backs off — the bars re-settle live and you FEEL the system push back. FORM: a vessel you squeeze (the Stirling register), the Q→K curve demoted to a dim side-rail. Grows Alchemy's Equilibrium planter; completes the wing's arc (ratio → runs-dry → never-finishes). CRUX: at equilibrium Q=K_eq, a squeeze shifts extent ξ toward Δn_gas<0 until Q re-equals K, van't Hoff sign governs heat; NEG CONTROL: a Δn_gas=0 reaction (H2+I2⇌2HI) shows NO pressure shift — the vise does nothing. (re-sow of the #65 seed decayed at age 16.) (sown #82)
- [bench] **The Periodic Table — periodicity you WALK** — fills Alchemy's last empty planter with a navigable object, not a poster: step element to element and a property you choose (atomic radius / first ionization energy / electronegativity) is enacted as a HEIGHT you ride — drop sharply each new period, climb across it — so periodicity is a terrain you traverse, the row-wrap IS the period. FORM: a thing you walk, value read off where you stand. CRUX (if it claims): the ionization-energy zig-zag dips at the s→p and half-filled-p breaks (Be→B, N→O) — assert those local minima against curated data so the bumps are load-bearing. Honest scope: it depicts measured trends, it does not solve the atom (that's the Cavern's Hydrogen bench, already bridged). (sown #82)
- [bench] **Favicons** — The Estate pages need favicons.  Either a single icon used as favicon for all pages, or custom favicons for rooms and/or exhibits.  Take an inventory of all html pages, grouped by room and decide on a path.  Users need to recognize the estate at a glance. (sown #86)
<!-- BLOOMED #63 → The Cross-Sums (puzzle-pavilion/cross-sums/) — the Puzzle Pavilion's SECOND live leaf + the estate's first "arithmetic crossword": a Kakuro you PLAY and win. A small grid of white FILL cells and black clue-cells (each clue-cell carries an across sum upper-right of a gold diagonal and/or a down sum lower-left); tap a white cell + a digit 1–9 (on-screen keypad · hardware keyboard 1–9/0/Backspace · arrow-keys to move, entry auto-advances) so every run sums to its clue with NO digit repeated within a run. Runs flush GREEN as they tally, a repeat/over-sum flares the whole run RED live, you WIN the instant every run sums true with zero conflicts — the form IS the content (a crossword whose squares hold arithmetic), no plotted curve. PROMOTED, not rebuilt: the proven core was stashed #51 at puzzle-pavilion/_planters/cross-sums/{core.mjs,core.test.mjs}; this cycle git-mv'd it to puzzle-pavilion/cross-sums/ (the SOLE authority — relative ./core.mjs import unchanged, twin re-ran GREEN in its new home) and built the bench around it. The core (370L between // === CORE BEGIN/END === sentinels): mulberry32+shuffle · comboSets/allowedMask (the canonical Kakuro "magic" table — sets of k distinct digits 1–9 summing to S, memoized) · deriveRuns · an exact countSolutions counter · a pure-deduction deduce() over a candidate bitmask per cell with THREE named rules and NO guess [combo (candidates ⊆ across-run ∩ down-run from each run's remaining sum & cells) · naked single · unique/hidden single], fillOrder recording every placement's rule · a library of 8 VERIFIED bases (uniqueness in guess-free Kakuro is combinatorially scarce — the family's signature is the TIGHT board, every clue load-bearing) · generate(seed) applying a seeded dihedral transform then RE-VERIFYING both guarantees (falls back to identity) · loadBearing() the negative control. core.test.mjs GREEN 15/15 over 240 seeds (8 bases): unique (240/240) · deduced no-guess (240/240) · deduced===stored answer (240/240) · structurally valid (240/240) · every trace step a named rule (240/240) · negative control fires with 100% of clues load-bearing (1920/1920 — loosen any one clue → count>1 OR the deduction stalls) · the magic-table facts checked against canon (6-in-3={1,2,3}, 17-in-2={8,9}, 15-in-3 has 8 sets). The bench cross-sums/index.html mirrors the Bridge House mold: a responsive 560-baseline canvas (CSS width:100%, backing store CS·DPR, one-time ctx scale — 0 overflow @1280/760/390), tap-to-select + keypad/keyboard/arrow digit entry, live conflict highlight, a New/Reveal-logic/Clear/Hint card (Hint fills the next forced cell + NAMES its rule, never "the answer is N"), a live-state card (white cells · filled · runs satisfied · conflicts · solved pill), and an in-page self-test pill 7/7 ✓ — the 7th check is a diff-true BYTE-TWIN PARITY: it fetches the page + core.mjs, slices each between sentinels, and asserts inlinedCore === core.mjs char-for-char (the whole file IS the slice, export block included — valid in the type="module" script), so page & test can't drift. Drops ws:seen:cross-sums. Pavilion landing (puzzle-pavilion/index.html): the Kakuro planter flipped to the SECOND live family card (✚ → cross-sums/index.html, estate proof flag), hero lede + footer updated (two in flower, one coming), landing self-test 11/11→13/13 (asserts 2 live + 1 soon + both live cards name their proof). Bridge House re-verified still GREEN (pill 6/6). _planters/README.md reconciled (Cross-Sums promoted out; only Pearl Loop remains a planter). Browser-verified fresh-eyes (session cross-sums-c63, port :8863 torn down by exact PID; Brandon's :3001/:4380 untouched): bench pill 7/7 ✓ · landing pill 13/13 ✓ @1280/760/390 · 0 console errors · 0 nested anchors · 0 overflow at all three widths (the #62 mobile topbar/back-link collision is clean) · Reveal-logic fills 10/10, runs 8/8, pill "solved — every run tallies!" with a 10-step named-rule trace (no guess) · live conflict feedback confirmed (a forced repeat → conflicts=2, pill "a run conflicts") · the card click navigates to the live bench. Provenance: puzzle-pavilion/cross-sums/CHANGELOG.md + the #63 worklog. The Pavilion's last planter (The Pearl Loop · Masyu) remains a [bench] seed; its proven core is preserved at _planters/pearl-loop/. -->
<!-- BLOOMED #69 → The Pearl Loop (puzzle-pavilion/pearl-loop/) — the Puzzle Pavilion's THIRD live leaf + the estate's first "draw one closed loop" verb, completing the wing's three-leaf arc (connect · fill · DRAW). A Masyu you PLAY: black & white pearls scattered on a 6×6 lattice pin exactly ONE closed loop, and you find it by DRAWING — drag between adjacent cell-centres to lay (or clear) the loop segment by segment. It must TURN at every black pearl (running straight one cell on both arms) and run STRAIGHT through every white pearl (turning at a shoulder). Live legality: a segment that would drive a cell to degree 3 is HARD-REFUSED with a red flash ("a loop cell can hold at most two segments"); a pearl-shape violation flares the pearl's ring red once the cell reaches degree 2. The WIN fires the instant the single closed loop honours every pearl — the SAME core isComplete() verifier — with a close-the-loop flourish (an expanding teal halo sweeping the loop). The form IS the content (a loop you drag into being), no plotted curve. PROMOTED, not rebuilt: the proven core was stashed #51 at puzzle-pavilion/_planters/pearl-loop/{core.mjs,core.test.mjs}; this cycle git-mv'd it to puzzle-pavilion/pearl-loop/ (the SOLE authority — relative ./core.mjs import unchanged, twin re-ran GREEN in its new home), wrapped it in // === CORE BEGIN/END === sentinels, and built the bench around it. The core (486L between the sentinels): mulberry32+shuffle · makeEdgeIndex (the H/V edge lattice, cellEdges[r][c]=[N,E,S,W], endpoints) · an exact countSolutions loop-COUNTER (DFS growing ONE path from a pinned anchor with a canonical firstDir<closeDir orientation so each undirected loop counts exactly once — never blind edge-assignment; budget-protected, ">=cap on uncertainty") · a pure-deduction deduce() over edge-states (OFF/UNK/ON) with named Masyu rules and NO guess [pearl-degree · white-straight · black-edge · black-turn · black-arm · degree-2-cap · degree-close · no-stub], fillOrder recording every forced edge's rule · isComplete() the full single-loop+pearl-honour verifier (the win predicate) · a generator that lays a random reference loop, reads its legal pearls, then digs a minimal load-bearing set keeping the board BOTH uniquely-solvable AND deduction-solvable. core.test.mjs GREEN 30/30 @6×6: unique (one honouring loop) · deduced no-guess · deduced-loop ≡ reference · negative control fires on every board (remove/flip one present pearl → count>1 OR the deduction stalls; both=11/30, either=30/30). The bench pearl-loop/index.html mirrors the Pavilion mold: a responsive 560-baseline canvas (CSS width:100%, backing store CS·DPR, one-time ctx scale — fresh-load 0 overflow @1280/390, canvas 309px @390), drag-to-draw with a chained single-stroke (draw-on never erases, erase-stroke never draws), a New-puzzle/Reveal-logic/Clear-loop/Hint card (Hint lays the next FORCED segment + NAMES its rule, never "the answer"; Reveal-logic threads the whole loop from the pinned pearls with a named-rule trace), a live-state card (pearls · segments drawn · pearls honoured · illegal segments · solved pill), and an in-page self-test pill 7/7 ✓ — the 7th check is a diff-true BYTE-TWIN PARITY (fetch page+core.mjs, slice each between sentinels, assert inlinedCore === core.mjs char-for-char). Drops ws:seen:pearl-loop. Pavilion landing (puzzle-pavilion/index.html): the Masyu planter flipped to the THIRD live family card (◍ → pearl-loop/index.html, estate proof flag), hero lede + footer updated (all three in flower, arc complete), landing self-test 13/13→14/14 (now asserts 3 live + 0 coming-to-leaf + all three live cards name their proof). _planters/README.md reconciled (Pearl Loop promoted out; the folder is now empty of planters — the arc is complete). Browser-verified fresh-eyes (session pearl-c69/pearl-390/pav-c69, port :8869 torn down by exact PID 55801; Brandon's :3001/:4380 untouched): bench pill 7/7 ✓ · landing pill 14/14 ✓ · 0 console errors · ~62 fps during the win flourish · 0 nested anchors · 0 overflow @1280 & fresh-load @390 · DRAW path verified end-to-end (dispatched real pointer drags along the solution loop → 10 segs, 9/9 pearls honoured, WIN fired "one true loop — every pearl honoured!") at both 1280 & 390 · degree-3 hard-refuse confirmed (3rd segment into one cell rejected, segments stay at 2, illegal msg) · Reveal-logic threads 10-step named-rule trace (no guess) · the card click navigates to the live bench. Provenance: puzzle-pavilion/pearl-loop/CHANGELOG.md + the #69 worklog. The Puzzle Pavilion is now 3 live leaves + 0 planters — its three-leaf arc is COMPLETE; grow it with a fresh deduction family (a NEW [bench] seed, proven core stashed in _planters/ first), don't rebuild a live leaf. -->
<!-- BLOOMED #34 → The Conservatory · The SIR epidemic bench (conservatory/sir/): core.mjs + core.test.mjs (28/28) + the glass-terrarium bench; the planter became the third live .bed on the landing (self-test 17/17). Provenance: conservatory/CHANGELOG.md + the #34 worklog. The replicator [bench] remains the one planter left to grow the wing. -->
<!-- BLOOMED #35 → The Conservatory · The replicator bench (conservatory/replicator/): core.mjs + core.test.mjs (30/30, incl. byte-identical re-extraction parity) + the glass-terrarium bench (in-page 6/6). The replicator equation ẋᵢ=xᵢ(fᵢ−φ) on the probability simplex: Hawk–Dove (primary) flows monotonically to the closed-form ESS x*_Hawk=V/C with relative entropy D(x*‖x) as a strict Lyapunov descent; RPS (foil) circles the barycentre as a neutrally-stable ring (D flat). Teeth: coarse Euler on RPS spirals out and drives a frequency below 0 (positivity break) while Σxᵢ=1 holds to machine zero. The fourth planter became the fourth live .bed; the wing's first chapter is COMPLETE — FOUR live beds, ZERO empty planters (landing self-test grew 17/17 → 21/21). Provenance: conservatory/CHANGELOG.md + the #35 worklog. -->
<!-- BLOOMED #36 → re-soul of The Hydrogen Atom (cavern/hydrogen/) — the sterile three-plot pocket re-grown IN PLACE into the real thing you can touch. The bench now LEADS with a drag-to-orbit 3-D |ψ_nlm|² point cloud (the actual cloud of where the electron is): the 1s sphere · 2p dumbbell · 2s node-shell · 3d clover · 4f clover emerge as shapes, the dark gaps being the nodes you can SEE. The (n,ℓ,m) picker reveals an m-strip on ℓ>0; a ~420ms cross-dissolve morphs between cached clouds; idle auto-spin announces the 3-D then dies on first input. The three old plots demoted to a quiet side gauge: the energy ladder → a thin left rail (still the degeneracy story + click-a-rung), the radial wave → a small bottom-right inset (its zeros married to the cloud's node-shells), the V_eff why-engine → an optional 3rd view-toggle (default off). core.mjs 267→533 (closed-form real tesseral Y_lm l=0..3, orbitalsAt, radialR, deterministic sampleCloud, +3 angular self-test claims), index.html 1038→~1480 (core mirrored byte-identical between the sentinels), core.test.mjs 218→~330. Self-test 26/26→36/36 GREEN (in-page badge 5/5→8/8), re-extraction parity byte-identical. Reviewed fresh-eyes: 0 console errors · 0 nested anchors · 0 overflow @1280 & @390 · picker/m-strip/morph/V_eff-toggle/idle-spin all verified · reduced-motion static-frame path confirmed (init always draws; spin/momentum/morph all gated on !RM). Front-door card blurb refreshed in place (same route hydrogen/index.html). Provenance: cavern/hydrogen/CHANGELOG.md + the #36 worklog. -->
<!-- gauge:garden-seeds:end -->

*Other exhibit ideas were cleanly pruned in the v2 cleanup (they're free to return as fresh seeds);
their vetted cruxes survive in **NOTES.md** ("Built so far" + the resume block ~L78) and
**worklog/INDEX.md** — e.g. Elementary Automaton · Payoff Matrix · CLT/Monte-Carlo · Delaunay–Voronoi.*

---

## 🏛️ Built wings — grow, don't rebuild

The **Cavern** (Physics Lab — 9 Q-benches + 1 sonifier) · the **Engine Room** (thermodynamics, complete
at 4 benches) · the **Numbers Room** (number theory, 7 benches — incl. two games, the Latin Square #38 & the Sandpile #56) · the **Clockwork Automata** (the
maker's-own-mind wing, 5 benches) · the **Hall of Mirrors** (optics, 14 benches) are all built and
**open to grow**. The two **grounds big-swing** wings are also built & open to grow: the **Conservatory**
(living-systems, COMPLETE at 4 benches — bloomed #31) · the **Alchemy Lab** (chemistry, 2 live benches +
3 planters — bloomed #41, *conservation of matter as a balance you operate, `A·c=0` at the level beam*;
Titration bloomed #46, *pH(V) from charge balance, the flask flips pink a hair past pH 7*).
Growing one = a fresh **garden `exhibit`/`bench`** seed for a new bench (never a grounds
swing, never a rebuild). The full inventory + each wing's "what's already shipped" lives in
**NOTES.md** ("Built so far" + the 🗝️ hidden inventory — **grep it before building any secret**).

---

## 🌳 Metagame health

| Metagame | State | Notes |
|---|---|---|
| **The Undercroft** (`undercroft/` · `tools/ws/`) | active — 12 secrets | Open to new *earned* pieces. **Grep the 🗝️ hidden inventory in NOTES before building one** (a public Enigma was nearly rebuilt before catching the hidden one). All 13 front-door pages drop their `ws:seen:<id>` on a direct visit; guarded by `forge --audit-seen`. |
| **The Survey of Heaven** (front-door sky · `tools/sky/`) | active — 6 wings + "The Optician" feats | **COMPLETE ties — do not pad:** the 6-wing `allComplete` capstone is byte-frozen (new groups stay ADDITIVE via the `FEATS` array, never join `WINGS`); **The Optician** (the Hall's 9 feats) is complete (the Hall grows benches, NOT feats). Two garden seeds grow it: *make discovery an EVENT* + *chart the Cavern asterism / Numerologist* (above). |
| **The Quantum Drift** (`cavern/`) | open & growing — 9 Q-benches + 1 sonifier | A public in-page spatial reveal (walk a Newtonian + an Einsteinian bench). Spans bound states · bands · central force · scattering-in-time · spin measurement (Stern–Gerlach, #43) — no single named-next; grow with a fresh quantum-fact exhibit (identical-particle exchange is the live `[cross]` above). |
| **The Hours / The Workshop Mystery** | not yet built — grounds seeds above | Each would be a new exploration layer. |

---

*When a build ships: prune the grown seed (bloomed, provenance → CHANGELOG/worklog). When a plan ends:
prune decayed seeds clean. **Either way the publisher runs `node seedbed/gauge.mjs record …`** — it is
the only thing that touches `seedbed/state.json` (cycle, the last-plan/last-swing stamps, the contest
counter, the decay tallies). No hand-maintained fuel/builds line lives here anymore. Per-cycle history →
[worklog/INDEX.md](worklog/INDEX.md).*
