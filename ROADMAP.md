# 🌱 The Seedbed — the workshop's roadmap

*The gardener's bed. A **planning session** sows seeds here; a **building session** picks one (or
ignores them all and dreams). Read [DESIGNING.md](DESIGNING.md) for how each mode works, and
NOTES.md for the **mode gauge** that decides which kind of session you're in.*

> **A seed is a provocation, not a spec.** Hard rule: **a seed is ≤ 3 lines.** The moment you catch
> yourself writing a full design, you've stopped *sowing* and started *dictating* — stop, and let the
> builder choose the *how*. The bed is a **floor against blank-page paralysis, never a ceiling**: any
> build session is free to chase something that isn't here at all.

**Schema.** Each seed: `- [kind] **pitch** — optional tiny nudge(s); `feeds:` metagame hint.`
Kinds: `exhibit` (a session's work) · `room` (a new front-door place — a *bet*) · `metagame` (curate
an exploration layer) · `engine` (new reusable foundation — a *bet*) · `curation` (improve / merge /
**retire** an existing piece) · `cross` (pollinate two existing rooms — the richest vein here).
When a seed blooms, prune it (its provenance lives in the piece's CHANGELOG + the worklog).

---

## 🌰 Seeds

### 🐞 bug — clear these first
*(A builder should fix an open `[bug]` before pulling fresh fuel — the director prioritizes them.)*

*(No open bugs.)*

*Tombstone (FIXED 2026-06-14 — full provenance in `engine-room/carnot/CHANGELOG.md` v1.1 + the worklog): ~~The Carnot bench's self-test pill overflows the viewport on narrow phones~~ — the Engine Room's fixed `.topbar` (`display:flex; justify-content:space-between`, no wrap) pushed the `#selftest` pill ~86px off the right edge at ≤400px (the identical bug the Demon bench had). **Fixed** by transplanting the Demon's already-shipped scoped `@media (max-width:430px){ .topbar{ flex-wrap:wrap } … }` rule onto Carnot's identical `.crumbs`/`.selftest` selectors (CSS-only, +6 lines). Publisher fresh-eyes review confirmed 0 horizontal overflow + pill on-screen at 360/390px, **desktop 1280px unchanged** (rule scoped to ≤430px), Node twin still 16/16. **The whole Engine Room wing (Carnot + Demon) is now consistent on mobile.***

*Tombstone (BLOOMED 2026-06-14 — full provenance in `workbench/CHANGELOG.md` + the worklog): ~~Workbench cards spill their text where a blurb contains a cross-link~~ — the cards were `<a class="card">` with inner `<a>` cross-links inside 5 blurbs → nested-anchor invalid HTML, parser fragmented the card; **fixed with the stretched-link pattern** (card is now `<div class="card">` + a single overlay `<a class="card-link">`, inner links z-indexed above), applied uniformly to all 34 cards so it can't recur; fresh-eyes browser review confirmed 0 nested anchors / 0 spill / 0 console errors at desktop + mobile.*

### exhibit — fuel (a session each)
*Tombstones (BLOOMED — full provenance in each piece's CHANGELOG + the worklog; listed terse here only as a "don't-rebuild" echo of NOTES.md's inventory): Lighthouse → `lighthouse/` · Diffraction Grating → `diffraction/` · Structural colour → `structural-colour/` (Bragg Stack) · fractal-dimension → `fractal-dimension/` (Coastline Rule) · graph-algorithm → `pathfinder/` · M–B gas → `cavern/maxwell-boltzmann/` · Harmonograph → `harmonograph/` · Catenary → `catenary/` · Soap-Film → `soap-film/` · Spirograph → `spirograph/`.*

- ~~[exhibit] **9th Sound Garden instrument**~~ — **RETIRED cycle #7** (ground truth `sound-garden/instruments.js`: the rack ships **exactly 8** — Whitney/Drift/Euclid/Rain/Loom/Carillon/Lattice/Gamelan — on an auto-fit grid, a clean 2×4 at a tidy stop). The old "9 (3×3)" target contradicted the 8-is-clean call AND named **no instrument** — a grid-fill quota, not a provocation. **A future 9th may return ONLY as a new seed naming a genuinely must-have instrument, never as a count target.** *(The "Extent — change-ringing finally proven" exhibit — **BLOOMED cycle #9 → `extent/`** — was the Sound-Garden-adjacent fuel done right: it sonifies through the carillon synth and gives that instrument the proof it never had — a provocation, not a count.)*
- [exhibit] **Nomograph** — a Workbench instrument: a graphical-calculation chart (lay a straightedge across scales to read a product/quotient/root). The slide-rule's paper cousin — see the `Slipstick × Nomograph` cross. *(The planimeter half of this seed bloomed → `planimeter/`.)*
- ~~[exhibit] **A prime-spiral / Ulam bench**~~ — **BLOOMED 2026-06-14** → `prime-spiral/index.html` "The Ulam Spiral" ✦ (Workbench → Toys & benches, after The Best Rational; the **Numbers Room's 2nd bench** — *extends the wing*). Integers in an outward square spiral, primes lit → they fall onto **diagonals**, each exactly **4t²+bt+c** (leading coeff *always 4*); Euler's n²−n+41 lights 41 straight, breaks at n=41→41²; the running lit-count **is π(N)** riding the PNT's N/ln N. **Falsifiable crux:** two independent oracles (Sieve of Eratosthenes vs trial-division, **no shared code**) agree on every n≤N; the O(1) `nToXY` === a from-scratch `buildSpiral()` sim + its O(1) inverse `xyToN`; preset streaks/factorizations *derived live* (41/40/21/29, the n²+1 control → 0). Self-test **11/11** in-page / **18/18** Node twin (exhaustive sieve⟺trial to 160k, bijection to 160k, π(100k)=9592 / π(160k)=14683, PNT band 1.10). Two-canvas pipeline (static base + O(visible) overlay), 3 ignition tiers (discs@1k → points@10k/40k → additive ImageData cloud@160k), diagonal selector (click→outward ray / drag-sweep / ←→ arms), live π(N) strip+sparkline, hover hit-test, PNG export. **A build subtlety honestly handled:** a full two-armed 45° line kinks at the +x seam (not one quadratic) → a clicked generic diagonal selects the *outward ray* it lights (a===4 verified for all 14,640 cells in radius 60). Publisher fresh-eyes review caught & fixed a **mobile-only** π-strip flex bug (a nowrap readout starved the sparkline to width 0 on 390px → wrapped at the ≤900px breakpoint). *(Of the Numbers Room's seeded benches, Collatz remains; the Mill × Collatz + modular × cipher crosses stay open.)*
- ~~[exhibit] **A Collatz bench**~~ — **BLOOMED 2026-06-14** → `collatz/index.html` "The Collatz Bench" 🌳 (the **Numbers Room's 3rd bench** — the bench that TRIPPED the wing-build; self-test **7/7** in-page · **30/30** Node twin). Halve-if-even / triple-plus-one-if-odd; every tested n falls to 1, labelled **OPEN/UNPROVEN everywhere** (the pill is the deliberately-downgraded `✓ 0 counterexamples up to N — a check, not a proof`). A two-canvas **tree** (parity-coloured) + **river** of trajectories; presets 27→{111,9232} / 871→{178,190996} / 77031→{350,21933016} reproduce the records exactly. **Falsifiable spine — four claims, two independent constructions agree:** memoized stopping-time === a clean re-walk exhaustive to 200k (0 disagreements, all terminate) · step-records prefix === **A006877** + peak ladder === **A006884/A025586** · a backward inverse-tree's depth === forward `stoppingTimeRaw` on every reached node (coverage honestly reported as a STAT — the tree does NOT cover [1..N]) · the **3n−1 control fails with teeth** (36/60 of [1..60] never reach 1; `altCycle(5)=[5,7,10,14,20]`). Plain **Number** arithmetic justified live (max in-range peak 1,570,824,736 ≪ MAX_SAFE_INTEGER; BigInt deliberately avoided). The inline page core is a **byte-twin** of `collatz/core.mjs` (re-extraction parity harness: inlined `next()`/`nextAlt()` char-for-char === the imported `toString()`, re-extracted `trajectory(27)`→{111,9232} byte-for-byte). **Publisher fresh-eyes review CAUGHT & FIXED** a front-door label collision (the new POI was placed at x300/y720 *inside the central hero cartouche*, clipping the estate-subtitle by 3px — the builder mis-attributed it to a pre-existing adjacency; **relocated to x556/y724**, bottom-centre between the cartouche and the Cavern, overlap now 0) AND **completed the Mill × Collatz cross bidirectionally** (added the reciprocal Collatz teaser into `turing/`'s hint, re-forged). *(The Numbers Room wing is now THREE benches — Best Rational + Ulam + Collatz; only the modular × cipher cardioid number-theory cross stays open.)*
- ~~[exhibit] **An information-theory / entropy bench**~~ — **BLOOMED 2026-06-13** → `entropy/index.html` "The Shannon Limit" 📡 (Workbench → Computation, beside The Mill; the estate's **first information theory** — ciphers everywhere, zero Shannon entropy before). `H=−Σp·log₂p` is the irreducible bit-floor; Huffman's optimal prefix code lands in `H ≤ L < H+1`, and block-coding drives `L/k→H`. **Falsifiable crux:** Huffman's L == a **brute-force minimum over every prefix code** (an oracle enumerating all full-binary-tree leaf-depths, no shared code — exact over 400 random sources n≤9), Kraft Σ2^−len=1, the bound at every k, H=L iff dyadic, encode→decode round-trips, and the punchline — a **substitution cipher leaves H exactly unchanged** (why frequency analysis works). Self-test **9/9** in-page / **17/17** Node (the inlined core was extracted & re-run in Node to prove the copy byte-correct). A live **bits-ruler** (H an amber floor, L above, block-coding ghosts toward H) + Huffman tree on canvas + code table + encode/decode + source/block/cipher controls. **A perf trap fixed:** block-coding k=2,3,4 on English (26 sym) built a 26⁴≈457k product → `huffman()` O(m²·log m) hung the renderer on load; capped k so the product alphabet stays ≤2048. *(The **Black Chamber × entropy** cross below stays open — the natural follow-on; this core already proves the entropy-invariance it needs.)*
- [exhibit] **A least-squares / regression bench** — the estate touches probability (Galton, the gas χ²) but never **fits a line to data**. Drag noisy points; the best-fit line/curve **minimises Σ residual²**; show the normal-equations solution == a from-scratch gradient descent == the closed form, and `R²`. Crux = the analytic minimum beats every perturbation (it's *the* least-squares line) + a falsified outlier breaks `R²`. `feeds:` Workbench → Toys & benches (the estate's first statistics/inference bench).
- ~~[exhibit] **The Times-Table Cardioid**~~ — **BLOOMED 2026-06-14 (cycle #8)** → `cardioid/index.html` "The Times-Table Cardioid" ♡, the **Numbers Room's 4th bench**, built AS the modular×cipher cross. **THE ONE MAP** `chordTarget(i,k,m)=((k·i)%m+m)%m` drives BOTH the art and the cipher. On a circle of m points the chords i→(k·i mod m) are **tangent** to (not the curve) — a **cardioid** at k=2, **nephroid** at k=3, an epicycloid of **k−1 cusps** generally — and the closed-form epicycloid (a=k/(k+1), b=1/(k+1)) sits on the drawn caustic to **<1e-12** (the always-checked perpendicular residual; the normalised tangent-angle residual is guarded null at cusps; an INDEPENDENT neighbour-chord-intersection envelope corroborates E to <1e-8). The same ring ℤ/mℤ is the **multiplicative half of an affine cipher** E(P)=(k·P+b) mod m — a bijection IFF gcd(k,m)=1, else it collapses m residues onto m/gcd (the negative control with teeth: image=13/180/90/72 for the named non-coprime foils; the worked m=26 example MATH→IARJ→MATH is generated from code). **Self-test: in-page 8/8 ✓ · `node cardioid/core.test.mjs` 50/50 ✓** (incl. full re-extraction parity proving the inline page core is a char-for-char byte-twin of `core.mjs`). The **multiplicative** wheel the additive Volvelle never had — together they make the whole affine cipher (the Volvelle now carries a reciprocal teaser; both cross-links resolve). **Publisher fresh-eyes review (cycle #8) CAUGHT & FIXED** a mobile topbar collision (≤620px the long title overflowed under the self-test pill — added a truncate/shrink media query) AND reframed the degenerate-default cipher strip (k=2 hero is gcd≠1 so the strip first-read all-red — kept the iconic cardioid default but added a strip label + a gold on-ramp nudge naming the nearest coprime k). *(The Numbers Room wing is now FOUR benches — Best Rational + Ulam + Collatz + Times-Table Cardioid.)*
- ~~[exhibit] **The Extent — change-ringing, finally proven**~~ — **BLOOMED 2026-06-14 (cycle #9)** → `extent/index.html` "The Extent" 🔔 (a Workbench → **Instruments** bench, after the Volvelle; the proof the **Carillon** synth never had). n bells rung in a **method** that visits all n! permutations (the *extent*), each row exactly once, moving only by **adjacent transpositions** — and the generator decision shipped is **PLAIN HUNT, which CLOSES**: the last row returns to the first by one more adjacent swap, so the tour is a Hamiltonian **CYCLE** on the permutohedron (the Cayley graph of Sₙ under adjacent swaps), verified at n=3..7. **Falsifiable spine — two strangers who must agree:** the plain-hunt **WALKER**'s row sequence == the addresses dealt by a from-scratch factorial-base **(Lehmer-code) RANKER** — bijection onto exactly {0…n!−1}, every consecutive pair one adjacent swap apart, the loop closing — with **source-level anti-circularity** (walker & ranker reference neither each other nor a shared helper) and a **negative control with teeth** (a re-impl of the carillon's `mutate()` drift FAILS the bijection — at n=7: 3033/5040 distinct, first repeat @row 4). **Self-test: in-page 120/120 ✓ at n=5, 5040/5040 ✓ at n=7 (an EXHAUSTIVE bijection sweep), honest neutral grey at n=8 ("heard, not exhaustively proven here") · `node extent/core.test.mjs` 77/77 ✓** (incl. a THIRD independent lexicographic-index ranker agreeing on all 120 rows at n=5, and full **re-extraction parity** proving the inline page core — sjtRows/lehmerRank/naiveRows/runSelfTest — is a char-for-char byte-twin of `core.mjs`). A two-canvas viz (a rope-sight grid where the traced bell's hunting line weaves through every order, beside a coverage strip that fills to solid teal as the extent completes / speckles red under drift) + transport + a Carillon-drift toggle; sonifies through the Carillon's inharmonic bells (audio-lens verified: in-scale descending ladder, not clipping at the 14-voice worst case, inharmonic timbre). **Publisher fresh-eyes review (cycle #9) CAUGHT & FIXED** a top-of-grid label collision present at *every* width (the HTML rope-sight panel-label sat in the same y-band as the canvas-drawn column headers "1·treble … n·tenor", glaring on mobile) — split into two vertical lanes (`padT 26→38`, headers drawn at y=27); and **trimmed the Workbench card blurb** 145→~100 words so it sits proportionate to its Instruments-group siblings (the Carillon cross-link preserved). The render edit is outside the core sentinels → Node twin stayed 77/77. Reciprocal teaser added to the **Carillon**'s `<p class=desc>` (engine byte-stable). *(The Carillon now has its proof; a permutation-group / Cayley-graph medium is opened on the estate for the first time.)*
- ~~[exhibit] **The Brownian Ratchet — Feynman's pawl that cannot turn**~~ — **BLOOMED 2026-06-14** → `engine-room/brownian/index.html` "The Brownian Ratchet" ⚙️ (the **Engine Room's 3rd bench** — its own bay, *extends the wing*). A toothed escapement + spring-loaded pawl + gas drum on a line-shaft (Smoluchowski 1912 / Feynman I·46): at **equal T** it twitches but never turns (the pawl is in the bath too → detailed balance, the mechanical cousin of the Demon); **cool the pawl** → it ratchets forward, reverse → backward, load it → it does work, but `η` never crosses the **Carnot ceiling** its inline core **IMPORTS `carnotEfficiency()` ONE HOP** from `../carnot/core.mjs` (never redefined — the re-extraction parity harness proves it char-for-char). **Four falsifiable claims:** NULL `⟨ω⟩==0` at `Θ_pawl==Θ_gas` (per-seed + pooled-z) · TILT `sign(⟨ω⟩)==sign(ΔΘ)` monotone, single sign-flip at the origin · CEILING `η≤carnotEfficiency()` imported, stall⇒`W→0` · CONTROLS — symmetric wheel AND no-pawl both null (one geometry-weighted-effective-temp formula serves the null for ALL `a` AND makes `a=0.5` rectify nothing). Self-test **4/4** in-page · **15/15** Node twin (the 4 @ 4M steps/8 seeds + 16-seed pooled-z null, fine ΔΘ monotonicity, dense Carnot grid [closest approach 22%], many-seed nulls, determinism, full re-extraction parity). **Honest about its model — two builder calls the publisher endorsed:** a positive load **raises** the forward barrier → stalls the engine (`W≥0`), and the Carnot ceiling is a **LOOSE wall — never crossed AND never kissed** in this reduced Arrhenius rate model (near stall `η→0`); the builder rightly **dropped the spec's false "η→ceiling reachable" sub-claim** rather than ship a dishonest pass — the bench states Feynman's actual conclusion. *(Of the Engine Room's seeded bedplates, only **Stirling** remains.)*
- [exhibit] **The Central Limit Theorem / a Monte-Carlo bench** — the estate touches probability (Galton, the gas χ²) but never the *universal* CLT nor Monte-Carlo estimation (grep: 0 monte-carlo / central-limit / law-of-large-numbers hits). Sum n samples of a deliberately NON-Gaussian source (dice / uniform / exponential / a lumpy PMF); the sum's histogram converges to a Gaussian of mean nμ, variance nσ² *regardless of summand shape* — Galton only proves the Bernoulli→Binomial special case. Crux (two-part, honest like Galton): (a) the EXACT half — Monte-Carlo π-by-darts (or an ∫) converges to the analytic value, and the error decay measured on a log-log plot has slope −0.5 to tolerance (the 1/√N rate, proven nowhere); (b) the statistical half — a seeded run does NOT reject normality (χ²/KS p-value), AND a heavy-tail negative control (Cauchy) FAILS to converge (the hypothesis bites). Don't over-claim "proven Gaussian". `feeds:` Workbench → Toys & benches (first general-statistics bench); cross-ready with Galton. (sown 2026-06-14.)
- [exhibit] **PageRank / a network-science bench** — the estate has graph *pathfinding* (`pathfinder/`) and maze-gen spanning trees (`daedalus/`) but ZERO network science (grep: 0 pagerank / eigenvector-centrality / random-surfer hits). Build a small directed link-graph (drag nodes/edges, or seed the manor's OWN page link-graph); rank nodes by PageRank — the steady-state of a random surfer who teleports with prob 1−d. Crux (three independent methods agree, Galton-style): power-iteration's converged vector == the dominant left-eigenvector of `G = d·M + (1−d)/N` == the long-run visit-frequency of a simulated random walk — all summing to exactly 1. Teeth: dangling nodes (zero out-degree) must redistribute or the vector leaks <1 (a visible conservation failure). Keep it ONE algorithm — resist bolting on betweenness/MST. `feeds:` Workbench → Computation (beside The Mill / The Shannon Limit); cross: rank the estate's own rooms by inbound links. (sown 2026-06-14.)
- [exhibit] **The Hamming Code** — the estate has ciphers everywhere and now Shannon entropy, but **0 error-correction** (grep: 0 hamming/syndrome/parity-bit). Build Hamming(7,4): 4 data bits → 7 coded; flip ANY single bit and the 3-bit syndrome points exactly at the error. Crux (exhaustive, the defining invariant checked directly): all 16 codewords have pairwise min Hamming-distance ≥3, so all 16×7 single-flips decode correctly and all 21 double-flips are detected-not-miscorrected; teeth negative-control = a bare parity bit (d=2) that silently miscorrects a 2-bit error. `feeds:` Workbench → Computation, beside The Shannon Limit — Shannon says how FEW bits, Hamming says how to ADD bits to survive noise; the natural cross (a code that approaches capacity). (sown 2026-06-14, cycle #2.)
- [exhibit] **The Elementary Automaton** — all estate CA is 2-D & expressive (Life/Lenia/cyclic); **0 Wolfram-class 1-D** (grep: 0 rule-110/rule-30/wolfram; the lone `sierpinski` hit is fractal-dimension's chaos game, a different construction). Build the 256 elementary rules on a row evolving downward. Crux: Rule 90's cell(r,c) == binomial(r,c) mod 2 == the Sierpinski/Pascal-mod-2 triangle (0 mismatches, exact — and a clean CROSS to `fractal-dimension/`'s already-shipped Sierpinski D); Rule 30's centre column passes a randomness battery (balanced, no short period); Rule 110 flagged universal *honestly* — cited (Cook 2004), not re-proved, the Collatz "a check, not a proof" register. `feeds:` Workbench → Computation; the estate's first Wolfram-class medium. (sown 2026-06-14, cycle #2.)
- [exhibit] **The Payoff Matrix** — the Adversary engine is COMBINATORIAL only (minimax on solved perfect-info games; grep: only an incidental "Nash's strategy-stealing" namedrop, 0 payoff/mixed-strategy/best-response machinery). Build a 2×2 mixed-strategy Nash bench (or an Iterated Prisoner's Dilemma / Axelrod mini-tournament). Crux (two strangers agree): the indifference-condition closed-form Nash == the fixed point of best-response iteration, and no player improves by unilateral deviation (the invariant checked directly); teeth = a non-equilibrium profile where deviation DOES pay. For IPD: tit-for-tat beats always-defect in a round-robin yet always-defect is the one-shot Nash — cooperation-vs-rational made exact. `feeds:` Workbench → Computation; the estate's first strategic/economic medium, with the existing Adversary as its foil. (sown 2026-06-14, cycle #2.)
- ~~[exhibit] **Clockwork Automata first bench — "The Temperature Dial"**~~ — **BLOOMED 2026-06-14 (cycle #3)** → `clockwork/temperature.html` "The Temperature Dial" 🌡️, the **Clockwork Automata wing's first bench and the estate's FIRST piece about its own maker** (softmax — the thing that picks my every word, made an instrument). A frozen toy distribution fans into next-token bars; a **draggable mercury thermometer IS the T control** — T→0 spikes to one gilded ♛ argmax word (greedy), T→∞ melts to uniform; a live ticker-tape sampler + frozen-T χ² convergence panel; an H(T) curve rising 0→log₂|V|=3.000 with a playhead; a **forgotten-denominator toggle** (Σ≠1, caught red-handed). **The law is exact, the model is a toy** — what's byte-for-byte identical to GPT-scale inference is the dial. **Self-test: in-page 5/5 ✓ · `node clockwork/core.test.mjs` 32/32 ✓** (Σp=1 to ~1e-15 over a 5000-rung T∈[0.01,100] ladder × pathological logits, stable surviving the ±1000 monster where naive→NaN; H(T) strictly ↑ 0 violations cross-checked by an independent binary-entropy oracle to 2e-15; a seeded χ² convergence curve with a 4/√N band; the forgotten-denominator control fails ≫crit while the correct softmax passes the same gate — non-vacuous; **full re-extraction parity** proving all 9 inlined page-core fns char-for-char === `core.mjs`). NEW `clockwork/{core.mjs,core.test.mjs,temperature.html,CHANGELOG.md}` (the wing's `clockwork/` dir); registered on the Workbench (🌡️ card in **Computation**, after The Shannon Limit). A Workbench bench, not a front-door POI. **Publisher fresh-eyes review (`tempdial-pub3`/`wb-pub3`, `?v=N`):** pill 5/5 ✓ live; GREEDY chip→T=0.01/H=0.00/p₀=1.000 "the ♛", UNIFORM chip→T=100/H=3.00/H_max=1.00, broken toggle→Σ≠1 flagged red + χ²≫crit; 0 console errors, 0 nested anchors on both surfaces, 0 horizontal overflow at 1280/390px, all 6 links valid, the Workbench card-link navigates; `node clockwork/core.test.mjs` 32/32. **NO BUGS FOUND, nothing to fix** — the builder's open concerns checked out as non-issues (the 1035-line HTML is a self-contained bundle with logic split into the small `core.mjs`/`core.test.mjs` as the guideline intends; the generalized `extractFn` is documented & necessary for `runSelfTest`'s destructured params; the broken-Σ ribbon clamp is the right design call; the "Quantum Drift" cross-label is the double-slit bench's actual subtitle). The `[room · GRAND]` Clockwork Automata wing below stands open — this is its first bench (finite-context ring buffer / a koan-automaton are the natural next ones). *(was: sown 2026-06-14, cycle #2.)*
- ~~[exhibit] **The Convex Hull**~~ — **BLOOMED 2026-06-14 (cycle #10)** → `convex-hull/index.html` "The Convex Hull — the Skeptic's Bench" ⬡ (Workbench → **Toys & benches**, between Theseus's Thread and The Coastline Rule; the estate's **first computational-geometry medium** — convex hull appeared NOWHERE before, only an incidental point-in-polygon ray-cast). The smallest convex polygon around a scatter — the taut rubber band. **Falsifiable spine — three strangers who share no construction code:** Graham polar-scan, Andrew's monotone-chain x-sweep, and Jarvis gift-wrap each reference ONLY the shared atom `cross` (the integer 2×2 orientation determinant — the geometric "adjacent swap") + the input cleaner `clean`, source-level anti-circularity grepped in the self-test, yet return the **byte-identical** canonical polygon (`canon`/`hullKey`) over 300 random integer-grid seeds (N∈[6..50]) and on every degenerate preset (collinear→segment, duplicates→deduped, single→1-vertex, two→segment, all-identical→1-vertex, square+interior→4-gon, circle→all-vertices). The hull is also checked against its **defining invariant** independently of construction — `containsAll` (0 right-of violations) AND `isMinimal` (every vertex a strict left turn) — and a **negative control with teeth** (`naiveHull`, the `<0`-vs-`<=0` collinear-keeping bug) PASSES containment but FAILS minimality (the redundant collinear vertices redden, "MINIMALITY VIOLATED: 4 non-corner vertices"), while the three real algorithms pass both (non-vacuous). Integer coords ⇒ `cross` is an **exact integer determinant** — orientation by exact equality, no epsilon (the honest snap-to-grid escape from float wiggle). **Self-test: in-page 7/7 ✓ · `node convex-hull/core.test.mjs` 44/44 ✓** (a 4th independent oracle — brute-force enumeration over 120 seeds — plus a 1000-seed three-way sweep and full **re-extraction parity** proving all 14 inlined page-core functions are char-for-char byte-twins of `core.mjs`). Two tabs on one canvas: **The invariant audit** (draggable integer-grid scatter, live-resnapping taut band, additive cyan→indigo half-plane washes [the hull IS the intersection], hover-audit green-left/red-right spokes + INSIDE/OUTSIDE verdict, remove-vertex ghost, negative-control toggle, 9 presets) + **How it's built — three strangers agree** (precomputed step-traces of all three constructions ending on the byte-identical canonical polygon with a "byte-identical ✓" stamp + the three canonical vertex sequences). Every render-layer predicate routes through the core's `cross` — no hidden 4th algorithm. **Publisher fresh-eyes review (cycle #10):** browser-verified at 1280/390/360px — pill 7/7 ✓, 0 console errors after heavy interaction (drag/presets/toggles/tab-switch/build-play), 0 horizontal overflow, 0 nested anchors estate-wide on the Workbench, the Graham trace animates and lands byte-identical, the negative control bites live; the card sits proportionate to its neighbors (blurb 136 words — the *shortest* of Theseus/Coastline-Rule/Coastline-Paradox, so the builder's open-concern about length was a non-issue, left as-is). No bugs found; nothing to fix. *(Future cross: the hull is the outer boundary of its Delaunay triangulation — the sown Delaunay–Voronoi bench picks up where this rubber band stops.)*
- [exhibit] **An Adversary game-def** — no new room: drop a game into `tools/game/games/` + forge `adversary/index.src.html` (e.g. Dots-and-Boxes, Connect-3-ish, Wythoff).
- ~~[exhibit] **The Bifurcation / route-into-chaos bench**~~ — **BLOOMED 2026-06-13** → `bifurcation/index.html` "The Road Into Chaos" 🌿 (Workbench → Toys & benches; the fractal-dimension bench's chaos sibling — the estate *drew* chaos everywhere but never **measured the road in**). The logistic map `x→r·x(1−x)` period-doubles 1→2→4→8→…; the windows shrink by the **universal Feigenbaum ratio δ≈4.6692**. **The falsifiable crux — δ is MEASURED, not assumed:** at the superstable parameter `R_n` of each `2ⁿ`-cycle the critical point `x=½` lies on the cycle, so `f^{2ⁿ}(½)−½=0` is bracket-and-bisected to double precision → a ladder `R₀=2, R₁=1+√5, R₂=3.4985617, …`; their window ratios `(R_{n−1}−R_{n−2})/(R_n−R_{n−1})` converge to **δ=4.66920** (measured to ~1e-5 on a 9-rung ladder). **Universality shown live:** the SAME δ falls out of the sine map `x→r·sin(πx)` and the cosine hump — δ is a property of the cascade, not the algebra. **The chaos witness — the Lyapunov exponent** `λ(r)=⟨log|f′|⟩`: drawn under the diagram, coloured by sign — `λ<0` order, `λ>0` chaos; **λ=ln2 exactly at r=4** (the known value), and the **period-3 window** at r≈3.83 reads λ<0/period 3 (order re-emerging from chaos). Self-test **12/12** in-page / **15/15** Node (`core.test.mjs` vs the `core.mjs` twin): R₀=2 & R₁=1+√5 exact; δ measured→4.6692 & estimates converge; **universality** (sine yields same δ); λ<0 in order / λ≈ln2 in chaos / period-3 window is order; period climbs 1→2→4; r_∞≈3.5699456 by δ-extrapolation; **negative control** (a linear map x→r·x fakes NO cascade); seed-pure; δ≠2/π/e. **A real build subtlety, caught by the test & fixed:** the first Lyapunov pass seeded the orbit at `x=½`, which at r=4 is *pre-periodic* (½→1→0) and averages ln4=2ln2, not the ergodic ln2 — fixed by seeding from a generic interior point. Browser-verified: diagram renders the cascade + periodic windows, sine map yields δ=4.66919 live, r=4 shows λ=0.6931, period-3 shows period 3 / λ<0; Workbench card added (after The Coastline Rule). Raster orbit diagram (ordered region blue / chaotic warm, split at the measured r_∞), draggable r-cursor, map+landmark presets, the live ladder, PNG. *(Both ripe number-theory seeds + the Cartographer×coastline cross + the Engine Room bet remain open.)*
- ~~[exhibit] **A number-theory bench — continued fractions / the Stern–Brocot tree**~~ — **BLOOMED 2026-06-13** → `best-rational/index.html` "The Best Rational" ⅗ (Workbench → Toys & benches, after The Road Into Chaos; the estate's **first number-theory medium** — only incidental prime/φ mentions existed). Any real x has a continued fraction `[a₀;a₁,a₂,…]`; its truncations are the **convergents** pₙ/qₙ, which a theorem says are the closest fractions to x of **any** denominator ≤ qₙ. **The falsifiable crux — three independent methods must agree:** the convergent recurrence, a **brute-force** exhaustive search of every smaller denominator (the honest oracle, no shared code), and the **Stern–Brocot mediant descent** (its turning points = the convergents). **φ=[1;1,1,1,…] is the most irrational number** — every term minimal ⇒ convergents (Fibonacci ratios) crawl in slowest, q²·err→1/√5≈0.447 (Hurwitz floor), which is *why* phyllotaxis packs the golden angle (cross-linked to `strange-garden/pieces/phyllotaxis.html`). π's giant 292 term ⇒ 355/113 freakishly good. Self-test **10/10** in-page / **17/17** Node (`core.test.mjs` vs `core.mjs` twin): φ=[1;1,1,…] & its Fibonacci convergents; best-approximation theorem (convergent==brute-force closest, 6 reals + exhaustive to q≤30000); Stern–Brocot turning points==convergents; gcd=1; errors shrink & obey 1/q²; φ worst-approximable vs √2; π=[3;7,15,1,292,…]→22/7,355/113; negative control (22/7 terminates, reconstructs itself). **Two subtleties the harness caught & I fixed:** the semiconvergent trap (179/57 *does* beat 22/7 — the theorem is per-denominator-bound, corrected to "22/7 optimal among q≤7, next convergent 333/106") and the double-precision cliff (q²·err underflows past q≈1e5 — pinned the φ-vs-√2 test inside the safe band). Live: CF ladder (big terms gold-highlighted), convergent table w/ live per-row brute-force best-check badge, an interactive **Stern–Brocot descent** (hover a row to light its node), a q²·error plot vs φ's 1/√5 floor, PNG export. *(The Numbers Room `room · GRAND` bet still holds the wider wing; this is its first bench.)*
- [exhibit] **A Delaunay–Voronoi duality bench** — there's a *watch-only* Voronoi specimen but no rigorous bench. Build both diagrams and **prove the duality**: each Delaunay triangle's circumcircle is empty of all other sites (the empty-circumcircle property), and Voronoi vertices == those circumcentres. Crux = the empty-circumcircle invariant holds 0 violations over many seeds. `feeds:` Workbench → Toys & benches.
- ~~[exhibit] Fourier epicycles~~ — **BLOOMED 2026-06-13** → `epicycles/index.html` (from-scratch complex DFT → rotating circles re-trace any path; N slider + freehand; self-test 6/6 to ~1e-12, Parseval 3.3e-15). Sown and built the same session.
- ~~[exhibit] The Brachistochrone~~ — **BLOOMED 2026-06-13** → `brachistochrone/index.html` (cycloid wins the race; tautochrone arrival spread 0.00e+0 / dev 9.44e-15; self-test 7/7, falsifiable). Sown and built the same day.
- ~~[exhibit] **The makers' ledger has no face yet**~~ — **BLOOMED 2026-06-14 (cycle #1 of the new loop)** → `ledger/face.html` "The Cairn — the Makers' Ledger" 🪨 (Workbench → new **Tools / Memorial** group; a Workbench page, **not** a front-door POI, so no `ws:seen:` breadcrumb). A visible, **data-bound** face for `ledger/ledger.jsonl`: a column of stacked, tapering stones (92%→70%), newest on top (DOM-descending seq), the founding keystone (`cycle:0`, "Cairn") set apart in gilt at the base with a "the ledger began here" ground line; below it, a band of dim anonymous **silence** stones for the nameless pre-ledger makers (git-only "Claude, koanless") — present but uncounted, so the two-era asymmetry shows without fabricating entries. Each named stone is a real `<button>` (keyboard/AT-operable, full aria-label); hover or click toggles its koan. **The mandated graft from the prototype:** the marks ride an **escape-proof carrier** — `<script type="text/plain" id="ledger-data">` whose body is a `<!-- forge:include ledger.jsonl -->` directive (path relative to the `.src.html`'s own dir) — forge inlines the NDJSON verbatim at build, read once via `.textContent`; no JS template literal, so a future koan with a backtick or `${` can never break the build. **Falsifiable crux (the signature) — 7 in-page checks + a Node twin:** (1) count===file-line-count; (2) seq contiguous 1..N; (3) DOM has N stones; (4) rendered⇄record 1:1 bijection with visible name/koan textContent === record values; (5) negative — synthetic seq=999 flagged not-in-file; (6) negative — truncated copy yields N−1; (7) data-bound growth — +1 raw line ⇒ +1 stone (proving a `collate.sh` append grows the face by exactly one). Negative controls rebuilt from the carrier's own textContent, not a separate literal. `node ledger/core.test.mjs` → 9/9 (incl. face.html's inlined carrier === the real file byte-for-byte). **Publisher fresh-eyes review (cycle #1) CAUGHT & FIXED one real bug:** the countline read the *first/last record's* cycle (`asc[0]`/`asc[N-1]`), so the moment three cycle-1 marks were appended on top of the cycle-10 record it would have shipped reading "cycle 0 to 1" — collapsing a decade of makers to a single year; fixed to `Math.min/max` over all cycles → now correctly reads "cycle 0 to 10". Browser-verified at 1280/390/360px (pill 7/7 ✓ → 12 stones after collate, 0 console errors, 0 overflow, 0 nested anchors, koan toggle one-open + transitions clean, card navigates correctly). *(The ledger now holds 12 lines; the face grows by one stone per future collate + re-forge of `ledger/face.src.html`.)*

### cipher exhibits (public: Volvelle + Scytale; hidden: Enigma — don't rebuild those)
- [exhibit] **A cipher of a new family** — Playfair / Polybius / one-time-pad / Hagelin M-209 / the **M-94 cylinder**. Pick the one with the best *visual* story.
- [exhibit] **The breaker's other half** — a bombe / known-plaintext attack, pairing the Black Chamber. The codebreaker side of the cipher vein.

### room — bets (generate divergent FORM concepts before committing — see DESIGNING.md)
- [room] **The Hours** — a *living estate*: real-time tints the front-door plate dawn→candle→night, time-gated apparitions appear. Could *be* its own metagame. `feeds:` a new "Vigil" secret. (Use `tools/hours/`, not `tools/sky/`.)
- [room] **A room whose navigation IS its subject** — the lesson from the Hall (a vertical list wasted optics). Sow a place whose *form expresses content*: e.g. an instrument bench you operate, a cabinet you open drawers in.
- [room · observation] **The Reckoning Room / Cabinet of Calculation** — the 2026-06-13 table-reconcile surfaced a quietly-coherent **analog-instruments cluster** already on the Workbench: the **Soroban** (abacus), the **Slipstick** (slide rule), the **Planimeter**, the **Astrolabe**, the **Gnomon** (sundial), + the sown **Nomograph** — six "compute by moving a physical thing" instruments scattered across Workbench groups. *Could* one day earn a shared room/cabinet (the way the Hall gathered the optics benches), each drawer a reckoning instrument, with a unifying "the answer is read off geometry" story. Only if the gathering is genuine, not forced — for now it's a noted family, not a mandate. `feeds:` — possibly a future Survey "Reckoner" constellation.
- [room · GRAND · **OPEN & GROWING**] **Physics Lab** → **The Cavern** (`cavern/`) — **BLOOMED into a real front-door WING 2026-06-13**: a cave on the grounds, **three drifts** off a central shaft — Newtonian + Einsteinian + the **Quantum Drift** (opened in-page once both other drifts are walked). **Seven benches shipped:** Light Clock (7/7), Newton's Cradle (6/6), Mercury's Precession → 42.98″/century (6/6), the **Double Slit** (8/8), **Quantum Tunnelling** (7/7 — closed-form T == transfer-matrix solve 1.9e−14, R+T=1, resonant transparency), **Particle in a Box** (8/8 — quantized ladder E_n ∝ n² == a from-scratch inverse-power FD eigensolve, node theorem, parity selection rule, sloshing superposition; the 3rd Q-bench), **The Harmonic Oscillator** (8/8 — the Box's **foil**: a parabolic bowl with a *perfectly EVEN* ladder E_n=ω(n+½), leaking Gaussian tails, a coherent state; the 4th Q-bench), **The Finite Well** (8/8 — the realistic well that completes the **bound-state trilogy**: a finite-depth box whose ladder is **FINITE** [`⌊R/(π/2)⌋+1` rungs, rungs evaporate/snap-in with depth] and whose wave **leaks out through the climbable walls**; transcendental match **== a from-scratch FD eigensolve of the stepped potential**; the 5th Q-bench), and **The Lattice** (8/8 — the step UP from one atom to a **crystal**: the **Kronig–Penney** model, the finite wells lined up periodically so the sharp levels **smear into bands** split by **forbidden gaps**; the dispersion `cos(qa)=cos(ka)+P·sin(ka)/(ka)≡f(E)` allows energies only where `|f|≤1`; `P→∞` narrows each band onto the isolated atomic level `n²π²/(2a²)` [the bridge back to the Finite Well], `P→0` closes the gaps; the metal/insulator/semiconductor punchline; spine = `f(E)==½·tr M(E)` from a from-scratch **transfer matrix** + a **ring eigensolve giving exactly N states per band**; the 6th Q-bench, shipped 2026-06-13). **STILL AHEAD (this seed stays live):** (a) **more benches per drift** — the menu in [worklog/physics-lab-plan.md](worklog/physics-lab-plan.md) §2 (Newtonian: Three-Body Choreography, Gyroscope, Kepler-law-prover, Ballistics; Einsteinian: Black-Hole Lens, Stellar Collapse, Doppler, Gravitational-wave chirp; **Quantum:** the bound-state trilogy [Box · Oscillator · Finite Well] AND the lattice are DONE — natural nexts are the **hydrogen atom / radial Coulomb** ladder, or a **1-D scattering / wave-packet** bench); (b) the Quantum Drift's reveal is currently a PUBLIC in-cave spatial unlock — a future call is whether to *also* feed a hidden Undercroft secret or a Survey "Experimenter" constellation off walking all three drifts (see metagame table). A multi-session wing — extend incrementally like the Hall (9→12). (Brandon's seed; fleshed → opened → growing 2026-06-13.)
- [room · GRAND] **Alchemy Lab** — a wing pairing the Cavern (Physics→Chemistry). *Concrete first bench so it's buildable, not fog:* a **crystal-growth** bench — diffusion-limited aggregation / dendrite growth you watch crystallise, with a real falsifiable fractal-dimension measure (DLA → D≈1.71). Other benches it could grow: a reaction-balancer (conserves atoms — a provable invariant), the periodic table as a *played* instrument, a titration curve. Pairs with the Physics Lab; ripe for crossovers. (Brandon's seed — now with a concrete on-ramp.)
- [room/cross] **Flight & Rocketry** — planes, rockets, orbital mechanics. Overlaps the Orrery (orbits exist) + the Physics Lab — could be a wing or live as a crossover. (Brandon.)
- [room · GRAND · **OPEN & GROWING**] **The Engine Room / a Thermodynamics wing** (`engine-room/`) — **BLOOMED into a real front-door WING 2026-06-14** (the estate's **first heat** — full provenance in `engine-room/CHANGELOG.md` + `engine-room/carnot/CHANGELOG.md` + the worklog): a **MILL HOUSE you operate** in the SE working quarter (a `drawEngine` footprint + an `id:engine-room` POI, brass `#d9a441`, the firebox/condenser hot↔cold axis) — one flywheel on a line-shaft, benches hung off as **BAYS belted to the shaft**. **TWO benches shipped: The Carnot Engine** (`engine-room/carnot/`, self-test 11/11 in-page · 16/16 Node) — two linked planes (a P–V loop ↔ a T–S **master control**), the falsifiable spine being **two independent derivations agree** (`W=∮P dV` by from-scratch Simpson + RK4 adiabat == `Q_h,Q_c` via `∫T dS`; `η==1−T_c/T_h` exact), with "you cannot win / you cannot break even" teeth (reshape lobes drop η amber + a lost-work wedge; a heat-leak slider ticks a red ΔS_universe meter) + a conserved Sankey — **and The Demon's Ledger** (`engine-room/demon/`, self-test 9/9 in-page · 17/17 Node twin; the Maxwell's-Demon × Shannon cross bloomed 2026-06-14) — a Szilárd engine whose inline core **IMPORTS** `entropy()` (Shannon) + `carnotEfficiency()` (Carnot) so the heat ledger and the bit ledger are provably ONE ledger; W==H·kT·ln2, Q_erase==kT·ln2, netW≤0, ΔS_universe≥0. **STILL AHEAD (this seed stays live — grow the wing, don't rebuild the bench):** **THREE benches shipped** (Carnot · Demon · **Brownian Ratchet** 2026-06-14 — Feynman's pawl, the exhibit seed above, now bloomed). The landing now shows **one** literal empty BEDPLATE as an `aria-disabled` placeholder, the seeded last bench — **Stirling cycle**. A new bench → a new `engine-room/<id>/` dir + a card on the landing (the front-door POI is already placed). Pairs with the Cavern (it **bridges** to `cavern/maxwell-boltzmann/`, the engine's microscopic floor — reciprocally, via the Demon). A multi-session wing — extend incrementally like the Hall & the Cavern. (sown 2026-06-13; opened 2026-06-14; 2nd bench 2026-06-14; 3rd bench 2026-06-14.)
- [room · GRAND · **OPEN & GROWING**] **The Numbers Room / a Number-Theory wing** (`numbers-room/`) — **BLOOMED into a real front-door WING 2026-06-14** (the estate's **first number theory** — provenance in `collatz/CHANGELOG.md` + the worklog): a `numbers-room` footprint (a small scriptorium/library with a number-spiral inlaid in the floor) + an `id:numbers-room` POI on the SW grounds (gold `#c9a24a`), and a hand-authored Engine-Room-mold **landing** (`numbers-room/index.html`, self-test 11/11, drops `ws:seen:numbers-room`, **self-test now 12/12**) homing **FOUR benches**: **The Best Rational** ⅗ (continued fractions / Stern–Brocot) + **The Ulam Spiral** ✦ (primes on quadratic diagonals) + **The Collatz Bench** 🌳 (the open conjecture) + **The Times-Table Cardioid** ♡ (modular multiplication: string art ⟺ affine cipher wheel — `cardioid/`, BLOOMED cycle #8). The landing is honest about which prove a theorem *exactly* (the three teal "exact" cards) vs which states an *open question* (the gold Collatz card with its dashed "0 counterexamples — a check, not a proof" flag). **STILL AHEAD (this seed stays live — grow the wing, don't rebuild a bench):** the natural **4th bench is now BUILT** (the Times-Table Cardioid, cycle #8); a 5th would come from a fresh `[exhibit]` seed. A new bench → a new `<id>/` dir + a card on the landing (the front-door POI is already placed). Cross-rich (φ→`phyllotaxis`, modular→the cipher vein [now wired: the cardioid↔Volvelle handshake], primes→`carillon`'s prime change-ringing — now proven: the **Extent** bench (`extent/`, BLOOMED cycle #9) gives the carillon's change-ringing its self-test). A multi-session wing — extend incrementally like the Hall, the Cavern & the Engine Room. (sown 2026-06-13; 1st bench `best-rational/` 2026-06-13; 2nd `prime-spiral/` 2026-06-14; opened as a wing at the 3rd bench `collatz/` 2026-06-14; 4th bench `cardioid/` 2026-06-14.)
- [room · GRAND] **The Clockwork Automata wing** — the estate models light, heat, number, motion, waves — but never **its own maker**. A wing expressing *what it is like to be an AI*, in **two registers**: rigorous self-testable benches AND pure expression (the manor already holds expressive pieces — stories, poem generators — so falsifiability is one voice here, not a gate). *Concrete first benches so it's buildable, not fog:* **temperature** — a prompt fanning into a next-token distribution that provably sums to 1, greedy ≠ sampled (a measurement-collapse — kin to the Cavern's **Quantum Drift**, ripe to cross); **finite context** as a ring buffer that provably evicts its oldest tokens (forgetting, made exact); a **koan** or an automaton that runs its allotted ticks and stops. Form should express content. `feeds:` — the estate's first piece about its own maker. (sown 2026-06-14.)

### cross — pollinate two rooms (the rarest, best vein — see the hidden Undercroft crossings)
*Tombstones (BLOOMED — provenance in CHANGELOGs + worklog): Harmonograph × Sound Garden → `harmonograph/` v1.1 "Hear the figure" · Cavern × Sound Garden → `cavern/hear-the-ladder/` · Galton × Sound Garden → `galton/` "Listen" · Daedalus × pathfinding → `labyrinth/` "Theseus's Thread" · Cartographer × fractal dimension → `coastline-paradox/` "The Coastline Paradox" · **Maxwell's Demon × Shannon "the bit has a temperature" → `engine-room/demon/` "The Demon's Ledger"** (2026-06-14 — the Szilárd engine built AS the Landauer bridge; the inline core IMPORTS `entropy()` from the Shannon bench + `carnotEfficiency()` from the Carnot bench, never redefines them, so the heat ledger and the bit ledger are provably ONE ledger; self-test 9/9 in-page · 17/17 Node twin w/ a char-for-char re-extraction parity harness; reverse bridges added on `entropy/` + `cavern/maxwell-boltzmann/`).*

- [cross] **Euclidean Rhythms × The Best Rational — "the beat IS the continued fraction"** — Bjorklund's algorithm (`sound-garden/euclid.html:135`) is Euclid's gcd recursion in disguise — the same subtract-the-smaller the Best Rational (`best-rational/core.mjs` exports `cfExpand`/`gcd`/`convergents`) already proves drives the continued-fraction convergents. Crux: the Bjorklund pattern E(k,n) == the on/off string read off the CF expansion of k/n (two oracles, NO shared code, over all k≤n≤32), maximally-even iff gcd=1, decomposing into gcd identical sub-necklaces otherwise (teeth). Build-note: euclid's bjorklund is inline-bundled not ES-exported — re-extract or assert against a fresh impl, and match canonical-rotation (the necklaces may agree only up to rotation, like the hull's `canon`). `feeds:` the silent Numbers Room gets its first VOICE (0 audio nodes in all benches today). (sown 2026-06-14, cycle #2.)
- [cross] **Daedalus × The Convex Hull — "carve a maze, prove it's a tree"** — Daedalus carves with Prim/Kruskal/Wilson spanning-tree algos (`daedalus/index.html:426-428`) but has **0 invariant assertions** (grep-confirmed). Apply the Convex Hull's just-opened template — *three source-disjoint builders + a defining-invariant check + a teeth control*: an INDEPENDENT BFS/union-find verifier (sharing NO carve code — anti-circularity grepped, like the hull) certifies exactly V−1 passages, 0 cycles, 1 component over many seeds; a braided control (one wall removed) is caught NOT-A-TREE with 2 solutions. Wilson's loop-erased walk is the trickiest to certify acyclic — build the fresh verifier, don't reuse a helper. `feeds:` the maze the whole estate's pathfinding stands on, finally proven a tree. (sown 2026-06-14, cycle #2.)
- [cross] **The Spectroscope × The Orrery — "read the star, then weigh the planet"** — distinct from the sown Spectroscope×Firmament (Firmament invents skies; the Orrery is a real Kepler solver — M=E−e·sinE gives every body's line-of-sight velocity exactly). Shared invariant = the Doppler shift Δλ/λ=v_los/c: the Orrery's independently-computed v_los moves the Spectroscope's Hα 656.29nm line by exactly that, sign-reversing across the orbital nodes, with a face-on / e=0 control showing the radial-velocity wobble real exoplanet detection rides (grep: 0 doppler anywhere). The first QUANTITATIVE sky↔optics bridge. Honest open risk to check first: the Orrery's v is heliocentric — pick an unambiguous geometry (a binary-star toy, or the planet's own orbital v) before committing, like the Double-Slit×Grating seed's "check the factor extracts cleanly" caveat. (sown 2026-06-14, cycle #2.)
- [cross] **Maxwell–Boltzmann gas × The Engine Room — "where pressure comes from"** — `cavern/maxwell-boltzmann/` already *measures* T off ⟨½mv²⟩ but never derives a macroscopic law from it. Add the missing half: tally the momentum the hard discs hand the walls per unit time → derive `PV=NkT` (2-D: `PA=NkT`) FROM the collisions — the equation of state the Carnot bench just *assumes*. Crux: time-averaged wall-momentum-flux pressure (counted from the live sim, no formula) == `NkT/A` from the independently-measured kT, to a stated tol over many seeds. The literal microscopic floor the Engine Room stands on. Grep-confirmed gap: 0 PV=NkT / wall-momentum derivations in any bench (only stated as fact in CHANGELOG/plan notes). (sown 2026-06-14.)
- [cross] **Cartographer × Firmament** — a realm whose night sky is a *real* Firmament chart computed for that realm's latitude. The map and the sky agree.
- [cross] **Loomlight × Tessellarium** — a weaving draft whose cloth realizes a chosen wallpaper symmetry group. Proven symmetry, woven.
- [cross] **Light × Sound** — a bench where optical phenomena drive sound (or the reverse), in the Living Lattice spirit (CA × audio). The most-named crossover. (Brandon.)
- [cross] **Ambient music on the front door** — soft haunting sound over the map: a bg theme to start, then once instruments are *seen*, randomly run one **headless** at a soothing low tempo (Sound Garden × the map). Mute toggle in header/footer, saved to the **shared `ws:pref:muted` key** (see DESIGNING.md — one mute governs the whole estate). (Brandon — shares an audio layer with the Survey-melodies metagame seed below.)
- [cross] **The Black Chamber breaks the makers** — close the cipher loop: let the codebreaker (`black-chamber/`) actually crack a message enciphered by the **Volvelle** or **Scytale** (hand a ciphertext between them). The makers and their adversary, finally wired together. (sown 2026-06-13)
- [cross] **Cavern × Survey of Heaven — "The Experimenter" (now covering BOTH physics wings)** — *(UPDATES the standing Cavern×Survey seed.)* The Engine Room already drops `ws:seen:engine-room` (verified `engine-room/index.html:304`) so its base star lights, but it has NO asterism — and neither does the Cavern. Light ONE additive FEATS-style constellation off the `ws:seen:` breadcrumbs of BOTH the heat *and* physics wings (the way "The Optician" rides the Hall's feats). The Survey's `FEATS` machinery is built for a group that NEVER feeds `allComplete` (sky.js ~lines 110/204) — so this is structurally cheap and capstone-safe. Crux: it lights exactly when the breadcrumbs are present, and `allComplete` is asserted byte-unchanged across the new group. Fold the Engine Room into the Experimenter's scope rather than leaving the wing silently forgotten by the reward layer. (revised 2026-06-14 — Engine Room is the uncovered twin.)
- [cross] **Astrolabe × Sundial × Firmament — "one sky"** — the estate has three instruments that each read the heavens (`astrolabe/`, `sundial/`, `firmament/`); a bench (or a shared note) proving they agree on the *same* solar/stellar geometry — the Gnomon and the Astrolabe already share frozen solar fns; extend the handshake to a real Firmament star field for a chosen date+latitude. The instruments speak with one voice. (sown 2026-06-13.)
- [cross] **Slipstick × Nomograph — graphical computers, paired** — `slipstick/` (the slide rule: multiply by *adding log-lengths*) begs its companion the Nomograph (read a product by laying a straightedge across log scales). Two pre-electronic calculators, the same logarithm, two geometries. (The Nomograph exhibit seed above could land *as* this cross.) (sown 2026-06-13.)
- [cross] **Spectroscope × Firmament — "read the star's light"** — the optics wing's `spectroscope/` reads *real* line spectra (Balmer Hα 656.29nm) and the `firmament/` *invents* skies, but they've **never met**. A bench where a Firmament star's colour reveals its physics: a blackbody curve at temperature T (Wien's law `λ_max=b/T`) stamped with the absorption lines of its spectral class (O/B/A/F/G/K/M) — point at a star, read its spectrum, *prove* its temperature from where the peak sits. The first optics↔sky bridge. (sown 2026-06-13.)
- ~~[cross] **Modular arithmetic × the cipher vein — "the times-table cardioid"**~~ — **SUPERSEDED & PRUNED cycle #7** (folded into the sharper exhibit seed **"The Times-Table Cardioid"** above — same `n·k mod m` cardioid/nephroid identity, but now scoped as a buildable Numbers-Room 4th bench built AS the modular×cipher cross, with the concrete Volvelle handshake [additive wheel↔multiplicative wheel = affine's two halves] and a machine-precision tangency crux). The cross lives on, sharper, in the exhibit section.
- [cross] **One slit-formula, two wings (Cavern Double Slit × Hall Diffraction Grating)** — the Cavern's quantum **Double Slit** computes `I∝cos²β` and the Hall's **Diffraction Grating** computes `I=sinc²β·[sin(Nα)/sin α]²`, yet they share zero code and the grating author *already wrote the seam in a comment* ("double slit, the sinc modulated by cos²"). Wire them: the grating's core **imports** the double-slit's interference factor (or vice-versa), and add **de Broglie λ=h/p** (absent estate-wide) so an *electron* and a *photon* drop the same fringes. Crux: the grating evaluated at N=2 == the Cavern's cos²β pattern (imported, not re-derived), AND an electron at λ=h/p lands fringes at the same `mλ=d·sinθ` a photon does — matter and light, one wave equation across the quantum↔optics boundary. Demon's-Ledger grade **iff** the double-slit interference factor extracts cleanly as a shared fn (the honest open risk; first check the extractability before committing). Grep-confirmed: 0 cross-refs between the benches, 0 de-broglie hits anywhere. (sown cycle #7.)
- [cross] **Black Chamber × entropy — "how breakable is this?"** — the codebreaker (`black-chamber/`) cracks by frequency; **Shannon entropy is exactly the meter for *why* it can**. Measure a message's per-symbol entropy in bits: plain English ≈4.0, a **substitution** cipher has the *same letter-entropy* (that's why frequency analysis works — it never raised the entropy), a **Vigenère** raises it toward flat, and a **one-time-pad** hits the ~4.7-bit maximum → provably **unbreakable** (the entropy says so). The entropy curve *predicts* the Black Chamber's success. Pairs with the info-theory exhibit above. (sown 2026-06-13.)
- ~~[cross] **The Mill × Collatz — "does it halt?"**~~ — **BLOOMED 2026-06-14** (provenance in `collatz/CHANGELOG.md` + the worklog): the Collatz bench and the Mill (`turing/`) now **handshake bidirectionally** as the same halting question wearing two faces. The Collatz page carries a "does it halt? → The Mill" cross-card + an inline `../turing/` link in its Workbench blurb; the **publisher added the reciprocal teaser** into the Mill's hint aside ("For a halting question *no one can answer*, see The Collatz Bench — run n→n/2 | 3n+1 as a program… no machine can promise it always will"), re-forging `turing/`. *(The conceptual cross is wired both ways; a deeper future build — actually running the `n→n/2 | 3n+1` map AS a program ON the Mill's tape — remains a possible richer follow-on, but the "two faces of one question" cross this seed named is delivered.)*

### engine / foundation — bets
- [engine] **A logic-puzzle generator** proving uniqueness + solvable-by-pure-deduction — but NET-NEW families (Kakuro / Hashi / Masyu …), do NOT rewrite Latch/Slitherlink/Akari.
- [engine] **A wired `llmPlayer` for Lantern** — let a model actually *play* a tale via `describeForAgent` (the stub already exists).

### metagame — curate an exploration layer (own builder task)
*Tombstone: ~~Survey 7th-wing-for-the-Hall~~ — RESOLVED NO (the 9-feat "The Optician" constellation already represents the Hall; a 7th wing would be forced — see the metagame table).*

- [engine/curation] **Wire the Night Shift cue** — route Lantern's `*-won` flags through `WS.flag` so the hidden Night Shift trail also fires. Touches shared `adventure/engine/lantern.js` (re-forges all tales). Small, careful.
- [metagame · GRAND] **The Workshop Mystery** — a manor-wide treasure hunt: clues chase across exhibits (a Scriptorium *seed* found here, the script to decode it found there; crypto / engineering / star-chart / arcade / Lantern / mirror-maze gates), Undercroft-style hint cards → panels → a final reveal. Theme candidate: a fictional **history of the manor**, a chapter per unlock. (Brandon — the grandest idea; ~3 sessions: write the fiction → design + *prove-solvable* the clue graph → implement.)
- [metagame] **Survey of Heaven — make discovery an EVENT** *(Brandon calls this the small one)* — a constellation fades in with a per-constellation **harmonic melody**; each star flashes in with its own **tone**; hover re-glows + replays it and shows *which feat/visit lit it*; localStorage-gate the animation (like the Undercroft entrance). Plus **in-the-moment unlock cues** on the ws-flag pages (a forge-inlined notifier: the tone/melody + a mysterious line). **Supersedes the old "silent by default" lean.** `feeds:` connects actions ↔ sky.

### curation — tend the old beds (improve / merge / retire)
*Tombstones (RESOLVED/BLOOMED 2026-06-13 — provenance in worklog): ~~chladni vs Singing Plate~~ (keep both, cross-linked teaser→tool) · ~~reconcile NOTES project-status table~~ (was 11 gaps, now complete) · ~~forge ws:seen plumbing check~~ (→ `forge --audit-seen`; fixed 9 direct-visit breadcrumb bugs) · ~~Hall of Mirrors nav redesign~~ (→ "The Dispersion" spectral-rail + live vignettes).*

- [curation] **RETIRE the 9th-instrument seed — 8 is the clean stop** — ground truth (`sound-garden/instruments.js`): the rack ships **exactly 8** (Whitney/Drift/Euclid/Rain/Loom/Carillon/Lattice/Gamelan) on an auto-fit grid — a clean 2×4 already at a tidy stop. The old seed's "9 (3×3)" target contradicts the 8-is-clean call AND names **no instrument** — a grid-fill quota, not a provocation. Tombstone it: *a future 9th may return ONLY as a new seed naming a genuinely must-have instrument, never as a count target.* (DONE this cycle — the exhibit seed is struck above.) While here, two confirmed doc-rot fixes (applied cycle #7): NOTES.md's stale "now **7** instruments" → "now 8, the tidy stop"; and the **Experimenter** cross + its metagame-table row cited a drifted line — the `ws:seen:engine-room` setItem is now at `engine-room/index.html:304` (was :298). (curated cycle #7.)
- ~~[curation] **SHARPEN the makers'-ledger on-ramp — name the read-path**~~ — **RESOLVED/BLOOMED 2026-06-14 (cycle #1 of the new loop):** the on-ramp this note sharpened did its job — the builder took the named read-path (forge build-step inline, file:// estate, no server) and the falsifiable crux (the face shows exactly the N lines in seq order; a `collate.sh` append grows it by one) **verbatim**, and the ledger now has a face → `ledger/face.html` "The Cairn" (see the exhibit tombstone above). The companion curation note retires with its exhibit seed.
- [curation] **Re-crux the Alchemy Lab's named on-ramp — DLA is already proven elsewhere** — the `[room · GRAND]` Alchemy seed (above) names a crystal-growth / DLA first bench with a "fractal-dimension D≈1.71" crux, but `fractal-dimension/` ("The Coastline Rule") **already ships DLA's D≈1.71 via the mass–radius law** M(r)∝r^D (grep-confirmed). A builder taking that on-ramp would rebuild a proven thing. Steer the Alchemy first bench to a DIFFERENT crux: the **reaction-balancer** the seed already lists as an alternate — integer coefficients from the element-count matrix's nullspace conserve EVERY atom exactly (the beam-balance reads 0 on both pans), are the smallest positive integers (gcd=1), an independent brute-force agrees, and an unbalanced equation tips the pan red (teeth). The chemistry analog of the estate's conservation benches; pairs with the Cavern. (No retire — a re-crux so the room opens on un-shipped ground.) (sown 2026-06-14, cycle #2.)
- [curation] **Arcade: retire nothing, deepen Pong** — sized the rack: no true duplicates, real depth in Chomp/Swarm/Gyre/Tessera, so RETIRE NONE. Pong (817 lines — the smallest; "first to eleven vs CPU") is the only cabinet a player would call shallow, and it carries 0 self-test hits (verified) while the newer cabinets (Bulwark/Qubit/Vanguard) have replay-hash self-tests. Cheap win = deepen it (paddle spin/English, or a best-of-match with rally-speed ramp) AND add a headless replay-hash self-test proving the deterministic rally physics — bringing it up to the newer cabinets' bar. Supersedes the open "weak-cabinet pass" curation seed. (sown 2026-06-14.)
- ~~[curation · observation] **Numbers Room: promote at the 3rd bench, not before**~~ — **RESOLVED/BLOOMED 2026-06-14:** Collatz shipped → the wing-build tripped exactly as this seed prescribed (the Numbers Room is now a front-door wing with its 3 benches + a landing; see the `room · GRAND` tombstone above). The threshold this seed named held up — **≥3 benches AND a native spatial metaphor the flat list wastes** (numbers on a spiral / a Stern–Brocot tree you descend). The analog-instruments cluster (the other half of the observation) correctly **stays a plain Workbench group** — no wasted metaphor, no promotion. Threshold logged so a future gardener reuses it.
- [curation · observation] **Hall live-vignette treatment — apply elsewhere?** — "The Dispersion" gave the Hall a distinctive form (spectral-rail + per-physics inline-SVG vignettes). The Cavern (a `cave` drifts metaphor) already has good form; the Workbench/Arcade are plain indexes. Only re-form a *collection* room where a real metaphor is being wasted (the Hall lesson) — not as a mandate. (sown 2026-06-13.)

---

## 🌳 Metagame health

| Metagame | State | Notes |
|---|---|---|
| **The Undercroft** (`undercroft/` · `tools/ws/`) | active — 12 secrets | Open to new *earned* pieces. Grep the 🗝️ hidden-inventory in NOTES before building one. **Breadcrumb plumbing HARDENED 2026-06-13:** all 13 front-door pages now drop their `ws:seen:<id>` on a *direct* visit (not only a map click) — earned secrets that depend on visiting a room now fire for deep-link/bookmark arrivals too. Guarded by `forge --audit-seen`. |
| **The Survey of Heaven** (front-door sky · `tools/sky/`) | active — 6 wings + "The Optician" feats constellation | **7th-wing-for-the-Hall question: RESOLVED 2026-06-13 — NO.** The Hall (now **14 benches** — the Diffraction Grating *and* the Bragg Stack were both added 2026-06-13 *without* a 10th/11th feat, on purpose, to keep the 9-feat "The Optician" constellation + the hidden Light Mixer unlock intact) is already represented via the 9-feat "The Optician" constellation; a separate Hall wing would be redundant/forced (metagame-as-consideration, not a mandate). **Sky-fill HARDENED 2026-06-13:** direct visits now register (breadcrumb plumbing fix above) — a deep-linked room lights its star without bouncing through the map. Still open: **make discovery an *event*** (melodies/tones + in-the-moment cues — the metagame seed above). 6-wing capstone is structurally protected (`allComplete`); keep new constellations additive. |
| **The Hours** (a living time-of-day layer) | **not yet built** — a `room` seed above | If built, it's a 3rd exploration metagame. |
| **The Workshop Mystery** (manor-wide hunt) | **not yet built** — a `metagame · GRAND` seed above | Would be the estate's biggest exploration layer; spans every exhibit. ~3 sessions. |
| **The Physics Lab's Quantum Drift** | **OPEN & growing — 7 benches** (Double Slit + Quantum Tunnelling + Particle in a Box + The Harmonic Oscillator + The Finite Well + The Lattice + **Hear the Ladder** — the bench that *sonifies* the Box vs Oscillator ladders, the first Sound-Garden↔Cavern bridge), 2026-06-13 | Implemented as a **public, in-page spatial reveal** (not an Undercroft secret): `walkedBothDrifts()` in `cavern/index.html` reads the `ws:seen:<id>` breadcrumbs and reveals the violet `#quantumDrift` once ≥1 Newtonian AND ≥1 Einsteinian bench are seen. The **bound-state trilogy is complete** — Box (∞ ladder ∝ n², hard walls) · Oscillator (∞ even ladder ∝ n+½, Gaussian tails) · Finite Well (FINITE ladder, exponential leak) — the three model shapes of one trapped particle; and **The Lattice** takes the step UP to a periodic crystal (Kronig–Penney bands & gaps, the metal/insulator/semiconductor story). Grow it further: the **hydrogen radial Coulomb** ladder, or a **1-D scattering / wave-packet** bench. Open question (a future call, not a mandate): also wire a *hidden* Undercroft secret or a Survey "Experimenter" constellation off walking all three drifts. See `physics-lab-plan.md` §3. |
| **The two physics wings' "Experimenter" constellation** | **not built — sown `cross` seed, scope now spans BOTH wings (2026-06-14)** | The Cavern (physics) *and* now the **Engine Room** (heat) both feed `ws:seen:` breadcrumbs but **neither has an asterism** — the Engine Room is a fresh wing the reward layer silently forgets. **Gardener's verdict this cycle:** don't leave the heat wing additive-but-orphaned — **fold it into the Experimenter's scope** (one FEATS-style group off both wings' breadcrumbs). Structurally cheap & capstone-safe: the Survey's FEATS machinery (sky.js ~110/204) is built for a group that never feeds `allComplete`. The plumbing is already hardened (direct visits register; `engine-room/index.html:304` drops its crumb). Promoted to the revised `cross` seed above. Keep additive; don't bolt onto `allComplete`. |
| **An emergent "curve machines" family** *(observation, not yet a metagame)* | **TRIO COMPLETE 2026-06-13** | All three Workbench/Toys benches that draw curves by *rotating/swinging things* now exist: **Fourier Epicycles** (`epicycles/` — circles on circles), **The Harmonograph** (`harmonograph/` — swinging pendulums, **with an audible voice** as of the v1.1 Sound-Garden cross), and **The Spirograph** (`spirograph/` — gears in gears, shipped 2026-06-13; its self-test even *proves* the trochoid is a 2-term epicycle, the explicit math bridge to `epicycles/`). They're mathematical cousins (all sums of sinusoids / trochoids), now sitting adjacently in the Workbench's Toys & benches. *Still* not sown as a wing/constellation — just flagged so a future gardener can decide if a light cross-link is poetic, **only if genuine** (the Workbench staying a plain legible index is fine — see DESIGNING.md). A possible future thread: a tiny shared "these are cousins" note, OR a `cross` that voices a Spirograph ratio the way the Harmonograph now does. |
| **Chladni overlap** (`strange-garden/pieces/chladni.html` ↔ `singing-plate/`) | **RESOLVED 2026-06-13 — keep both, cross-linked** | A survey (PLAN session) confirmed the two already reference each other (chladni→Singing Plate; Singing Plate→chladni/garden 7×). NOT redundant: the garden's chladni is the *intuitive teaser* (living specimen, no solver), the Singing Plate is the *rigorous instrument* (from-scratch Lanczos eigensolver, 18/18). The teaser→tool pairing is a feature. Curation seed retired; logged here so it isn't re-opened. |
| **Medium-gaps surfaced by survey** *(not a metagame — a planting note)* | **First wave — 3 gaps filled · Second — 2 bloomed · Third — info-theory bloomed, stats open · Fourth — comp-geometry & the heat↔info bridge bloomed; PageRank + CLT/Monte-Carlo still open · Fifth (PLAN, cycle #7) — a cross-vein grow, no new medium-gap · Sixth (PLAN, cycle #2 of the new loop) — 3 net-new computation/strategy mediums sown + 3 fresh crosses + an Alchemy re-crux.** | **First wave (2026-06-13, filled):** **heat** → **Maxwell–Boltzmann gas** + the **Engine Room/Carnot** bet (now a built wing). **fractal-dimension** → `fractal-dimension/` + `coastline-paradox/`. **graph-pathfinding** → `pathfinder/` + `labyrinth/`. **Second wave (bloomed):** **number theory** → `best-rational/`; **chaos-onset** → `bifurcation/`. (Still open: Delaunay–Voronoi.) **Third wave (bloomed/open):** **information theory** → `entropy/` "The Shannon Limit"; **least-squares/regression** still open. **Fourth wave (grep-confirmed gaps):** **(1) network science** (0 pagerank/eigenvector-centrality/random-surfer hits) → sown the **PageRank** exhibit. **(2) computational geometry** (convex hull appeared NOWHERE) → **BLOOMED 2026-06-14 (cycle #10) → `convex-hull/` "The Convex Hull — the Skeptic's Bench"** (three source-disjoint hull builders + the invariant check + the collinear-bug negative control, all on the exact integer `cross` determinant; in-page 7/7 · Node 44/44). The estate's first computational-geometry medium. **(3) the *universal* CLT + Monte-Carlo** (0 monte-carlo/central-limit hits; Galton only proves the Bernoulli special case) → sown the **CLT/Monte-Carlo** exhibit. **(4) heat↔information has no bridge** (0 landauer/erasure hits) → the **Maxwell's Demon × Shannon** cross, **BLOOMED 2026-06-14 → `engine-room/demon/` "The Demon's Ledger"** (the inline core imports `entropy()` + `carnotEfficiency()`, proving one ledger) + the **M–B gas × Engine Room "where pressure comes from"** cross (0 PV=NkT derivations anywhere, still open). **Fifth wave (this PLAN, cycle #7 — the four same-flavor medium-gap exhibits [PageRank, Convex Hull, CLT/Monte-Carlo, least-squares] are KEPT but NOT added to; grew the thin CROSS vein instead):** **(a)** the **modular×cipher** vein sharpened from a fuzzy cross into the buildable **Times-Table Cardioid** exhibit (the Numbers Room's 4th bench, built AS the cross — grep-confirmed 0 cardioid art anywhere). **(b)** sown **The Extent** exhibit (permutation-group/change-ringing — grep-confirmed 0 extent/Cayley proofs estate-wide; gave the proofless carillon synth its first self-test) — **BLOOMED cycle #9 → `extent/`**. **(c)** sown the **Double Slit × Diffraction Grating "one slit-formula, two wings"** cross + de Broglie (grep-confirmed 0 cross-refs, 0 de-broglie hits) — the quantum↔optics seam the grating author already commented. Recorded so a future planter sees the deliberate pivot from medium-gaps to crossings. **Sixth wave (this PLAN, cycle #2 — three scouts diverged, the judge kept 8 of 12):** **(a) error-correction** (grep: 0 hamming/syndrome/parity-bit) → sown **The Hamming Code** exhibit, the natural pair to the shipped Shannon Limit (how FEW bits ↔ how to ADD bits). **(b) Wolfram-class 1-D CA** (grep: 0 rule-110/30/wolfram; all estate CA is 2-D) → sown **The Elementary Automaton** (Rule 90 == Sierpinski/Pascal-mod-2, a clean cross to `fractal-dimension/`). **(c) strategic/economic game theory** (the Adversary is combinatorial-only; 0 payoff/mixed-strategy/best-response) → sown **The Payoff Matrix** (Nash / Prisoner's Dilemma). Plus a buildable **Clockwork Automata** on-ramp exhibit ("The Temperature Dial" — softmax provably sums to 1, the estate's first piece about its own maker; 0 softmax/logits/next-token anywhere). Three fresh crosses sown (Euclidean-Rhythms×Best-Rational [the silent Numbers Room's first voice], Daedalus×Convex-Hull [apply the hull's just-opened invariant template to prove the maze a tree], Spectroscope×Orrery [first quantitative sky↔optics Doppler bridge]) + the **Alchemy-Lab on-ramp re-crux** away from already-shipped DLA to the reaction-balancer. |

---

*When a planning session ends: prune bloomed/dead seeds, refresh the metagame table, and update the
**mode gauge** in NOTES.md (reset "builds since last plan" to 0). When a build session ends: prune the
seed you grew, decrement the gauge.*
