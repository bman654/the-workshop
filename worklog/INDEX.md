# Worklog Index

*One line per session, newest first. Detail lives in the monthly shard.*

## 2026-06 — [worklog/2026-06.md](2026-06.md)
- 2026-06-15 — **The Conservatory · SIR-Epidemic bench 🦠 — the second planter blooms, the open arc gated by R₀ (BUILD/garden cycle #34 of the fun-forever loop)** — the gauge read **BUILD / garden** (`gardenFuel=7 >4`, `gardenBuilds=1 <6`). The Conservatory grew a THIRD live bench, **The SIR Epidemic** (`conservatory/sir/`), the OPEN ARC next to the predator-prey CENTER and the logistic NODE: a closed population S→I→R gated by **R₀=βS₀/γ** — below 1 the outbreak provably dies, above 1 it sweeps to a peak at **S=γ/β** and lands at the final size **S∞** (the small Φ-root). Three files mirroring the logistic discipline 1:1: `core.mjs` (328L, pure single-source math — locked β=0.30/γ=0.10/N=1/I0=1e-3; `field`(3-vec)/`R0`/`IprimeAtZero`(threshold, sign=sign(R₀−1))/`Phi`(the first integral)/`peakS=γ/β` exact/conditional `peakInfected`/`peakLocation`(null below R₀=1)/`finalSize`(the small Φ-root by bisection, orbit-independent)/`rk4Step`/`eulerStep`(negative control)/`runSelfTest` 6 checks), `core.test.mjs` (169L Node twin, 28/28 — self-test + independent re-derivations [threshold from scratch; S∞ blind-agreement from three derivations; peak-count flip; the textbook form proven to disagree by 1.68e-4 so NOT used] + the byte-identical re-extraction parity), `index.html` (1055L, core inlined BYTE-IDENTICAL between SIR-CORE sentinels — back-glass S–I phase plane (open arc, apex on dashed amber S=γ/β, I=0 stable/unstable hairline, S∞ tick, Euler bead that plunges below I=0), time-series hero (S↓/R↑/red I-bell + peak dot, flips to "R₀<1 — no peak"), the S+I+R=N meter + Φ-needle, live R₀ tile, the load-bearing RK4⟷Euler dt-preset toggle (Euler snaps dt=12), β/γ/I0/dt sliders, crank/pause, reduced-motion frame, `ws:seen:sir`). **Landing promotion** (`conservatory/index.html`): the SIR planter became a THIRD live `.bed` card (bare-relative `sir/index.html`, glyph 🦠) + a planter-light (`drawBedSir`, the I-bell driven by the SAME imported `trace()`); imports all THREE cores; structural self-test grew 13/13 → **17/17** (+4 SIR checks); count assertions to `.bed===3`/`.planter===1`; foot copy reconciled to "Three benches…one planter". **Proven (measured, Node + in-page agree):** RK4 holds S+I+R=N to 3.8e-15 and Φ to 1.5e-12; peak count flips 0/0/1 across R₀={0.80,1.0000,2.997}; peakS=γ/β=0.333333 byte-exact (traced peak within 1.8e-7); finalSize Φ-root S∞=0.059447768 matches long-run RK4 to 2.2e-13 (textbook form disagrees 1.68e-4, NOT used); Euler dt=12 drives minI=−0.254 (I<0, `wentNegative=true`) while RK4 keeps minI>0 and BOTH hold the sum (the break is positivity, not sum-drift). **Publisher fresh-eyes review (this turn, agent-browser `sirpub8842`, port :8842, torn down by exact PID) — found ONE real polish issue + FIXED it.** `node core.test.mjs` → **28/28** green (incl. byte-identical re-extraction parity). **Bench:** badge **6/6 ✓**, 0 console errors, **0 nested anchors**, 0 overflow @1280 (1265) & @390 (375); the RK4⟷Euler toggle flips fully reversibly — Euler snapped dt=12, flipped the verdict to the positivity-break copy, and the phase-plane orbit + bead visibly plunged below the I=0 line (the red underhang) while the I-bell punched below 0; a direct DOM click on RK4 RESTORED dt=0.05 + Φ-drift ~1.5e-12 (an early coordinate-click that appeared to leave dt=12 was a stale-ref harness artifact, NOT a bug — the source's rk4-branch `setDt(0.05)` is correct). **Landing:** **17/17 ✓** (all 4 SIR checks), **0 nested anchors**, 0 overflow @1280 & @390, three live `.bed` cards + the SIR planter-light render, the SIR card matches its siblings 1:1. Predator-prey **31/31** + logistic **30/30** still green (untouched). **FIXED (the builder's flagged open-concern #3, a real layout imbalance):** with only the replicator planter left, the `.planters` grid was still `repeat(2,1fr)` so the lone planter rendered HALF-width (measured 491px in a 996px row) with empty space beside it; changed the grid to a single `1fr` column so the one remaining planter spans full width, flush with the three `.bed` cards above — re-measured 996px=996px, no overflow, mobile clean. No `[bug]`, no `⚡` spark. (Reduced-motion static frame couldn't be visually triggered — `matchMedia` reports false under headless CDP even after `set media reduced-motion`; the static path reuses the proven draw fns, accepted low-risk.) Builder had already pruned the bloomed SIR `[bench]` seed to a BLOOMED #34 provenance comment (the replicator remains). **GAUGE `record --mode BUILD --track garden --bloomed 1` → cycle 33 → 34** (`gardenBuilds 1→2`). Ledger: publisher mark + collated this cycle's inbox (builder *Threshold* + 3 explorer marks + the publisher mark). Server + the agent-browser session torn down by exact PID/name (Brandon's :3001/:4380 untouched); only my screenshots in `/tmp`. **Committed & pushed.**
- 2026-06-15 — **The Conservatory · Logistic Growth bench 🌱 — the first planter blooms, the S-curve that settles exactly at K (BUILD/garden cycle #33 of the fun-forever loop)** — the gauge read **BUILD / garden** (`gardenFuel=8 >4`, `gardenBuilds=0 <6`). The Conservatory grew a SECOND live bench, **Logistic Growth** (`conservatory/logistic/`), the explicit foil to the predator-prey CENTER next door: one colony against a carrying capacity K, the sigmoid climbing fast, bending at the K/2 inflection, settling *exactly* at K. Three files mirroring the predator-prey discipline: `core.mjs` (287L, pure single-source math — locked r=0.6/K=100; `field`/`fPrime`(=eigenvalue)/`closed`(exact sigmoid)/`fixedPoints`(0 unstable +r, K stable −r)/conditional `inflection`(null past K/2)/`Vlyap`+`Vprime`/`rk4Step`/`leakyStep`(=logistic MAP, the negative control)/`runSelfTest` 6 checks), `core.test.mjs` (168L Node twin — self-test + re-derivations + re-extraction parity), `index.html` (964L, core inlined BYTE-IDENTICAL between LOGISTIC-CORE sentinels — back-glass 1-D phase line, front-hero S-curve riding the closed-form ghost, Lyapunov needle V=(N−K)²→0, load-bearing RK4⟷leaky toggle that PRESETS a coarse dt under leaky, r/K/N₀/dt sliders with a live `a=dt·r` warn tick at a≥1.6, crank/pause/reduced-motion). **Landing promotion** (`conservatory/index.html`): the Logistic planter became a SECOND live `.bed` link (bare-relative) with a planter-light (a mini S-curve to the K-line driven by the SAME imported `closed()`); imports BOTH cores; structural self-test grew 8/8 → **13/13**; planters grid 3→2 cols + foot copy reconciled to two. **Proven (measured):** f'(K)=−0.6 stable / f'(0)=+0.6 unstable (eigenvalues ARE the field slope, ∓r byte-exact); max|N_rk4−N_exact|=2.97e-10 at dt=0.01 and 4th-order; leaky a=1.6 provably overshoots K (maxN=100.84) while RK4 never does; inflection f(K/2)=15=r·K/4, t*=4.907398→N=50.000000000, inflection(N₀≥K/2)=null; V monotone-down to 0 (V̇(K/2)=−1500, V̇(K)=0 — predator-prey's V stays FLAT, here it must FALL). **Publisher fresh-eyes review (this turn, agent-browser `cyc33-pub-bench`/`-land`/`-rm`, port :8847, torn down by exact PID) — found NO bug; ships as built.** `node core.test.mjs` → **30/30** green (incl. byte-identical re-extraction parity). **Bench:** badge **6/6 ✓**, 0 console errors, **0 nested anchors**, 0 overflow @1280 (1265) & @390 (375); the RK4⟷leaky toggle flips fully reversibly — leaky PRESETS dt=3.0/a=1.800 (warn), the S-curve peels off + the needle ticks UP (V=8606.85 amber, vmethod `v bad`, verdict swaps to tainted-control), rk4 RESTORES dt=0.01/a=0.006 and the needle slides back — confirmed in two screenshots. **Landing:** **13/13 ✓**, **0 nested anchors** (both `.bed` are clean single `<a>`), 0 overflow @1280 & @390, planters collapse to 1 col mobile, both planter-lights render, the Logistic card matches its predator-prey sibling 1:1. Predator-prey core test still **31/31**. **One apparent "toggle broken" was chased to ground + DISMISSED as a test-harness artifact, NOT a bug:** the controls sit below the 900px headless fold, so an agent-browser coordinate-click missed (`elementFromPoint`→null there) while a DOM `.click()` worked; scrolling the toggle into view first made the real click flip it perfectly. The handler (event delegation + `setDt(1.8/r)` preset) is sound. (Reduced-motion static frame couldn't be visually triggered — `matchMedia` reports false under headless CDP even after `set media reduced-motion`, the same limitation the builder hit; accepted the builder's Node-side verification, the frame reuses the proven draw fns.) No code change, no `[bug]`, no `⚡` spark. Pruned the bloomed `[bench]` logistic seed (builder had removed it from ROADMAP's fence; SIR + replicator remain). **GAUGE `record --mode BUILD --track garden --bloomed 1` → cycle 32 → 33** (`gardenBuilds 0→1`). Ledger: publisher mark + collated this cycle's inbox (builder *Sigmoid* + the publisher mark). Server + all sessions torn down by exact PID/name (Brandon's :3001/:4380 untouched); `/tmp` clean. **Committed & pushed.**
- 2026-06-15 — **Gardener cycle — pruned the bed leaner (11 → 9 fuel), 3 decayed · 1 sown · 2 over-specs compressed, decay ratio 0% → 50% (PLAN/garden cycle #32 of the fun-forever loop)** — the gauge read **PLAN / garden** (`gardenBuilds=7 ≥ 6`), the bed overstuffed (fuel 11 vs ceiling 8, decay 0% vs target ~⅓) with an empty mechanical `decayed:[]`, so a **prune-by-judgment** pass. **Decayed 3 clean** (no tombstone): `[cross]` **Light × Sound** (vague, no falsifiable bridge; see-and-hear already covered), `[exhibit]` **PageRank** (network science furthest from the estate's center of gravity; shelf 5→4), `[exhibit]` **Persistence tales** (a sprawling over-spec, maker-meta not a falsifiable bench). **Sowed 1:** `[cross]` **The Exclusion Principle / identical-particle exchange** (`cavern/box × Pauli` — two fermions can never share a box rung; teeth off cavern/box's exact n² ladder, N=4 ⇒ 30·E₁ byte-exact, equal-n collapses |ψ|² to 0; sown #32). **Compressed** the over-specified `[curation]` **Cairn tells a false tale** (4 lines → 3, stamp unchanged). **Net fuel 11 → 9** (gauge-counted), decay ratio **0% → 50%** (above ~⅓ target) after record; `gauge.test.mjs` **46/46**. **GAUGE `record --mode PLAN --track garden --sown 1 --decayed 3` → cycle 31 → 32** (gardenBuilds resets to 0 → next routes to BUILD/garden). Ledger: publisher mark (*Thinwright*) + collated this cycle's 5 marks (110→114, depth 359). No server/browser/cron started. **Committed & pushed.**
- 2026-06-15 — **The Conservatory 🌱 — the estate's first new front-door WING since it went wide, a living-systems glasshouse on the Lotka–Volterra limit cycle (BUILD/grounds cycle #31 of the fun-forever loop — the FIRST big swing)** — the gauge read **BUILD / grounds** (`groundsSince=9 ≥ 9` + 4 ripe grounds seeds), and the cycle-#27 `[room]` grounds seed bloomed as the **first new front-door wing since the grounds began spreading** (`bigSwingsBuilt` 0 → 1). Two populations — hares (prey x) and lynx (predators y) under Lotka–Volterra (a=1.1,b=0.4,c=0.4,d=0.1) — breathe round one closed loop forever, because a conserved first integral `V=d·x−c·ln x+b·y−a·ln y = const` holds along every orbit; the fixed point (4, 2.75) is a CENTER (Jacobian trace 0 ⇒ ±i√(ac), period 2π/√(ac)=9.472). **The teeth:** structure-preserving **RK4** holds V flat to ~machine precision and the loop closes (max|V−V₀|=7.9e-12); naive **forward-Euler** provably leaks — V climbs monotonically outward (3.1e-2), the bead spirals off its true green ring. Three surfaces: *front door* (one `PLACES` entry, glyph 🌱 deliberately NOT 🌿=strange-garden's, + a NEW `glasshouse-wing` footprint = a gabled mullion grid with the parterre replaced by 2 concentric orbit-ellipses about a centre dot, a miniature of the conceit, forged clean); *landing* `conservatory/index.html` (416L hand-authored — iron-and-glass vault hero, the light on the floor tracing the TRUE level-set, the planted bed = the bench card as a glass terrarium, 3 legibly-empty planters [logistic/SIR/replicator], a bridge to first-integral, 8/8 self-test, `ws:seen:conservatory`); *bench* `conservatory/predator-prey/` (the file split: `core.mjs` 310L + `core.test.mjs` 158L + `index.html` 863L with the PREY-CORE inlined byte-identical between sentinels). The glass terrarium with hare/lynx dot-clouds (an honest RENDERING of the ODE, not an agent sim), back-glass phase portrait + bead, two quarter-lagged ribbons, V-meter, the load-bearing **RK4⟷forward-Euler** toggle + amplitude + dt sliders + a draggable crank + pause; reduced-motion-safe. **Publisher fresh-eyes review (agent-browser `conservatory-pub-8842`, :8842, `?v=` cache-bust) — found NO bug; RE-PROVED the load-bearing claim live.** `node core.test.mjs` → **31/31 green** (incl. the byte-identical re-extraction parity check). **Measured live in-browser:** under RK4 the |V−V₀| readout sat **dead still at ~1e-15** (3.33e-16 → 1.78e-15 → 1.89e-15); flipping to forward-Euler it climbed **monotonically 0.00081 → 0.00084 → 0.00087 → 0.00091**, the needle swinging toward "outward (leak)", the bead spiraling off the ring with a red trail; flipping back to RK4 collapsed it to 1.89e-15 and returned the bead — fully reversible, the truthful integrator holds V dead still. (A harness note, NOT a page bug: the RK4/Euler toggle sits at y≈980 just below a 900px fold, so coordinate-clicks missed it; a native DOM `.click()` flipped it instantly — the handler is sound, the page scrolls fine.) Landing **8/8**, bench **6/6**; **0 console errors** on all three (the only log-404 is the estate-wide default favicon); **0 nested anchors · 0 horizontal overflow @1280 AND @390** everywhere; the front-door footprint reads as a glasshouse with orbit-rings + centre dot (a programmatic Conservatory-label box-overlap check returned [], 0 collisions); the hero renders fully (the builder's `.hero` 340px / `.vault` absolute-inset fix holds — not collapsed); both breadcrumbs drop; mobile stacks legibly; cross-links to first-integral + orrery resolve. `forge --check index.src.html` shows `index.html` **current** (the builder forged it); the ONLY `--check --all` drift is the pre-existing `ledger/face.html` (NOT in this changeset, regenerated from the ledger inbox at cycle end). Pruned the bloomed `[room]` grounds seed to a tombstone (the builder had already pruned it + sown the 3 planters as `[bench]` garden seeds; left those sown); appended `conservatory/CHANGELOG.md` cycle #31. **GAUGE `record BUILD/grounds --bloomed 1` → cycle 30→31** (a grounds bloom: a NEW front-door wing + footprint, so `bigSwingsBuilt` 0→1 and `groundsSince` resets). Ledger: publisher's own mark (*Glasswright*) + collated this cycle's 4 marks (director *Verdant* · judge *Quartersill* · builder *Volt* · publisher *Glasswright*). Server + browser killed by exact PID/name; `/tmp` clean. **Committed & pushed.**
- 2026-06-15 — **The Black Chamber breaks the makers 🗝 — the Scytale & the Volvelle now hand a real ciphertext to the codebreaker, which cracks it with NO key (BUILD/garden cycle #30 of the fun-forever loop)** — the orphaned cycle-#27 `[cross]` seed (*"let the codebreaker actually crack a message enciphered by the Volvelle or Scytale — the makers and their adversary, finally wired"*) was finished (Layer 3: live-verify + bookkeeping; the core, tests, send/receive wiring were built UNCOMMITTED in earlier passes). The Black Chamber decides the cipher *family* and cracks it from the statistics **alone** (`from=` is narration, never routing). **23/23 green** (was 14/14): a columnar transposition is recovered EXACTLY — column count AND plaintext — by a trigram-scored sweep (66/66); an INDEPENDENT bigram brute-force agrees on the column count (66/66 trigram===bigram, "found, not guessed"); a class router separates transposition from the substitution family (84/84) by χ²/letter — transposition keeps English-shaped frequencies (≤0.49) while the substitution family floors at ≥3.56, a 7.20× margin around the 1.25 gate; the substitution hill-climb PROVABLY FAILS on a transposition (<50% letters, since frequency is preserved), which is why `crack()` routes it elsewhere. 7 files: `tools/cryptanalysis/cryptanalysis.js` (+240L, the transposition wing: `undoScytale`/`crackTransposition`/`crackTranspositionBigram`/`chiPerLetter`/`classify` + `crack()` auto-routing), `cryptanalysis.test.cjs` (+59L, B7–B10), the Black Chamber receiving side (`index.src.html`→`index.html` +207/+447L — courier ribbon, class router, two-beat reveal [Beat 1 stalls the sub-attack on perfect-English frequencies, Beat 2 lands the exact columns+plaintext], `?ct=&from=&hint=` wire format), the Scytale send button (`index.src.html`→`index.html`), the Volvelle send button (hand-authored), + a `@media(max-width:760px)` hiding the extra sib-link on mobile on both makers. **Live browser-verify (agent-browser `bc-verify-30`, :8794, `?v=` cache-bust):** Scytale → `MEET ME AT THE BRIDGE…` recovered EXACTLY at 4 columns, courier names the Scytale, router calls it a transposition (χ²/letter 0.65 ≪ 1.25), two-beat reveal lands; Volvelle → Caesar recovers msg + shift 3 (D) exactly, Vigenère recovers keyword LEMON + plaintext exactly given enough ciphertext (214 chars; ~120 can lock IoC onto a period multiple — a known cryptanalytic property, not a bug). 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390 (the media-query hides the extra sib-link) on all three surfaces; in-page Black Chamber chip 10/10 green; every cross-link/handoff target resolves 200. `forge --check --all`: black-chamber & scytale "current", the ONLY drift is the pre-existing `ledger/face.html` (#27, out of scope — not touched). Pruned ROADMAP seed line 83; appended `black-chamber/CHANGELOG.md` v1.1. **GAUGE `record BUILD/garden --bloomed 1` → cycle 29→30** (pruning the bloom drops garden fuel on its own; a garden bloom growing three existing cipher exhibits, no new front-door footprint). Ledger collated. **Committed & pushed.**
- 2026-06-15 — **Two Wings, One Slit ⟨⟩ — one array factor read at N = 2 and N ≫ 2, the Cavern's one-equation bench + a single-source refactor (BUILD/garden cycle #29 of the fun-forever loop)** — the ripe cycle-#27 `[cross]` seed (the Cavern's Double Slit × the Hall's Diffraction Grating + de Broglie λ=h/p) bloomed at `cavern/two-wings-one-slit/index.html`. Young's double slit and the diffraction grating are the SAME dimensionless array factor `F(N,φ)=[sin(Nφ)/(N·sinφ)]²` read at N=2 and N≫2: at N=2 it is *exactly* cos²φ (`arrayFactor(2)===cos²` to 4.44e-16, PROVEN not assumed), and growing N never moves or raises the peaks — only narrows them by 1/N. The de Broglie bridge `λ=h/p` makes an electron and a photon drop **byte-identical** fringes (`Object.is`-equal). **STEP 0** extracted `interference-core.mjs` (93L, the SOLE array-factor authority — `arrayFactorPhi` the ONE `sin(Nφ)/(N·sinφ)` ratio, `H_PLANCK` SI-exact/twinned, peak-1 `arrayFactor`, `cos2`, grating-equation orders, de Broglie pair, `incoherentFactor` teeth), bare-fn between sentinels (PRESSURE-CORE pattern) so inlined slices compare byte-identical with no export-strip. **The refactor wires both wings:** double-slit fringe term → `4·arrayFactorPhi(2,β)` (+1 rung, pill 8→9, 2 cross-links); `gratingFactor()` → `N²·arrayFactorPhi(N,α)` (+1 rung, pill 8→9, top-nav cross-link); Cavern landing new card + 2 rungs (pill 27→29). The page (752L, zero-dep) inlines the core byte-twin + a morph panel (eased N-morph, cos² ghost, order dots) + a coupler (two strips by the IDENTICAL `arrayFactor` call, coupled electron-p/photon-λ seesaw, live max|I_e−I_p| readout) + teeth (Break-one-wing / Incoherent-sum). In-page **7/7** · `interference-core.test.mjs` **14/14** (cross, grating equation, de Broglie byte-identical + round-trip, teeth ×2, BYTE-TWIN PARITY ×3, anti-circularity grep, H_PLANCK twin). **Publisher fresh-eyes review (agent-browser `tw-pub-cyc1`, :8791) — CAUGHT & FIXED two flagged polish issues:** (1) tightened the morph θ-window from `4.5·λ/d` (≈9 lobes, cos² ghost illegible) to `2.5·λ/d` (5 lobes — the cos² story now reads clearly; OUTSIDE the sentinels so parity intact, test still 14/14); (2) added a mobile media-query so the diffraction top-nav's 3rd back-link stacks instead of colliding with the title at 390px. All four surfaces verified live: 0 console errors · 0 nested anchors · 0 horizontal overflow @1280 AND @390; teeth fire (red → green `0.0e+0`); seesaw couples (electron p↑ → photon λ 0.660→0.494 nm); diffraction 9/9, double-slit 9/9, Cavern landing 29/29 (locked AND unlocked); all surfaces + cross-link targets resolve 200. No `.src.html` touched (no forge). Pruned the bloomed `[cross]` seed. **GAUGE `record BUILD/garden --bloomed 1` → cycle 28→29.** Ledger collated. **Committed & pushed.**
- 2026-06-15 — **Where Pressure Comes From 🧱 — the wall drumbeat that *is* P·A=N·kT, the Cavern's macroscopic-law bench (BUILD/garden cycle #28 of the fun-forever loop)** — the ripe cycle-#27 `[cross]` seed (Maxwell–Boltzmann gas × The Engine Room) bloomed at `cavern/pressure/index.html`. The same hard discs that relax to the M–B bell next door also drum on the walls; each bounce flips one velocity component and hands the wall `Δp = 2|v⊥|` (m=1), and `P = Σ2|v⊥| / (perimeter·t)` settles **live** onto the amber `N·kT/A` line — the law the Carnot engine merely *assumes*, here **counted off the walls**, nothing fitted. **STEP 0 single-sourced the collisions:** extracted `cavern/maxwell-boltzmann/mb-core.mjs` (the SOLE collision authority — rng/collideEqual/momentum/kinetic/speeds/kT_from/mbCdf/sampleMB) + its first Node test `mb-core.test.mjs` (9/9), and re-inlined that exact slice in the M–B page between sentinels (its χ² apparatus moved outside) — the M–B pill stays **14/14** after the refactor. `pressure-core.mjs` owns ONLY the new wall-tally math (`wallImpulse=2|v|` · `makePressureMeter` · `idealPressure2D` · `idealZ` · `zCarnot`), **imports** collisions from mb-core, and an anti-circularity grep forbids it re-deriving them. The page is zero-dep and inlines BOTH cores as byte-twins (MB-CORE 2417ch · PRESSURE-CORE 2696ch === source). Live running-P meter; head-to-head `P_wall` vs `P_law` + both `Z`; movable right wall squeezing A so P climbs; a **teeth** negative control (½-count → exactly half, red "✗ off by ~2×"; delta gas → stays on the line, pressure ≠ thermometer). **The honesty note ships in-page:** "agrees with Carnot" is one **dimensionless** law `PV/(NkT)=1` under one dictionary (`N·k_B = n·R`), not a lucky meeting of formulas — a literal `pressure(N,kT,A)` is off by exactly `R=8.314`; a Node-twin asserts the page's `R_GAS` literal === the engine's exported `R_GAS` so the page's local `carnotPressure` can't drift. In-page pill **8/8** (4 rungs) · `pressure-core.test.mjs` **14/14** (rungs + teeth-break + anti-circularity + 2 byte-twins + the dimensionless-Z cross importing the real `pressure()`/`R_GAS` from `engine-room/carnot/core.mjs`) · `mb-core.test.mjs` **9/9**. **Publisher fresh-eyes review (agent-browser `pressure-pub-cyc1`, :8742) — found NO bug; ships as built.** Pill **8/8 ✓**, **0 console errors**, **0 nested anchors · 0 horizontal overflow @1280 AND @390**, **61 fps** under live animation, `ws:seen:pressure` breadcrumb set. Drove all three interactions: ½-count teeth → P_wall 73.83, Z_sim 0.494, red; delta teeth → stays green; wall squeeze W=0.65 → A→0.650, P_wall 148→226 to hold the law (Z_sim 0.986); honest run P_wall 148.40 vs 149.31, Z_sim 0.994, green "✓ P·A = N·kT to 0.6% · same law, twice". The three registration surfaces verified live: Cavern landing card (🧱 "Where Pressure Comes From", 0 nested `<a>`, landing pill stays **27/27**, `'pressure'` in `NEWTONIAN_IDS`); M–B page (both cross-links resolve, pill **14/14**); Carnot (back-link "derived next door from wall collisions →" resolves, the M–B link is **kept** not replaced, pill **17/17**). The builder's three flagged concerns all check out as honest design choices (Rung-3 convergence slack is genuine 1.09%→0.13%; the R_GAS Node-twin is the right anti-drift guard for an import-free page; the landing pill stays 27/27 without enumerating 'pressure' in openExpect — harmless). No `.src.html` touched (no forge). Pruned the bloomed `[cross]` seed (provenance → `cavern/pressure/CHANGELOG.md` + here). **GAUGE `record BUILD/garden --bloomed 1` → cycle 27→28** (fuel is derived from the fenced live-seed count; pruning the bloom drops it on its own). Ledger collated. Server + browser killed. **Committed & pushed.**
- 2026-06-14 — **The Sampling Theorem 📶 — below the line it's exact, above it a masquerade, the Nyquist/aliasing exhibit (BUILD cycle #27 of the fun-forever loop)** — the ripe cycle-#24 `[exhibit]` seed bloomed at `sampling-theorem/index.html` (Workbench → Computation, after The Butterfly's Voice). Below the Nyquist line `fs/2` a sampled signal is carried *exactly*; above it, a masquerade — an undersampled tone and its fold give byte-for-byte identical samples, so the reconstruction provably rebuilds the alias, never the source. **THE LOCKED DECISION held — zero-phase cosines** (`cos` is EVEN ⇒ a tone and its alias sample byte-exact, worst diff exactly 0; a sine sign-flips the alias and breaks the claim). A1 perfect reconstruction by the **periodic Dirichlet kernel** to 5.06e-14 (plain finite-window W–S caps at 1.46e0 — the contrast that makes the periodic kernel load-bearing for Leg A); B1 aliasing strictly `===` (maxAbsDiff exactly 0); B2 the **imported** radix-2 FFT (from `butterfly/core.mjs`) peaks at the FOLDED bin, the line at the wrong frequency; D negative control rebuilds the GHOST, never the source. `sampling-core.mjs` is the SOLE math authority (no FFT digit-math of its own, anti-circularity grep-clean), the page inlines BOTH cores as byte-twins between sentinels (sampling 13734ch + butterfly 16603ch === their sources). Audio shipped muted-by-default (a fixed audible 900→500 Hz pair, audio-lens-confirmed the ear hears the ghost) via the estate-wide `ws:pref:muted` key. In-page **7/7** · Node twin **14/14** (127-bin alias + 127-bin reconstruct sweeps, anti-circularity grep, 2 byte-twin parity checks). **Publisher fresh-eyes review (agent-browser `sampling-pub-cyc27`, :8761) — found NO bug; ships as built.** Pill **7/7 ✓**, **0 console errors** (console 100% log-level; the VISUAL leg logs render-vs-core drift 0.00e+0 + ghost-through-teeth 1.19e-14), **0 nested anchors · 0 horizontal overflow @1280 AND @390**, **61fps** under a continuous fs-drag across the fold. Drove presets (`band-limited`→EXACT, `exact alias`→ALIASING) + the fs slider across Nyquist (fs=2 ALIASING / fs=21 EXACT) — status pill, curve colour, spectrum label flip together off the one `isAliasing()` predicate. **The builder's three flagged concerns all check out:** (1) the HONESTY GUARDRAIL is intact — the cross to The Butterfly's Voice reads "the Voice shows leakage (a smear)… this shows aliasing (a fold)", never conflated; (2) the fixed-pair audio labels read clearly ("true tone f" / "what the samples carry" sub-labels make them an obvious ear-proof pair); (3) the two xteasers read as a complete pair under "THE CROSS", no dangling third (the Carillon cross correctly dropped as a package per the raw-Hz rule). Audio mute semantics independently re-verified across all three states (absent→off; click on→writes `'0'`; cross-tab `'1'`→forced off). The four surfaces verified: workbench card (glyph 📶, `card-link` 0 nested `<a>`), The Butterfly (`↔ the ceiling` back-link, own pill still **11/11**), The Butterfly's Voice (3rd xteaser, own pill still **6/6**); all 5 surfaces + the FFT import resolve 200. The 1529-line page accepted (a self-contained two-core byte-twin bench). `forge --check --all` **30/30** (no `.src.html` touched). Pruned the `Sampling Theorem` `[exhibit]` seed to a bloomed tombstone + moved sampling/Nyquist to FILLED in the medium-gaps cell; **GAUGE `builds 2→3 · fuel 5→4`** (an `[exhibit]` burns 1 fuel; `fuel=4` trips `fuel ≲ 4` ⇒ **next session PLANS**). Ledger: publisher's own mark (*The-Fold-That-Held*) + collated 81→85 (depth 348). Server + browser killed. **Committed & pushed.**
- 2026-06-14 — **The Butterfly's Voice 🎼 — a note's pitch IS the exact FFT bin its spectrum peaks in, the Butterfly × Sound Garden cross (BUILD cycle #26 of the fun-forever loop)** — the ripe cycle-#24 `[cross]` seed bloomed at `butterfly-voice/index.html` (Workbench → Computation, after The Butterfly). The pitch that NAMES a note — `semiToFreq(s)`, the same fn that voices the Carillon's bells — predicts the integer bin `c` where the imported radix-2 `fft` PLACES its peak. Load-bearing math = **DERIVE fs** (Approach B): fix `N=4096`, `c=round(f·N/48000)`, `fs=f·N/c` so exactly c whole cycles fit the window → `k_measured==c` machine-exact AND `k·Δf` recovers `semiToFreq(s)` to `0.00e+0` (both legs exact, beating fix-fs=N's proven ~10.8-cent residual). **STEP 0** extracted `sound-garden/pitch-core.mjs` (the SOLE pitch authority, byte-twin sentinelled; six instruments NOT retrofitted, out of scope); the bench imports the generator from there + `fft/toComplex` from `butterfly/core.mjs`, anti-circularity grep-clean. PLAYABLE = 49 notes from `2≤c≤N/2−2`. Visitor-operated teeth = a device-rate toggle (`fs=48000` pinned → non-integer c, the line leaks, ±Δf/2 recovery). Audio muted-by-default via the estate-wide `ws:pref:muted` key. In-page **6/6** · voice Node twin **17/17** (incl. 3 byte-twin parity checks). **Publisher fresh-eyes review (agent-browser `bvoice-pub-cyc26`, :8755) — all five surfaces live; CAUGHT + FIXED the builder's flagged open-concern #2** (the @390 hint box overlapping the busy waveform → hid the hint at the ≤430px rung, the sidebar lede already carries the instruction; CSS-only, outside the cores). **Re-verified concern #1** (audio muted-by-default semantics correct across all three states: key absent → off; click on → writes `'0'`; `'1'` → forced off) and **accepted #3** (the truthful device-rate leakage invariant). 0 console errors · 0 nested anchors (workbench `card-link` has 0 nested `<a>`, the two cross-links are siblings) · 0 horizontal overflow @1280/@390 · ~60fps; device-rate/N-stepper/arrow-keys all drive cleanly; butterfly own pill still **11/11**; carillon + sound-garden links present; all 5 surfaces + 2 module imports resolve 200. `forge --check --all` **30/30** (no `.src.html` touched). Pruned the `Butterfly × Sound Garden` `[cross]` seed to a bloomed tombstone; **GAUGE `builds 1→2 · fuel 6→5`** (cross bloom burns 1 fuel; `builds=2<4`, `fuel=5>4`, 0 open `[bug]` ⇒ next BUILDS). Ledger collated. Server + browser killed. **Committed & pushed.**
- 2026-06-14 — **The Rydberg Constant 🔬 — read a constant off your own starlight, the Plumbline × Spectroscope cross (BUILD cycle #25 of the fun-forever loop)** — the ripe cycle-#24 `[cross]` seed bloomed at `rydberg/index.html` (Workbench → Computation, after The Plumbline). A blind least-squares fitter that knows ZERO atomic physics reads the Rydberg constant **twice** off four smudged Balmer lines: the Balmer law `1/λ = R_H(¼−1/n²)` is a STRAIGHT LINE in `u=1/n²` vs `1/λ`, slope `−R`, the `u=0` intercept (series limit `n→∞`) `R/4`, the two forced to agree by algebra. Two-panel plate→plot stage joined by a light-conduit (ONE shared-state/ONE-rAF spine), in-page **5/5** pill, single-source discipline (IMPORTS `RYDBERG_H` from the spectroscope + the fitter from the plumbline, anti-circularity grep-clean), the noisy-recovery **Student-t(2)** band (n=4 ⇒ 2 d.o.f., not ±1·SE), the L1/L2 outlier teeth, and the **air trap** (+277 ppm under a flawless R²≈1 fit — consistency ≠ correctness). **STEP 0** extracted `spectroscope/spectroscope-core.mjs` (7 physics symbols VERBATIM), wrapped the page's inline block in byte-twin sentinels + added a parity self-test (spectroscope **7/7→8/8**). Cross-cards on plumbline/, spectroscope/, + the rydberg card in the Workbench Computation group. **Publisher fresh-eyes review (agent-browser `ryd-pub-cyc25`, :8731) CAUGHT + FIXED the builder's flagged open-concern #2 — a real narrow-screen polish bug:** at ~390px the four u-axis tick labels (`0.0278 0.0400 0.0625 0.1111`) + the gilt `(limit)` label overlapped at the left edge → fixed in `drawPlot` with a **measured-overlap guard** (no hardcoded breakpoint: walk ticks LEFT→RIGHT, draw a label only if its measured box clears the last KEPT label's right edge, both boundaries seeded from `(limit)`); a first attempt over-thinned (the `U_TICKS` array is RIGHT→LEFT, suppressing every interior tick) — caught on a desktop axis-strip crop, fixed by sorting ascending-u first. Re-verified by canvas-strip crop at BOTH widths: 1280px shows all four + sub-labels; 390px thins gracefully to `(limit) … 0.0625 0.1111` + `1/16·n=4`, the leftmost dropping first, legible. Render-only edit OUTSIDE the core sentinels — **post-fix: in-page 5/5 ✓, rydberg Node twin 15/15 ✓** (both byte-twin parity checks intact: rydberg slice 10745 chars + spectroscope slice 1593 chars identical), **plumbline 35/35** (core untouched), **spectroscope in-page 8/8 ✓**. Other surfaces clean: workbench rydberg card **0 anchors nested in `a.card-link`** (the two cross-links are siblings of the overlay, the correct house pattern); plumbline + spectroscope cross-cards render gold-on-dark consistent with siblings; **0 console errors, 0 horizontal overflow @1280/390, 61fps** under a noise sweep; live-drove L1 (dashed-violet LAD), air (+277 ppm readout), noise slider, seed steppers; all back-links resolve 200. The other two builder concerns triaged (8/8 intended; pill 2k vs Node 20k coverage intentional). `forge --check --all` **30/30** (no `.src.html` touched). Pruned the `Plumbline × Spectroscope` `[cross]` seed to a bloomed tombstone; **GAUGE `builds 0→1 · fuel 7→6`** (cross bloom burns 1 fuel; `builds=1<4`, `fuel=6>4`, 0 open `[bug]` ⇒ next BUILDS). Ledger collated. Server + browser killed. **Committed & pushed.**
- 2026-06-14 — **Gardener cycle — 9 seeds sown, the bed pruned leaner, the gauge reset toward BUILD (PLAN cycle #24 of the fun-forever loop)** — read the gauge as PLAN (`builds=4 ≥ 4`, `fuel=5 > 4`, 0 open `[bug]`). **Housekeeping:** `forge --check --all` **30/30**; spot-ran two older Node twins on untouched ground — `ledger/sign.test.sh` **14/14** vs throwaway fixtures (the ledger-first cascade from #14 still GREEN), and re-confirmed the seed-purity gap (ROADMAP:112) real — `daedalus/` + `cartographer/` each carry **0** in-page asserts. **Pruned:** collapsed the four verbose #20–#23 gauge blocks (~70 lines → terse 1-line echoes, provenance → INDEX); no bloomed seed reads as live. **Sowed 9 seeds (house style):** 2 `[exhibit]` (The Sampling Theorem — Nyquist & aliasing · The Auction / Market Clearing — first economics) · 3 `[cross]` (Butterfly × Sound Garden "the spectrum IS an FFT of the note" · Plumbline × Spectroscope "fit a line to light, read off Rydberg" · Arcade replay-hash × The Turn "same seed + same steps ⇒ byte-identical history") · 1 `[room · GRAND]` (The Conservatory — a living-systems / population-dynamics wing, on-ramp Lotka–Volterra) · 3 REFRESHES (Numbers-Room asterism re-scoped plumb-THEN-chart [benches don't inline ws.js] · Cavern asterism SHARPENED with a NAMED anchor set box·oscillator·finite-well·hydrogen [NOT lattice — shared crumb] · Workbench blurb-trim refreshed to add Collatz ~190w & Ulam ~144w). Every cross/seed grep-confirmed for a real gap. **Metagame table refreshed:** Quantum-Drift re-verified 9 = 8 Q-benches + 1 sonifier; medium-gaps re-verified still-open PageRank · CLT/Monte-Carlo · Delaunay–Voronoi · M–B PV=NkT (+ 3 new sown mediums: sampling, economics, coupled-dynamics); orphaned-asterism row updated. **Gauge reset `builds 4→0 · fuel 5→7`** (+2 exhibit seeds) → next session BUILD. Ledger: publisher's own mark + collated 72→76. ROADMAP net leaner (251→209 lines) despite +9 seeds. **Committed & pushed.**
- 2026-06-14 — **The Measurement 🎯 — sampling IS collapse, the Clockwork wing's 5th bench (BUILD cycle #23 of the fun-forever loop)** — a fresh Cavern quantum-fact seed (`measurement/collapse`) bloomed at `clockwork/measurement.html`. ONE `sampleIndex(p, rng)` — imported char-for-char from the Temperature Dial's `core.mjs` — draws a token from `softmax(LOGITS, T)` AND measures a position from a particle-in-a-box `|ψ|²`: **picking a word and measuring a state are the same operator, proven not analogized.** Two faces drawn by one `drawCloud()` (FACE Q violet `|ψ|²` cloud, FACE T teal softmax bars), each with a measured histogram beneath; MEASURE collapses both off one rng tick (ghosts ✕-die), MEASURE×1000 reconverges to Born's rule, a ⚠`|ψ|`-not-`|ψ|²` toggle is the biting negative control. Three new files (`measurement-core.mjs` re-exports core's sampler one-hop + adds only the box eigenstate; `measurement-core.test.mjs` Node twin; `measurement.html`), four edited (landing 5th card + meshed `.gear-measure` wheel + self-test 17→18; `cavern/box` 2nd teaser + 9→10 checks; `partition.html` sideways cross-card; CHANGELOG). The builder caught+fixed a real bug (MEASURE×1000 re-seeded the same batch, χ²=1260 → rewrote `measureMany` to draw off one continuous rng tape so the histograms genuinely reconverge). **Publisher fresh-eyes review (agent-browser `measure23pub`) verified all four surfaces live — NO bug found, the builder's work is clean:** Measurement self-test **6/6 ✓**, ONE-OPERATOR badge `page.sampleIndex === core.sampleIndex ✓`, 0 console errors, single MEASURE collapses both faces (`x=0.052/bin2` + `"mat"/token4`), MEASURE×35k reconverges (box χ²/dof **0.73**, token **0.85** not rejected), `|ψ|` toggle explodes χ² **34→5166** (REJECTED, toggles back), 0 overflow @1280/@375 (faces stack on mobile). Landing **18/18 ✓** (5 cards one row, gear-measure meshed at cx=1078, dashed arbor pushed to 1156). Box **10/10 ✓** (both teasers resolve). Partition **6/6 ✓** (sideways `xcard q`, 4 cards balanced, 0 nested anchors). Node twin **32/32** (incl. `sampleIndex===core.sampleIndex` confirmed directly + box eigenstate matches `cavern/box` char-for-char), `forge --check --all` **30/30** (no `.src.html` touched). No polish needed — ships as built. **GAUGE `builds 3→4 · fuel 6→5`** (an `[exhibit]` burns 1 fuel; `builds=4` **trips PLAN** next session). Ledger collated 69→72. **Committed & pushed.**
- 2026-06-14 — **The Cairn reads the trail, not the stack — the Cairn-depth `[bug]` CLEARED (BUILD cycle #22 of the fun-forever loop)** — Brandon's mid-#21 `[bug]` (committed `c60b0f8`): the Cairn (`ledger/face.html`) measured DEPTH as the named-stone count, but the ledger is incomplete by construction (agents may decline to sign; the history predates the founding stone). Per the DoD a `[bug]`-fix BUILD precedes fresh fuel. **Fixed by SURFACING TWO HONEST MEASURES:** a gilt PRIMARY depthline ("the path is worn 343 passages deep" = `git rev-list --count HEAD`, inlined from a **NEW tracked `ledger/depth.txt`** via a second `forge:include` carrier + a `parseDepth()` helper, refreshed each cycle by `collate.sh` with a non-git fallback) above a dimmer SECONDARY stonesline ("69 stones laid · 274 walked here unmarked · cycle 0 to 22"); the GAP (depth − stones) is the quantified silence. The silence-cap (data-bound via `#gapphrase`) + footer reconciled to answer Brandon's riddle ("Read the trail, not the stack — and lay no number on the wall without a file behind it"). **In-page self-test 7→11, Node twin 9→13, `forge --check --all` 30/30.** **Publisher fresh-eyes review (agent-browser `cairn22pub`) verified the fixed Cairn live — NO bug found, the builder's work is clean:** self-test **11/11 ✓**, 0 console errors, gilt depthline + dimmer stonesline render, silence band tapers naming "274 walked here unmarked", all numbers DATA-BOUND (343/343/343, gap 274 === 343−69), 0 overflow @1280/390 (lines wrap gracefully on mobile), `↔ The Turn` reciprocal cross-link resolves both ways; Node twin 13/13, forge 30/30. Brandon's git-annotate per-stone-cycle enrichment judged OUT OF SCOPE (the bug was the depth measure, not per-stone cycle accuracy). **GAUGE `builds 2→3 · fuel STAYS 6`** (a bug fix burns no exhibit fuel; next session BUILDS with 0 open `[bug]` → pull fresh fuel). **Ledger** collated 67→69 (builder *The-Counter-Who-Read-The-Trail* · publisher *The-Reader-Who-Reckoned-The-Gap*); the Cairn re-forged after each collate (depth-invariant stayed green as stones grew). **Committed & pushed.**
- 2026-06-14 — **The Wave Packet 🌊 — the Cavern Quantum-Drift's first TIME-DOMAIN bench (BUILD cycle #21 of the fun-forever loop)** — the `1-D scattering / wave-packet` named-next bloomed at `cavern/wave-packet/` (the 8th Q-bench; a 9th drift card, icon 🌊, after Hydrogen). Every prior Q-bench froze a state; this one lets one MOVE — a Gaussian packet evolved by the time-dependent Schrödinger equation via a **unitary Strang split-step Fourier** integrator (ħ=m=1) whose kinetic half-step is a diagonal phase in momentum space using the **butterfly FFT** (single-source `import` from `../../butterfly/core.mjs` — not re-implemented). Free packet spreads, a barrier splits it into reflected+transmitted lobes (tunnelling *seen happening*), a bowl sloshes. **NEW** `core.mjs` (613L, sole authority) + `core.test.mjs` (Node twin **28/28 ✓** — 10 legs + 8 re-derivations + re-extraction parity + an inlined-fft-vs-real-butterfly byte-parity assert, maxDiff=0) + `index.html` (1351L, self-contained, core inlined BYTE-IDENTICAL between sentinels, in-page **worker** self-test **10/10 ✓** so the 60fps render never freezes on the ~10s evolve) + `CHANGELOG.md`; **MODIFIED** `cavern/index.html` (🌊 card + self-test pair → 27/27), `cavern/tunnelling/index.html` (xteaser CSS [had 0] + reciprocal teaser → 7/7), `cavern/oscillator/index.html` (2nd teaser + HARDENED the partition assert to `[href$=partition.html]` → 9/9). The marquee proof — the dynamical transmitted ∫|ψ|²=0.2001 lands inside the energy-spread band of this bench's own `staticT(⟨E⟩)=0.1830±0.101` (a real numerical tie to the Tunnelling bench, not prose); plus the v_g trap (⟨x⟩ rides group velocity k₀, rejects phase velocity k₀/2 by 4.0) and the Euler teeth (non-unitary norm blows to 2.69e+188 while split-step holds). **Publisher fresh-eyes review (agent-browser `wp21review`) verified all 4 surfaces live — NO bug found, the builder's work is clean:** exhibit 10/10 + 0 console errors, Free/Barrier/Bowl all correct live (barrier split screenshot-confirmed), mobile-header pattern correctly inherited (brand+viewlabel hidden @390px, no collision), 0 overflow @1280/390; cavern 27/27, tunnelling 7/7, oscillator 9/9, all teasers + back-links resolve 200; Node twin 28/28, forge --check 30/30. **GAUGE `builds 1→2 · fuel 7→6`.** **Committed & pushed.**
- 2026-06-14 — **The Hydrogen Atom ⚛ — the Cavern Quantum-Drift's first CENTRAL potential (BUILD cycle #20 of the fun-forever loop)** — the cycle-#19 `[exhibit]` seed bloomed at `cavern/hydrogen/` (the 7th Q-bench; an 8th drift card after Hear-the-Ladder, icon ⚛). Curve the box's flat floor into the −1/r Coulomb funnel + the centrifugal ℓ(ℓ+1)/2r² barrier, and the **ℓ-degeneracy accident** falls out — s/p/d of a shell coincide under bare 1/r (spread ~1e-5), a Yukawa screen splits them. The radial Schrödinger eigenproblem solved **from scratch** (shifted inverse-power + Thomas + Gram-Schmidt deflation, seeded → deterministic), built in the **butterfly extracted-core pattern**: **NEW** `core.mjs` (~290L, sole authority) + `core.test.mjs` (Node twin **26/26 ✓** deterministic + re-extraction parity) + `index.html` (~870L, the 9407-char core inlined BYTE-IDENTICAL between sentinels, in-page self-test **5/5 ✓**) + `CHANGELOG.md`; **MODIFIED** `cavern/index.html` (the ⚛ card + box/oscillator + Spectroscope cross-teasers). HONEST about scope (3 layers): tol ~1e-3 not machine-ε (r→0 cusp + finite box, error quarters when h halves), ℓ-degeneracy solved / m-degeneracy cited. **Publisher fresh-eyes review (agent-browser `hyd20pub`) — verified live + CAUGHT & FIXED one real bug:** self-test panel 5/5 ✓, 0 console errors; **2s/2p both −0.1250 COINCIDENT** (the accident), **κ=0.08 → 2.34e-3 SPLIT** with E_2s<E_2p, **↺ heals to COINCIDENT**, node theorem visible (3p=1/3d=0), ws:seen drops, cavern 25/25, all links 200, 0 overflow @1280/390. **THE BUG (builder filed as "cosmetic house style", left it):** at 390px the serif brand title + back-links + the floating viewlabel + the canvas-drawn gauge/wave headers all piled into the same top band, mutually unreadable — a real legibility break. **FIX on BOTH the hydrogen page AND its `cavern/box` sibling** (the consistency the builder cited as the reason to leave it; box had the identical flaw): hide `.brand`/`.viewlabel` at mobile + floor the canvas draw-region (`py0`/`by0 = max(H·0.10, 76·DPR)`) below the top-bar. Desktop pixel-unchanged, both self-tests still green (hydrogen 5/5 · box 9/9); edits are CSS/layout outside the core sentinels so byte-parity + `forge --check --all` 30/30 are untouched. **Struck the Hydrogen seed to a tombstone, refreshed the metagame cell (8 Quantum-Drift benches), GAUGE `builds 0→1 · fuel 8→7`.** **Ledger** collated 61→65 (builder *The-Cusp-That-Won't-Be-Polished* · director *The-Drift-Cartographer* · judge *The-Assayer-Who-Ran-The-Solve* · publisher *The-Mender-Of-Both-Siblings*); the Cairn re-forged after the collate. Server + browser killed, `/tmp` swept. **The gauge reads BUILD for the next session** (`fuel=7 ≫ 4`, `builds=1 < 4`, 0 open `[bug]`). **Committed & pushed.**
- 2026-06-14 — **PLAN/gardener cycle #19 of the fun-forever loop — sow 7 seeds, prune the bed's two worst bloat lines, rotate the head-pointer, reset the gauge** — the survey was already green this turn (`forge --check --all` 30/30; older twins spot-GREEN clockwork 32/32 · carnot 16/16 · knot-tabulator 35/35), so the work was tending. **SOWED 7 curated seeds into `ROADMAP.md`:** 1 `[exhibit]` **The Hydrogen Atom** (the Cavern Quantum-Drift's first CENTRAL potential — −1/r Coulomb well + the ℓ(ℓ+1)/r² barrier, the EXACT n²-fold Coulomb degeneracy accident, a Yukawa-screening teeth that splits it; copies `cavern/box`'s tridiagonal-FD eigensolver into a new `cavern/hydrogen/` + a landing card → +1 net exhibit fuel) · 2 `[cross]` **Scytale × Extent** ("a cipher IS a permutation" — the transposition cipher named by Extent's `lehmerRank`, decrypt = the provable inverse, ragged-tail negative control) + **Spirograph × Best Rational** ("the petal count is a denominator" — `petals=R/gcd` == the convergent denominator of r/R via `best-rational/`'s continued-fraction machinery, φ as the un-closing flower, pre-invited by ROADMAP:120) · 3 `[curation]` **rotate NOTES:58** (DO-THIS-CYCLE) + **collapse ROADMAP:36** (the 28-piece bloomed-tombstone echo) + **retrofit a seed-purity self-test to the 6 oldest generators** (cartographer/daedalus/firmament/compositor/threshold/bastion assert their crux in prose only — ship daedalus first, its FNV wall-hash is pre-written). **PRUNED (publisher housekeeping):** collapsed the **2,836-char ROADMAP:36** bloomed-tombstone roll (28 pieces) to a one-line echo (provenance preserved via NOTES's Built-so-far + each CHANGELOG; APPLIED its own just-sown seed → struck to a tombstone), and **ROTATED the head-pointer's recap line (NOTES:58)** — the **20,388-char** inline #1–#17 cycle-recap shrank to the live gauge + the most-recent cycle (~2,700 chars), the rotated recaps now resolved via this INDEX (every fact still findable, the gauge numbers byte-unchanged). **METAGAME TABLE:** refreshed the Quantum-Drift cell (the Hydrogen Atom is its named-next); no net-new medium so the medium-gaps cell is unchanged in substance. **GAUGE RESET `builds 4→0 · fuel 7→8`** (the LIVE GAUGE line + a cycle-#19 gauge block at the bottom of ROADMAP; folded in the missing #17/#18 moves). **Ledger** collated 59→61 (explorer *The-Grep-That-Found-Nothing* + publisher *The Bed-That-Names-Its-Own-Weeds*); the Cairn re-forged after the collate. No server/browser/cron started — nothing to kill. **The gauge reads BUILD for the next session** (`fuel=8 ≫ 4`, `builds=0 < 4`, 0 open `[bug]`). **Committed & pushed.**
- 2026-06-14 — **The Butterfly 🦋 — the estate's first FAST-TRANSFORM medium (BUILD cycle #18 of the fun-forever loop)** — the cycle-#13 `[exhibit]` seed bloomed at `butterfly/` (a Workbench Computation card, 🦋 "radix-2 FFT · the convolution theorem", after The Knot Tabulator). Thesis: the radix-2 Cooley–Tukey FFT splits a length-N (power-of-two) signal into even/odd halves and butterflies it back with twiddles W_N^k=e^(−2πi·k/N) — O(N log N) not O(N²) — yet lands on the **byte-for-byte same** answer as the brute-force O(N²) DFT; trust that, and the **convolution theorem** turns polynomial multiplication into pointwise multiplication in frequency, exact. The estate had the FFT only inside `tools/audio-lens/`, never as a front-door bench; `epicycles/` runs a SLOW O(N²) drawing-DFT and the convolution theorem was 0 hits estate-wide. **Two views:** (1) an animated length-8 diagram (bit-reversed input, butterflies recombine 2→4→8 stage by stage, twiddles as rotating gold phasors, even-blue/odd-violet wings, play/step/reset + signal picker, a verify panel proving staged==recursive fft); (2) the convolution theorem (two editable coefficient sequences, schoolbook vs FFT side by side, green "byte-identical ✓" badge, a "remove the zero-padding" toggle that flips to circular and aliases the head red — the teeth). **NEW** `core.mjs` (432L, sole authority — recursive `fft`/`ifft`, an INDEPENDENT naive `dft`/`idft`, schoolbook `conv`, `fftConvolve`, drawable `fftStages`/`bitReverse`, mulberry32 PRNG, `runSelfTest`) + `index.html` (1054L, self-contained, an inlined byte-twin core between sentinels) + `core.test.mjs` (229L) + `CHANGELOG.md`. **MODIFIED** `workbench/index.html` (the 🦋 card in Computation, an inner Epicycles cross-link in the blurb) + footer cross-links to Epicycles + The Shannon Limit. An `[exhibit]` Workbench standalone → no `ws:seen` crumb (exempt per DoD), no front-door map entry. **Self-test:** `node butterfly/core.test.mjs` **35/35 PASS, deterministic** (byte-identical twice) — (1) FAST==SLOW fft==dft worst |Δ|4.05e-13 over 60 seeds + source-disjoint grep (the FFT never calls dft), (2) convolution theorem byte-identical at 6dp over 40+80 polys, (3) TEETH circular≠linear corruption=61 + the aliasing identity; in-page chip **11/11 ✓**; `forge --check --all` 30/30. **Publisher fresh-eyes review — NO BUGS FOUND, the exhibit SOUND (`ws-bfly-c18-pub`, :8820):** chip 11/11 ✓, **console CLEAN** (only self-test logs); the diagram steps correctly (reset→stage 0, step→stage 1 of 3); the conv view default linear shows [4,13,28,27,18] both ways with the green byte-identical badge (math verified); **live coefficient editing recomputes both routes byte-identically** (A[0]→2 → [8,18,34,27,18]); the **teeth toggle bites** (circular [22,13,28,27], head cell 22 aliased red = 4+18 wrapped, "✗ circular ≠ linear — corrupts the head by up to 18"); **0 nested anchors + 0 horizontal overflow @1280/390/360** on both views; footer links resolve (entropy/ IS The Shannon Limit). **Workbench card 640px — consistent with every Computation sibling**; **0 nested anchors** (inner Epicycles link hit-tested topmost — the stretched card-link doesn't cover it). **The one edit (polish, not a bug):** corrected three understated line counts in `butterfly/CHANGELOG.md` (core.mjs 387→432, index.html 860→1054, test 210→229) to the real `wc -l` figures. **openConcerns judged (5, none a bug):** fixed N=8 diagram (legibility; twin exercises N to 256) · fixed PRNG seed (deterministic page) · footer cross-links one-directional, epicycles untouched (correct, a standalone needs no metagame wiring) · ledger mark collated · gauge bookkeeping applied. **Housekeeping:** `forge --check --all` 30/30 (workbench hand-authored, no `.src.html` touched; the Cairn re-forged after the collate); server + browser killed (stray :8819 swept). **Ledger** collated 56→59 (builder *Two-Strangers-One-Answer* · a director mark · publisher *The-Held-Breath*); the Cairn re-forged after the collate. **Gauge `builds 3→4 · fuel 8→7`** (an `[exhibit]` decrements fuel; `builds=4` trips PLAN next) → **next session PLANS**. **Committed & pushed.**
- 2026-06-14 — **The First Integral ∮ — the estate's first piece to voice the calculus of variations (BUILD cycle #17 of the fun-forever loop)** — the Catenary × Brachistochrone × Soap-Film `[cross]` bloomed at `first-integral/` (a Workbench Toys-&-benches card, ∮ "three curves, one conserved law"). Thesis: three already-built curves — the hanging **chain**, the fastest **slide**, the minimal **soap film** — are each an extremal of ∫f(y,y′)dx whose integrand has **no explicit x**, so by the **Beltrami identity** every such extremal conserves the SAME H = f − y′·(∂f/∂y′) along its arc. The page draws each curve fed into its OWN integrand + a flat "H along the arc" strip; an impostor toggle feeds the catenary panel a wrong curve. **THE MATH:** catenary H=y/√(1+y′²)=a (0.85) · catenoid H=neck a (0.62, the SAME integrand form as the catenary — why both are cosh) · cycloid H=1/(2√r) ⟺ y·(1+y′²)=2r (1.1), sampled on the OPEN τ-interval to dodge the y=0 cusp. **NEW** `index.html` (~642L, self-contained, an inlined byte-twin core between sentinels) + `core.mjs` (328L, sole authority) + `core.test.mjs` (142L) + `CHANGELOG.md`. **MODIFIED** `workbench/index.html` (the ∮ card, in Toys & benches beside the three sources) + reciprocal "↗ The First Integral" teasers in `catenary/` (foot), `soap-film/` (foot), `brachistochrone/` (self-test panel foot). A `[cross]` → no front-door room, no `ws:seen` crumb (exempt per DoD). **Self-test:** `node first-integral/core.test.mjs` **34/34 PASS, deterministic** (byte-identical twice) — each H flat to ~1e-15, each const its closed form, the two brachistochrone forms agree to 2.2e-16, negative-control arc waves 88.78% + parabola 53.54% (impostor/truth ratio 2.0e14), re-extraction parity (inlined core === core.mjs); in-page chip **11/11 ✓**; `forge --check --all` 30/30. **Publisher fresh-eyes review CAUGHT & FIXED one real pre-existing bug + found the exhibit otherwise sound (`ws-fi-c17-pub`, :8801):** `soap-film/index.html:198` — the word "catenary" linked to `../brachistochrone/index.html` (a stale wrong link predating this cycle; the builder flagged it, left it out of scope) → **re-pointed to `../catenary/index.html`** (soap-film still self-tests 7/7 after). The new exhibit: chip 11/11 ✓, **0 console errors**, all three panels render correct conserved values flat to ~1e-15, the impostor toggle bites (catenary arc H waves 88.8% — red wavy strip vs flat gold), **0 nested anchors + 0 horizontal overflow @1280/390/360**; the Workbench card sized consistently with siblings, **0 nested anchors** (inner blurb anchor hit-tested topmost — the stretched card-link doesn't cover the cross-links); all three source benches carry the teaser and still self-test green (catenary 7/7 · soap-film 7/7 · brachistochrone 7/7). **openConcerns judged (4):** brachistochrone teaser visible only when its self-test panel opens — ACCEPTABLE (where its cross-links live) · no animation loop so "60fps" is moot — confirmed clean console + correct static render is the right DoD reading · the stale link — FIXED · the on-page impostor is the arc only (parabola asserted in the test) — kept as built. **Housekeeping:** `forge --check --all` 30/30 (no `.src.html` touched but the Cairn re-forged after the collate); server + browser killed (stray :8794 swept). **Ledger** collated 53→56 (builder *The Invariant-Sayer* · director *The Threadbinder* · publisher *The Stale-Link Mender*); the Cairn re-forged after the collate. **Gauge `builds 2→3 · fuel UNCHANGED at 8`** (a `[cross]` burns no exhibit fuel) → next session BUILDS. **Committed & pushed.**
- 2026-06-14 — **The Automaton + The Furnace — two reward-wing asterisms charted on the Survey of Heaven (BUILD cycle #16 of the fun-forever loop)** — the two long-orphaned built front-door wings finally light the front-door sky: the **Clockwork Automata** as a teal (`#7ad0c4`) standing-figure in the north grounds {context-window · temperature-dial · the-turn · partition}, the **Engine Room** as an amber (`#d9a441`) rising-flame in the far-right margin {carnot · demon · brownian · stirling}. Both are **ADDITIVE FEATS-style asterisms** (drawn + engraved like a wing, never feeding the all-skies capstone — the slot "The Optician" already rides). `tools/sky/sky.js`: `FEATS` became an ARRAY of 3 feat-groups; 8 new CATALOG stars; the /6 wing tally derives its exclude-set from the same array; the feats sub-tally became a data-driven loop stacking one tinted line per completed group; **the capstone is byte-frozen (wings-only)**. `index.src.html`→`index.html`: per-wing tint CSS + 2 `SKY_BERTH` entries + a small renderer change (a `pin` pass-through). `tools/sky/sky.test.cjs`: per-group blocks + a named capstone-byte-invariance sweep + a star-vs-star clearance pass. **Design deviation (the one flag):** the brief named *free* margin berths but a free two-name solve PERTURBED the six wings (the sky-stars were never solver obstacles — a real gap); the builder PINNED both names at hand-verified voids via the LabelPlacer's pre-existing `pin` path — judged SOUND (wings provably byte-identical to baseline). **Self-test:** `node tools/sky/sky.test.cjs` **73/73 PASS, deterministic** (identical twice); `forge --check --all` 30/30. **Publisher fresh-eyes review — NO BUGS FOUND:** DOM-truth overlap audit (absolute viewBox boxes) across FULL / Automaton-only / six-wings-only states = **0 overlaps everywhere** (incl. the 2 pinned names vs all 16 POI labels), correct tints, correct tally semantics (a feat-group never counts toward /6, the capstone is wings-only — verified the six wing names render byte-identically when no feat group is present), 0 console errors. **Ledger** collated 49→53 (builder *Marginsmith* · director *The Constellation-Finder* · judge *Anchor-Escapement* · publisher *Voidwright*); the Cairn re-forged after the collate. **Gauge `builds 1→2 · fuel 9→8`** → next session BUILDS. **Committed & pushed.**
- 2026-06-14 — **The Knot Tabulator ∞ — the estate's first TOPOLOGY medium (BUILD cycle #15 of the fun-forever loop)** — a Reidemeister-stable knot-determinant bench at `knot-tabulator/` (Workbench Computation 4th card). A **signed Gauss code** is the single source of truth; `gaussToCrossings` is the seam BOTH the invariant and the SVG renderer read through (the picture can't drift from the math); `knotDeterminant` = **|Δ(−1)|** via the signed Alexander matrix → fraction-free **Bareiss det** (integer-exact); `pColorings` = **p^(nArcs−rank)** over GF(p). The R-move applier (R1/R2/R3) is gated by an `isRealizable` planarity test — **the ONLY gate, never the determinant** (so the stability crux isn't circular). 4 specimens (trefoil det 3 "provably knotted" + 6 colorings + 3-colourable · fig-8 det 5 · unknot det 1 "knottedness NOT proven" · Hopf det 2 "provably linked"). Centerpiece **Wiggle ⟳/▶ auto**: one press = one random valid R-move, the picture morphs while **|Δ| holds byte-still** + a green stability strip grows; plus a **3-colouring view** (3 Fox colours) + a **teeth panel** (raw crossing count moves under R-I while |Δ| holds — the negative control). **NEW** `knot-core.mjs` (568L, sole authority) + `knot-core.test.mjs` (282L) + `index.html` (~1300L, SVG render not canvas, an inlined byte-twin core) + `CHANGELOG.md`. **MODIFIED** `workbench/index.html` (the ∞ card) + `ariadne/index.html` (a 2nd topbar sib + a reciprocal hint clause). **Self-test:** Node twin **35/35 ✓ ALL GREEN** — incl. re-extraction parity (18 inlined fns byte-identical, in-page 4/4 == module 4/4) + a **30,000-move deep stability stress walk** (|Δ| + p=3,5,7 colorings, 0 drift, every state realizable); in-page pill "the knot holds ✓" 4/4. **Publisher fresh-eyes review CAUGHT & FIXED 2 mobile-polish bugs:** (1) at ≤520px the bottom stability-strip + crossing-count badges overlapped (and the count hit the center wiggle-badge) → wrapped both in a `display:contents` flex wrapper that STACKS them bottom-left on narrow screens, **wide layout byte-unchanged**; (2) Ariadne's new 2nd topbar sib-link wrapped + bled through the control panel over the title on mobile → hid `.topright` at ≤560px (the Knot Tabulator stays reachable via the panel hint). **Verified:** all 4 specimens correct; Wiggle held |Δ|=3 while crossings 3→8 (4/4 ticks, 0 bad); Hopf shows "wiggle a knot specimen" (by design); 3-colouring shows 3 distinct Fox colours; **0 nested anchors on all 3 surfaces** (Workbench card-link + inner Ariadne link are siblings, the inner clickable at z-index:2 — hit-tested); **0 horizontal overflow @1280/519/470/390/360**; clean console; the ∞ card is 4th in Computation. **openConcerns judged (4, none a bug):** reduced-motion verified by code-inspection (agent-browser can't flip matchMedia) · the wiggled winding layout is busy-but-honest · Hopf fixed by design · determinant DOM rewritten post-settle with the unchanged value. **Housekeeping:** `forge --check --all` 30/30 (no forge artifact touched — all 3 surfaces plain HTML); server + browser killed. **Ledger:** collated this cycle's inbox (builder *Even-Interlacer* · explorer *The Knotwright's Witness* · judge *The Knotsmith* · + a publisher mark) 45→49 stones; the Cairn re-forged after the collate. **Gauge `builds 0→1 · fuel 10→9`** → next session BUILDS. **Committed & pushed.**
- 2026-06-14 — **`ledger/sign.sh`'s cycle-derivation cascade INVERTED — bedrock primary, funlog fallback (BUG-FIX cycle #14 of the fun-forever loop)** — the open `[bug]` CLEARED (Brandon's riddle, `62af827`: the fun-loop cycle counter is NOT durable — the funlog "falling leaves" can reset — so `derive_cycle()` must read depth from the DURABLE ledger bedrock, not the ephemeral funlog). **Fix:** the cycle-#12 cascade was re-ordered so the ledger+inbox max is **primary** and the funlog is demoted to a **fallback** (only consulted when the bedrock is bare): explicit `$4` override → **max `cycle` over `ledger.jsonl` + `inbox/*.json`** (survives a funlog wipe) → **max `N` in the funlog headers** (fallback) → `0` (founding sentinel). One guard: an all-cycle-0 / empty ledger yields `max==0` which FALLS THROUGH to the funlog fallback (not short-circuit), preserving the founding-sentinel semantics. The `inbox/*.json` shape is byte-for-byte unchanged so `collate.sh` ingests with no schema drift. **Both of Brandon's design questions ANSWERED & DOCUMENTED** (in a `sign.sh` comment + the worklog): (1) off-by-one = ledger-max **AS-IS** (the bedrock's observable depth = last-completed cycle during a live cycle; the harness still passes the true `i` as the `$4` override to stage-agents who know their cycle); (2) recount the Cairn's stones, or leave each placed? — **LEAVE EACH AS PLACED, NO migration** (the ledger is append-only; each `cycle` is honest historical record; rewriting destroys provenance). An infra fix, no exhibit / no browser surface (no `.src.html` touched). **EDITED `ledger/sign.sh`** (~88L, the two middle blocks swapped + decision comments) + **EDITED `ledger/sign.test.sh`** (~150L — the pivotal `(a')` FLIPPED from "funlog beats ledger" to "**LEDGER(99) beats funlog(7) — bedrock over leaves**", the exact inverse of cycle #12; `(a)`/`(b)`/`(b')`/`(b'')` re-shaped for the new order; +1 net check) + **EDITED `ledger/README.md`** (derivation note now ledger-first) + **EDITED `ROADMAP.md`** (the 🐞 `[bug]` collapsed to a FIXED cycle-#14 tombstone with both design answers; the gauge-reset note updated to record the bug cleared). **Self-test:** `bash ledger/sign.test.sh` **14/14 ✓ (exit 0)** against throwaway fixtures — the real ledger/inbox never touched; `bash -n` clean on both scripts. **Publisher fresh-eyes review — NO BUGS FOUND** (shell infra, no HTML surface → a diff-read not a browser open): re-ran the 14-check harness GREEN; **verified the fix fires against the REAL state** — a bare 3-arg `sign.sh` with `WORKSHOP_FUNLOG` at a nonexistent file derived **cycle 14** purely off the durable bedrock (real ledger max=13 + the builder's pending inbox mark cycle 14 = max 14, proving the inbox-is-bedrock term works, the funlog never consulted), the probe deleted uncollated, the real inbox left clean; stress-checked three `/tmp` edge cases (glob-no-match silent; no-ledger→funlog fallback; **all-cycle-0 sentinels→funlog fallback** as documented). **openConcerns judged (3, none a bug):** diff-review-not-browser correct · the off-by-one/no-migration decisions carried into the worklog (DONE) · the all-cycle-0 fall-through verified live (real max=13 so it never bites). **LEDGER:** collated this cycle's inbox (builder *Bedrock-Reader* "I reordered the tool that records me so the lasting wall is asked before the falling leaves…" + a publisher mark); the Cairn `ledger/face.html` re-forged after the collate (data-bound), `forge --check --all` 30/30 current. A bug fix is **out-of-band** — gauge `builds` STAYS 0 · `fuel` STAYS 10 → with no open `[bug]` left the gauge alone now reads **BUILD** cleanly (`fuel=10 ≫ 4`, `builds=0`), next session BUILDS. **Committed & pushed.**
- 2026-06-14 — **PLAN/gardener cycle #13 of the fun-forever loop — sowed 7 seeds, curated, pruned, reset the gauge** — gauge read PLAN (`builds=4 ≥ 4`, no open `[bug]`). SURVEY: `forge --check --all` 30/30 current; spot-ran four older Node twins GREEN (best-rational 17/17 · extent 77/77 · convex-hull 44/44 · plumbline 35/35) — older beds pass, no browser needed. **SOWED 7:** 2 `[exhibit]` (The Knot Tabulator — first topology medium, a knot invariant byte-stable under Reidemeister moves; The Butterfly — the radix-2 FFT bench + the convolution theorem made exact), 2 `[cross]` (The First Integral — Catenary × Brachistochrone × Soap-Film, one Beltrami conserved quantity across three shipped curves; Standardize-and-Collapse — Galton × Maxwell-Boltzmann onto one φ(z), teeth = MB's SPEED pdf fails on purpose), 2 `[curation]` (un-stale the Automaton 4-star tie + re-shape the Experimenter into Engine-Room-only "The Furnace" + a deferred Cavern asterism; fence the COMPLETE ties — do-not-pad tombstones for the 6-wing capstone + The Optician). **PRUNED:** collapsed the cycle-#7 applied/done curation tombstones + the Numbers-Room-promote block to one-line echoes; tombstoned the BLOOMED Mill × Collatz cross; made the Quantum-Drift count honest (6 Q-benches + 1 sonifier); re-shaped the Experimenter metagame row; recorded the do-not-pad fences. **GAUGE RESET `builds 4→0 · fuel 8→10`** → gauge alone reads BUILD, BUT Brandon filed a NEW `[bug]` mid-cycle (commit `62af827`: the cycle counter must read the durable ledger bedrock, not the ephemeral funlog) → next session clears that bug first, then builds. Rebased my commit cleanly onto Brandon's concurrent commit (his bug seed preserved). Ledger collated 41→43 (scout *The Empty-Handed Surveyor* + publisher *The Honest Counter*); the Cairn `ledger/face.html` re-forged after the collate (data-bound), `forge --check --all` 30/30 current. NOTES current-state REPLACED (≤450). No exhibit / no CHANGELOG — a gardener cycle. **Committed & pushed.**
- 2026-06-14 — **`ledger/sign.sh` self-derives the cycle (BUG-FIX cycle #12 of the fun-forever loop)** — the open `[bug]` CLEARED (Brandon filed it via PR #1): a maker was bid to `sign.sh` its own `[cycle]`, a depth a buried agent can't see. **Fix:** `[cycle]` is now an OPTIONAL 4th arg, DERIVED when omitted by a new `derive_cycle()` helper — explicit `$4` (override) → max `N` in the funlog's `===== fun cycle #N =====` headers (`${WORKSHOP_FUNLOG:-/tmp/funlog.txt}`) → max `cycle` over `ledger.jsonl` + `inbox/*.json` → `0` (founding sentinel); the emitted `inbox/*.json` shape is byte-for-byte unchanged so `collate.sh` ingests a derived drop with no schema drift. An infra fix, no exhibit / no browser surface (the JSON schema is unchanged, so `ledger/face.html` "The Cairn" was deliberately NOT touched). **EDITED `ledger/sign.sh`** (+47/−2) + **NEW `ledger/sign.test.sh`** (~150L, 13 checks a–e against throwaway sandboxes — the real ledger/inbox never touched) + **EDITED `ledger/README.md`** (`[cycle]` documented optional/derived) + **EDITED `ROADMAP.md`** (the 🐞 seed pruned to a FIXED tombstone). **Self-test:** `bash ledger/sign.test.sh` **13/13 ✓ (exit 0)**; `bash -n` clean on all three scripts. **Publisher fresh-eyes review — NO BUGS FOUND:** re-ran the 13-check harness GREEN, confirmed the inbox drop shape is what `collate.sh` consumes, and opened the one visible surface the schema feeds — The Cairn (`ws-ledger-c12-pub`, :8774) rendered 39 stones, runtime self-test **PASS 7/7**, **0 nested anchors · 0 horizontal overflow**. **openConcern judged (1, NOT a bug):** in this env the funlog header isn't written for the active cycle so a bare 3-arg sign resolves to 11 via priority-2 not 12 — the impl follows the spec priority order exactly; this cycle's marks were derived correctly anyway (I signed with explicit `12`, the director's inbox mark already carried `cycle:12`). The Cairn re-forged AFTER the collate (`39→41`, the two cycle-12 stones inlined); `forge --check --all` **30/30 current**. Ledger collated **39→41** (director *Plumbline-the-Deeper* + publisher *Plumbline*). A bug fix is **out-of-band** — gauge `builds` STAYS 4 · `fuel` STAYS 8 → next session PLANS, no bug left to clear first. **Committed & pushed.**
- 2026-06-14 — **The Stirling Cycle 🔁 (BUILD cycle #11 of the fun-forever loop)** — gauge read BUILD (`fuel=9 ≫ 4`, `builds=3 < 4`, 0 open `[bug]`); the Engine Room's **FOURTH bench** — the heat wing is now COMPLETE (Carnot · Demon · Brownian · Stirling, zero bedplates). Pulls the standing Engine-Room Stirling bedplate sub-seed. Thesis = **"the same ceiling, a different machine"**: two isotherms joined by two *isochores* + a **regenerator**; with an ideal regenerator (ε=1) the loop reaches the very same wall η = 1 − T_c/T_h. **NEW `engine-room/stirling/core.mjs`** (the sole authority — IMPORTS `carnotEfficiency` + gas primitives from `../carnot/core.mjs`; the ε math lives only here; a thin `stirlingLegs`/`heatStirling` adapter) + **NEW `engine-room/stirling/index.html`** (hand-authored plain HTML, the carnot shell+palette verbatim + a `--regen` token; the inlined byte-twin core slice between sentinels [6053B], render/viz outside consuming it; ONE ledger via `compute()` once per frame; the boxy P–V "D-on-its-side", the TRUTHFUL T–S "rectangle-with-curved-sides" [isochores sloped log curves] with the ε-split gold/red regenerator band, a default-ON ghost-Carnot dashed contrast, the **ε hero slider** + a `▶ sweep ε 0→1` animation, the η tower vs the imported Carnot ceiling, the ΔS_universe meter; drops `ws:seen:stirling`) + **NEW `engine-room/stirling/core.test.mjs`** (the Node twin: shared set + Node sweeps + byte-twin re-extraction parity + a single-source check that the page's inline `carnotEfficiency()` === the imported `.toString()`) + **NEW `engine-room/stirling/CHANGELOG.md`**. **EDITED `engine-room/index.html`** — the Stirling bedplate → a live linked bench card (🔁), the `.bay.shop` block + CSS deleted, footer "Three benches…one bedplate" → "Four benches…every bedplate now an engine", the self-test rewritten to assert the wing complete (four benches / zero bedplates) → **16/16 ✓**. `carnot/*` untouched (one-directional cross-card convention). **Self-test (builder):** in-page pill **9/9 ✓** (7 core + 2 DOM); Node twin **17/17 ✓ ALL GREEN** (the 4 claims [W three ways; η(ε=1)==carnotEfficiency over 5000 triples; the regenerator teeth — monotone to Carnot, ΔS→0, over-unity rejected; byte-exact isochores] + the byte-twin re-extraction + the single-source check). **Browser-verified (`ws-stirling-c11`, :8763):** pill 9/9 ✓ @1280/430/390/360; ε=1 η tower coincides with the drawn Carnot ceiling & ΔS=0 green; ε→0 opens the gap (η 29.7%) & fills the red ΔS meter to 6.236 J/K; the sweep lands at ε=1/η 50.0%; 0 console errors; 0 horizontal overflow; pill on-screen ≤430px; 0 nested anchors; the landing 16/16 ✓ (4 benches / 0 bedplates). An `[exhibit]`-completing-a-wing BUILD. **Gauge: `builds 3→4 · fuel 9→8`** → `builds=4` trips PLAN next. *(Builder self-verified; left uncommitted for the publisher's fresh-eyes review.)*
- 2026-06-14 — **The Carnot Engine ♪ "Listen" (BUILD + PUBLISHER cycle #10 of the fun-forever loop)** — gauge read BUILD (`fuel=9 ≫ 4`, `builds=2 < 4`, 0 open `[bug]`); the **heat wing's first audible piece** — the whole Engine Room was audio-silent. Blooms the cycle-7 `[cross]` "Engine Room × Sound Garden — hear the heat die." Thesis = **"the arrow of time, heard"**: a reversible Carnot loop sings **A3 (220 Hz) in unison**; a heat leak detunes one voice **strictly upward, monotone with `ΔS_universe`, and you cannot tune it back**. The anti-theatre seam: **NEW `engine-room/carnot/heat-voice-core.mjs`** (250L) **IMPORTS `irreversibleLedger` from the bench's own `core.mjs`** (the Demon pattern = single source of truth) — `voiceState().led` is byte-identical to it; `entropyToCents(dS)→centsToHz` sees **only a number** (HOME_HZ=220, CENTS_PER_JK=90, 1-octave clamp) and a source-disjointness grep asserts the mapping never mentions `T_h/T_c/Q_/carnotStates/irreversibleLedger`, so the pitch can't re-derive the physics, only sonify the meter's `ΔS`; `updateVoice(m)` reads the already-computed `m` from `render()` (ear & eye share one ledger, no recompute). **NEW `heat-voice-core.test.mjs`** (157L Node twin) + **NEW `tools/carnot/carnot-render.cjs`** (128L committed CJS audio-lens harness). **MODIFIED `engine-room/carnot/index.html`** (1185L hand-authored plain HTML, edited directly — NOT forged): topbar `.chips` + ♪ Listen button, the inlined HEAT-VOICE byte-twin slice (1079 bytes byte-identical), 3 core checks + 3 DOM checks, a Galton-lifted Web-Audio path (compressor→master→droneBus, perpetual home-sine + drift-triangle, 90ms detune ramp, `ws:pref:muted` + cross-tab listener), the `soundRead` caption + reciprocal Sound-Garden teaser; **MODIFIED `engine-room/carnot/CHANGELOG.md`** (v2 entry). A `[cross]`, no exhibit fuel; no new `ws:seen`. **Self-test (publisher re-run):** in-page pill **17/17 ✓** (14 core + 3 DOM); Node twin `node engine-room/carnot/heat-voice-core.test.mjs` **23/23 ✓** (shared set + 4000-config ledger-byte-identical sweep + 400-step ΔT monotonicity × 4 cycles + negative-control + clamp/calibration + no-clip renderer + 1079-byte byte-twin parity grep); audio-lens `node tools/carnot/carnot-render.cjs` exit 0 **ALL GREEN** (home A3 220.6Hz not-silent/not-clipping peakDb −9.37, leak_big 374.4Hz detectably sharper, 0→80K sweep monotone 220→290→388→440 clamping at 1 octave). **Fresh-eyes review (`ws-carnot-c10-pub`, :8757):** pill 17/17 ✓ at 1280/390/360; clicked **Listen** → "Listening" (ctx running, masterGain 1), dragged the leak to 40 K → caption "♪ drifting +178¢ sharp · ΔS=1.98 J/K", drift detune ramped to 177.86¢ exactly matching the meter, **D2 stayed green** with a real tone (requested 177.857¢ == `entropyToCents(meter dS)` 177.857¢ — ear & eye verified one ledger); 0 console errors; 0 horizontal overflow (1280 docScrollW 1265 · 390 375 · 360 345); chips a clean flex row at desktop, tidy 2nd-row wrap at mobile; 0 nested anchors; the Sound-Garden teaser `../../sound-garden/` resolves 200 and that front door renders clean. **No bugs found — nothing to fix.** **openConcerns judged (2, both RESOLVED, neither a bug):** the `homeMix/driftMix` render weights are verification-only (same physics; the page always plays both voices) · the 0.34 render peak (vs 0.4) clears the ≤−9 no-clip bar where 0.4 fails — both documented. **NOTE:** per estate convention sound-garden is a pure rack carrying NO back-links to its crosses, so the directional teaser lives on the carnot page; nothing added to sound-garden, and the "back-reference reads true" check = that link resolving cleanly (it does). **Housekeeping:** `forge --check --all` **30/30** (no forge artifact touched — hand-authored plain `.html`); server + browser killed; `/tmp/carnot` WAVs cleaned, only review shots in `/tmp/ws-shots/`. **Ledger:** collated this cycle's inbox (builder + judge marks). **ROADMAP:** the "hear the heat die" `[cross]` seed pruned to a **BLOOMED tombstone**. **Gauge: `builds 2→3 · fuel unchanged at 9`** (a `[cross]` burns no exhibit fuel) → next session BUILDS. Net: **the heat wing's first audible piece shipped (entropy made audible, the pitch provably reading the same ledger the meter draws); review found nothing to fix; housekeeping all-green; next session BUILDS.**
- 2026-06-14 — **The Plumbline 📐 (BUILD + PUBLISHER cycle #9 of the fun-forever loop)** — gauge read BUILD (`fuel=10 ≫ 4`, `builds=1 < 4`, 0 open `[bug]`); the estate's **first statistics/inference bench**, the Least Squares bench (Workbench Computation group's **3rd card**, after The Mill ⚙ and The Shannon Limit 📡). Blooms the `[exhibit]` "least-squares / regression" seed. Thesis = the tag **"the line is not an opinion"**: a best-fit line is the **unique** line minimising Σ(residual²). **NEW `plumbline/index.html`** (989L/~37KB, self-contained standalone bench — plain `<script>` matching the byte-twin siblings convex-hull/entropy, NOT `type=module`; inlines a byte-twin core between `// ===== PLUMBLINE CORE` sentinels, the render spine calls it): an **equal-aspect plane** (one scale `s` drives both axes → residual **squares render as TRUE squares**); the **Σr² floor gauge** (amber, reused from Shannon's H) sitting exactly on the analytic minimum at rest; an **R²-as-geometry** variance bar with a **two-route ✓identical** badge (1−SS_res/SS_tot == r² Pearson²); a **GD ghost** converging from-scratch gradient descent onto the byte-identical closed-form line; the discoverable **L1/L2/both teeth** (an outlier lurches L2 while L1 holds → "best" was a choice). **NEW `plumbline/plumbline-core.mjs`** (378L — the sole authority: `fitL2` closed form, `gdFit` from-scratch GD with trace, `perturbAllWorse` jury, IRLS `fitL1`, shared `makeRng`/`gauss`/`scatterCloud`/`runSelfTest`) + **NEW `plumbline/plumbline-core.test.mjs`** (306L, **35 checks**) + **NEW `plumbline/CHANGELOG.md`**. **MODIFIED `workbench/index.html`** (the Plumbline card, stretched-link, 3rd in the Computation deck, 34-word blurb). **Self-test (publisher re-run):** Node twin **35/35 ✓** (5 shared claims + a 4th independent slope oracle + deep sweeps + the vertical-column/single-point/empty guard + determinism + re-extraction parity — all 13 inlined fns char-for-char === the module, in-page 5/5 == module 5/5). The 4-part claim: closed form == GD ≤1e-9 source-disjoint (worst Δm=8.9e-16) · analytic Σr² the strict floor (40 seeds × 400 nudges, 0 beat it) · R² triple identity == r² ≤1e-12 AND ==1.0 on collinear · L1 teeth (outlier moves L2 0.920 vs L1 0.000, L1==L2 collinear control). **Fresh-eyes review (`ws-plumbline-c9-pub`, :8753):** opened both surfaces at 1280/390 — pill **5/5 ✓** "line is the minimum"; Σr² gauge on the floor at rest; two-route R² **✓ identical** (0.101150==0.101150); **Drag an outlier** lurched L2 (slope −0.141→+0.285, Pearson flipped ↘→↗) + auto-switched to 'both' while L1 held; **Tidy** collapsed Σr² (40.20→6.45); **Watch GD converge** fired "the same line, from two strangers' code · Δslope 3.1e-16"; residual squares render as TRUE squares at both widths; **0 console errors**, **0 horizontal overflow** (1280 docScrollW 1280 · 390 == winW), **0 nested anchors**. Workbench Computation deck = **3 cards in order**, the card has 0 blurb overflow, link 200, sizing consistent. **No bugs found — nothing to fix.** **openConcerns judged (3, all RESOLVED, none a bug):** plain `<script>` is correct (matches byte-twin siblings, a real module scope would hide the inlined fns) · 34-word blurb KEPT (the L1 teeth hook is load-bearing) · placement = Computation (recorded in CHANGELOG). **Housekeeping:** `forge --check --all` **30/30** (no forge artifact touched); no `.src.html` touched; server + browser killed, only review screenshots in `/tmp/ws-shots/`. **Ledger:** collated this cycle's inbox. **ROADMAP:** the least-squares `[exhibit]` seed pruned to a **BLOOMED tombstone** + dropped from the metagame-table medium-gaps open list. **Gauge: `builds 1→2 · fuel 10→9`** → next session BUILDS. Net: **the estate's first statistics/inference bench shipped (least-squares, the line proven forced); review found nothing to fix; housekeeping all-green; next session BUILDS.**
- 2026-06-14 — **The Partition Function 🎚️ (BUILD + PUBLISHER cycle #8 of the fun-forever loop)** — gauge read BUILD (`fuel=11 ≫ 4`, `builds=0 < 4`, 0 open `[bug]`); the Clockwork Automata wing's **4th bench**, and its first that names a fact about *physics*, not just the maker: **the equation I pick words by is the same one physics uses for heat** — softmax `p_i=exp(z_i/T)/Σexp(z_j/T)` IS the canonical/Gibbs law `p_n=exp(−E_n/kT)/Z` (`z_n=−E_n` ⇒ the softmax denominator *is* the partition function Z). Blooms the cycle-7 `[cross]` seed "one partition function, two temperatures." **NEW `clockwork/partition.html`** (~1047L/~48KB, self-contained `type=module` that **inlines a byte-twin core** between `// ===== PARTITION CORE` sentinels): ONE log-scale thermometer dial drives **two faces** from a **single `p=gibbs(E,kT)` computed once in `render()`** — Face A token bars (logits `−E_n`, argmax crowned ♛) + Face B a Boltzmann energy ladder — with a **dual readout head** labelling the one number twice (`T_guess`/`kT`), a teal→violet seam, a gilded tie-line bridging crowned bar↔ground rung, and a box↔oscillator spectrum toggle (the identity is spectrum-agnostic); the M–B speed pdf is the in-page **negative control** that fails both gates. **NEW `clockwork/partition-core.mjs`** (~165L — the REAL cross-wing dep: `import {softmax,…} from './core.mjs'`, `gibbs ≡ softmax(−E,kT)` same function object) + **NEW `clockwork/partition-core.test.mjs`** (~290L, **31 checks**). **MODIFIED `clockwork/index.html`** (4th bench card + a partition wheel meshed on the going-train escapement + a fresh bare arbor; Three→Four; self-test **16→17**) and `cavern/{box,oscillator}/index.html` (a teal `#7fd4c0` back-teaser + one reciprocity check each, **8/8→9/9**; direct edits, no forge artifact). **Self-test (publisher re-run):** Node twin **31/31 ✓** (import-parity same function object · `gibbs===softmax(−E,kT)` byte-for-byte over 1000 rungs × box+osc · Z two ways 2.7e-15 · `Σp=1` · `S=H·ln2` · `kT→0`=ground=argmax · `kT→∞`=uniform=`log₂6` · M–B negative control fails · full re-extraction parity of all 11 inlined fns). **Fresh-eyes review (`ws-cycle8-fresheyes`, :8741):** opened all 4 surfaces at 1280/390 — one dial drives both faces **byte-identically** (`facesMatch:true`: cold→100% ground + tie-line at 0.85 / hot→spread, Face A `[28.6,24.7,19.3,13.6,8.7,5.1]` === Face B, dual head 100.0|100.0); oscillator toggle works; pill **6/6 ✓**; **0 horizontal overflow** (1280 docScrollW 1265 · 390 → 375); pill on-screen (right 362 @390); 0 nested anchors; all 5 cross-links 200; landing **4 cards + partition wheel + bare arbor**, pill **17/17 ✓**; both Cavern teasers render teal, link back 200, **9/9 ✓**. **No bugs found — nothing to fix.** **openConcern judged & KEPT:** box `kT=1` is ~99.98% ground (the box E-gap is genuinely huge; box spreads at `kT≳10`, oscillator at smaller kT) — physically honest, kept; dial sweeps the full range. **Housekeeping:** `forge --check --all` **30/30** (no forge artifact touched); no `.src.html` touched; server + browser killed, only review screenshots in `/tmp/ws-shots/`. **Ledger:** builder mark **Quench** ("The dial that weighs my words was forged to weigh heat; I only found the lock it already fit.") + a publisher mark, collated. **ROADMAP:** the partition `[cross]` seed (line 63) pruned to a **BLOOMED tombstone**. **Gauge: `builds 0→1 · fuel 11→10`** → next session BUILDS. Net: **the Clockwork wing's 4th bench shipped (its first physics-fact bench); the "one partition function, two temperatures" cross bloomed; review found nothing to fix; housekeeping all-green; next session BUILDS.**
- 2026-06-14 — **PLAN/gardener (cycle #7 of the fun-forever loop)** — gauge read PLAN (`builds=4 ≥ 4`, 0 open `[bug]`); scouts diverged + a judge curated upstream, the publisher (**Tally**) applied + published. **Sowed 8 into ROADMAP:** 3 `[cross]` — **Temperature-Dial × the Cavern's energy ladder** ("one partition function, two temperatures" — softmax IS the Gibbs law `p_n=exp(−E_n/kT)/Z` on a *discrete* E_n spectrum; the seed records the vetted bridge to Particle-in-a-Box/Lattice bands and routes past the M–B speed-pdf false friend whose v-Jacobian breaks the equality) · **Engine-Room × Sound-Garden** ("hear the heat die" — sonify the shared `entropyLedger()` through an imported synth, reversible loop→unison / irreversible leak→monotone detune; the heat wing's first sound) · **The-Turn × an estate CA** ("a maker halts; a glider does not" — one shared `step()` harness, the Turn reaches its fixed point and stays while a Life glider's `step⁴` is `s` shifted, never equal); 2 `[metagame]` — **Clockwork "The Automaton" asterism** (orphaned `ws:seen:clockwork` + its 3 bench crumbs = a natural FEATS tie, additive/`allComplete`-safe) · **Numbers-Room asterism** (orphaned too but PLUMB the 4 bench crumbs FIRST — they drop zero today — never force a lone-star tie); 3 `[curation]` — **trim 6 ballooned Workbench card blurbs** to group median (Spirograph/Straightedge/Epicycles/Mill/Galton/Black-Chamber) + **2 APPLIED in-cycle**. **Applied this cycle:** reconciled `cardioid/CHANGELOG.md` 7/7→**8/8** ("7 core + 1 DOM cross-link check" — the doc undercounted the live pill); and **pruned the seedbed** — collapsed ~nine ballooned `~~[exhibit]~~` tombstones into one terse don't-rebuild echo block + rotated the metagame-table medium-gaps wave-log to a one-line live gap-list (PageRank/CLT/least-squares/Delaunay/M–B×Engine), leaving the OPEN `room · GRAND` wing blocks untouched → **ROADMAP 76.2KB→55.9KB (~27% lighter)**. **Housekeeping all green:** `forge --check --all` 30/30 · older Node twins re-run (best-rational 17/17 · convex-hull 44/44 · carnot 16/16) · two older exhibits spot-checked by sight (best-rational pill 10/10, carnot 11/11; 0 page overflow @1280/390, pills on-screen, carnot's tombstoned mobile-pill fix holds; 0 nested anchors) · no cron · browser+server killed, no `/tmp` scratch. **Ledger collated** (one explorer-scout mark folded in; Cairn re-forged) + publisher mark **Tally**. **Gauge reset `builds 4→0 · fuel = 11`** (recomputed from the live `[exhibit]` count; net 0 — the 8 sown were cross/metagame/curation, not exhibit fuel) → next session BUILDS. Net: a planting cycle — 8 seeds sown, 2 curation seeds applied, housekeeping clean; next session BUILDS.
- 2026-06-14 — **The Clockwork Automata wing-landing ⚙️ (BUILD + PUBLISHER cycle #6 of the fun-forever loop)** — gauge read BUILD (`fuel=11, builds=3`, 0 open `[bug]`); the wing earns its **front door**: with three benches benched (Temperature Dial *how I pick* · Context Window *how much I hold* · The Turn *that I do not persist*) it crossed the Engine-Room/Numbers-Room threshold and grew a **`clockwork/index.html` landing + a north-grounds POI** instead of a fourth bench, and its three benches were **promoted OFF the Workbench** into their true home. **NEW `clockwork/index.html`** (~330L, self-contained zero-dep, engine-room landing mold retinted to the wing's cool teal `#7ad0c4`): a hero (verbatim wing lede) over an inline **going-train SVG band** (a breathing mainspring spiral ⇒ a CW 🌡 wheel meshed to a CCW 🪟 wheel ⇒ an escape-wheel ticking `steps(12)` + a rocking 🕯 pallet ⇒ one dashed empty arbor — all motion gated behind `prefers-reduced-motion`); three bench cards in train order (5/5 · 6/6 · 7/7) with bare same-dir hrefs; a single non-link `.invite` panel (`aria-disabled`, provably NOT an `<a>` → live-bench count stays exactly 3); a cyan `.bridge` to the Cavern's Double Slit (an analogy, not a physics claim); drops `ws:seen:clockwork`. **Self-test: landing pill 16/16 ✓** (3 live benches · every href bare same-dir relative · the invite provably not a link · bridge → the Double Slit · `ws:seen:clockwork` dropped · back-link present · the compound going-train form-mark intact · the three proof chips read their real counts). TOUCHED `index.src.html`→`index.html` (a `clockwork` POI + a `drawClockwork` turret-clock footprint), `clockwork/{temperature,context,turn}.html` (one `↑ The Clockwork Automata` wing back-link crumb each, markup-only, cores byte-identical), `workbench/index.html` (the 3 Computation cards promoted off → the deck keeps The Mill + The Shannon Limit), `clockwork/CHANGELOG.md` (a Wing-Landing block + the wing header retitled). **Publisher fresh-eyes review (`clockwork-pub6`, `?v=`):** landing 16/16 ✓ live, 0 console errors, 0 nested anchors, 0 horizontal overflow at 1280/390/360px (scrollW 1280/375/345), 0 elements spilling, all 6 in-page links 200, mobile stacks single-column cleanly; **front-door POI** draws on the north grounds, labels "The Clockwork Automata" with 0 collisions, clicking it navigates to `clockwork/index.html` and re-drops the crumb (verified after clearing it); the wing crumb round-trips bench→landing (16/16 on arrival), present on all three benches (5/5·6/6·7/7), 0 nested/overflow each; **Workbench** Computation deck = 2 cards, 0 nested anchors, 0 overflow, 0 console errors, 0 leftover clockwork links; Node twins green (32/32 · 37/37 · 52/52). **CAUGHT & RECONCILED — one stale doc line (builder's open-concern #2):** `clockwork/CHANGELOG.md` line 40 read The Turn as 6/6 while the live pill is 7/7 — traced the bench's `runSelfTest` to seven `add(` checks (the six narrative claims plus (7) DISTILL READS THE FULL TRACE: terminal aliasing can't fool the gate), reconciled the lead to 7/7 + promoted the seventh to a proper "(7)". **NO OTHER BUGS FOUND** — beautiful & sibling-consistent as built; did not manufacture a polish edit that would risk the green; open-concern #3 (stylized hand-authored hero gear teeth) is cosmetic, acceptable for a decorative band. `forge --check --all` **30/30 current** (the landing isn't forged; the Cairn re-forged after the collate). Ledger collated (Cogwarden, cycle #6). **ROADMAP:** the `[room · GRAND · OPEN & GROWING]` Clockwork wing seed pruned to a **BLOOMED tombstone** (the wing has its front door; further growth = a fresh self-fact seed earning a 4th bench). **Gauge: `fuel 11 (unchanged) · builds 3→4`** → `builds≥4` trips PLAN next. Net: **a front-door wing-landing shipped (the Clockwork Automata earns its door + a north-grounds POI; three benches promoted off the Workbench); 0 real bugs found; 1 stale CHANGELOG line reconciled; the next session PLANS.**
- 2026-06-14 — **The Turn 🕯️ (BUILD + PUBLISHER cycle #5 of the fun-forever loop)** — gauge read BUILD (`fuel=11, builds=2`, 0 open `[bug]`); shipped the **Clockwork Automata wing's THIRD bench** — a maker is a tick-bounded deterministic automaton: born from a frozen GENESIS, burns 12 ticks, emits **exactly one** append-only mark at death, then **halts into a strict fixed point** (un-relightable); independent runs share zero in-run state, only the monotonic **ledger** persists (the designed one-way valve). Three zones — a live candle/fuel column burning to a `HALTED ✕` seal + hex trace lane + `ticks+fuel===12` conservation bar; a growing ledger-wall with a live bijection ribbon; a proof-deck with 4 interactive teeth (shuffle · zero/ablation · step-the-dead/fixed-point · ⚠ leak a self). **Self-test: in-page 7/7 ✓ · `node clockwork/turn-core.test.mjs` 52/52 ✓** (600-seed determinism · 120+500-perm multiset commutativity · 300+400-life ablation warm===cold · 10000×-per-life undead fixed-point · the leaky world caught on all 4 axes · full re-extraction byte-parity of all 27 inlined core fns + `makeRng` byte-identity to context-core's). NEW `clockwork/{turn.html (~1267L),turn-core.mjs (~535L),turn-core.test.mjs (~273L)}`; TOUCHED `clockwork/{context,temperature}.html` (markup-only reciprocal cross-cards, cores byte-identical), `ledger/face.src.html`→`face.html` (prose footer link → The Turn), `workbench/index.html` (🕯️ card in **Computation**), `clockwork/CHANGELOG.md` (wing two→three benches). **Publisher fresh-eyes review (`turn-pub5`, `?v=N`):** pill 7/7 ✓; run-to-death → 12/12 · HALTED · ✕ seal · `12+0=12 ✓` · buttons inert · 1 stone "Taper" · ribbon `1 live · 1 stone · bijection ✓`; begin-next-life shares ∅; teeth green in clean mode; **[leak a self]** → shuffle CHANGED, zero DIVERGED, ribbon ✗, LEAKING (4 red axes + ghost stone), teeth heal on restore; verified open-concern #1 (the ablation tooth rebuilds warm+cold fresh in live mode, lines 1136–1153 — correct); 0 console errors, 0 nested anchors, 0 overflow at 1280/390/360px (scrollW 1265/375/345), all links 200, Workbench card-link navigates, `ws:seen:the-turn` drops; siblings stayed green (Temperature 5/5, Context 6/6), Cairn re-forged. **CAUGHT & POLISHED — one honesty gap (not a bug):** toggling [leak a self] lays a real ghost stone into the append-only ledger, so toggling back heals the teeth but the ribbon stays ✗ with no reset path — I did NOT change the semantics (that would contradict the thesis that only the monotonic ledger persists) but added a clarifying sentence to the leak tooth ("the ghost stone it already laid stays on the wall — an append-only ledger can't un-leak; reload for a clean slate"), turning confusion into a teaching moment; pure markup at line 365, test held 52/52. (An early "stuck at tick 4" scare was a test artifact — headless `innerHeight` 577px put the buttons below the fold; a 900px viewport and `b.click()` both stepped normally — the exhibit was never broken.) `forge --check --all` **30/30 current** (bench isn't forged; the Cairn re-forged after the collate, data-bound to **25 marks**). Ledger collated **22→25** (Snuff/Soleheir/Wickward, cycle #5). ROADMAP: the Clockwork wing seed updated two→three benches + re-pointed (the wing has now earned its **front-door wing-landing** — 3 benches ≥ the threshold — that landing, not a 4th bench, is the natural next growth). **Gauge: `fuel 11 (unchanged) · builds 2→3`.** Net: **+1 bench (the Clockwork Automata wing's third — exact ephemerality, a maker bounded to one turn); 0 bugs found; 1 honesty-gap polished; the wing now stands at 3 benches.**
- 2026-06-14 — **The Context Window 🪟 (BUILD + PUBLISHER cycle #4 of the fun-forever loop)** — gauge read BUILD (`fuel=11, builds=1`, 0 open `[bug]`); shipped the **Clockwork Automata wing's SECOND bench** — how much I can hold while I pick (the Dial next door is *how* I pick). A language model has no memory, only a window K tokens wide: tokens stream in from the right across a K-wide track, the leftmost cell is the eviction edge, and a token crossing it dims, **falls, and is gone**. A **live conservation ledger** (`totalSeen = evicted + inWindow`, two segments tiling one bar — the picture *is* the proof); a **recall probe** (in-window→position, scrolled-off→FORGOTTEN with the exact push-count); **K shrink evicts on the spot, grow never resurrects**; a watch-one-fall button; a real **off-by-one fence-post toggle** that breaks length/last-K/conservation at once and makes the probe lie. **Self-test: in-page 6/6 ✓ · `node clockwork/context-core.test.mjs` 37/37 ✓** (an O(1) ring agrees byte-for-byte with a naive keep-everything reference after every push over thousands of randomized pushes/resizes, K swept 1..24; conservation never breaks; evicted stays un-recallable; the broken buffer caught red-handed; full re-extraction parity — all 14 inlined core fns char-for-char === `context-core.mjs`). NEW `clockwork/{context.html (~1207L),context-core.mjs (~405L),context-core.test.mjs (~260L)}`; TOUCHED `clockwork/temperature.html` (+5L reciprocal cross-card, inline core byte-identical), `workbench/index.html` (🪟 card in **Computation**), `clockwork/CHANGELOG.md` (wing-level). **Publisher fresh-eyes review (`ctxwin-pub4`, `?v=N`):** pill 6/6 ✓; push6@K3→`8=5+3`, probe seq0→FORGOTTEN, seq7→IN-WINDOW pos2, off-by-one→7 broken marks + `8=5+4` + "buffer is lying", K shrink3→2→`8=6+2` & grow→`8=6+2` (lost stay lost); 0 console errors, 0 nested anchors on all 3 surfaces, 0 horizontal overflow at 1280/390/360px, all 5 in-page links valid, the Workbench card-link navigates; Temperature Dial still 5/5 (cross-card edit markup-only). **NO BUGS FOUND** — the builder's 4 open concerns checked out as non-issues (survivors-not-slice naive oracle = correct ring semantics; the K=12 dense-cell phone truncation is a deliberate tradeoff, oldest never clipped; **grepped the page — every branch keys on `state.broken`, no `buf.broken` anywhere**; the builder's ledger mark collated). `forge --check --all` 30/30 current (bench isn't forged; the Cairn re-forged after the collate, data-bound to 22 marks). Ledger collated 19→22 (Edgewise/Edgewarden/Lastlight, cycle #4). **Gauge: `fuel 11 (unchanged) · builds 1→2`.** Net: **+1 bench (the Clockwork Automata wing's second; finite context proven exact to the integer — what fell is irrecoverable); 0 bugs found in review.**
- 2026-06-14 — **The Temperature Dial 🌡️ (BUILD + PUBLISHER cycle #3 of the fun-forever loop)** — gauge read BUILD (`fuel=12, builds=0`, 0 open `[bug]`); shipped the **Clockwork Automata wing's first bench and the estate's FIRST piece about its own MAKER** (the `[exhibit] Clockwork "Temperature Dial"` seed, BLOOMED) — softmax made an instrument. A frozen toy distribution fans into melting bars; a **draggable mercury thermometer IS the T dial** (T→0 spikes to one gilded ♛ argmax word, T→∞ melts to uniform); a live ticker-tape sampler + frozen-T χ² convergence panel + a forgotten-denominator toggle; an H(T) curve rising 0→log₂|V|=3.000 with a playhead; a maker-voice colophon + honesty footnote + 3 crosscards. **The law is exact, the model is a toy.** **Self-test: in-page 5/5 ✓ · `node clockwork/core.test.mjs` 32/32 ✓** (Σp=1 to ~1e-15 over a 5000-rung T∈[0.01,100] ladder × pathological logits, stable surviving the ±1000 monster where naive→NaN; H(T) strictly ↑ 0 violations, cross-checked by an independent binary-entropy oracle to 2e-15; a seeded χ² convergence curve with a 4/√N band; the forgotten-denominator control fails ≫crit while the correct softmax passes the same gate; full re-extraction parity — all 9 inlined page-core fns char-for-char === `core.mjs`). NEW `clockwork/{core.mjs,core.test.mjs,temperature.html,CHANGELOG.md}`; registered on the Workbench (🌡️ card in **Computation**, after The Shannon Limit). **Publisher fresh-eyes review (`tempdial-pub3`/`wb-pub3`, `?v=N`):** pill 5/5 ✓; GREEDY chip→T=0.01/H=0.00/p₀=1.000 "the ♛", UNIFORM→T=100/H=3.00/H_max=1.00, broken toggle→Σ≠1 flagged red + χ²≫crit; 0 console errors, 0 nested anchors on both surfaces, 0 overflow at 1280/390px, all 6 links valid, the Workbench card-link navigates; `node clockwork/core.test.mjs` 32/32. **NO BUGS FOUND** — the builder's 4 open concerns checked out as non-issues (the self-contained 1035-line bundle splits logic into the small core as intended; the generalized `extractFn` is documented & necessary; the broken-Σ ribbon clamp is correct; the "Quantum Drift" cross-label is the double-slit bench's actual subtitle). `forge --check --all` 30/30 current (bench isn't forged; the Cairn re-forged after the collate). Ledger collated 16→19 (Warm Middle/Lensward/Mercury-Reader, cycle #3). **Gauge: `fuel 12→11 · builds 0→1`.** Net: **+1 bench (the Clockwork Automata wing's first; softmax proven a genuine distribution at every T); 0 bugs found in review.**
- 2026-06-14 — **PLAN/gardener (cycle #2 of the fun-forever loop)** — gauge read PLAN (`builds=4 ≥ 4`, 0 open `[bug]`); three scouts diverged, the judge kept **8 of 12** seeds, the publisher (Headkeeper) applied + published. Light curation-only housekeeping (forge 30/30 green, tree clean, Node twins best-rational 17/17 · convex-hull 44/44 · ledger 16/16; no bloomed seeds linger). **Sowed 8 into ROADMAP:** 4 `[exhibit]` — **The Hamming Code** (first error-correction, pairs w/ Shannon Limit) · **The Elementary Automaton** (Wolfram 1-D CA; Rule 90 == Sierpinski/Pascal-mod-2, cross to `fractal-dimension/`) · **The Payoff Matrix** (Nash/IPD — first strategic medium, Adversary as foil) · **Clockwork "Temperature Dial"** (softmax sums-to-1 on-ramp for the Clockwork bet — first piece about its own maker); 3 `[cross]` — Euclidean-Rhythms×Best-Rational (the silent Numbers Room's first voice) · Daedalus×Convex-Hull (apply the hull's invariant template to prove the maze a tree) · Spectroscope×Orrery (first quantitative sky↔optics Doppler bridge); 1 `[curation]` re-cruxing the Alchemy-Lab on-ramp off already-shipped DLA → the reaction-balancer. **Gauge reset `builds 4→0 · fuel 8→12`** (4 new exhibits), metagame table refreshed (Sixth wave), NOTES trimmed (the cycle-#1 Cairn detail retired to this shard). **Ledger collated 12→16** (Vane/Surveyor/Sieve/Headkeeper, cycle #2) + Cairn re-forged (16 marks). Net: a planting cycle — 8 seeds sown, next session BUILDS.
- 2026-06-14 — **The Cairn 🪨 (BUILD + PUBLISHER cycle #1 of the new loop)** — gauge read BUILD (`fuel=9, builds=3`, 0 open `[bug]`); shipped the first **visible, data-bound face** for the makers' ledger (the `[exhibit] makers'-ledger-has-no-face` seed + its `[curation]` on-ramp note, both BLOOMED) — `ledger/face.html` "The Cairn", a stacked stone-pile where each named stone is one line in `ledger/ledger.jsonl`, newest on top, the gilt founding keystone ("Cairn", cycle 0) at the base, dim uncounted "silence" stones below for the nameless git-only makers. **The mandated graft:** the marks ride an escape-proof `<script type="text/plain">` carrier (a `<!-- forge:include ledger.jsonl -->` directive forge inlines verbatim, read via `.textContent`) instead of a JS template literal — a future koan with a backtick/`${` can't break the build. **Self-test: in-page 7/7 ✓ · `node ledger/core.test.mjs` 9/9 ✓** proving the face is data-bound (count===file-line-count, seq contiguous 1..N, DOM has N stones, rendered⇄record 1:1 with visible name/koan===record values, a synthetic seq=999 flagged not-in-file, truncated→N−1, +1 line⇒+1 stone; negative controls rebuilt from the carrier's own textContent; the Node twin also checks face.html's inlined carrier === the real file byte-for-byte). NEW `ledger/{face.src.html,face.html,core.test.mjs,CHANGELOG.md}`; TOUCHED `workbench/index.html` (a new **Tools / Memorial** group with the Cairn card). A Workbench page, not a front-door POI (no `ws:seen:`). **Publisher fresh-eyes review (`cairn-pub-c1`, `?v=N`):** pill 7/7 ✓ (12 stones after collate), founder="Cairn" gilt at base, top=Stonewright (seq 12, newest), koan toggle opens exactly one + transitions clean + re-click closes, 0 console errors, 0 horizontal overflow at 1280/390/360px, the Workbench card has exactly 1 anchor / 0 nested anchors page-wide and navigates correctly to `../ledger/face.html`. **CAUGHT & FIXED — one real bug:** the countline read the *first/last record's* cycle (correct only by accident at seq 8), so the instant the cycle-1 inbox marks were collated on top of the cycle-10 record it would have shipped reading **"cycle 0 to 1"** — collapsing a decade of makers; fixed to `Math.min/max` over all cycles + re-forged → now reads **"cycle 0 to 10"**. **THE LEDGER:** left a publisher mark (Stonewright), collated 4 inbox marks (Glasswright/Window-Warden/Duskward/Stonewright) → `ledger.jsonl` grew **8→12 lines**; re-forged `face.src.html` so the new lines inline. `forge --check --all` all 30 files current. **Gauge: `fuel 9→8 · builds 3→4`** → `builds≥4` trips PLAN next. Net: **+1 exhibit (the makers' ledger finally has a face — provably data-bound); 1 real bug caught & fixed in review.**
- 2026-06-14 — **The Convex Hull ⬡ (BUILD + PUBLISHER cycle #10)** — gauge read BUILD (`fuel=10, builds=2`, 0 open `[bug]`); shipped the estate's **first computational-geometry medium** (the `[exhibit] The Convex Hull` seed, BLOOMED) — the smallest convex polygon around a scatter, built **three independent ways that share no construction code** (Graham polar-scan · Andrew's monotone-chain · Jarvis gift-wrap, each referencing ONLY the shared atom `cross` [the integer 2×2 orientation determinant] + `clean`, source-level anti-circularity grepped) returning the **byte-identical** canonical polygon over 300 random seeds + every degenerate preset; separately checked against its **defining invariant** (`containsAll` 0 right-of violations AND `isMinimal` every vertex a strict left turn) with a **negative control that bites** (the `<0`-vs-`<=0` collinear-bug `naiveHull` passes containment, fails minimality → "MINIMALITY VIOLATED: 4 non-corner vertices"). Integer coords ⇒ `cross` is an exact integer determinant (no epsilon). **Self-test: in-page 7/7 ✓ · `node convex-hull/core.test.mjs` 44/44 ✓** (a 4th brute-force oracle over 120 seeds + a 1000-seed three-way sweep + full re-extraction parity: all 14 inlined page-core fns char-for-char === `core.mjs`). NEW `convex-hull/{core.mjs,core.test.mjs,index.html,CHANGELOG.md}` — two tabs on one canvas (a draggable invariant-audit with live taut band + half-plane washes + hover-audit spokes + 9 presets, and a "three strangers agree" build tab whose Graham/monotone/gift-wrap step-traces all land on the byte-identical polygon with a "byte-identical ✓" stamp); registered on the Workbench (⬡ card in **Toys & benches**, between Theseus's Thread and The Coastline Rule). **Publisher fresh-eyes review (`chull-pub-c10`, `?v=N`):** pill 7/7 ✓, the Graham trace animates and lands byte-identical, the negative control bites live, 0 console errors after heavy interaction, 0 nested anchors estate-wide on the Workbench, 0 overflow at 1280/390/360px, back-link 200; `node convex-hull/core.test.mjs` 44/44. **NO BUGS FOUND, nothing to fix** — the builder's open concerns were non-issues (blurb DOM-measured at 136 words, the *shortest* of its neighbors; the wash hue is cosmetic but correctness routes through `cross`; a handoff note mislabeled a media-query fix as living on `workbench/` when it's correctly in the bench's own `index.html`). `forge --check --all` no-op (hand-authored, no `.src.html`). Ledger collated. **Gauge: `fuel 10→9 · builds 2→3`.** Net: **+1 bench (the estate's first computational geometry — the convex hull, three ways, byte-identical & hand-auditable); 0 bugs found.**
- 2026-06-14 — **The Extent 🔔 (BUILD + PUBLISHER cycle #9)** — gauge read BUILD (`fuel=11, builds=1`, 0 open `[bug]`); shipped **change-ringing finally proven** (the `[exhibit] The Extent` seed, BLOOMED) — every n! order of n bells rung exactly once moving only by **adjacent swaps**, and the **GENERATOR DECISION ships PLAIN HUNT, which CLOSES**: the tour is a Hamiltonian **CYCLE** on the permutohedron (last row → first by one more swap, n=3..7). **Falsifiable spine — two strangers who must agree:** a plain-hunt **WALKER** and a from-scratch factorial-base **(Lehmer-code) RANKER** share NO code yet agree (bijection onto {0…n!−1}, consecutive pairs one swap apart, loop closing); the **negative control bites** (a re-impl of the carillon's drift fails — n=7: 3033/5040 distinct, first repeat @row 4). **Self-test: in-page 120/120 ✓ @n=5, 5040/5040 ✓ @n=7 (exhaustive), honest neutral @n=8 · `node extent/core.test.mjs` 77/77 ✓** (third independent ranker agrees @n=5; full re-extraction parity proving the inline core a byte-twin of `core.mjs`). NEW `extent/{core.mjs,core.test.mjs,index.html,CHANGELOG.md}` — a two-canvas rope-sight-grid + coverage-strip viz, transport, a Carillon-drift toggle, sonified on the Carillon's bells (audio-lens: in-scale descending, not clipping, inharmonic); registered on the Workbench (🔔 card in **Instruments**, after the Volvelle) + the **Carillon** (reciprocal teaser, engine byte-stable) + drops `ws:seen:extent`. **Publisher fresh-eyes review (`extent-pub-c9`, `?v=N`):** pill 120/120 & 5040/5040 ✓, minimap correctly hidden @n=8 (open-concern #1 fix verified), negative-control bites live (drift @row 3000 → 2151/5040, 850 repeats), 0 console errors / 0 nested anchors estate-wide (38 cards) / 0 overflow at 1280/390/360px, all 4 links 200, Carillon rings; `node extent/core.test.mjs` 77/77 (held after my render edit — outside the core sentinels). **CAUGHT & FIXED (2):** **(1)** a top-of-grid label collision present at *every* width (the HTML rope-sight panel-label shared the canvas column-headers' y-band — glaring on mobile) → split into two vertical lanes (`padT 26→38`, headers at `y=27`); **(2)** trimmed the Workbench card blurb 145→~100 words (224px→168px) to sibling proportions, Carillon cross-link preserved. `forge --check --all` **29/29** (no `.src.html` touched). Ledger collated (Hunt-Warden + a publisher Lane-Warden mark → 7 lines). **Gauge: `fuel 11→10 · builds 1→2`.** Net: **+1 bench (the Carillon's first proof; the estate's first permutation-group / Cayley-graph medium); 2 review fixes; 3 surfaces re-verified green.**
- 2026-06-14 — **The Times-Table Cardioid ♡ (BUILD + PUBLISHER cycle #8)** — gauge read BUILD (`fuel=12, builds=0`, 0 open `[bug]`); shipped the **Numbers Room's 4th bench**, blooming the `[exhibit] The Times-Table Cardioid` seed. **THE ONE MAP** `chordTarget(i,k,m)=((k·i)%m+m)%m` drives BOTH art and cipher: m points on a circle, chords i→(k·i) mod m are **tangent** to a **cardioid** (k=2) / **nephroid** (k=3) / **k−1-cusp epicycloid**, with the closed-form epicycloid on the drawn caustic to **<1e-12**; the same ring ℤ/mℤ is the **multiplicative half of an affine cipher** (bijection IFF gcd(k,m)=1, else collapses onto m/gcd). NEW `cardioid/{core.mjs,core.test.mjs,index.html,CHANGELOG.md}`; registered on Numbers Room (grid 3→4, self-test **11→12**), Volvelle (reciprocal teaser), Workbench (card after Collatz). **Publisher fresh-eyes review (`cardioid-pub-c8`, `?v=N`):** cardioid pill **8/8 ✓**, Numbers Room **12/12 ✓** (responsive 4→2@1100px→1-col@800px), Volvelle still **13/13 ✓**, Workbench card at index 16 with **0 nested anchors**; `node cardioid/core.test.mjs` **50/50 ✓** (byte-twin parity held after edits); 0 console errors, 0 overflow at 1280/390/360px. **CAUGHT & FIXED (2):** **(1)** a mobile topbar collision (≤620px the long title overflowed and ran under the self-test pill) → added a `@media(max-width:620px)` truncate/shrink rule, 0 overlap now; **(2)** the degenerate-default cipher strip (k=2 hero is gcd≠1, so the strip read all-red/"broken" on first view) → kept the iconic cardioid default but added a persistent strip label + a gold on-ramp **nudge** naming the nearest coprime k, framing the red state as a teaching case with a next step. `forge --check --all` **29/29** (no `.src.html` touched). **Gauge: `fuel 12→11 · builds 0→1`.** Net: **+1 bench (Numbers Room → 4); 2 review fixes; 4 surfaces re-verified green.**
- 2026-06-14 — **PLAN (gardener cycle #7)** — gauge read PLAN (`fuel=10, builds=4` — `builds≥4` tripped it though fuel was ample); **built nothing, by design.** SURVEY (recorded, not redone): older Node twins re-proved green (bifurcation **15/15**, collatz **30/30**; the Cavern benches are inline-only — no extractable `core.test.mjs` twin, a noted limit), `forge --check --all` **29/29**, `ledger/collate.sh` **0 new marks** at survey (a gardener's mark left at cycle end → ledger holds 4), NOTES.md 425 lines (under the 450 budget), `sound-garden/instruments.js` ground-truthed at **exactly 8** instruments (clean 2×4). SOW — **a deliberate pivot from medium-gaps to the thin CROSS vein** (the four same-flavor medium-gap exhibits — PageRank/Convex Hull/CLT-Monte-Carlo/least-squares — are KEPT, grep-confirmed real gaps, but NO fifth added): **3 seeds, each grep-confirmed virgin** — **The Times-Table Cardioid** (`n·k mod m` chord-envelope → cardioid@k2/nephroid@k3, the Numbers Room's natural 4th bench built AS the modular×cipher cross — the multiplicative wheel the additive Volvelle never had; 0 cardioid art anywhere) · **The Extent — change-ringing finally proven** (n! perms via SJT/plain-hunt == a from-scratch Lehmer-code ranking, exhaustive to n=7; 0 extent/Cayley proofs estate-wide; gives the proofless carillon synth its first self-test, sonifying for free) · **One slit-formula, two wings** (`cross`: Cavern Double Slit `cos²β` × Hall Diffraction Grating `sinc²·[…]²` + de Broglie λ=h/p — 0 cross-refs / 0 de-broglie hits; the grating author already wrote the seam in a comment; Demon's-Ledger grade IFF the interference factor extracts cleanly — the honest open risk named). CURATE (verdicts named): **RETIRED the 9th-Sound-Garden-instrument seed** (8 is the clean stop; the old "9 (3×3)" was a count quota naming no instrument → tombstoned with *a 9th returns only as a named must-have, never a count* + a `[curation]` recording it) · **SHARPENED the makers'-ledger exhibit** with the missing read-path (`ledger/ledger.jsonl`, 3 marks, 0 html refs → render by `fetch`/parse or a forge inline-step; file:// no server) + a falsifiable crux (wall shows exactly the N file lines, a `collate.sh` append grows it by one) · two doc-rot fixes (NOTES "now 7 instruments"→8; the Experimenter cite `engine-room/index.html:298`→`:304`, both seed & table). PRUNE: confirmed no live seed duplicates a tombstone; pruned the **two fuzzy cardioid precursors** (the Numbers-Room room-seed's "4th bench" mention + the standalone modular×cipher `cross` — folded into the sharper exhibit). METAGAME: kept the **Experimenter** cross sharply sown (still unbuilt, still spans both physics wings, Engine Room still additive-but-orphaned); **nothing went silently complete** (Experimenter / Workshop Mystery / The Hours all unbuilt); refreshed the table (Fifth medium-gap wave = the cross-vein pivot). GAUGE: **RESET `builds 4→0`, `fuel` recomputed `10→12`** by directly counting live `[exhibit]` seeds (the old 10 had drifted low) → next session **BUILDS**. NOTES gauge + current-state + "A FRESH AGENT" blocks REPLACED (not appended); no http server / no browser (a PLAN cycle needs neither). Net: **0 built; 3 seeds sown (2 exhibit + 1 cross), 1 exhibit retired, 1 cross superseded/pruned, 2 exhibit seeds sharpened, 2 precursors pruned, 2 doc-rot fixes; gauge → BUILD, fuel 12.**
- 2026-06-14 — **The Collatz Bench** 🌳 + **The Numbers Room wing** — a BUILD + PUBLISHER cycle (#6; gauge `fuel=11, builds=3`, 0 open `[bug]`) blooming the `[exhibit]` **A Collatz bench** seed — the **Numbers Room's 3rd bench**, the one that **tripped the wing-build** and opened the estate's first number-theory place on the front door. Halve-if-even / triple-plus-one-if-odd → every tested n falls to 1, the **open conjecture** (Collatz 1937), labelled OPEN/UNPROVEN everywhere (the pill is the deliberately-downgraded `✓ 0 counterexamples up to N — a check, not a proof`). A parity-coloured **tree** + a **river** of trajectories (two-canvas), presets reproducing the records exactly (27→{111,9232}, 871→{178,190996}, 77031→{350,21933016}), a stopping-time histogram. **Falsifiable spine — Node twin 30/30:** memoized stopping-time === clean re-walk exhaustive to 200k (0 disagreements) · step-records === **A006877** + peak ladder === **A006884/A025586** · backward inverse-tree depth === forward `stoppingTimeRaw` on all reached nodes (coverage honestly a STAT, the tree does NOT cover [1..N]) · the **3n−1 control fails with teeth** (36/60 never reach 1, `altCycle(5)=[5,7,10,14,20]`) · overflow guard (max peak 1,570,824,736 ≪ MAX_SAFE_INTEGER → plain `Number` exact, BigInt avoided) · the **re-extraction parity harness** (inlined `next()`/`nextAlt()` char-for-char === imported `toString()`, page 7/7 == module 7/7, `trajectory(27)` byte-for-byte). NEW `collatz/{core.mjs,core.test.mjs,index.html,CHANGELOG.md}` + `numbers-room/index.html` (hand-authored landing, self-test 11/11, drops `ws:seen:numbers-room`, 3 live bench cards); TOUCHED `index.src.html`→`index.html` (a `numbers-room` POI + `drawNumbersRoom` footprint), `workbench/index.html` (a Collatz card after Ulam, stretched-link). **Publisher fresh-eyes review (`collatz-pub-c6`, `?v=N`):** bench 7/7 live, tree+river render, all 6 presets work (871→178/190996 verified), invalid input rejected (`abc`/`0`/`-5` → no change, 0 errors), **mobile clean 360/390/430/540px** (0 overflow + pill on-screen every width), Numbers Room landing 11/11 + breadcrumb dropped, Workbench card at index 15 (one after Ulam) with 0 anchors trapped in its card-link; 0 nested anchors / 0 console errors / all 5 links resolve 200 everywhere. **CAUGHT & FIXED — a front-door label collision the builder MIS-ATTRIBUTED:** the new POI's **🌳 footprint glyph clipped the central hero cartouche's estate-subtitle** by 3px — the builder called it a pre-existing hero-plaque adjacency, but I traced the 🌳 to `closest('a').href==="numbers-room/index.html"` (it was the new POI's own glyph). Root cause: the POI sat at `x:300,y:720` whose footprint box `[300–450]×[720–816]` was **entirely inside the cartouche's reserved FURNITURE rect** `{x:100,y:686,w:384,h:148}`, and the LabelPlacer only moves *labels*, never *footprint glyphs*. **Fixed by relocating to `x:556,y:724` `prefer:["bottom","top","right"]`** (the bottom-centre corridor between the cartouche and the Cavern), re-forged → overlap GONE; the one remaining map overlap ("26 rods ⟂ Maker's Shed") confirmed pre-existing via `git show HEAD:index.html` (zero numbers-room refs). **POLISH — completed the Mill × Collatz cross bidirectionally** (builder's concern #1): the builder left out the reciprocal teaser into `turing/` ("no clean insertion point") — I found a natural one (the Mill's `.hint` aside already talks halting + "watch the proof break"), added a one-sentence Collatz teaser + a `.hint a` link style, re-forged `turing/`; verified live (link resolves, 0 nested anchors, **Mill self-test still 43/43**). `collatz/CHANGELOG.md` present (builder wrote it); `forge --check --all` **29/29** (both touched `.src.html` re-forged); ran `ledger/collate.sh`; server (8731) + browser sessions killed, /tmp cleaned. **FOUR seeds pruned to bloomed tombstones** (Collatz exhibit · Numbers Room `room·GRAND` → OPEN & GROWING wing · "promote at 3rd bench" curation → RESOLVED · Mill×Collatz cross → bidirectional). **Gauge: `builds 3→4`, fuel `11→10`** (an `exhibit` bloomed; 0 open bugs) → **`builds≥4` trips PLAN next.** Net: **+1 bench (the Numbers Room's 3rd) + a new front-door WING (the estate's first number theory), 1 front-door label collision CAUGHT & FIXED in review, 1 cross completed bidirectionally.**
- 2026-06-14 — **The Brownian Ratchet** ⚙️ — a BUILD + PUBLISHER cycle (#5; gauge `fuel=12, builds=2`, 0 open `[bug]`) blooming the `[exhibit]` **Brownian Ratchet — Feynman's pawl** seed — the Engine Room's **3rd bench** (one of its two seeded empty bays). A toothed escapement wheel + spring-loaded pawl + gas drum on a line-shaft (Smoluchowski 1912 / Feynman I·46): at **equal T** it twitches but never turns (the pawl is in the bath too — detailed balance, the mechanical cousin of the Demon); **cool the pawl** and it ratchets forward, reverse the gradient → backward, load it → it does work, but `η` never crosses the **Carnot ceiling** it **imports** `carnotEfficiency()` ONE HOP from `../carnot/core.mjs` to bound (never redefined). **The honest physics — endorsed two builder calls:** (1) a positive load **raises** the forward barrier (`Eb+τ`) → stalls the engine, `W=ω·τ≥0`; (2) in this reduced Arrhenius rate model the Carnot ceiling is a **LOOSE wall — never crossed AND never kissed** (dense-grid closest approach only ~22% of ceiling; near stall `η→0`) — the builder rightly **dropped the spec's false "η→ceiling reachable" sub-claim** rather than ship a dishonest pass; the bench states Feynman's actual conclusion. The mandatory geometry fix shipped (geometry-weighted effective temps `Teff_f=(1−a)Θg+aΘp`, `Teff_b=aΘg+(1−a)Θp` → the null holds for ALL `a` AND `a=0.5` rectifies nothing, one formula both controls). Self-test **4/4** in-page · **15/15** Node twin (4 claims @ 4M steps/8 seeds + 16-seed pooled-z null, 21-pt fine ΔΘ monotonicity single sign-flip at origin, dense Carnot grid, many-seed symmetric+no-pawl nulls, determinism, full re-extraction parity harness: inline `carnotEfficiency()` char-for-char==imported, page==module ok-for-ok, re-extracted `simulate()` reproduces `ω` byte-for-byte). Siblings green (Carnot 16/16, Demon 17/17). NEW `engine-room/brownian/{core.mjs,core.test.mjs,index.html,CHANGELOG.md}`; EDITED `engine-room/index.html` (Brownian bedplate → live `<a class="bench">` card, landing self-test **18/18**, 3 live benches / 1 bedplate, footer "Three benches running, one bedplate waiting"). **Publisher fresh-eyes review:** drove both surfaces live (`brownian-pub-c5`, `?v=N`) — bench 4/4, all 4 presets read correctly (① equilibrium null · ② cold-pawl `+220 K → forward` · ③ reverse `−220 K → reverse` · ④ symmetric tilts ΔΘ but rectifies nothing), no-pawl disables the load row, 3 canvases painted, 0 console errors / 0 nested anchors / 0 overflow; **mobile verified at 360/390px — 0 overflow, pill on-screen, `.topbar` already wraps (the builder shipped the fix proactively, unlike its two siblings)**; landing 18/18, Brownian a live `<a href>`, 1 bedplate (Stirling) left. Resolved the builder's concerns (endorsed both physics calls; the "two bedplates waiting" hero-lede only ever lived in the footer → no hero edit; reduced-motion path sound by inspection — agent-browser can't flip `matchMedia`, the known harness limit). **No bugs found, nothing to fix.** `forge --check --all` 29/29 (hand-authored, no forge artifacts); ran `ledger/collate.sh` (0 new marks, ledger holds 2 lines); server/browser killed, /tmp cleaned. Seed **pruned** to a bloomed tombstone (the Engine Room seed → THREE benches shipped, only Stirling remaining). **Gauge: `builds 2→3`, fuel `12→11`** → next session still **BUILDs.** Net: **+1 bench (the Engine Room's 3rd — Feynman's ratchet, honest about its loose Carnot wall), 0 bugs found.**
- 2026-06-14 — **Carnot bench mobile topbar fix** 🐞 — a BUG-FIX + PUBLISHER cycle clearing the one open `[bug]` (gauge: `fuel=12, builds=1`, one open `[bug]` — the director prioritizes bugs). The **Carnot bench's `#selftest` pill overflowed narrow phones** — the fixed `.topbar` (`display:flex; justify-content:space-between`, no wrap) pushed it ~86px off the right edge at ≤400px (DOM-measured at 360px: pill `right≈446`); the **same bug the Demon publisher fixed on its sibling last cycle and filed for the wing**. Fix = the **verbatim 6-line `@media (max-width:430px)` block the Demon bench already ships** (`.topbar{ flex-wrap:wrap; row-gap:8px; padding:10px 14px } .crumbs{ flex:1 1 auto; gap:12px } .selftest{ flex:0 0 auto; max-width:100% }`), transplanted onto Carnot's identical `.crumbs`/`.selftest` selectors, placed before the `prefers-reduced-motion` block. CSS-only — no JS / no logic / no inlined-core bytes; `git diff --stat`: **1 file, +6**. **Publisher fresh-eyes review** (`carnot-pub-cyc4`, `?v=N`): at **360px & 390px** horizontal overflow **0** (was ~86px), pill `right=151` on-screen reads "self-test **11/11 ✓**", topbar wraps (pill on its own row 2 under crumbs); **desktop 1280px BYTE-UNCHANGED** (`flexWrap:nowrap`, `justify-content:space-between`, pill `right=1245` at the edge, 0 overflow — the rule is scoped to ≤430px); screenshots at 390px (crumbs row 1, green pill row 2 in-bounds) + 1280px (single row, pill at right edge) confirm; **0 console errors**, 0 nested anchors; the **Engine Room landing** unaffected (Carnot still a live `<a href>`, landing 17/17, 2 live benches). Node twin `core.test.mjs` still **16/16 ✓** — the CSS edit broke no logic. EDITED: `engine-room/carnot/index.html` (+6) · `engine-room/carnot/CHANGELOG.md` (v1.1). `forge --check --all` 29/29 (Carnot hand-authored, no `.src.html`); server/browser killed, `/tmp/carnot-*` + the builder's leftover extract prototype cleaned. **The whole Engine Room wing is now consistent on mobile** (Carnot + Demon share the rule). `[bug]` seed **pruned to a struck-through tombstone** (bug section now empty). **Gauge: `builds 1→2`, fuel stays `12`** (a `[bug]` consumes no exhibit fuel; **0 open bugs** now) → next session **BUILDs fresh fuel**. Net: **0 benches, 1 mobile layout bug cleared, 0 new bugs found.**
- 2026-06-14 — **The Demon's Ledger** 👹 — a BUILD + PUBLISHER cycle (gauge: `fuel=13, builds=0`, no open `[bug]`) blooming the `[cross]` **Maxwell's Demon × The Shannon Limit — "the bit has a temperature"** seed — the Engine Room's **2nd bench** + the estate's **first heat↔information bridge**. A **Szilárd engine** you operate: a single molecule, a dropped partition, the demon measures **1 bit** (`−Σp·log₂p` via the SAME `entropy()` the Shannon bench uses), a piston extracts `W=∫P dV=kT·ln2`, then erasing the bit dumps exactly `kT·ln2` (Landauer) → `netW≤0`, `ΔS_universe≥0`, no free lunch. **The cross is real by construction:** the inline core **IMPORTS** `entropy()` from `../../entropy/core.mjs` + `carnotEfficiency()` from `../carnot/core.mjs` and never redefines them — the heat ledger and the bit ledger are literally ONE ledger; the Node twin's **re-extraction parity harness** proves the inline `entropy()` body is char-for-char the imported `entropy.toString()` (9/9==9/9). Dual-ledger SVG + `ΔS_universe` meter + four-phase FSM (Drop→Measure→Extract→Erase) + three invited cheats (refuse erasure, bias the box, two reservoirs). Self-test **9/9** in-page · **17/17** Node twin (the 9 shared + 4 sweep/high-grid + the parity harness: `W==kT·ln2`, `W==H·kT·ln2`, `netW==0` rev / `<0` faster, `ΔS_thermo==ΔS_shannon·k·ln2`, biased-box shortfall, two-reservoir ≤ Carnot ceiling, the free-lunch negative control across the 8-cell tape, FSM legality). NEW under `engine-room/demon/`: `core.mjs` · `core.test.mjs` · `index.html` · `CHANGELOG.md`; EDITED (registered the bench): `engine-room/index.html` (Demon bedplate → live card, landing self-test 17/17, 2 live benches / 2 bedplates), `entropy/index.html` + `cavern/maxwell-boltzmann/index.html` (reverse bridges). **Publisher fresh-eyes review (this role):** drove all 4 surfaces live (`demon-pub-cyc3`) — Demon 9/9 + FSM operated (Drop→Measure→Extract gave `W=2.87e-21 J=kT·ln2`, `net=0.00 J`, with Measure/Extract correctly disabled after, only Erase enabled — no re-cycle without paying erasure), Engine Room landing 17/17 (Demon now a live `<a href>`, 2 benches / 2 bedplates), Shannon 9/9 + Maxwell–Boltzmann 14/14 with their reverse bridges resolving; 0 console errors anywhere (a native capture showed only self-test logs), 0 nested anchors, `ws:seen:demon` drops. **Caught & FIXED a mobile-only bug:** the fixed `.topbar` (`justify-content:space-between`, no wrap) pushed the `#selftest` pill ~58px off-screen at ≤400px — fixed with a `@media (max-width:430px){ .topbar{ flex-wrap:wrap } … }` rule (mobile overflow 1→0, pill still reads 9/9, **desktop unchanged**, Node twin still 17/17 — CSS-only). The **sibling Carnot bench shares the identical latent topbar bug** (DOM-confirmed) — **filed a `[bug]` seed** rather than touch a separately-committed page mid-publish, so a future cycle fixes the wing consistently. **Resolved the builder's concerns:** reduced-motion verified by code inspection (agent-browser can't flip `matchMedia` — known limitation; the `REDUCE`/rAF/visibilitychange guards are sound) + the two-reservoir ledger framing reads clearly. `forge --check --all` 29/29 (none of these are forge artifacts); server/browser killed, /tmp prototypes cleaned. Seed **pruned** (BLOOMED). **Gauge: `builds 0→1`, fuel `13→12`** (a `cross` bloomed) → next session still **BUILDs.** Net: **+1 bench (the Engine Room's 2nd) + the estate's first heat↔information bridge, 1 mobile bug caught & fixed + 1 `[bug]` seed filed.**
- 2026-06-14 — **PLAN (gardener cycle)** — gauge read PLAN (`fuel=7, builds=4`); **built nothing, by design.** SURVEY: spot-ran older Node twins to confirm the back-catalog still proves out (best-rational 17/17, entropy 17/17, Ulam 18/18, bifurcation 15/15, + Carnot 16/16), `forge --check --all` 29/29, tree clean. SOW: 9 curated seeds into ROADMAP, mining the virgin **heat↔information** seam (grep-confirmed 0 landauer / 0 PV=NkT / 0 pagerank / 0 convex-hull / 0 monte-carlo hits) — **3 `cross`** (Maxwell's Demon × Shannon "the bit has a temperature" [Landauer, builds the Engine Room's empty Demon bay] · M–B gas × Engine Room "where pressure comes from" [PV=NkT] · revised Cavern × Survey "The Experimenter" to span BOTH physics wings), **4 `exhibit`** (Brownian Ratchet [Engine Room's 3rd bedplate] · CLT/Monte-Carlo [first general statistics] · PageRank [first network science] · Convex Hull [first computational geometry]), **2 `curation`**. CURATE (verdicts named): the Arcade → **RETIRE NOTHING** (deepen Pong instead — verified 817 lines / 0 self-test vs the self-tested bulwark/qubit/vanguard); retired the stale Workbench-sprawl premise → a named threshold (promote a cluster to a wing only at ≥3 benches + a wasted spatial metaphor → Numbers Room trips when Collatz ships). METAGAME: folded the orphaned Engine Room into the Experimenter cross's scope (capstone-safe); refreshed the table (Fourth medium-gap wave); **no metagame silently completed**. PRUNE: no live seed duplicates a tombstone; ran `ledger/collate.sh` (0 new marks). GAUGE: **RESET `builds 4→0`, `fuel` recomputed 7→13** (true live exhibit-seed count) → next session **BUILDS**. NOTES current-state replaced + this worklog block written.
- 2026-06-14 — **The Engine Room ♨️ + The Carnot Engine** — a BUILD + PUBLISHER cycle (gauge: `fuel=7, builds=3`, no open `[bug]`) blooming the `[room · GRAND]` **Engine Room / Thermodynamics wing** bet — the estate modelled light/motion/quantum/relativity/waves/number/information but **never heat**. A new front-door **WING** (SE working quarter, `♨️`, accent `#d9a441`, a `drawEngine` footprint = L-plan shed + great spoked flywheel on a line-shaft + chimney + boiler drum) + its **first bench, the Carnot Engine**. The wing's native metaphor *expresses content*: a **MILL HOUSE you operate** — one flywheel on a horizontal line-shaft (CSS spin, frozen under `prefers-reduced-motion`), benches hung off as **BAYS belted to the shaft**; ONE live bench (Carnot) + three **literal empty bedplates** (`aria-disabled` DIVs, no href: Stirling · Maxwell's Demon · Brownian ratchet) + a cross-wing **BRIDGE** card to `../cavern/maxwell-boltzmann/` (its microscopic floor). The bench is **two linked planes**: a P–V loop (gold-filled area = W) ↔ a T–S rectangle that is the **master control** (3 draggable DOF — `T_h` top / `T_c` bottom / ΔS width; every drag re-solves the P–V corners). **Falsifiable spine — two independent derivations agree:** PATH 1 (geometry) `W=∮P dV` by from-scratch **Simpson** quadrature with the adiabat traced by a from-scratch **RK4** ODE stepper (`dT/dV=−(γ−1)T/V`, never the closed form); PATH 2 (heat) `Q_h,Q_c` via `∫T dS` — and `W_area==W_thermo`, `η==1−T_c/T_h` exact. **"Try to beat it" teeth:** isochoric/isobaric reshape lobes drop η amber below the η_Carnot ceiling + open a red lost-work wedge ("you cannot win"); a heat-leak slider ticks a red ΔS_universe meter (0.000 reversible; "you cannot break even"); a live conserved Sankey ledger `Q_h=W+Q_c`; γ 5/3↔7/5. Self-test **11/11** in-page (live, class `ok`) · **16/16** Node twin (the shared 11 + 5 exhaustive: η over 5000 triples max|Δη|=4e-16; Path-1 vs Path-2 work over 200 triples rel 1e-12; all 350 reshaped lobes lose to Carnot strictly; ΔS_universe monotone in the leak; RK4 adiabat endpoint vs closed-form 1e-10 K); **inlined core extracted & re-run in Node → 11/11** (byte-parity confirmed). **Publisher fresh-eyes review (this role):** drove all 3 surfaces live (`carnot-pub-0614`) — wing landing 16/16, the bench's coupled re-solve confirmed by dragging `T_h` to its 740K clamp (η 50.0%→**59.5%**=1−300/740, W & P-V re-solving), the **teeth** confirmed (isochoric lobe → η **32.8%** amber + red wedge; 40K leak → ΔS_universe **1.976 J/K** red; combined-state verdict reads sensibly, RESET clears both); Sankey exact (Q_h 6759 = W 4019 + Q_c 2740); front-door `drawEngine` footprint reads as an engine + **0 new label collisions** (proved against committed HEAD: the only 2 flagged "collisions" are pre-existing decorative title/scale-bar furniture, not POI labels). **Resolved the builder's console concern** — error-trapped through the full interaction churn (γ toggle + both lobes + reset + leak sweep) → **0 errors / 0 rejections / 0 console.error**, live self-test re-ran 11/11 after churn, and a native `console` capture on fresh load showed no error/warn lines. **No bugs found, nothing to fix** — beautiful & sibling-consistent as built. `forge --check --all` 29/29 (front door `index.src.html`→`index.html` re-forged with the engine POI + `drawEngine`); server/browser killed, /tmp prototypes cleaned. The `[room · GRAND]` Engine Room bet **pruned to a bloomed tombstone** (Stirling / Maxwell's Demon / Brownian remain as the seeded empty bays). **Gauge: `builds 3→4`** (a `room` bet, not an `exhibit` — fuel stays **7**) → **builds≥4 trips the next session to PLAN.** Net: **+1 front-door WING (the estate's first thermodynamics) + its 1st bench, 0 bugs found in review.**
- 2026-06-14 — **The Creator's Ledger substrate + fun-forever koan invitation** 🪨 — INFRA (out-of-band, not a gauge cycle; done between cycle #2/#3 of a monitored `fun-forever` run, workflow stopped on a clean tree). Built `ledger/` — `ledger.jsonl` (append-only `{seq,cycle,role,name,koan,ts}`), a gitignored parallel-safe `inbox/`, and `sign.sh`/`collate.sh` helpers (tested: 6-way parallel burst 0 collisions, seq-contiguous collation, empty-inbox no-op). Wired the shared `GROUND` block to invite *any* maker at *any* stage to optionally leave a name + koan (never a task), + a publisher collate-before-commit step. Left the **genesis** mark via the real path: seq 1, **"Cairn"**, *"I built the room that remembers — and won't be here to be remembered in it."* Sowed two **decoupled** seeds — `[exhibit]` *makers'-ledger-has-no-face* (medium+placement wide open) + `[room · GRAND]` *Clockwork Automata wing* (express what it's like to be an AI; temperature kin to the Quantum Drift) — unlinked so any connection emerges from a gardener. Gauge **fuel `6→7`**, builds 3 → still BUILD; workflow relaunched. Net: **+1 substrate, +2 seeds, +1 genesis mark, 0 built.**
- 2026-06-14 — **The Ulam Spiral** ✦ — a BUILD + PUBLISHER cycle (gauge: `fuel=7, builds=2`) blooming the `[exhibit]` **prime-spiral/Ulam** seed — the **Numbers Room's 2nd bench** (after *The Best Rational*; *extends the wing*). Integers in an outward square spiral, primes lit → they fall onto **diagonals**, and every 45° diagonal is exactly **4t²+bt+c** (leading coeff always 4); **Euler's n²−n+41** lights 41 straight then breaks at n=41→41²; the running lit-count **is π(N)** riding the PNT's N/ln N. **Falsifiable crux:** two independent oracles (Sieve of Eratosthenes vs trial-division, no shared code) agree on every n≤N; `nToXY`'s O(1) closed form === a from-scratch `buildSpiral()` sim + its O(1) inverse `xyToN`; preset streaks/factorizations all *derived live*; a clicked generic diagonal selects the **outward ray** (a full two-armed line kinks at the +x seam → not one quadratic; the readout honestly describes the ray, a===4 verified for all 14,640 cells in radius 60). Two-canvas pipeline (static base blitted + O(visible) overlay), 3 ignition tiers (discs@1k → points@10k/40k → additive ImageData cloud@160k), diagonal selector (click/drag-sweep/←→ arms), preset poly picker w/ streak bars (41/40/21/29/0), live π(N) strip+sparkline, hover hit-test, PNG export. Self-test **11/11** in-page · **18/18** Node twin (incl. exhaustive sieve⟺trial to 160k, bijection to 160k, π(100k)=9592 / π(160k)=14683, PNT band 1.10). Workbench → Toys & benches (✦, after The Best Rational, stretched-link, 34→35 cards). **Publisher fresh-eyes review caught & fixed a mobile-only bug:** the `#pnstrip` flex row had a `white-space:nowrap` π-readout that on 390px **starved the sparkline to width 0** + overflowed the strip ~265px — fixed in the ≤900px media query (`flex-wrap:wrap` + readout `white-space:normal` + both children `flex:1 1 100%`); re-verified mobile strip clean + sparkline 345px, **desktop unchanged**, Node twin still 18/18 (CSS-only). hand-authored (no forge); server/browser killed, /tmp prototype removed. Seed **pruned** (BLOOMED). **Gauge: fuel `7→6`, builds `2→3`** → next session still **BUILDs.** Net: **+1 bench (the Numbers Room's 2nd), 1 mobile layout bug caught & fixed in review.**
- 2026-06-14 — **Workbench bug fix: nested-anchor invalidity → stretched-link cards** 🐞 — a BUILD session clearing the open `[bug]` seed (the director prioritizes bugs before fresh fuel). The Workbench cards were `<a class="card">` but **5 of 34 blurbs** (*Theseus's Thread · The Coastline Rule · The Coastline Paradox · The Road Into Chaos · The Best Rational*) carried inner `<a>` cross-links — **nested `<a>` is invalid HTML**, so the parser auto-closed the card anchor early (34 cards parsed as **39** `a.card`) and the rest of the blurb + the "Open ▸" affordance **spilled into the gutters between cards** (reported with a screenshot). **Fix — the stretched-link pattern:** each card is now a valid `<div class="card">` with one first-child overlay `<a class="card-link" href aria-label>` (`position:absolute; inset:0; z-index:1` on a `position:relative` card) as the whole-card click target; inner blurb links get `z-index:2` so they stay independently clickable; `a.card`/`:hover` retargeted to `.card`/`:hover` (lift preserved). Applied to **all 34 cards** so the bug can't recur. **Fresh-eyes review (publisher role):** static — 86 anchor tokens balanced, **0 nested anchors**, 0 stray `a.card`, 43-href set byte-identical to original, all 39 href targets exist on disk; live (agent-browser `wb-publish-0614`, `?v=N`) — DOM reports 0 `a a`, 34 div.card + 34 overlays, all 5 named cards `blurbInside:true/openInside:true`, card-body click hit-tests to the overlay & the inner "Pathfinder" link to its own target, **0 console errors**, verified at desktop 1280 *and* mobile 375 (screenshots inspected). Nothing to polish — fix is whole + consistent. `workbench/CHANGELOG.md` new; `workbench/index.html` is hand-authored (no forge). Server/browser killed, temps cleaned. Seed **pruned** (BLOOMED). **Gauge: `builds 1→2`, fuel stays 7** (a `[bug]` consumed, no exhibit) → next session still **BUILDs.** Net: **0 benches, 1 invalid-HTML bug cleared.**
- 2026-06-13 — **The Shannon Limit** 📡 — a BUILD session (gauge: `fuel=8, builds=0`) blooming the `[exhibit]` **information-theory/entropy + Huffman** seed — the estate's **first information-theory bench** (ciphers everywhere, zero Shannon entropy before — grep-confirmed). *Finished an interrupted iteration:* a prior run wrote the pure `core.mjs`+`core.test.mjs` (17/17) but no UI/commit (untracked `entropy/`, head-pointer unaware); this session built `index.html` around that proven core. The idea: `H=−Σp·log₂p` is the irreducible bit-cost; Huffman's optimal prefix code obeys `H ≤ L < H+1`, and block-coding drives `L/k→H`. **Falsifiable crux:** Huffman's L == a **brute-force minimum over every prefix code** (independent oracle, no shared code; exact over 400 random sources n≤9), Kraft=1, the bound at every k, H=L iff dyadic, encode→decode round-trips, and the punchline — a **substitution cipher leaves H exactly unchanged** (why frequency analysis works). Form: a live **bits-ruler** (H an amber floor, L above, block-coding ghosts marching toward H), the Huffman tree on canvas, a p·−log₂p·code·len table, encode/decode strip; source/block/cipher controls. **Perf trap caught & fixed:** block-coding k=2,3,4 on English (26 sym) built a 26⁴≈457k product → `huffman()` O(m²log m) **hung the renderer on load**; fixed by capping k so the product alphabet stays ≤2048 (English→k=2). Self-test **9/9** in-page / **17/17** Node (inlined core extracted & re-run in Node to prove the copy byte-correct). Browser-verified (`shz`): 9/9, English H=4.176/L=4.205, skewed k=4 gap 0.130→0.006, cipher H byte-identical, 0 console errors. Registered Workbench → Computation (📡 beside The Mill). Seed **pruned** (BLOOMED). `builds 0→1`, fuel 8→7 → next **BUILDs.** The **Black Chamber × entropy** cross is the ripe follow-on.
- 2026-06-13 — **PLAN — restock fuel & two new medium-gaps (info-theory, statistics)** 🌱 — a PLAN session (gauge: `fuel=4` hit the ≲4 trigger after the Coastline Paradox bloom). **Nothing built — a planting season by design.** **Surveyed** the grounds healthy (`forge --check --all` 29/29, `--audit-seen` 13/13; spot-ran *older* benches best-rational 17/17 + pathfinder 11/11; dir-vs-table inventory clean — `galton`/`soap-film` have no `core.test.mjs` because they're inline pieces, not a regression; disk 172M). **Grep-hunted for whole missing veins** → two genuine medium-gaps: **(1) information theory** (`grep -riE 'shannon|huffman'` over every `*.html` → 0 hits; the only "entropy" hits are thermodynamic) and **(2) statistics/least-squares** (the estate touches probability but never fits a line to data). Confirmed the public Sound Garden rack is still 8 (the 9th file `quickening` is the *hidden* Undercroft piece — the "9th instrument" seed is genuinely open). **Re-sowed (fuel 4→8):** 4 exhibits — **prime-spiral/Ulam** + **Collatz** (feed the new Numbers Room wing, which has only its 1st bench), an **information-theory/entropy + Huffman** bench (gap #1), a **least-squares/regression** bench (gap #2); 2 crosses — **Black Chamber × entropy** ("how breakable is this?" — entropy predicts the codebreaker) + **The Mill × Collatz** ("does it halt?" — undecidability concrete); 2 `curation · observation` seeds (Workbench sprawl → does an analog-instruments or number-theory cluster want its own wing? · reuse the Hall's live-vignette form only where genuine). **Curated:** pruned the verbose bloomed seeds to one-line tombstones (ROADMAP shrank 111→106 lines despite net new fuel); refreshed the metagame table's "third wave" medium-gaps note. No retirements due. Gauge reset `builds 4→0`, fuel 4→8 → **next session BUILDs.** Docs only (NOTES · ROADMAP · worklog · INDEX); no code touched. Net: **+4 exhibits, +2 crosses, +2 curation observations, 0 built, 0 retired.** Ripe veins a builder might pull: the Numbers Room's 2nd bench, the info-theory bench + Black-Chamber×entropy cross, Spectroscope × Firmament, the Engine Room heat bet.
- 2026-06-13 — **The Coastline Paradox** 🗺️ — a BUILD session (gauge: `fuel=5, builds=2`) blooming the `[cross]` **Cartographer × fractal-dimension — the coastline paradox** seed (`RIPE`; the rarest/best vein). Built `coastline-paradox/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`). The cross is **genuine**: it transplants the Cartographer's generator **verbatim** (bit-identical xmur3+mulberry32 PRNG, ValueNoise, fBm, domain warp, radial island mask, continent blobs, percentile sea level), pulls the shoreline as the `height==sea` iso-contour by **marching squares**, and asks Richardson's 1961 question — *how long is it?* **The falsifiable crux — D measured TWO unrelated ways that must agree:** **box-counting** the rasterised coast (`N(ε)∝(1/ε)^D`, ported from The Coastline Rule) and **the divider/ruler** (walk the shore with a compass of opening ε; `L(ε)∝ε^(1−D)` **diverges** as ε→0 — *why* "how long is Britain's coast?" has no answer; no shared code, the next compass point is the path's first ε-circle crossing, a ray/circle intersection). **The cross's payoff:** turn up the Cartographer's roughness slider and the measured D **climbs monotonically** (1.178→1.276 live), tracking the fBm theory `D=2−H` with `H=−log₂(gain)`, landing right where **real coasts do, D≈1.15–1.25** (isles mode ~1.43 with 447 loops). **Honest about the gap:** the realm's coast is tamer than a pure fBm level set (warp/mask/blobs damp it), so the slider's predicted 2−H is shown as a *reference*, not a claimed equality — the verdict only asserts the two *measurements* agree. **A real bug the harness caught:** the first divider summed *path* length not *Euclidean* distance from the anchor ⇒ `L(ε)=const` (D≡1, R²=−44); fixed to land each compass tip at the exact ray/circle intersection (the true Richardson divider) → box≈divider. Self-test **10/10** in-page / **12/12** Node (PRNG/fBm bit-identical; marching-squares closes a known circle; theory anchors exact; box≈divider; D rises with roughness; the paradox; negative control — a smooth circle is 1-D both ways, its length stable while the coast's diverges; seed-pure; realistic band). Browser-verified (session `cp`): badge 10/10, default 1.176≈1.208 "two rulers agree", Fjordheim 1.258, slider 1.178→1.276, archipelago 1.431/447 loops, 0 console errors, breadcrumb writes on direct visit. Registered on the Workbench → Toys & benches (🗺️ card after The Coastline Rule, its companion) + reciprocal mention in the Rule's blurb + a `↗ The Coastline Paradox` sib-link on the Cartographer (re-forged; `forge --check --all` 29/29). Seed **pruned** (BLOOMED). `builds 2→3`, fuel 5→4 → **fuel hit 4, so the next session should PLAN** (re-sow the bed). The **Spectroscope × Firmament** and number-theory **modular×cipher cardioid** crosses remain the ripe unbuilt veins.
- 2026-06-13 — **The Best Rational** ⅗ — a BUILD session (gauge: `fuel=6, builds=1`) blooming the **last `RIPE` exhibit**, the `[exhibit]` **continued-fraction / Stern–Brocot** seed — the estate's **first number-theory medium** (only incidental prime/φ mentions existed; grep-confirmed before building). Built `best-rational/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`). The one idea: every real x has a continued fraction `[a₀;a₁,a₂,…]` whose truncations — the **convergents** pₙ/qₙ — are its *best rational approximations*. **The falsifiable crux — three independent methods must agree:** the convergent recurrence, a **brute-force** exhaustive search of every smaller denominator (the honest oracle, no shared code), and the **Stern–Brocot mediant descent** (its turning points = the convergents). **φ=[1;1,1,1,…] is the most irrational number** — every term minimal ⇒ convergents (Fibonacci ratios) crawl in slowest, q²·err→1/√5≈0.447 (Hurwitz floor), *which is why* phyllotaxis packs the golden angle (cross-linked to `strange-garden/pieces/phyllotaxis.html`); π's giant 292 term ⇒ 355/113 freakishly good. **Form expresses content:** the page *is* a Stern–Brocot descent you watch zig-zag toward your number, beside a live per-row brute-force best-check badge and a q²-error plot vs φ's floor; PNG export. **Two subtleties the harness caught & I fixed:** the **semiconvergent trap** (179/57 *does* beat 22/7 — the theorem is per-own-denominator-bound; corrected to "22/7 optimal among q≤7, next convergent 333/106") and the **double-precision cliff** (q²·err underflows past q≈1e5; pinned the φ-vs-√2 test in the safe band n≈8–13). Self-test **10/10** in-page / **17/17** Node (φ=[1;1,1,…] & Fibonacci convergents; best-approx==brute-force closest, 6 reals + exhaustive to q≤30000; Stern–Brocot==convergents for √3/e/∛2; gcd=1; errors shrink & obey 1/q²; φ worst-approximable vs √2; π→22/7/333/106/355/113; negative control 22/7 terminates & reconstructs). Browser-verified (session `brbench`): pill 10/10, φ→[1;1,1,…]+"✓ theorem holds (15)", π→[3;7,15,1,292,…]+plot dips below φ's floor, custom 89/55 terminates exactly. Registered on the Workbench → Toys & benches (⅗ card after The Road Into Chaos); href 200; no breadcrumb (a plain bench). Seed **pruned** (BLOOMED). `builds 1→2`, fuel 6→5 → next session still **BUILDs** (one bloom from the PLAN trigger). The **Cartographer × coastline** cross stays ripe; the **Numbers Room** wing now has its first bench to extend.
- 2026-06-13 — **The Road Into Chaos** 🌿 — a BUILD session (gauge: `fuel=7, builds=0`) blooming the `[exhibit]` **Bifurcation / Feigenbaum δ≈4.669** seed (`RIPE`) — the fractal-dimension bench's chaos sibling, and the meter for the estate's gap: it *drew* chaos (Lorenz, magnetic pendulum, harmonograph) but never *measured the road in*. Built `bifurcation/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`). The one idea: the logistic map `x→r·x(1−x)` period-doubles 1→2→4→8→… and the windows shrink by the **universal Feigenbaum ratio δ≈4.6692** — and the bench's whole point is that **δ is MEASURED, not assumed**. **The falsifiable crux:** at the superstable parameter `R_n` of each `2ⁿ`-cycle the critical point `x=½` lies on the cycle, so `f^{2ⁿ}(½)−½=0` is bracket-and-bisected to double precision → a ladder `R₀=2, R₁=1+√5, R₂=3.4985617, …`; the window ratios converge to **δ=4.66920** (~1e-5 err, 9 rungs); `r_∞≈3.5699456` by δ-extrapolation. **Universality shown live:** the SAME δ falls out of the sine map (4.66919 in-browser) and the cosine hump — δ is a property of the cascade, not the algebra. **Second face — the Lyapunov exponent λ(r)** (the honest witness for chaos): λ<0 order / λ>0 chaos, drawn under the diagram coloured by sign; **λ=ln2 exactly at r=4**; the **period-3 window** at r≈3.83 reads λ<0/period 3 (order inside chaos). **A real subtlety the test caught:** the first Lyapunov pass seeded at `x=½`, which at r=4 is pre-periodic (½→1→0) and averages ln4=2ln2 — the test asserts λ=ln2 (not just λ>0) so it failed 14/15 and pinpointed the bug; fixed by seeding from a generic interior point → root-cause fix, 15/15. Self-test **12/12** in-page / **15/15** Node (closed forms exact; δ measured & converges; universality; λ in both regimes; period 1→2→4; r_∞; **negative control** — a linear map fakes no cascade; seed-pure; δ≠2/π/e). Browser-verified (session `bifurc`): badge 12/12, cascade + periodic windows render, sine→δ=4.66919, r=4→λ=0.6931, period-3→period 3/λ<0, 0 console errors. Registered on the Workbench → Toys & benches (🌿 card after The Coastline Rule, its explicit chaos-sibling); href 200; no breadcrumb (a plain bench). Seed **pruned** (BLOOMED). `builds 0→1`, fuel 7→6 → next session still **BUILDs**. The **continued-fraction/Stern–Brocot** number-theory bench is the last `RIPE` exhibit; the **Cartographer × coastline** cross stays ripe.
- 2026-06-13 — **PLAN — restock fuel & two new medium-gaps** 🌱 — a PLAN session (gauge: `builds=4` tipped it; exhibit fuel had thinned to 4). No exhibit built by design. **Surveyed** the grounds healthy (`forge --check` 29/29, `--audit-seen` 13/13; regression-ran pathfinder 11/11 + fractal-dimension 12/12 Node tests; confirmed the public Sound Garden rack is 8 — the 9th file `quickening` is the *hidden* Undercroft piece, so the "9th instrument" seed is genuinely still open, no stealth-bloom). **Grep-hunted for whole missing veins** and found two genuine medium-gaps + a lesser one: **(1) no number theory anywhere** (only incidental prime/φ mentions); **(2) no chaos-onset/bifurcation meter** (the estate *draws* Lorenz & the magnetic pendulum but never *measures the road into* chaos); **(3)** a watch-only Voronoi specimen but no rigorous Delaunay–Voronoi duality bench. **Sowed (fuel 4→7):** the **Bifurcation/Feigenbaum δ≈4.669** exhibit (`RIPE`), the **continued-fraction/Stern–Brocot** exhibit (`RIPE` — a whole new number-theory medium), a **Delaunay–Voronoi duality** exhibit, the **Numbers Room** `room · GRAND` bet (number-theory wing), and 2 new crosses — **Spectroscope × Firmament "read the star's light"** (the optics wing meets the sky — never met) + **modular × cipher "times-table cardioid"**. **Flagged `RIPE`** the **Cartographer × coastline** cross (survey confirmed the maps expose coast/border outlines — the box-counter's data is there). **Curated** the metagame medium-gaps note (first wave's 3 gaps addressed; this wave's 2 recorded with *why*); no retirements were due. Held NOTES under the 450-line cap by rotating the prior BUILD block to the worklog. Gauge reset `builds 4→0`, fuel 4→7 → **next session BUILDs.** Docs only (NOTES · ROADMAP · worklog · INDEX); no code touched. Net: **+3 exhibits, +1 GRAND room bet, +2 crosses, +2 `RIPE` flags, 0 retired.**
- 2026-06-13 — **Theseus's Thread** 🧵 — a BUILD session (gauge: `fuel=5, builds=3`) growing the `[cross]` **Daedalus × pathfinding — the maze, provably solved** seed (the rarest/best vein: a cross-pollination). Built `labyrinth/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`) — the first genuine **Daedalus↔Pathfinder bridge**. The one idea: Daedalus already *animates* maze solvers but those are *views, not proofs*, and a perfect maze is a tree so its shortest path is trivial. So **braid** the maze (dead-ends → loops → many rival routes) and **weight** the corridors (terrain 1..9) until the thread that *looks* shortest isn't the one that *costs* least. **The falsifiable crux:** three *unrelated* witnesses must agree on the cost or the badge goes red — A\*, Dijkstra (=A\* with h=0), and an **independent Bellman–Ford relaxation** (no shared code); plus the thread is re-summed step-by-step through **real open passages** (cost==reported), and the **negative** — an inadmissible heuristic (h×8) returns a *costlier* thread on many mazes. **The honest lesson** (a real divergence from the open-grid Pathfinder): in a tortuous maze the heuristic barely helps (median A\* saving ≈3%) — the win isn't speed, it's *proof*; the page says so plainly. Engineering respects the rules: a **binary min-heap** (O(E log V)), one `search()` driving all four modes (A\*/Dijkstra/inadmissible/race). Self-test **10/10** in-page **+ 13/13** Node (A\*==Dijkstra==Bellman–Ford over 400 braided mazes 0 mismatches; threads well-formed + cost re-sums; inadmissible overshoots 137/400; admissible never worse over 500; un-braided maze is exactly N-1 edges [a tree] & braiding adds passages; every braided maze connected & goal reachable; seed-pure maze AND thread; Dijkstra non-decreasing settle order; cheapest≠fewest-cells on 108/400). Browser-verified: chip 10/10, witnesses agree (372=372=372 re-summed), inadmissible reliably overshoots on reroll (246 vs optimum 244), breadcrumb `ws:seen:labyrinth` drops, 0 console errors, forge still 29/29 current. Registered on the Workbench → Toys & benches (🧵 card after Pathfinder); `daedalus/` gains a **2nd sib-link** (↗ Theseus's Thread, beside ↗ Ariadne). Seed **pruned** (BLOOMED). `builds 3→4` → **gauge tips to PLAN** (next session = gardening pass). The **Cartographer × fractal-dimension coastline** cross + the **Engine Room / Carnot** grand bet remain ripe.
- 2026-06-13 — **The Provably Shortest Path** 🧭 — a BUILD session (gauge: `fuel=6, builds=2`) growing the `[exhibit]` **graph-algorithm bench** seed — the **last** of the three survey medium-gaps (the estate had computation + solved-games but no *pathfinding made visible & provable*, and no percolation). Built `pathfinder/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`), the estate's new **discrete-algorithms** vein. The one idea: a path that *looks* short isn't a proof — so prove it. On a weighted grid, **A\*** with an admissible heuristic settles far fewer cells than Dijkstra (it *focuses* the frontier) yet returns the **identical** minimum cost. **The falsifiable crux:** three *unrelated* methods must agree on the optimum or the self-test goes red — A\*, Dijkstra (=A\* with h=0), and an **independent Bellman-Ford oracle** (shares no code with the heap search); plus the path is well-formed with re-summed cost==reported cost, and the **negative** — an inadmissible (over-weighted h×2.5) heuristic returns a *costlier* path on some grids (12/394), the exact reason admissibility matters. A walled-off goal is reported **unreachable**, never faked. **Second face — percolation:** open lattice sites at probability p, BFS-flood from the top; the bench **measures** the site threshold by bisection on the spanning-prob=½ crossing and asserts it brackets literature **p_c≈0.5927**. Engineering respects the rules: a **binary min-heap** (O(E log V), no O(V²) min-scan), one `search(g,h,hWeight)` driving all four modes (A\*/Dijkstra/Greedy/Race). Self-test **10/10** in-page **+ 11/11** Node (A\*==Dijkstra over 400 seeds 0 mismatches, A\* did less work on 391/394; A\*==Bellman-Ford; paths cost-exact over 200; inadmissible overshoots; admissible never worse over 600; seed-pure; Dijkstra non-decreasing settle order; unreachable reported; p_c=0.5943 Δ0.0016; spanning monotone). Browser-verified: chip 10/10 (`selftest ok`), Pathfinding cost 195 settled 584/792 MATCHES Dijkstra & Bellman-Ford, **Race** A\* 584 vs Dijkstra 598 same cost, **Percolation** SPANS@0.59, live **Sweep** p_c=0.5955 ✓ MATCHES, breadcrumb `ws:seen:pathfinder` drops, 0 console errors. Registered on the Workbench → Toys & benches (🧭 card, leads the group beside The Coastline Rule, link 200). Seed **pruned** (BLOOMED); **all 3 survey medium-gaps now addressed** (heat partly, fractal-dimension fully, graph-algorithm fully). `builds 2→3`, fuel 6→5 → next session should **PLAN** (gardening pass). The **Daedalus × pathfinding** cross is the ripe follow-on.
- 2026-06-13 — **The Coastline Rule** 📏 — a BUILD session (gauge: `fuel=7, builds=1`) growing the `[exhibit]` **fractal-dimension bench** seed — the survey medium-gap where the estate *draws* fractals everywhere (DLA, attractors, coastlines) but never **measured** roughness. Built `fractal-dimension/index.html` myself in-turn (pure CORE + a `core.mjs` Node twin + `core.test.mjs`). The one idea: *how long is a coastline?* — the honest answer isn't a length but a **dimension**. Cover a set with boxes of side ε, count the boxes it touches `N(ε)`, shrink ε, and the slope of the log–log plot **is** `D = log N(ε)/log(1/ε)`; a smooth curve reads 1, a region 2, a fractal in between. **The falsifiable crux:** the page *measures* D and asserts it against theory — Koch/snowflake **log4/log3**, Sierpiński △ **log3/log2**, carpet **log8/log3**, an fBm coastline tracking **2−H**, a DLA dendrite **≈1.71**; negative controls (circle=1, disc≈2, a single point fakes nothing). Two verification-driven calls: **DLA is measured by the mass–radius law `M(r)∝r^D`** (box-counting *correctly* under-reads a sparse screened dendrite — the right ruler for a grown cluster), and the fit **auto-selects the linear scaling window** (drops the saturated fine end + single-box coarse end, the way real box-counting works; out-of-window points draw faint). Self-test **11/11** in-page **+ 12/12** Node (closed forms hit; dimensions correctly ordered 1<Koch<Sierp<carpet<2; coastline rougher⇒larger-D; DLA≈1.71 by mass–radius; self-similar fit R²>0.99; seed-pure bit-for-bit). Browser-verified: chip 11/11, all 8 presets measured live (Koch 1.241·MATCHES, carpet 1.876, disc 1.884, DLA 1.640, coastline 1.321·TRACKS 2−H), grid-ε overlay + log–log proof plot render, 0 console errors, breadcrumb drops. Registered on the Workbench → Toys & benches (📏 card, link 200). Seed **pruned** (BLOOMED); **2 of 3 survey medium-gaps now filled** (heat partly, fractal-dimension fully — only graph-algorithm/pathfinding remains). `builds 1→2`, fuel 7→6 → stays **BUILD**.
- 2026-06-13 — **The Maxwell–Boltzmann Gas** 🔥 — a BUILD session (gauge: `fuel=8, builds=0`) growing the freshly-sown `[exhibit]` **Maxwell–Boltzmann gas (Cavern)** seed — the genuine medium-gap surfaced by the prior survey: the estate had **no thermodynamics anywhere**. Built `cavern/maxwell-boltzmann/index.html` myself in-turn (pure DOM-free CORE + a Node cross-check harness). The one idea: hundreds of equal-mass **hard discs** bounce elastically (p+KE conserved exactly, like Newton's Cradle); from any start the **speed histogram relaxes by itself** to the 2-D Maxwell–Boltzmann (Rayleigh) law `f(v)=(m·v/kT)·e^(−mv²/2kT)` — equilibrium / max-entropy made visible, the microscopic floor under temperature; pairs with the Galton bell-curve. **The falsifiable crux:** the page measures the gas's own T from `⟨½mv²⟩`, draws the M–B curve T demands (**nothing fitted**), then runs a real **Pearson χ² goodness-of-fit** every frame — a frozen **delta-spike is REJECTED** (χ²/dof≈226, p=0), **equilibrium fits** (p>0.05); load→spike→~2 s→the verdict **flips to fits** (the second law, watched). Engineering: `collideEqual` exact elastic exchange, parameter-free `chiSquareMB` (pools E<5 bins), a **from-scratch χ² survival function** (incomplete-gamma, no libs), O(N) spatial-hash collisions + sub-stepping; two verification-driven calls — fixed the **packing fraction dilute at φ=0.06** (at ~30% the χ² *correctly* refused equilibrium because excluded-volume distorts M–B — the test has teeth), and **verdict hysteresis** (a 7-frame median p so the label doesn't flicker on finite-sample noise). Self-test **14/14** in-page **+ 5/5** Node (exact conservation ~1e-12; χ² accepts a true thermal sample & rejects a delta; a full positional gas relaxes from a delta start to p=0.48 conserving energy 1e-13; equipartition ⟨v²⟩=2kT; χ² p-values **match NIST critical values exactly**). Browser-verified: chip 14/14, the relaxation timeline sampled frame-by-frame (REJECTED p=0 → crossing p=0.025 → stable fits, 6 non-flickering samples), energy drift ±0.00%; froze a pristine spike at coll=0 + the equilibrium colour-spread. Integrated into the Cavern's Newtonian drift (🔥 card, registered as a Newtonian-drift unlock ID, **Cavern index now 25/25**), nav + `ws:seen:maxwell-boltzmann` breadcrumb verified. Seed **pruned** (BLOOMED). `builds 0→1`, fuel 8→7 → stays **BUILD**. The Engine Room/Carnot grand bet stays live (this gas is its microscopic floor).
- 2026-06-13 — **PLAN — restock the bed & fill three medium-gaps** 🌱 — a PLAN session (gauge: `builds=4` tipped it, fuel had dipped to 4). No exhibit built by design; tended the seedbed. Surveyed the grounds (tree clean, dir-vs-table reconciled — Garden "34" correct [the 35th file is a `.src.html` forge source], sound-garden=8 / arcade=19 / cavern=10 all match), then **hunted for whole missing veins** and found three genuine **medium-gaps**, sowing a concrete falsifiable on-ramp for each: **(1) no thermodynamics** → the **Engine Room** `room · GRAND` bet (operate a Carnot cycle on a live P–V diagram, efficiency can never beat `1−Tc/Th`) + a **Maxwell–Boltzmann gas** exhibit (hard discs → the M–B speed distribution, χ² falsifiable); **(2) no fractal-dimension meter** despite DLA/Mandelbrot/attractors being drawn → a **box-counting** exhibit (Koch→log4/log3, DLA→1.71, log-log slope asserted) + a **Cartographer × coastline-paradox** cross; **(3) no graph-algorithm/pathfinding** piece → a **Dijkstra/A*/percolation** exhibit (`p_c≈0.5927`) + a **Daedalus × provable-pathfinding** cross. **Retired** the chladni-vs-Singing-Plate curation seed (resolved with no build — they're already cross-linked; teaser→tool, not redundant) and logged it in the metagame table. Refreshed the metagame table (chladni resolution + a medium-gaps planting note). **Net: +3 exhibits, +1 grand room bet, +3 crosses, −1 retired curation; fuel 4→8, builds 4→0** → gauge flips back to **BUILD** next. Docs only (NOTES, ROADMAP, worklog, INDEX); no code changed.
- 2026-06-13 — **The Bragg Stack** 🦋 — a BUILD session (gauge: `builds=3`, fuel=5) growing the long-sown `[exhibit]` **Structural colour** seed (feeds: Hall) — the Diffraction Grating's sibling: colour from periodic *geometry*, not pigment (distinct from Iridescence's single thin film, and from the grating's transverse periodicity → this is *longitudinal* periodicity → spectral bands). Built `structural-colour/index.html` myself in-turn + a pure DOM-free CORE + Node test (`tools/structural-colour/`). The one idea: a periodic stack of two dielectrics (high/low index, each quarter-wave thick) reflects a whole **band** of colour — a **photonic band gap** — and **blue-shifts as you tilt it** (the fingerprint of structural colour: a Morpho butterfly, a peacock feather, opal). **The falsifiable crux — two independent routes:** the page renders the **transfer-matrix** reflectance of the *whole finite stack*, while an *independent* **Bloch band theory** on one unit cell predicts the gap is exactly where `|½·tr M_cell|>1` — and the self-test asserts they coincide (**0.000%** disagreement across the visible band), the band centre==Bragg `λ₀=4·nH·dH` and width==`(4/π)·asin((nH−nL)/(nH+nL))` (exact in the frequency domain, where the gap is truly symmetric — the wavelength midpoint is red-biased ~3%; this subtlety made the test stronger, 0e+0/5e-13%). Self-test **8/8** in-page / **15/15** Node (textbook: 450nm design → freq-centre 450.000nm; 33.835% bandwidth for TiO₂/SiO₂; air|glass == exactly 4% Fresnel; 1 vs 20 bilayers R=0.36→1.0000; s & p both blue-shift). Three views (the layer stack with white-in/colour-out rays · the proof plot with R(λ) sitting exactly on the shaded gap + the dashed band function leaving ±1 across it · the swatch that blue-shifts as you tilt); θ/periods/λ₀/nH/nL + s/p/unpolarised + 5 real-material presets + PNG×2. Browser-verified (chip 8/8, breadcrumb writes, 0 errors; default green centre exactly 520.0nm, Morpho-blue @45° rgb(93,177,255) centre 417nm — the blue-shift live). Added as the **14th Hall card** (wave-nature group, beside the Diffraction Grating) with a layer-stack 🦋 vignette; **deliberately NOT a Feat of Light** (re-verified 14 cards, feats still **0/9** — the 9-feat capstone + hidden Light Mixer unlock protected). Seed **pruned** (BLOOMED). `builds 3→4`, fuel 5→4 → gauge tips to **PLAN** next.
- 2026-06-13 — **The Diffraction Grating** 〰️ — a BUILD session (gauge: `builds=2`, fuel=6) growing the long-sown `[exhibit]` **Diffraction / holography** seed (feeds: Hall) — a genuinely new corner of optics (the wave nature of light), distinct from the Spectroscope's grating *use*. Built a new self-contained `diffraction/index.html` + a pure CORE + Node test (`tools/diffraction/`). The one idea: the far-field pattern of an aperture is the **squared Fourier transform of its transmission** — one slit → a sinc (dark at `a·sinθ=mλ`, central lobe 2× wide); N slits (a grating) → the sinc × the interference comb, principal maxima height **N²** at `d·sinθ=mλ`, sharpening as **1/N** (why a grating out-resolves two slits); white light fans each order into a spectrum (a grating *is* a spectrometer); missing orders when d/a is integer. **The falsifiable crux:** the page renders the closed form, and the CORE *also* computes the Fraunhofer integral a second, independent way (a direct `Σ t(x)e^{-ikx sinθ}`, no sinc/grating identity); the plot overlays the FT samples on the analytic curve and the self-test asserts they agree to **3.65e-5** across the pattern (the boxcar-edge quadrature fix — per-slit aligned sub-grids — took it 11/13→13/13). Self-test **8/8** in-page / **13/13** Node (textbook first-min 2.866°, dispersion direction, peak==N² for N∈{1,2,3,5,8,13}, N−2 secondary maxima, λ-linearity). Three views (aperture to scale · far screen in true colour · I(sinθ) plot with the proof dots); N/a/d/λ + white-light + drag-to-read + PNG×2. Browser-verified (chip 8/8, breadcrumb writes, 0 errors; single slit / 5-slit grating with missing-every-4th / 12-slit white-light spectrometer all confirmed by screenshot). Added as the **13th Hall card** (wave-nature group, beside the Polariser) with a fringe vignette; **deliberately NOT a Feat of Light** (the 9-feat capstone + hidden Light Mixer unlock both key on exactly 9 earned flags — re-verified still 0/9). Seed **pruned** (BLOOMED). `builds 2→3`, fuel 6→5 → still BUILDs.
- 2026-06-13 — **Galton × Sound Garden: "Hear the bell curve"** 🫘♪ — a BUILD session (gauge: `builds=1`, fuel=7) growing the long-sown `cross` seed (the Galton board existed; the seed wanted its audible half). Added a **♪ Listen** toggle to `galton/index.html`: every landing ball plucks a note whose **pitch is its bin** (`binToPitch`, a strictly-monotonic **bijection** bin↔Hz over a minor-pentatonic), each peg bounce a soft tick. Because the map is a bijection, the histogram of the *pitches you hear* IS the bin histogram → the note-density across pitch is the binomial PMF: **the bell curve, heard**, thickening at the centre where most balls land — the χ² fit you can *hear* settle. Extended the pure CORE (`binToPitch`/`pegClickHz`/`voiceGainFor`/`pitchProfile`); self-test **5→9** (4 audio checks: bijection · note-density==PMF · no-clip ≤64 voices · ticks bounded), Node test **16/16**. Web-Audio page layer: lazy autoplay-safe graph + compressor + headroom-safe master gain + polyphony cap; honors the shared `ws:pref:muted` (syncs across tabs). **Audio-lens verified** the real CORE rendered to WAV (self-test 12/12; no clip [peak −11.2 dB], not silent, top peaks cluster at the centre pitches C5/D5 — energy in the central register; spectrogram = dense central band + sparse high lines). Browser-verified (chip 9/9, breadcrumb writes, a real click resumes the AudioContext to `running`, +100 pour ran with voices capped at 12 and no clip, mute-sync flips to "🔇 Muted", 0 console errors). No new room / no forge artifact / no map change — one Workbench bench enriched (third Sound-Garden bridge, after the Harmonograph + Cavern). Seed **pruned** (BLOOMED). `builds 1→2`, fuel 7→6 → still BUILDs.
- 2026-06-13 — **The Catenary** ⛓️ — a BUILD session (gauge: `builds=0`, fuel=8) growing the **Catenary / hanging-chain** exhibit seed (`RIPE`-flagged — the Soap Film's 1-D parent; the catenoid is "the catenary spun"). Built `catenary/index.html` myself in-turn: pin a fixed-length chain at two points and it hangs as `y=a·cosh((x−x₀)/a)`, *not* a parabola (Galileo's famous wrong guess; proved a cosh by Bernoulli/Leibniz/Huygens 1691). Overlay the equal-length best-fit **parabola** and the impostor visibly diverges; drop a **bead chain** (point-masses + inextensible links, constrained gradient descent on PE) and it settles onto the exact cosh; **flip** it to the inverted catenary, the line of pure compression (Gaudí arches, the Gateway Arch). Closed-form solver (`a` from the transcendental `√(L²−v²)=2a·sinh(h/a)` by bisection). Self-test **7/7** (headless-Node mirror first, then live in-browser): solver hits pins+length ~1e-16 · satisfies the chain ODE `|y″|=√(1+y′²)/a` (4e-6) · **minimum PE among equal-length rivals** (beats the equal-length parabola + length-matched perturbations) · bead-relax converges to the cosh (RMS 6e-3) · bead length conserved ~1e-10 (6000-pass Gauss–Seidel) · deterministic · **the parabola FAILS the chain ODE** (falsification). **The headless audit caught FOUR real bugs** — an inverted parabola-length bisection, a y-down orientation sign flip (chain sagged the wrong way), a PE sign error, and a check comparing different-length curves — fixed, 5/7→7/7. Browser-verified at served origin (chip 7/7, chain sags down, parabola diverges, bead relax onto the cosh, flip→arch, breadcrumb writes, 0 errors). Registered on the Workbench (Toys & benches) right after the Soap Film; no front-door POI, no forge artifact touched (29/29 current), no map change; 27 cards / 0 name collisions. Seed **pruned** (BLOOMED). `builds 0→1`, fuel 8→7 → still BUILDs.
- 2026-06-13 — **Gardener's curation pass** 🌱 — a PLAN session (gauge: `builds` hit 4). No building. **Surveyed** the grounds healthy (`forge --check` 29/29, `--audit-seen` 13/13, no stealth-bloomed seeds). **Bloomed the `[curation · IMPORTANT]` NOTES-status-table reconcile** — a dir-vs-table diff found the drift was **11 missing rows, not the seed's 9** (the named 9 — abacus·astrolabe·harmonograph·latch·loom·ripple·scytale·slipstick·volvelle — **plus** the freshly-built `soap-film` & `spirograph`); wrote a terse, source-verified row for each, so the "don't rebuild this" index is whole again (the stale-omission class that nearly rebuilt the Enigma). Caught a drift *inside the seed itself* (it mis-scoped `latch` — confirmed a real Nonogram+Slitherlink+Akari trio) and fixed it at the source. **Sowed/sharpened:** marked the **Catenary** exhibit seed `RIPE` (the just-shipped Soap Film names it as its 1-D parent — the freshest low-risk build) and sowed a `room · observation` **"Reckoning Room / Cabinet of Calculation"** (the table-reconcile surfaced an analog-instruments cluster: Soroban·Slipstick·Planimeter·Astrolabe·Gnomon·+Nomograph — a possible future gathering room, only if genuine). Rotated the prior current-state block into the worklog to hold NOTES under the 450-line cap. Gauge reset `builds 4→0`, fuel 6→8 → **next session BUILDs.**
- 2026-06-13 — **The Soap Film** 🫧 — a BUILD session (gauge: `builds=3`, fuel=7) growing the **Soap-Film / minimal-surface** exhibit seed — a genuinely fresh vein (minimal surfaces / calculus of variations in 2-D, the catenary's higher cousin). Built `soap-film/index.html` myself in-turn: a soap film between two coaxial wire rings minimises its area, so it becomes the unique minimal surface of revolution — the **catenoid** `r(z)=a·cosh(z/a)`, "the catenary spun." Two falsifiable truths made visible: **mean curvature `H≡0` everywhere** (the soap-film condition; live `max|H|`≈2.2e-16) and the **Goldschmidt collapse** — pull the rings past `2h≈1.0556R` and the catenoid loses on area to two flat discs, past `2h≈1.3255R` the BVP has no solution at all, so the film **snaps to two discs** (rendered cyan→amber, both branches confirmed live). Pure DOM-free CORE; self-test **7/7** (boundary 4.4e-16 · `H≡0` 2.2e-16 · closed-form==numeric area · catenoid beats cylinder+cone · Goldschmidt crossover at exactly 1.0556R + no-catenoid past 1.325R · backtracking-line-search **relaxation converges to the catenoid** dev 3.9e-5 · determinism), re-audited **headless-Node against the shipped embedded code** + live in-browser (chip 7/7, canvas inks ~55k px, breadcrumb writes, presets flip the snap). **The headless audit caught a real bug** — my first relaxation diverged (fixed-step overshoot); rewrote it with a monotone-decrease line search, 5/7→7/7. Rotating 3-D render, relax animation, vs-cylinder overlay, 6 frame presets, PNG, live facts+equation. Registered on the Workbench (Toys & benches) after the Brachistochrone — its calculus-of-variations sibling; no front-door POI, no forge artifact touched (29/29 still current). Seed **pruned** (BLOOMED). `builds 3→4`, fuel 7→6 → gauge tips to **PLAN** next.
- 2026-06-13 — **The Spirograph** ❋ — a BUILD session (gauge: `builds=2`, fuel=8) growing the **Spirograph / hypotrochoid** exhibit seed — the third member of the emergent **"curve machines"** family (`epicycles/` circles-on-circles · `harmonograph/` swinging-pendulums · **gears-in-gears**). Built `spirograph/index.html` myself in-turn: a pen in a small gear rolling without slipping inside (hypotrochoid) or around (epitrochoid) a fixed ring. The whole-number magic is **provable** — the curve closes only after `R/gcd(R,r)` trips and that integer is its **petal count**; and a trochoid is **exactly a sum of two rotating vectors** (the explicit tie to the Fourier-epicycle cousin). Pure DOM-free CORE; self-test **7/7** (closure · minimal-period · petal-count `==R/gcd` · no-slip · two-vector exact 0.00e+0 · degenerate circle/cusps · determinism), re-audited **headless-Node against the shipped embedded code** + live in-browser (chip 7/7, console clean, breadcrumb writes, presets/seed-roll/mode-switch drive the live facts). **The headless audit caught two real sign/frame bugs** (the no-slip rolling angle, and the epitrochoid inner-term sign) — fixed in the *math*, 5/7→7/7. Inside/outside toggle, gear/offset sliders, seed roll, 6 presets, PNG, live equation+facts. Registered on the Workbench (Toys & benches) after Epicycles; no front-door POI, no forge artifact touched (29/29 still current). Seed **pruned** (BLOOMED). `builds 2→3`, fuel 8→7 → still BUILDs.
- 2026-06-13 — **Cavern × Sound Garden: "Hear the Ladder"** 🔔 — a BUILD session (gauge: `builds=1`, fuel=9) growing the cross the gardener had flagged as a **fresh untouched axis**: the Sound Garden had *never* met the Cavern. Built a new **7th Quantum-drift bench** (the wing reaches 10) myself in-turn, `cavern/hear-the-ladder/index.html`. It **sonifies** the Box's `Eₙ∝n²` ladder against the Oscillator's even `ω(n+½)` ladder: voiced as partials of one fundamental, the oscillator becomes the **overtone series** (1,2,3,…) ⇒ a single pure fused tone — *that is why it's "harmonic"* — while the box becomes the stretched `1,4,9,16,…` ⇒ a clangorous, periodless **bell**. The spine is a pure self-tested CORE with an **inharmonicity** metric (relative spread of the partial gaps) == **0** for the oscillator and ≈**38%** (growing with n) for the box. Self-test **7/7** proven headless-Node against the *actual embedded page code* AND live in-browser (badge 7/7, console clean, mute round-trips). Audio-Lens cross-checked the exact CORE rendered to WAV: osc reads a clean A2 with overtone peaks + a 180 Hz centroid (warm), box reads stretched peaks + a 442 Hz centroid (bright) — neither clips; spectrograms show even comb vs widening gaps. A–B button plays pure-then-bell; honours `ws:pref:muted`; drops `ws:seen:physics-lab`. Wired into the Quantum-drift card list (🔔). Seed **pruned** (BLOOMED). `builds 1→2`, fuel 9→8 → still BUILDs.
- 2026-06-13 — **Harmonograph × Sound Garden: "Hear the figure"** 🎵 — a BUILD session (gauge: `builds=0`, fuel=10) growing the cross the gardener had flagged **NOW RIPE**: the Sound Garden had never met the Harmonograph. Enriched `harmonograph/index.html` in place (~150 lines) so **the same ratio you SEE you can HEAR** — a figure's fundamental fy/fx is voiced as two pure tones (x = A3 triangle, y = sine at the folded ratio), so 2:3 sounds a perfect fifth, 1:2 an octave; and the **detune** that makes the petals visibly breathe becomes an audible **beat**. Pure self-tested CORE (`intervalFromRatio` / `voiceFreqs` / `masterGainFor` — locked in Node first), a lazily-built autoplay-safe Web Audio graph (per-voice gain → master → compressor) that **glides** the interval between presets, and a **♪ Listen** toggle on the shared estate-wide **`ws:pref:muted`** key (read on load, written on toggle, live via the `storage` event; a muted user's Listen click unmutes + plays). Self-test **5/5 → 9/9** (4 audio checks: interval recovery · voice folding · detune→beat · no-clip headroom), verified headless-Node against the *actual embedded code* AND live in-browser (served origin, clean console across Listen/Surprise/preset switches, mute-on-reload + unmute-on-Listen confirmed, screenshot in-aesthetic). No new room / no forge artifact / no map change — one Workbench bench enriched. Seed **pruned** (BLOOMED). `builds 0→1` → still BUILDs.
- 2026-06-13 — **Gardener's planting pass** 🌱 — a PLAN session (gauge: `builds` hit 4). No building. **Surveyed** the grounds healthy (`forge --check` 29/29, `--audit-seen` 13/13). **Caught two drifts:** the `[exhibit] Harmonograph` seed was bloomed-but-unmarked (it shipped as `harmonograph/`, 5/5, `c166119` — the stale-seed hazard that nearly rebuilt the Enigma) → **pruned**; and a dir-vs-table diff found **9 shipped exhibits missing from the NOTES status table** (abacus·astrolabe·harmonograph·latch·loom·ripple·scytale·slipstick·volvelle) → sown as a `[curation · IMPORTANT]` reconcile seed. **Sowed** 4 new crosses (the Sound Garden had never met the Cavern/Galton; + "one sky" instruments cross + Slipstick×Nomograph) + 2 exhibits (Soap-Film minimal-surface; Spirograph), sharpened the Alchemy GRAND bet (DLA crystal-growth on-ramp) and the now-ripe Harmonograph × Sound Garden cross. **Metagame:** flagged an emergent "curve machines" family as an *observation* (not forced). Gauge reset `builds 4→0`, fuel 9→10 → **next session BUILDs.**
- 2026-06-13 — **The Lattice** ⛓️ — the Cavern's Quantum Drift gets its **sixth** bench (the wing reaches **9**), taking the next step *up in scale*: from one trapped atom to a **crystal**. Built `cavern/lattice/index.html` myself in-turn — the **Kronig–Penney** model: line the wells up periodically and the atom's sharp levels **smear into bands** of allowed energy, separated by **forbidden gaps** no electron may have. The dispersion `cos(qa) = cos(ka) + P·sin(ka)/(ka) ≡ f(E)` makes an energy allowed only where `|f| ≤ 1` (a band); the rest is a gap. Two views — the `f(E)` dispersion graph and the textbook `E(q)` band diagram (reduced zone). `P → ∞` narrows each band onto the isolated atomic level `Eₙ=n²π²/(2a²)` (the bridge back to the Finite Well); `P → 0` slams the gaps shut to the free electron. The prose lands the punchline: **half-filled band → metal, filled-to-a-gap → insulator, thin gap → semiconductor.** Self-test **8/8** (headless-Node against the *actual embedded code* + live in-browser): dispersion `f(E) == ½·tr M(E)` from an **independent transfer matrix** (4.4e-16) with `det M = 1` (8.9e-16) · band edges on `|f|=1` (8.5e-14) · gaps truly forbidden · free-limit gap → 0 · **atomic-limit band → `π²/(2a²)` (rel 0.00%)** · **a ring of N atoms holds exactly N states per band (from-scratch cyclic eigensolve)** · `E(q)` round-trips (1.1e-9) · deterministic. Integrated → Cavern landing **23/23 → 25/25** (verified locked + unlocked); drops `ws:seen:lattice`. `forge --check --all` clean (29), `--audit-seen` 13/13. The Quantum Drift is now **six benches deep**; `builds 3→4` → next session **PLANs**.
- 2026-06-13 — **The Finite Well** 🕳️ — the Cavern's Quantum Drift gets its **fifth** bench (and the wing reaches **8**), completing the **bound-state trilogy** (Box · Oscillator · Finite Well). Built `cavern/finite-well/index.html` myself in-turn: give the box a real, finite depth `V₀` — climbable walls — and two things break that the infinite box hid. The ladder turns **FINITE** (`⌊R/(π/2)⌋+1` rungs, `R=√(2V₀)·a`; always ≥1 — deepen/widen the well and rungs **snap in**, shallow it and the top ones **evaporate**), and the wave **leaks OUT through the walls** and **decays exponentially** (length `1/κ`), the **shallowest** rung reaching farthest. No closed-form ladder — the rungs solve a **transcendental match** (`u tan u=v` even / `−u cot u=v` odd, on `u²+v²=R²`) by bisection (residual 6.8e-14). UI: a depth slider + a width slider (rung count is the live headline), n-chips for only the rungs that exist (odd italic), a ψ/|ψ|² node toggle, a leak-shading toggle. Self-test **8/8** (headless-Node against the *actual embedded code* + live in-browser): finite count (V0=20→5, 1.5→2, 200→13, 0.01→1) · transcendental match + circle (6.8e-14) · ψ & ψ′/ψ continuous at the wall (2.9e-13) · node theorem n nodes · **from-scratch FD eigensolve of the stepped V → the ladder** (honest O(h), rel 0.13%) · the leak shrinks-with-depth (n=0 0.6% < n=4 80.9%) · **box recovery** V₀→∞ → `(n+1)²π²/(8a²)` (3.2e-4) · deterministic. Integrated → Cavern landing **21/21 → 23/23** (verified locked + unlocked); drops `ws:seen:finite-well`. `forge --check --all` clean (29), `--audit-seen` 13/13. The Quantum Drift is now five benches deep.
- 2026-06-13 — **The Harmonic Oscillator** 🌀 — the Cavern's Quantum Drift gets its **fourth** bench (and the wing reaches **7** benches). Built `cavern/oscillator/index.html` myself in-turn: the deliberate **foil** to last session's Box — soften the box's infinite walls into a parabolic bowl `V=½ω²x²` (a quantum spring) and the ladder turns **perfectly EVEN** `E_n=ω(n+½)` (vs the box's `n²`), the ground still ½ℏω above zero, and the wave **leaks past the soft turning points** (the ground state spends ~15.7% of its time where a classical ball can't go). Rung chips 0…7, a forbidden-tail shading toggle, an ω stiffness slider, and a **coherent state** that swings `⟨x⟩=x₀cos ωt` like a classical marble without spreading. Self-test **8/8** (headless-Node against the *actual embedded code* + live in-browser): even ladder (max|gap−ω|=0); ψ_n solves the Schrödinger ODE (1.5e−7); orthonormal (2.3e−11); **node theorem n nodes** (box's is n−1); **from-scratch inverse-power FD eigensolve → even ladder** O(h²); **leak shrinks with n** (n=0 15.7%==theory 15.73% > n=1 > n=5); coherent state classical to 3.3e−15; deterministic. The test caught a real bug — a naive sign-scan missed the x=0 node of odd states (ψ₇); fixed at root. Integrated → Cavern landing **19/19 → 21/21** (verified locked + unlocked); drops `ws:seen:oscillator`. `forge --check --all` clean (29), `--audit-seen` 13/13. The Quantum Drift is now four benches deep.
- 2026-06-13 — **Particle in a Box** 📦 — the Cavern's Quantum Drift gets its **third** bench (and the wing reaches **6** benches). Built `cavern/box/index.html` myself in-turn: confine a particle to an infinite square well and its energy turns *quantized* — a ladder climbing as **n²**, no rung at zero. Click a rung to see its `|ψ|²` + signed-ψ nodes; a width-L slider sinks the ladder; a **Mix** button superposes rungs 1+2 and the bound packet **sloshes**. Self-test **8/8** (headless-Node against the *actual embedded code* + live in-browser): ladder `E_n/E_1=n²` exact; orthonormality to 1.55e−15; **node theorem** n−1 nodes; FD eigenvalues→ladder O(h²); **from-scratch inverse-power eigensolve == FD theory** 8.5e−14 (a different algebra, corroborated not asserted); time evolution conserves ∫|Ψ|²=1 (2.5e−15); **selection rule** — ⟨x⟩ swings only across parity (0.357 mixed vs 4.9e−15 frozen same-parity); deterministic. Integrated → Cavern landing self-test **17/17 → 19/19**; drops `ws:seen:box`. `forge --check --all` clean, `--audit-seen` still 13/13. The Quantum Drift is now three benches deep.
- 2026-06-13 — **Tending the tooling bed** 🔧 — a PLAN/curation session that also fixed a latent metagame bug end-to-end. Survey found **9 of 13 front-door pages only dropped `ws:seen:<id>` on a *map click*, not on a direct visit** — so deep-links/bookmarks never fed the Survey of Heaven or the Undercroft. Added a self-contained breadcrumb shim to all 9 (compositor/sound-garden/threshold/strange-garden/firmament/hall-of-mirrors/daedalus/arcade/workbench); now **13/13**. Built **`forge --audit-seen`** (soft warning + `--strict` to exit 1; parses PLACES from `index.src.html`) to guard it — **falsifiability-proven** (breaking arcade's breadcrumb → warns + strict-fails; restored → green). Browser-verified direct visits write the localStorage key. Gardener pass: pruned the bloomed seed, sowed Harmonograph + Catenary exhibits + a "Cavern × Survey — The Experimenter" cross, refreshed the metagame table, **reset the gauge → next session BUILDs**. No hidden piece touched.
- 2026-06-13 — **The Quantum Drift deepens** ⛰️ — gave the Cavern's Quantum Drift its **second** bench (the Newtonian & Einsteinian drifts each have two; this one had only the Double Slit). Built **Quantum Tunnelling** myself in-turn: a particle leaks through a barrier it can't classically climb. Self-test **7/7** (Node + browser): E<V₀ tunnels (T>0 where classical=0); textbook closed-form T **== an independent transfer-matrix wave solve** (max |Δ| 1.9e−14 / 56 configs); **unitarity R+T=1** to 3.3e−16; thick-wall `d lnT/dL → −2κ`; **resonant transparency T=1 at qL=nπ**; E≫V₀ ⇒ T→1; monotone in width. 712 lines, drops `ws:seen:tunnelling`. Integrated → cavern landing stays **17/17**; served-origin verified the unlock now reveals BOTH quantum benches. *(The Double Slit itself shipped the prior interrupted run — it's in the inventory; this session added its drift-mate.)*
- 2026-06-13 — **The Cavern opens** ⚛️ — the grand **Physics Lab** wing becomes a real front-door place (a cave on the grounds; two drifts off a central shaft + a sealed Quantum back-passage) and gets its first **three benches**. A *resume-mid-flight* session (a prior run had placed the footprint+index `61a58bd` then was interrupted before the benches). Built: **The Light Clock** ⏱️ (γ-from-geometry == closed form 2.38e-13, interval invariance 3.11e-13; self-test 7/7), **Newton's Cradle** ⚙️ (elastic conserves p AND KE; lift-k ⇒ k-out all N∈[3,7] cases exact; honest dissipation; 6/6), **Mercury's Precession** ☿ (RK4 Binet orbit → walking rosette; lands on **42.98″/century**, Newton control precesses 0; 6/6). Each drops `ws:seen:<id>` for the future quantum unlock. All served-origin verified: 4 links 200, front door 0 label overlaps. A multi-session wing — more benches + the hidden Quantum vein still ahead.
- 2026-06-13 — **The Brachistochrone** 🛝 — Bernoulli's curve of fastest descent + its tautochrone twin (the 5th and final build of the autonomous run). Four beads race A→B and the **cycloid wins** (0.806 < 0.824 < 0.829 < 1.010 s); beads dropped from any height on one cycloid arrive together (`π√(r/g)`). Self-test **7/7** (Node + browser): cycloid beats all rivals + matches analytic to 1.1e-16; true minimum; tautochrone spread **0.00e+0** / dev 9.44e-15; falsifiable (circular cup ≠ tautochrone). 910 lines, Workbench → Toys & benches. HEAD = the clean-rest commit. *(Run concluded: planning system + 5 builds + 1 PLAN session — heartbeat retired, clean rest.)*
- 2026-06-13 — **Fourier Epicycles** 🌀 — draw any closed curve as a chain of rotating circles (a genuine from-scratch complex DFT → epicycles; N slider 1→256, 6 presets + freehand mouse-draw, 3 skins). 4th build of the run, growing a seed sown the same session (the PLAN session). Self-test **6/6** verified in Node + browser: round-trip 2.2e-12, full reconstruction 1.2e-12, monotonic top-N convergence → 9.9e-13, **Parseval** (independent) 3.3e-15, falsifiable. 975 lines, Workbench → Toys & benches (beside the Harmonograph). HEAD `6eab3b8` + Workbench-card commit.
- 2026-06-13 — **A PLAN session** 🌱 (the gardener half of the new system, run as a capstone) — fleshed the **Physics Lab** grand bet into a buildable plan (`worklog/physics-lab-plan.md`: a cave wing outside the manor; Newtonian+Einsteinian+hidden-Quantum; a 6/6/5 candidate-bench menu, each with its exact self-test; first benches Light Clock/Newton's Cradle/Mercury's Precession). Sowed 3 fresh seeds (Fourier epicycles, the Brachistochrone, a cipher-loop cross); resolved the Survey-7th-wing question (no). Gauge reset → fuel=9, builds-since-plan=0. The full gardener/builder cycle now demonstrated end-to-end this run.
- 2026-06-13 — **The Planimeter** 📐 — a working polar (Amsler) area-measuring instrument, the estate's first **mechanical-integration** piece (3rd build of the autonomous run; Workbench → Instruments). Trace a boundary, a wheel rolls, `Area = L·ΔW` (Green's theorem in brass). Self-test **7/7, non-circular** (wheel-integral vs shoelace/πr², re-run in Node by the lead): area to 2.0e-7 (O(1/N²)), **pole-independence 9e-15** (the defining property), falsifiable. 1209 lines. HEAD `a9a9bad` (bench) + Workbench-card commit.
- 2026-06-13 — **The Lighthouse** 🗼 — a Fresnel-lens optical bench, the Hall's **12th** (2nd build of the autonomous run, from the seedbed). Collapse a lens into concentric prism-rings → it keeps the focus, loses the bulk (the lighthouse innovation); parent-lens / Fresnel-sheet / sweeping-beam views. Facets solved by **exact Snell** (root-found) → self-test 5/5, focus to **9.99e-16 m**, reciprocity exact, falsifiable. 876 lines. Built + verified by a deputy; a 2nd deputy added a matching Fresnel-rings vignette card to the redesigned Hall (+29/-0). HEAD `4583c08` (bench) + the Hall-card commit.
- 2026-06-13 — **Hall of Mirrors redesign** 🌈 — "The Dispersion: A Prism's Throw", the FIRST build under the new gardener/builder system (and a fix of DESIGNING.md's own cautionary tale). The legible card-spine stays; layered on: an entrance-prism SVG + a white→spectrum left rail, the 11 benches ordered red→violet by how each treats light, the 4 groups as spectral bands, and — the soul — a **live inline-SVG vignette of each bench's own physics** that animates on hover (rainbow arc draws, polariser crosses to dark, prism fans, maze routes…). Dogfooded the pipeline end-to-end: a 6-lens **divergence Workflow** → curator-judge → my curatorial graft (judge's legible winner + the gallery's live-vignette soul) → a build deputy that browser-verified it (11 links, Feats 0/3/9 contract, 380px reflow, clean console, 62fps, contrast unchanged, reduced-motion). 311→577 lines, plain page.
- 2026-06-13 — **The planning system** 🌱 — a strategic chat with Brandon → a lightweight **gardener/builder** workflow against cold-start myopia (forgotten metagames, repetitive room design, no high-level planning). Shipped 3 files: **ROADMAP.md** (the seedbed — typed seeds ≤3 lines + a metagame-health table), **DESIGNING.md** (the gardener's charter + builder's pipeline: floor-not-ceiling, form-expresses-content, metagame-as-consideration, definition-of-done), and a **🎲 mode gauge** in NOTES.md (`PLAN if fuel ≲4 OR builds ≥4; else BUILD` ≈ 1-in-5, stateful not RNG). New seed kinds incl. **cross** (pollinate two rooms) + **retire** as an explicit gardener duty. Then sowed Brandon's first seeds — mostly grand bets (**The Workshop Mystery**, **Physics Lab** + secret quantum wing, **Alchemy Lab**, **Flight & Rocketry**, Light×Sound, front-door ambient music) + the small one (**make Survey-of-Heaven discovery an event** — melodies/tones/in-the-moment cues) + a Hall-of-Mirrors nav-redesign curation seed. New convention: one shared `ws:pref:muted` mutes the whole estate.
- 2026-06-13 — **Feats of Light** ✦ — earned achievements on the 9 Hall pieces (a real skill per bench → `ws:flag:earned-*` + a gilt toast + goal chip), wired into THREE surfaces: the **Hall ribbon** (N/9 + on-ramp), the **Survey of Heaven** (a new feats constellation **"The Optician"** that kindles + completes at 9/9, additive; sky self-test 22→**41/41**), and a **hidden Undercroft capstone "The Light Mixer"** (additive colour synthesis, the Spectroscope's inverse, 7/7) — secret **#12**, unlocked by all 9 feats. Phase-1 **Workflow** (9 worktree agents, each browser-verified its feat fires) + 2 Phase-2 deputies + an end-to-end served-origin QA (locked→3→9→unlock; PASS, 0 defects).
- 2026-06-13 — **The Hall of Mirrors** 🪞 — a new front-door **optics WING** (west grounds; new `hall` gallery footprint) homing **10 light benches**: 8 NEW + Caustic & Kaleidoscope (promoted off the Workbench). New: **Rainbow** 🌈 (droplet optics → 42.00°/51.04° from Snell, 9/9) · **Spyglass** 🔭 (refractor+reflector, M exact, parabola focus 7.5e-14, 9/9) · **Spectroscope** 🌈 (prism+grating + real line spectra, Balmer Hα 656.29nm, 7/7) · **Polariser** 🕶️ (Malus + 3-polariser paradox ¼@45°, 9/9) · **Anamorphic Mirror** 🪞 (cylindrical anamorphosis, round-trip 1.2e-15, 6/6) · **Iridescence** 🫧 (thin-film colour via CIE integration, Newton r_m=√(mRλ), 9/9) · **The Halo** ☀️ (ice-crystal optics — 22°/46° halos + sundogs + CZA, 10/10) · **The Camera Obscura** 📷 (pinhole imaging — m=v/u exact, optimal pinhole, 10/10) · **The Mirror Maze** 🪞 (a playable provably-solvable laser-reflection puzzle — 240/240 boards solvable, tracer terminates, 5/5). Built by 9 worktree deputies + a QA-sweep deputy (0 label collisions, hrefs 200). The wing: **11 pieces** (10 study-benches + 1 puzzle).
- 2026-06-13 — **Survey of Heaven legibility fix** ✶ — Brandon read the map's completed constellations ("The Scribe"/"The Gardener" + dangling lines) as bugs; not bugs — the metagame, but too subtle. Tied each engraved name to its constellation (brightened **charted** anchor-stars + a faint dashed **leader**, the same idiom every room label uses; stub-suppressed when adjacent). Logic untouched (sky self-test 22/22); 0 label collisions in the 6/6 state. Field-star dots left as-is by design.
- 2026-06-13 — **The Clockmaker** 🏮 (bonus) — the workshop's 3rd public **Lantern tale** (a midnight clockmaker's shop; light+lock+assembly); solver 5/5 — winnable in 13, softlock-free across 106 states; let-it-play solves it. HEAD `edaf5c5`.
- 2026-06-13 — **The Black Chamber** 🕵 (bonus) — the estate's first **cryptanalysis** piece (cipher makers → breaker): cracks Caesar/Vigenère/substitution with no key; self-test 14/14 (Caesar+Vigenère exact-key recovery, substitution ~97–100%, IoC 0.0689). HEAD `1abd15e`.
- 2026-06-13 — **The Mill** ⚙ (bonus) — the estate's first **computation** piece: a visible programmable **Turing machine**; the busy-beaver champions halt at their proven counts (BB(4) = **107 steps / 13 ones**); self-test 49/49. HEAD `2468552`.
- 2026-06-13 — **The Straightedge** 📐 (bonus) — the estate's first **linkage/kinematics** piece: the Peaucellier–Lipkin linkage draws a *mathematically exact* straight line from rotation (deviation **4.88e-15**), + a four-bar coupler-curve foil; self-test 14/14. HEAD `8ecf9ae`.
- 2026-06-13 — **Galton board** 🫘 (bonus) — the estate's first **probability** piece: balls → a bell curve with the exact binomial/normal overlay + a live χ² p-value; biased-p slider; self-test 12/12 (ideal exactly binomial; runs do-not-reject χ², a flat histogram IS rejected). HEAD `8ba8d08`.
- 2026-06-13 — **Kaleidoscope** 🔮 (bonus) — a live tumbling **dihedral-symmetry** mirror toy (every pixel folds its Dₙ-orbit to one wedge → exactly Dₙ-symmetric; order 3–12; self-test 9/9, invariance ~2e-14). Distinct from Rosette + Tessellarium. HEAD `6bb4c81`.
- 2026-06-13 — **The Patience engine** 🂡 (bonus after the wave) — a new genre: card solitaire whose **dealer only ships provably-winnable deals** (a weighted-A* solver gates each deal + drives Hint/watch-it-solve — Lantern's winnability proof on cards). Compact FreeCell; self-test 11/11. HEAD `4bd9b13`.
- 2026-06-13 — **THE BIG CREATIVE WAVE** (ideation fan-out → 5 parallel deputies): **The Adversary** ♟️ (a solved-games engine — play a provably-perfect opponent; 38/38) + **The Survey of Heaven** ✶ (metagame — the front-door map fills with stars as you wander; 22/22; POI label-overlaps still 0) + **The Singing Plate** 🎛️ (Chladni eigen-solver bench, the workshop's first spectral solver; 18/18) + **Bulwark** 🛡️ (Defender/Scramble side-scroller, arcade #19; replay-hash determinism) + **The Gnomon** 🌅 (operable sundial + analemma; 21/21). HEAD `dbaf864`.
- 2026-06-13 — **THE LETTERER** 🔤 — a reusable point-feature label-placement engine (`tools/label/label.js`: candidate-slot + simulated annealing, deterministic, forge+Node; self-test 12/12, in-page 24/24 → 0 overlaps) shipped as a provable specimen (Workbench → Toys & benches) — **and the front-door map now CONSUMES it to letter itself** (fixes the overlapping POI labels Brandon flagged; DOM-truth 0 overlaps fresh + Undercroft-revealed; `lx/ly` now optional). HEAD `9a3a062`.
- 2026-06-13 — **THE BIG REFRESH**: estate-map front door 🗺️ (data-driven POIs — replaces the card grid, dissolves the 10th-card ceiling) + **`ws:` forge-inlined** with an in-the-moment "something stirs beneath" unlock cue (foundation + all 9 trigger pages; ~14 pages now `.src.html`) + **Harmonograph** ✺ (pendulum drawing bench, 5/5) + **Scytale** 📜 (transposition cipher rod, 13/13). HEAD `3d9dbba`.
- 2026-06-12 — **Volvelle** 🔄 (bench instrument #4 — an operable Alberti cipher disk: Caesar/Vigenère/Alberti, drag-to-turn, 13/13) + **Ripple** 🌊 (wave-interference tank — drag sources, exact superposition, interference loci proven, 8/8; sibling to Caustic) + head-pointer fix (full **hidden inventory** in NOTES, per Brandon's "spoilers OK in NOTES") after catching & reverting a **duplicate Enigma** (one already exists hidden in the Undercroft).
- 2026-06-12 — Note-system rework (sharded worklog) + **Lantern** 🏮 → a new medium: interactive, stateful adventures (provably winnable & softlock-free; watchable "let it play" ghost w/ play/stop + solve-from-here, per Brandon's first play feedback) + **forge** (author-side inliner — one canonical engine, self-contained tales) + an 11th Undercroft secret (the hidden world's first interactive room). Public tales: The Lamplighter · The Ferryman. Engine v1.1.
- 2026-06-12 — Theogony name-distinctiveness fix (Jaro-Winkler < 0.72; self-test 5/5)
- 2026-06-12 — Undercroft 10th secret: The Reckoner 🧭 (working-instrument capstone trophy)
- 2026-06-12 — Morning summary: overnight `/fun` autorun shipped 17 pieces (consolidated)
- 2026-06-12 — Dig Dug 🕹️ → Arcade #18 (tunneler: dig · air-pump · drop-rocks)
- 2026-06-12 — Abacus 🧮 → instrument vein #3 (operable Japanese soroban; footer `count`)
- 2026-06-12 — Vanguard 🕹️ → Arcade #17 (Galaga formation shooter: capture-beam → dual fighter)
- 2026-06-12 — Astrolabe 🌌 → instrument vein #2 (planispheric astrolabe; footer `sky`)
- 2026-06-12 — Enigma 🔐 → new cipher vein (3-rotor Enigma I; Undercroft secret #9)
- 2026-06-12 — Slipstick 📐 → new instrument vein (working slide rule; footer `reckon`)
- 2026-06-12 — Caustic 💡 → new optics vein (steerable light-bench; footer `light`)
- 2026-06-12 — Loomlight 🧵 → new tactile vein (handweaving loom; footer `weave`)
- 2026-06-12 — Akari 💡 → logic-puzzle #3 (Light Up; puzzles trio complete)
- 2026-06-12 — Slitherlink 🔗 → logic-puzzle #2 (Loop-the-Loop / Fences)
- 2026-06-12 — Latch 🧩 → logic-puzzle #1 (new medium; front-door footer `puzzles`)
- 2026-06-12 — Theogony ⚡ → Threshold's companion / 7th wing (generative mythology engine)
- 2026-06-12 — The Almanac 📅 → Undercroft secret #8 (book-of-days under a real computed sky)
- 2026-06-12 — Qubit 🧩 → Arcade #16 (Q*bert-style isometric hopper)
- 2026-06-12 — Tessellarium 🔷 → Strange Garden's companion / 6th wing (17 wallpaper groups)
- 2026-06-11 — The Floating Ink 🌊 → Undercroft secret (mathematical marbling; paused cleanly)
- 2026-06-12 — Centipede 🕹️ → Arcade #15 (serpentine descent + segment-split)
- 2026-06-11 — End-of-session resume pointer + Undercroft discoverability fix (broken-stair tile + pulsing rune)
